Alright.

In our section 0901, we used simple Kubernetes secrets to store our MySQL credentials.

So that worked to a point.

So these secrets were base64 encoded and not encrypted in the background.

They were stored inside Kubernetes z storage, meaning anyone with access to the cluster could easily

decode and then view them in production grade setup.

That's a big security risk.

So in the next section, we will fix that problem by moving all our database credentials out of Kubernetes

and into AWS Secrets Manager, where they where the secrets will be stored securely with encryption,

access control and rotation options.

Here is how our architecture works.

Now all our secrets live safely inside the AWS Secrets Manager to connect our Kubernetes workloads with

AWS Secrets Manager.

We will install two key components in our EKS cluster.

One is Secrets Store, CSI driver, and AWS Secrets and Configuration Provider, which we also call

it as Ascp.

These run as Daemonsets inside the cube system namespace.

They are responsible for fetching the secrets from AWS and mounting them into our Kubernetes pods.

But this is not straightforward.

The most important point we need to know in this whole setup is authentication.

So normally AWS provides multiple options in that one is irca and the other is Pia.

For this authentication IAM roles for service account which is a older style.

But today at the latest it is easy.

It is recommended to use Pia which is ex pod identity agent right in 0902.

Already we learned about what is Pia and also we have installed the ex pod identity agent add on on

our EKS cluster and learned with a simple S3 bucket demo everything in detail.

Now we will focus on applying the same concept for accessing the AWS Secrets Manager APIs using this

ex pod identity agent for our catalog microservice.

Okay, so that's what we are going to do.

So with Pod identity agent, we don't need to manage Oidc providers or annotations manually.

Instead, AWS automatically handles the identity mapping between our Kubernetes service account and

also IAM roles using identity pod ex pod identity association.

This makes authentication simpler, faster, and more secure, especially for large clusters.

So far we have discussed about different components here.

So let's understand the complete flow now right.

Let's say our pod like catalog microservice runs under a specific Kubernetes account something like

this.

So that service account is associated with the ECS pod identity association which links it to the IAM

role.

Right.

Which has permissions to fetch the secrets present inside this AWS Secrets manager.

So when the pod starts, the pod identity agent authenticates the pod with the AWS IAM without any credentials

stored in the ECS cluster.

So that's the first thing.

Then the secrets store, CSI driver and ACP provider use that IAM role to securely fetch the credentials

from this secrets manager.

Finally, the secrets whatever it has fetched will get mounted inside the pods file system, right?

So the container can read it without ever storing it in Z or Kubernetes secrets.

Also, let's understand a little bit more about secrets.

Store CSI driver and keep this secret store.

CSI driver is a Kubernetes native component.

We also call it as CSI plugin that allows your pods to mount secrets and certificates stored outside

Kubernetes.

For example, it will get the secrets from AWS Secrets Manager.

So that part is done by ASCAP.

And once those secrets are with this secret store CSI driver, it will ensure that these things are

mounted in the Kubernetes pod when a pod with a secret provider class is created.

So here is the secret provider class, which is a custom resource definition.

And when this is created, Kubernetes calls the secret store CSI driver.

So once the call comes to the secret store CSI driver, the ACP is an AWS specific provider for the

CSI driver.

So now when the call comes here, this one, these two are tightly integrated.

And as CP will take the control.

And this has the ACP has the ability to authenticate to AWS via ECS pod identity and call the AWS Secrets

Manager APIs.

Right.

And it will fetch the requested secret value.

Right.

It will fetch the secret value and return them to Kubernetes Secrets store.

CSI driver and this CSI driver will take the responsibility of mounting them back to the Kubernetes

pod.

So that's what is the overall flow.

This whole thing is going to take a lot of time and going to look complex.

But when we implement it is very, very easy.

So I have break down these things into two sections.

One is 0903 and 0904.

So we will start with 0903.

We will prepare our EKS cluster in 0903 section.

We will prepare our EKS cluster by installing the secret store CSI driver and ACP, which is AWS Secrets

and Configuration Provider, and we will also create the necessary IAM roles, IEM policies, and also

create a Pod identity association with the service account for our catalog and MySQL deployments.

So all the configuration stuff related to Oh, installations, all those things we are going to do in

0903, not related to application, but related to primarily for our underlying configuration bed.

We will complete in 0903 and then we'll move to 0904 section.

And in 0904 section we will focus on the core application which is catalog microservice.

So we will create a AWS Secrets manager uh with MySQL database credentials.

And we will also define a secret provider class that tells ACP which secret to fetch and update both

our MySQL Statefulset and also catalog Kubernetes deployment to mount those secrets directly from AWS

Secrets Manager.

So via this complete path, which means we need to create a volume mount, uh, with this secret provider

class information.

So when this Kubernetes pod is deployed, means like catalog deployment and MySQL statefulset, the

whole flow process will initiate, which we have discussed just now.

Finally, once these catalog and then MySQL Statefulset and catalog deployment related changes are done

in their respective configurations, we will verify by deploying them, ensuring our applications can

securely connect to the database without any plain test credentials stored in Kubernetes.

So by the end of these two sections, we will move from simple base 64 Kubernetes secrets to a zero

trust production grade setup, where credentials live only in AWS and are dynamically fetched by our

pods using EKS pod identity.

This is a massive step towards production level security in our EKS environment.

So let's dive in and start with setting up the CSI driver and AWS provider.


Alright, before we jump into installing components like Secret Store, CSI driver, or ACP, let's

take a quick step back and understand what helm actually is and why it is so useful in Kubernetes.

So you might ask me all of a sudden, why are we directly getting helm concept here?

What is the use of it?

So actually we are going to use helm to install the Secrets Store, CSI driver and AWS secrets and configuration

provider ACP.

So that's the reason before going and using helm CLI here.

So we will first understand why helm and what is its benefits.

So it will be easy for us when we are running those commands.

So why helm.

Let's start with how we normally deploy things without helm.

So on the left side of the screen you will see the without helm workflow.

So this is the without help workflow.

So in a typical Kubernetes setup we manage multiple YAML files.

So like deployments configmaps, Statefulset secrets service accounts and even Kubernetes services.

So for this is just for one environment.

But if we talk about multiple environments like dev staging production, we manually update these YAML

files with the correct configurations before deploying them.

So that's a lot of repetition and plenty of room for human error.

And even let's say like one example here for a deployment.

So in in in dev environment you might take your replicas as two replicas in staging might be four,

but in production it might be 100.

So all these changes in your YAML files and all these things, how we need to do and how we need to

go and update all this is a problem without helm.

But with helm, what happens?

Let's understand that helm simplifies everything through a concept called helm chart.

Inside each chart you have two parts.

One is templates and the other is values.

Templates is your Kubernetes YAML files with variables inside them.

So and in values file you will have the actual values which define configuration differences for each

environment.

So whatever the differences are there example like replicas okay in dev you say two and in prod you

say something like 100.

So those things they will be present as replicas will be present as variables inside templates.

And those values you will define in this values section.

And then when it is run these values will be replaced in these variables based on the file which you

are using.

Example prod values dot YAML.

You are using.

Then your 100 replicas will go into the picture when this deployment is deployed.

In the same way, if it is when you are using the dev values, then only two replicas will be going

into the picture.

So this is a template and real values are defined here and here.

For the values which need to be defined in values section.

Those will be variables inside these deployments configmaps wherever it is required.

In very simple terms, with helm, we can maintain one common chart with all the templates inside that

and just plug in different value files for each environment.

So a separate value file for development, separate value file for staging and then separate for production.

That means no more editing YAML files manually.

Helm handles that for us automatically when we deploy.

So helm gives us common templates plus environment specific values, which is equal to simplified deployments

across environments.

Let's now talk about the benefits of helm.

And there are many benefits when we are using helm.

So the number one is reusability like write once and use everywhere.

And we also have something called versioning.

So versioning is another benefit in helm where you can track chart versions like application versions.

So there is one more benefit which is release management.

So easy upgrades and rollbacks with some if something breaks.

So you can using a simple single command, you can roll back and all your configurations present inside

it.

It might be 100 templates present inside it.

All those things will roll back to a previous version, simply using single command and packaging and

sharing.

Bundle all YAML files into a single helm chart installable helm chart.

And it also has another benefit like simplified deployments.

So one command to install all the complex apps.

And another important thing is consistency.

Every environment stays in sync.

And we also have something called helm repositories wherein you can publish and reuse charts across

teams and projects.

So these are the key benefits of helm.

And from DevOps perspective, helm is a package manager for Kubernetes, just like APT for ubuntu or

yum for Amazon Linux.

So you use to you use it to install, upgrade, rollback and manage applications on Kubernetes the same

way you would install the software on your server.

In our next demo, which is 0903, we will actually use helm to install the Secrets store, CSI driver

and also the ACP.

That way you will see how easy it is to deploy real production components with just a single helm chart.

And when we go to the section.

All right.

So there we will go deeper into helm itself, like creating our own helm chart or customizing templates

or deploying our own microservice using helm, all those things.

So there is a series of demos planned for our helm section.

So if we go here, so there is a series of demos planned there, we are going to learn more about it.

But here our focus is primarily on just using helm CLI to install the CSI driver, which is secret store,

CSI driver and Ascp.

So both the things using helm.

So that's what we are going to do.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


All right.

Let's go ahead and then complete the 0903 section, which is wherein we are going to install the Kubernetes

Secrets store, CSI driver and ACP and also complete creating the IAM roles, IAM policies, and also

X pod identity association between the Catalog Service account and the IAM role we have created, which

has permissions to fetch the secrets from AWS Secrets Manager.

So these things we are going to do as part of 0904.

So let's go back to our GitHub repository.

And we are entering into the demo 0903.

So all the steps are documented here.

So we can go ahead and then implement them one after the other.

So the first thing here is we need to ensure that our EKS cluster is up and running.

And also we need to install the helm CLI if it is not installed.

So the first thing is we can go here to the terminal and run the command cube CTL get nodes and my EKS

cluster should be running this.

So to use the helm we need to install the helm CLI so we can go to our helm CLI install documentation

here you can click on this.

And for whatever the local desktop environment you are using for all those things you have the options

to install it either via script or download the package and then uh, add it to your path or use the

package managers.

If it is Mac OS, we can use Brew Package Manager.

If it is windows Charcoal and or scoop you can use it.

So like this.

There are we can get all these options are available even Debian ubuntu you can use uh those DNF or

even apt.

Get all these things apt.

All these things you can use based on your local desktop environment.

And mine is, uh, Mac OS, so I'll just say brew install helm.

So when we run this, it will install the helm for us.

So I already installed it.

So I'll just say helm version.

And this is the version I am using as of now.

And the next thing is we need to add helm repositories.

So let's understand this a little bit right.

So helm works very much like a package manager like em or DNF like that okay.

So it helps us install, upgrade and manage applications as versioned packages called Helm charts.

So here the first command we are going to use is helm repo add.

And second is helm repo update.

And third is helm repo list.

And fourth is helm install okay.

And fifth is helm list.

So these are some of the commands we are going to use as part of this demo.

Right.

More we will learn when we go to the helm specific section.

So what this helm repo add is going to do us this command adds a new helm repository to your local setup.

Think of it like adding a software source in your local desktop.

So we will add these two repositories which are Secrets store CSI driver is located in this respective

location in this respective location.

So that repo we are going to add it here.

And in the same way we will also add this repo.

Also, if those were already added, it says that it's already present and existing.

You don't need to add them.

So I already added earlier.

So it is saying that okay.

And next we will run the command helm repo update okay.

So this refreshes all the repositories you have added locally.

Making sure helm knows about the latest version of charts available at the remote place, which means

at that URL, right?

So now if you run the command helm R not helm list.

Helm repo list is the command helm repo list.

Right?

So you can see whatever the things you have added secrets store CSI driver and AWS Secrets Manager.

So those charts you can see them here.

So helm repo list right.

This this command shows the repositories currently configured on your system.

So you can quickly confirm if your repo was added successfully or not.

And next we will go for the helm install command.

So we will go here.

And this is the helm install command.

And we are saying the release name means like each one whatever you install in your local desktop.

Right.

So those that is a release name okay.

Helm release we call it CSI Secret Store.

This is the desired name you can use.

So I am saying CSI Secret Store.

That's the name I am giving here.

Helm install and release name.

And this is the actual chart location okay.

So if you see it here, after adding it locally in secret store CSI driver inside that this is the path.

So if you say helm repo list right in secret store CSI driver again, we have the chart named secrets

or CSI driver.

So that's the thing.

And in namespace Cube system we are doing it.

So when we run this command it will install all the Kubernetes resources related to this respective

secrets store CSI driver application and in which namespace we are installing it in kube system namespace.

After that, you can list the helm releases whatever you have installed in that respective namespace

using helm list and then provide the namespace name, hyphen hyphen namespace cube system, or from

all the namespaces.

Whatever the helm releases are available, you can run the command helm list and then hyphen hyphen

all namespaces.

So currently it is installing and now you can see it here CSI secret store related release deployed

and in namespace cube.

System status is deployed and revision is one and there are no test suits.

So it is saying the secret store CSI driver is getting deployed to your cluster to verify that you can

run this command cube CTL hyphen hyphen namespace.

And in kube system you say get pods and the label is app equal to secrets store CSI driver.

Using this also we can verify.

First thing is let's verify the releases.

Helm list all namespaces CSI Secrets store release.

This is the release and installed in namespace cube system and revision is one and updated so and so.

Date status is deployed.

Chart name is secrets store CSI driver and it's version app version is also provided here.

So that's about it.

Now if you want helm list and then hyphen hyphen namespace and cube system.

Or you can say hyphen n and cube system only from cube system.

What are the releases?

Is there you can get the same as kubectl commands, only namespace specific.

You always need to provide the namespace related flag and namespace information to get the data right

now.

If you want, you can also verify the status.

Okay.

Verify installation status also using helm status command.

And it is providing the information whatever it is.

Okay.

And this this command whatever it is showing to verify all the things we'll try to run that also okay.

So we will run that command in kube system namespace.

We are trying to get the pods and who's whose label is app equal to secrets dot store secrets store

CSI driver.

So three parts are running because whatever it creates, right.

This secret store CSI driver it is going to be a daemon set.

So if I say kubectl get ds hyphen kube system right, and it is going to show us that we have something

called CSI Secrets store CSI driver.

It's a daemon set.

Daemon set means how many nodes you have in Kubernetes cluster all the worker nodes in all the worker

nodes.

That pod will be created.

If I say I have three nodes running here and in all the three nodes.

If I say get, I can say this one right.

So this is the command, right?

Uh, in all the three nodes this should be running.

So if I say get nodes hyphen, oh wide IP addresses of those nodes will come.

Let's see.

So these are the IP address 191 120 and 172.

And if I run this command right.

So this command with hyphen oh wide means like get pods with hyphen oh wide should give us the on which

nodes it is present okay.

Right.

So you can see it here it is present in 172.

Another one is in 120 and 191.

See?

172, 120 and then one nine.

So which clearly states that each pod is present on means like each node is having one pod related to

secrets store CSI driver.

It is running as a daemon set.

So that's about installing the secrets store CSI driver.

In the same line, we can also install the AWS Secrets and Configuration Provider, which we call it

as a CCP, right?

So helm install and secrets provider AWS.

And in AWS Secrets manager slash secrets store, CSI driver provider AWS is the path for it.

And in namespace cube system we are installing.

And one important flag we are setting here which is hyphen.

Hyphen set secrets store CSI driver install false.

As part of previous step, we already installed the secrets store CSI driver, so we don't need to install

that again.

So that's the reason we are giving it as false.

We have an option that within single step we can install both the ACP and also the secret store CSI

driver default value is true.

That way we can do it.

But there are some issues and then it is sometimes failing.

So for that purpose I have separated it.

First install the secret store CSI driver and when installing the ACP, ensure that you say false.

So that's the best recommended approach.

So we'll go ahead and then install this ACP now.

So we'll say clear here and install the ACP.

And its release name is secrets provider AWS.

So once it is installed so we can see it here it is deployed in kube system and everything looks good.

So we can go ahead and then run the command helm list hyphen n and kube system namespace.

So we should have two releases.

One is CSI Secrets store CSI driver and secrets provider AWS.

So both are now deployed.

So which means whatever we said the both things right.

So these two things are installed now.

Now let's go here.

And if you want you can say helm status.

And all those things are verify whatever secrets store CSI driver and secrets store CSI driver AWS related

things.

Also you can verify so already CSI driver ports.

We have verified we will verify the parts related to this secret provider AWS, ACP.

Right.

So when we run this command.

Right.

So when we run this command, we'll get the ACP related parts running.

And this is also a daemon set.

So if we say kubectl get ds hyphen n and then kube system.

So it should be also a which is you can see it here secret provider AWS hyphen secret store CSI driver

provider AWS.

This is the whole thing.

So this is also a daemon set.

So it is running three parts in each of the worker nodes because we have three worker nodes.

So that's about installing those things.

If you want you can also run this Daemonset command.

So you should see the both daemonsets running here okay.

And this is about installing the Secrets Store CSI driver and ACP.

In our next lecture we will focus on creating the IAM role policy and then ECS Pod Identity Association.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


In this lecture, we are going to create a I, a policy which allows reading specific secrets from the

Secrets Manager.

And we'll also create a IAM role with trust policy.

And then we will associate this IAM policy to this Im role.

And we will also create a I x pod identity association between this catalog Kubernetes service account

and also IAM role.

Whatever we have created here in AWS IAM.

So that's what we are going to do as part of this step.

So let's go back to our GitHub repository and let's start creating those things.

So first we will set our region EKS cluster name and also AWS account ID as environment variables.

So let's go to our terminal here.

And let's set the region and also set the cluster name and also the account ID.

And once those are set, also verify those things.

Okay.

US East one.

This is the cluster name and this is the AWS account ID.

In your case this account ID will be different.

Now we will go to CD IAM policy, JSON files in zero nine Kubernetes secrets and 0903 AWS Secrets Manager

Driver setup.

We'll go to this folder and here we will create the files.

One is catalog db secret policy dot JSON file.

So here we are giving the permissions IAM policy with get secret value and describe secret.

And we are also giving for a resource specific resource means like any uh AWS Secrets Manager resource

whose name starts with catalog hyphen db hyphen secret star.

If you don't want to use that, you can just remove this and just give resource equal to star like this.

So if you go back to Visual Studio Code and we have a backup here, catalog DB secret files backup.

And instead of writing the complete Arn.

And if you want to give star so that any name your desired name for your secret manager secret will

be allowed at that point of time, it's your choice.

Okay, so I'll try to use a specific name here and it is created now.

So if we go back here this is the one.

So not the backup.

This is the one.

And here this is the resource.

So when I'm creating my AWS Secrets Manager resource its name should start with catalog DB secret.

So if not again it is going to fail because my IAM policy with permissions which I am giving is for

this name only.

If you don't want that and then if you want any resource then you can just say star.

And then it is going to whatever the name you give in AWS Secrets Manager, it is going to work.

Okay, so I'll leave this as is.

And now we'll go ahead and then also create a IAM policy with this JSON file.

So aws iam create policy.

Policy name is catalog DB secret policy.

So let's go ahead and then create this IAM policy now.

So we'll go here I am in IAM policy JSON files.

And I'm creating this IAM policy.

And we can go here and verify that we can go to policies.

And we will say catalog hyphen.

What is that.

Catalog hyphen DB secret policy.

So this is the name and let's search for it.

And this is the catalog DB secret policy just now created with these permissions.

And next thing is we'll also create a trust policy.

So we'll create the trust policy that allows exports to assume this IAM role.

What we are going to create through the pod identity agent.

So we'll create this trust policy whose principle is ports dot x x.amazon.com.

So let me also create a trust policy first.

And you can see trust policy dot JSON created and AWS IAM create role role name is catalog DB secrets

role and assume role policy document is trust policy dot JSON file.

So let's go ahead and then create that.

Once it is created we will search for this IAM role first right.

And we'll go here to roles and search for that role and try to refresh.

And we can see catalog DB secret role.

Currently no permissions are there in the trust relationship.

We have given the trust policy whatever we have.

Uh see we have added it here trust policy dot JSON file.

And now we will also attach our catalog DB secret policy to this IAM role.

So we will do that using command line.

And after that we'll go here and it is added.

we can go here and then refresh in the permissions section when we refresh.

So we should see here catalog DB secret policy is added successfully in this IAM role catalog DB secrets

role.

And now the next step is if you want you can also verify the attachment all the attached policies using

even the command line.

Okay so we have directly verified on the console.

We don't need it now.

We will go ahead and then create the Pod identity association.

Right.

So if you go to the EKS cluster here right.

So we can say ECS and Elastic Kubernetes Service.

And here in the EKS cluster whatever we are seeing retail dev EKS demo one.

In the access section you can see the pod identity associations.

So before creating the Pod identity association, ensure that your ex pod identity agent is installed.

If not installed you can go to get more add ons and then here it will be listed.

Select it and then install it as part of 0902.

We have already installed it and it is present here.

We really don't need to worry about it.

In the access section in Pod Identity Associations, we can click on create and we can select the IAM

role which is catalog DB secrets role.

And uh we can select the namespace which is default namespace and Kubernetes service account, whatever

we want to name it later.

Okay.

So even though it is not created, its name looks this way.

So if you go to 0904 and go to the service account section.

So this is the service account name.

So those things you can provide it here and then click on create.

So this is creating the ECS pod Identity Association using console.

And we have also provided the steps here to create the same thing using using a command line.

So the first thing is whether your pod identity agent is installed or not.

You can verify using this command.

So let's go ahead and then verify X pod identity agent is running now.

Now we will create Pod Identity association AWS x Pod Identity Association and cluster name.

We will provide in for namespace default because our catalog application runs in default namespace.

If it is a different namespace like web or some catalog namespace, you need to provide that namespace

name and service account is going to be catalog MySQL SCA.

So this is the name right?

So even though it is not created in Kubernetes we can still use it.

When it creates it takes it initiates at that point of time or it triggers okay using Pia.

So enroll Arn.

So our catalog DB secret role Arn is there.

Right.

So that you need to provide it here and then create that Pod identity association.

So we will do that using command line.

We have seen how to create via console but we will do it via command line, so let's wait for it to

get created.

So here E is missing okay, so I'll try to add that E.

And now it is created.

Association is created with cluster name service account role Arn.

This is the association Arn Association ID everything is present here.

So if we go here to retail and go to access and here it is, we'll try to refresh it once so that we

can see that okay.

The access section see this is created Pod Identity Association.

The full information of it is listed here okay.

Now the next thing is we have successfully created the Pod Identity Association.

If you want you can list that pod identity association using even the command line aws eks list pod

identity association.

And then it is listed.

And another important thing which we have already discussed even though you are your catalog MySQL service

account is not created, it is perfectly fine when we are implementing 0904.

We can create it and then it will not be an issue.

So we have successfully created the IAM policy IAM role with the trust policy and trusted by export

identity.

And then we have also attached the IAM policy to IAM role.

And we have also created a Pod identity association which binds the IAM role and our Kubernetes service

account in specific namespace where our catalog application is getting deployed, which is default namespace.

I'll see you in the next lecture.

Our next demo.

0904 where we are going to actually implement this overall thing.

So now the current, uh, all the installations are setup is completed.

In the next section we will focus on implementing or integrating it for our catalog application.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


In the previous section, we have installed the secret store CSI driver and also AWS Secrets and Configuration

Provider.

Now in this section, we will actually use that setup to mount secrets directly from AWS Secrets Manager

into our catalog microservice running on ECS.

So we are going to create a AWS Secrets Manager secret with MySQL credentials.

So that's going to be step number one.

And then we are going to create a secret provider class to fetch that secret securely using ex pod identity

agent.

So that's step number two.

So one is create the secret manager secret and create a secret provider.

Class is step two.

And in the third step we will update both the MySQL stateful set and also Kubernetes catalog deployment

to mount those secrets using secret provider class.

So that's what we are going to do.

And finally, we'll go ahead and then deploy these things Kubernetes manifests and then verify.

So that's what is the uh outline for this demo.

So we'll go back to our uh GitHub repository and 0904 AWS Secret Managers Catalog integration.

And here we will go ahead and then start with the first step here, which is create a secret AWS manager

secret.

So you can directly go here right.

And go to Secrets Manager right.

And here you can go ahead and then create the secret using console or using command line.

So whatever the thing works for you.

So my my preferred version is always I try to use command line wherever it is possible.

So I'll try to use it okay.

AWS Secrets Manager create secret and name of the secret is catalog DB secret hyphen one.

So if you go to our IAM role here right.

So in 0903 we said that IAM policy JSON files.

And you said that catalog hyphen DB hyphen secret star which means hyphen one.

Whatever.

We are creating our secret name.

Right.

So this should work for us okay.

Let's see okay.

And region AWS region which we have set here.

And description MySQL credentials for catalog microservice.

And in the secret string you are providing the value here MySQL user.

My db admin and password is db 101.

So this is the secret we are going to create now.

So we'll go to the terminal here and we'll go to we'll come out of I am policy JSON files.

We'll go to 0904.

Just I am trying to be in that section okay.

Other than that nothing.

So this will create a secret for us directly in AWS Secrets manager.

So it is created catalogue DB secret one.

So and so so and so.

So now if I go here right so.

And then try to refresh this.

This is the catalog DB secret one okay.

So this is the secret it got created and secret value.

So if you want you can retrieve the secret value here.

This is the MySQL user.

And this is the secret value.

My db admin password and db 101 MySQL password and db 101.

And if you want in plain text you can see this way also.

So this is what we have created now.

And if you want to also uh run commands to verify using uh command line, you can run these commands

okay.

In your account.

Okay.

All the secrets will be listed with whatever the catalog.

DB secret one.

It's, uh, it is listed now.

List all secrets in your account.

And if you want to describe the secret, also, you can describe the secret using this command.

Okay.

All the information about that secret will be provided.

And in addition to that, if you want to retrieve the values of your secret, which means what is the

MySQL user and then password related information Mydb admin and db 101.

So those also you can get it using these commands.

These are optional right.

So you can all you have already seen using console right.

Now the next thing is create the secret provider class okay.

So we will go here and then review that directly.

So we'll go to 0904.

And in 090 4 in 0 one secret provider class you have the zero one catalog secret provider class dot

YAML file.

So this is the API version and the kind is secret provider class.

So this resource acts like a bridge between your Kubernetes, which means your catalog MySQL deployments

and your AWS secrets manager.

It tells the secrets store CSI driver what secret to fetch and from where it need to fetch, and how

to mount it inside the pod.

All the information will be present inside it.

Metadata.

You can see we have provided the name of this respective provider class.

We say this as catalog DB secrets.

We said secrets, right?

Plural.

Because not only one.

You can create multiple objects here.

This is one object.

So objects is a list item.

So you can create multiple secrets here.

Okay so now catalog DB secrets.

And here we have something called in the spec we are saying the provider as AWS.

This tells Kubernetes that the backing store for the secrets is AWS Secrets Manager and not Azure Key

Vault or some GCP Secrets Manager.

Now let's focus on the important thing which is object section okay.

So which is a list item.

So we define which AWS secrets to fetch and what keys to extract from that respective secret.

The object name is catalog DB secret one.

So that's what we have created right?

So catalog DB secret one and object type is Secret Manager.

And so we are saying to ACP that please connect to AWS Secrets Manager and fetch the catalog DB secret

one from the Secrets Manager.

Then comes to the JMS path here.

So this is how we pick specific key value pairs from that secret JSON.

We have two entries here, one for MySQL user and one for MySQL password.

Each one maps the AWS key to an object alias.

Basically the file name that will appear inside the container.

So inside the container we might have something like when it is mounted in the container.

Right.

In Kubernetes catalog pod slash slash secret store slash MySQL user is one file and MySQL password is

another file.

So both files will contain the corresponding values.

So that's the reason you are getting them individually here.

So finally you also said that use pod identity.

True.

So this is very crucial.

It tells ACP means like whatever the ACP we have installed.

Right.

So two ACP to authenticate using X pod identity instead of relying on older mechanisms like Irca IAM

role to service accounts or node IAM roles.

So those are uh older options or older mechanisms.

But latest we have x pod identity.

So use that.

Use pod identity.

True.

That means only pods running with the specific service account linked to pod identity association can

fetch the secret whatever we have created.

So no credentials, no access keys, nothing hard coded.

So that's about secret provider class.

In our next lecture, we'll focus on, uh, also reviewing the catalog, uh, deployment and catalog

statefulset.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


We have successfully created the secret in AWS Secrets Manager, and we have also reviewed the Secret

Provider class.

Now the next thing is, we need to mount this secret provider related thing in your Kubernetes pod and

also create a volume mount for the Container Catalog container.

So for both the things we need to do that okay.

So to do that we also need to first review the catalog service account.

And after that this service account is referenced in STS and also in catalog.

We need to verify and then also complete this secret provider class mounting inside these pods.

Okay.

Let's go back to our Visual Studio Code here.

And we will go to Write Zero to Catalog Manifest.

And here let me close this provider class and we will start with the service account first.

So we have the catalog MySQL service account which is the same name we have used when we have when we

are creating the exported identity association.

And now it is present.

Now rest all is as is.

We need to verify one is statefulset, another one is deployment.

So we'll go to the statefulset and we will start from the top.

All these are as usual.

But here the service name you can see catalog MySQL.

This is not the thing we need to see the service account okay.

So the service account is catalog MySQL esca.

So this should match with the service account which we are creating which is present in our ex pod identity

association.

So here service account thing we have verified.

And next is we need to discuss this part okay.

And after that we need to discuss this part.

And this part.

So let's review one after the other okay.

So the here first thing is in the volume section you have this thing AWS secrets.

Okay, so this defines a CSI volume using the secret store CSI driver.

So we are telling Kubernetes mount a volume backed by our secret provider class called catalog DB secrets.

So that's what we are telling Kubernetes here.

So that means the data inside this volume will actually come from the AWS Secrets Manager, not from

anywhere in the Kubernetes itself.

Okay.

So that's about this volume.

Whatever.

We have defined AWS secrets and CSI.

And this is the driver read only true and in volume attributes.

You are saying catalog secret provider class catalog DB secrets.

So if you go here.

So this is the secret provider class in zero one whose name is catalog DB secrets.

The same thing.

We are using it here.

That's about volumes.

And next is volume mount.

So you have the volume available.

And inside the MySQL container you need to mount this volume.

So for that you use the volume mounts okay.

So we mount those volumes using volume mounts okay.

Now here we have something called AWS secrets.

And this is where the secrets from AWS Secrets Manager get mounted.

In which location slash slash secret store.

In that location we are going to mount and in read only format we are going to mount them.

And next is the this is the command in your container.

So this is the MySQL container.

And you are passing some arguments to your container okay.

One is export MySQL user equal to cat mnt secrets store MySQL user.

And second is export MySQL password equal to cat of mnt secrets store slash MySQL password, which means

after this mount is completed in your container in n't secret store, you will have two files which

is MySQL user and MySQL password.

And when you say cat, whatever the content inside that is there that is set inside this variable.

And you are saying export means in this container.

In this container you are setting the environment variable.

MySQL user is equal to this value and MySQL password is equal to this value.

So that's what you are doing here, which means you are setting the username and password for MySQL

from the value you are getting using CSI volume, which is fetching the actual secrets from AWS Secrets

Manager.

So that's what is happening here.

So we read the username and password from these mounted files, store them in environment variables

MySQL user and MySQL port.

Print a small log just for visibility.

So that is what we are doing here.

And then start the MySQL server as usual.

Okay, so when you are starting the MySQL server, right.

This ensures that MySQL gets its credentials securely at runtime, fetched directly from AWS Secrets

Manager and not from Kubernetes secrets or something else.

So in very simple terms, volumes bring in the secrets and volume mounts make them available inside

the container which is MySQL container.

And the argument section ensures they are used securely to start the MySQL.

So that's a perfect example of how x pod identity, CSI driver and Ascp work together to enable dynamic

Zero trust secret access.

In our next lecture, we will focus on reviewing the deployment also.


Now let's look at zero one catalog deployment.

So here also whatever we have seen in Statefulset.

Same concept applies here.

So if we go down here right.

So you can see the similar volume mounting.

Same as AWS secrets and Driver is Secrets store CSI driver and provider class is catalog DB secrets.

It's the same thing here.

And even the volume mount it is going to look the same AWS Secrets and Secrets store and read only true

and only thing is in the arguments section instead of MySQL user for catalog application, it is retail

catalog, persistent user and retail catalog persistent password.

Those two are the environment variables it need to set in the container catalog application container.

So when those are set uh, what happens is catalog application can securely connect to the database

which is created using Statefulset.

So here you can see retail catalog, persistent user and cat of secret store MySQL user.

And for persistent password mnt secret store mysql password.

So that's the whole thing.

Okay so no changes as usual, but only the environment variables change here.

And this is a go application.

We are going to start the application with x slash app slash main.

Once these environment variables are set.

So when starting the application it can connect to the database using this username and password set

here.

So that's about catalog deployment.

Now in our uh we can go here and go ahead and then deploy these things and then verify.

So we have successfully reviewed all the things all the uh, YAML manifests.

We can go here and deploy.

First we need to deploy the secret provider class.

And after that we need to deploy the catalog manifest.

So that's the important thing here.

So we will go to our terminal here and we'll say clear.

We are in 0904.

We will say cube CTL apply hyphen F01 secret provider class.

So once the secret provider class is created, if you want you can say get secret provider class and

you can see it is created whose name is catalog DB secrets, which is created just eight seconds before.

Now we can also say apply hyphen f zero to catalog k test manifest.

So this should create the Kubernetes catalog deployment and then service and then catalog Configmap

Statefulset and also catalog MySQL headless service all those things it should get created okay.

And then if we want to verify if the secrets mounted in pods or not, you can run these commands.

Okay.

Provide your pod name and RLS and then M and secret store and cat and user all these things we can try.

Okay.

Right.

So we we will go here and we'll say cube CTL get pods.

Let's see whether the application is up and running okay.

So it is saying a error little bit.

Let's wait for it okay.

It will take a little bit of time for catalog to also come online because first MySQL should be online.

And once it is online then it will go ahead and then also connect to the application.

So let's wait for it.

See both are running now so both parts are running now we will say cube CTL exec hive will say first

get pods and cube CTL exec hyphen I t.

And this is the MySQL pod right.

Pod.

And uh we will run the command hyphen hyphen space.

This one okay.

This is the one.

And let's see what happens.

Okay.

It is saying that you have the files MySQL password and MySQL user.

Both are present there.

Okay.

And if you want you can list them directly as L LRT.

And now those will be listed properly.

Okay.

So these three are the files present inside the container MySQL user and MySQL password.

So if we want we can say slash MySQL underscore user.

And we will say cat for that okay.

So we will say cat and we should see the user name which is my DB admin.

So this is the one.

In the same way if we say MySQL password okay.

And you should see that you have the DB 101.

That is the password.

In the same way, if you want you can also verify for your catalog part.

I need to just change here.

To catalog we can access the catalog pod and then also verify we can do that now.

Okay.

So quickly kubectl get pods and kubectl exec hyphen it.

And this is the catalog pod running.

And we'll copy this and hyphen hyphen and I'll say cat.

And here we have the complete length okay.

So this is the one.

So we will run this okay.

And see whether we have the MySQL username.

So what happened.

No such file or directory.

So we can see here it said no such file.

And then I tried to do a ls and I have accessed a wrong file.

Actually we have said it here.

Okay.

The file is going to be MySQL user and MySQL password only right.

But only thing is we are setting the environment variable here as retail user persistent password that

one.

Okay.

So if you go here to deployment the file is going to be the same MySQL user and MySQL password.

So we need to search for these things only.

MySQL user and MySQL password in catalog.

Also only we are exporting it in our container with these names.

So if we go back here with these names we are exporting it inside container.

But mounting we are doing with these names only because our secret is with that name itself.

Right.

MySQL underscore user.

So that's the key thing there.

So we will go back here to the catalog and we will run the command ls and then mnt secret store.

And we should see the three files here.

Or we can say ls hyphen LRT.

And we can see MySQL user MySQL password and catalog db secret one.

And if we want we can also say now we can run that command okay.

So go back here.

And this is the one in catalogue part.

Also, we are running the Cat command to see the username mydb admin.

And in the same way, if we want we can also see the password also which is MySQL password.

So these are successfully mounted in our see this is the DB 101.

So we just verified that in both the catalog pod and MySQL pod this MySQL username and password were

successfully mounted.

Which means the whole logic, whatever we are talking about see, is installing the secrets store CSI

driver and also installing the ACP, and also creating a secret store provider class and mounting them

as volumes here.

And these volumes are added as volume mounts inside the container.

And from that volume mount we are extracting the information using arguments with export.

So the whole process is working or not.

We have tested and it worked as expected.

Now we can go ahead and then even test it via browser.

Also, if our catalog application is working or not.

So we can run kubectl port forward svc catalog service locally 7080 and container port is 8080.

And we can access the topology first.

So we can go here and access the topology endpoint.

And it is accessible catalog MySQL zero catalog MySQL.

So the whole thing is there.

And in the same way we can access health and it should say okay that is good.

And if we want we can access catalog products and all things are working as expected.

Nothing broke because of we have implemented the secrets manager here.

So we have implemented a secrets manager implementation.

But nothing broke end to end is working as expected.

So that's about uh, that's about uh, using AWS Secrets Manager for ECS, using CSI Secrets Store,

CSI driver, and ACP.

If you want, you can also run these commands and then verify and all those things.

Now let's go ahead and then clean up.

So we'll go ahead and then first delete whatever we have which is zero to catalog test manifest.

And after that we'll also remove the secrets store CSI driver.

So we'll say delete hyphen f and then zero one secret provider class okay.

And if you want to uninstall those things or those things you can do.

But in our next demo anyway we are going to use these things, which means we should not uninstall the

secrets store, CSI driver and ACP from the cluster because in the next section also we are going to

use Kubernetes Secrets itself.

Okay, Kubernetes secrets from AWS Secrets Manager.

I'll see you in the next video.

Until then, bye bye.

Thank you.


As part of section 1001, we are going to focus on using persistent disks for our Kubernetes Statefulset.

So let's start with a quick reality check of why we need this aws EBS, which is Elastic Block Store.

So far, our Kubernetes statefulset, like the MySQL deployment we built earlier, is using an empty

Dir.

So you can see it here.

Volumes we are using here is empty dir.

That means the moment our pod gets reset scheduled.

On another node all the data is gone.

So the storage here is ephemeral tied to the pods life cycle.

So if you see this volume, whatever we are seeing here right is mounted as a volume mount inside our

MySQL container for var lib MySQL path where our actual data is stored for MySQL database, actual data

for MySQL is stored there.

So if Pod gets rescheduled automatically.

The data, whatever is there for our catalog application is gone in the real world.

That's a very big problem.

So databases, message queues and any stateful workload need their data to survive pod restarts and

also node failures.

That's where the persistent storage comes in.

So Kubernetes itself doesn't provide storage.

Instead it depends on CSI CSI which are nothing but container storage interfaces.

So using these CSI drivers we can talk to cloud storage backends.

So each cloud provider like AWS, Azure or GCP has its own CSI driver implementation.

So in, in and in our course since we are running on AWS ECS, we will use the Amazon's EBS CSI driver

here.

So this is the driver we are going to use which allows Kubernetes to dynamically provision, attach

and mount EBS volumes to our Kubernetes pods.

So with EBS CSI, each pod in our statefulset gets its own dedicated EBS volume, which is automatically

created when the pod starts and safely reattached.

Even if the pod moves to another node or pod got restarted in whatever the case.

So those volumes will be safely reattached.

So before we dive into the architecture diagram and understand what we are going to implement, the

key important thing to remember here is empty.

Dir means temporary and EBS CSI means which is persistent.

So this is the important thing to remember.

So now let's see how it actually works inside our EKS cluster.

So there are a lot of concepts to learn like persistent volume claims storage classes persistent volumes.

So a lot of things are involved in this.

So in our next lecture using the detailed architecture diagram we are going to understand it in detail.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, let's walk through what actually happens behind the scenes.

When a pod or stateful set in ECS needs persistent block storage.

So it all starts with the workload.

So when we deploy something like our MySQL statefulset, Kubernetes begins creating the pod.

Because this pod needs a persistent disk, the Statefulset first checks for the persistent volume claim

or we call it as PVC.

So before going and understanding more about the flow, we need to understand on a very high level what

is PVC, PV and storage class.

So let's quickly recap these three components that work together.

So a PVC is nothing, but it is a template which requests the storage.

So in that you are going to define some configurations like I need five GB storage or six GB storage.

And accordingly you are going to define in that.

And if we talk about persistent volume.

PV is the actual storage resource provisioned in the EKS cluster.

And a storage class defines how that storage should be created.

For example, it should be AWS EBS volume with GP2 volume or GP3 volume.

So additional configuration related to that volume can be defined in the storage class.

So there are three things to remember.

One is persistent volume claim, another one is persistent volume and the other is storage class.

So in storage class we are going to define how the storage is going to look like.

It might be a GP2 volumes or GP3 volumes.

So all that configuration information and in persistent volume claim is nothing, but you are going

to request that storage using this PVC.

So you define how much storage you need and that you are going to request.

And when we talk about PV, which is the persistent volume actual storage resource provisioned in your

ECS cluster, we are repeating twice these three things because the whole section is about these three

things only.

So that's the reason we need to understand them and implement them practically.

Now let's go ahead and then understand more about additional resources.

Right.

So before going there, another important thing to make a note of is PV and storage class.

So these two things are cluster scoped resources.

But PVC from where you are requesting your storage right.

The PVC template is always a namespace specific resource.

This is very important to remember the persistent volume, what it creates when you request via PVC.

So it's again a cluster scoped resource.

Even the storage class is a cluster level resource.

Now let's go and then understand about additional things here right.

So something like EBS CSI driver.

So this whole thing whatever we are seeing.

Right.

So this whole thing is the EBS CSI driver related configuration.

So let's assume that cluster admin has already installed the Amazon EBS CSI driver and configured storage

class.

Okay.

So the same assumption, whatever we talked about, we are going to do as part of this section.

1001 so let's understand what exactly this driver is and then why there is one daemonset here and then

one deployment here.

So all those things.

Right.

So this driver right.

So this driver knows how to communicate with AWS and Dynamo and dynamically create the EBS volumes.

So this guy sits in the middle in kube system namespace, which ebs csi driver and he knows how to dynamically

provision the EBS volumes.

So that's the importance of this respective driver.

So it's made up of three key pieces inside the key key kube system namespace one is EBS csi controller,

EBS csi node plugin, and also EBS csi Controller Service account.

So these three things are the things related to EBS CSI driver.

So let's start with the controller.

So what does this controller do.

It's a Kubernetes deployment.

It handles provisioning and attach or detach operations of a volume.

And what about this CSI node plugin.

So it's a Daemonset runs on every node to mount and unmount volumes.

So that's the feature of node plugin.

And another one is Service account.

EBS CSI Controller Service account used by the controller to authenticate securely through ECS pod identity

to AWS EBS APIs.

And then once the authentication is successful, then it will be able to provision the EBS volumes.

So these three are the important things related to EBS CSI driver.

Now let's also talk about security.

So how we are going to securely access our EBS volumes for provisioning using ECS pod identity.

So to connect safely to AWS the EBS CSI controller.

So the controller, whoever is sitting here doesn't use any hard coded access keys.

Instead it uses ECS pod Identity.

So the pod identity agent running as a daemonset in kube system communicates with AWS I through a pod

identity association, which we are going to define, and that association maps the EBS CSI controller

and our IAM role, whatever we are going to create here and the service account.

So service account IAM role and in which namespace our workload is going to run.

It is going to be the kube system namespace where this is the workload right?

CSI controller.

For that only we are giving the permissions.

So for CSI controller it has the service account EBS, csi controller SCA.

So we are going to create a pod identity association with this service account.

And the IAM role created here.

And with the inside this means like this is the namespace where the CSI controller workload is going

to run.

So these things together, we create a ECS pod identity association.

So that association maps the service account to an IAM role that has EBS permissions.

The IAM role, whatever is present here will have the EBS permissions to provision means like create

delete EBS volumes.

So when the controller needs to talk to AWS, this EBS csi controller needs to talk to AWS EBS.

The pod identity agent authenticates and assumes that IAM role on its behalf.

So that's from a security perspective.

Now let's talk about dynamic provisioning in action.

So how it is going to look like right.

So now comes the real magic part.

So when the PVC request storage through a request storage through a storage class, which means when

this PVC requests a storage through this storage class, the EBS CSI controller springs into action.

So it calls the AWS EBS APIs using its IAM role and dynamically creates the EBS volumes and where those

EBS volumes are created in the same availability zone as the Pod node.

So this pod will be scheduled on some worker node, and that worker node will be in some availability

zone.

In the same availability zone our volume will be provisioned.

So that's that's what it is going to happen.

So Kubernetes then automatically creates a persistent volume object and binds it to the PVC.

So that is what is going to happen.

So it is going to bound PV PvE and PVC are binded.

Once the volume is created here.

All of this happens behind the scenes, so no manual steps, no static ML, everything happens dynamically.

So that's the reason it is called dynamic provisioning.

So so far we have seen the forward flow.

So from here like this it has provisioned via this.

And then these things got created.

So it's a forward flow.

Now also see volume attachment and mount things okay.

So once the EBS volume exists the EBS csi node daemonset running on that node attaches the volume and

mounts it to the Pods file system.

So whose responsibility is it to mount that respective volume to this part?

Is this EBS csi node's responsibility.

So now your pod has a durable block level storage backed by AWS EBS, Even if the pod restarts, the

data remains intact on the EBS volume.

So to sum it.

So because we are using Statefulset, we are using the volume claim templates.

Now why we are using volume claim templates?

Because in Statefulset whatever the pod created right.

So each pod is going to be unique.

So used by Statefulsets.

This volume claim template is used by Statefulsets only to create new persistent volume for each pod.

So here for the replicas.

Currently we are using only one.

Okay, but in future if you say three replicas, you are creating for those three replicas if you want,

right?

All these three replicas like MySQL pods are going to be independent for statefulset.

So at that point of time, you need to create each independent volume for each MySQL instance.

So that happens using this volume claim template.

So that's the importance of this volume claim template.

All in all, we have covered all the things from this picture like EBS, csi, node controller, persistent

volume storage class, volume template, persistent volume claim and everything and the flow.

Also we have successfully completed.

So in the next lecture we will see this in action as we deploy our workloads and watch EBS volumes get

created instantly inside AWS.

So actually the workloads we are going to deploy in next demo, which is ten zero two, but here we

are going to focus on in ten zero one, we are going to focus on creating our installing the CSI driver

and also creating the IAM roles and also creating the Pod Identity Association.

So those things we are going to do as part of ten zero one.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


All right.

Now that we have understood how the EBS CSI driver works inside ECS.

Now let's go ahead and then actually install the EBS CSI driver.

So we are entering into the section ten Kubernetes storage.

And in that we are going to enter into ten zero one which is AWS, ebs csi driver install.

So here we are going to create a trust policy file for EBS, CSI driver IAM role as the first thing.

And then once we create the trust policy we are also going to create an IAM role with the trust policy

and also attach the Amazon EBS CSI driver policy, which is a managed policy to this IAM role.

And after that we'll create the Pod Identity Association for the EBS CSI Controller Service account.

And after that finally we will install the Amazon EBS CSI driver add on.

And then we will verify the end to end installation.

So let's go ahead and then first set up some environment variables.

These variables will help us avoid typing long values repeatedly in every command.

So we are setting AWS region ECS cluster name and AWS account ID.

So in your case the region and then cluster name is different accordingly.

Update these commands.

So we'll go to our terminal here and then set those things.

And once those are set we are going to occur them to make sure they are correctly set or not.

So we'll also echo them.

And region is us East one.

This is my cluster name and this is my AWS account ID.

So next we are going to create a trust policy.

So we'll go to this folder IAM policy JSON files in section ten zero one.

So we'll go to section ten Kubernetes Storage.

And in that we'll go to ten zero one.

And we'll go to IAM policy JSON files.

And we'll create this file.

So this JSON file allows exports through the pod identity agent to assume this IAM role which we are

going to create.

So you can see the service principle here pods.x.amazon.com.

So that's what enables pod identity authentication between Kubernetes and AWS IAM.

So now let's go ahead and then create this file for us okay.

And this file is created so you can see it here EBS CSI Driver Trust policy.

And once that is done we are going to go ahead and then create a IAM role and attach the default managed

policy for Amazon EBS which is Amazon EBS CSI driver policy.

So first create a IAM role with name Amazon EKS, ebs CSI driver role underscore cluster name.

So we are also giving it with cluster name.

So let me first create that IAM role.

And once it is created we are going to attach the AWS managed policy name Amazon ebs CSI driver policy

to this IAM role.

So let's complete that okay.

So now once creating an IAM role with the trust policy and then attaching the managed policy to this

IAM role and finally verifying it using list attached role policies, we are going to verify that.

So let's verify that.

And you can see we have the uh for this respective role you have the policy attached is Amazon EBS CSI

driver policy.

And if we want we can also go ahead and then verify the same thing.

So we can go here and go to it and we can verify that thing.

So let me go here and in roles we will search for retail.

And this is the policy IAM role we have created and which should have the Amazon EBS, CSI driver policy

associated with it.

And the trust relationship also added accordingly.

So that's about creating the IAM role, attaching the policy and also the trust policy.

Now we are going to focus on creating the Pod Identity Association.

So this Pod identity association maps the IAM role which we have created.

This is the role Arn right.

And the service account for EBS, CSI driver which is named as EBS CSI controller in kube system namespace

for the cluster.

This in the cluster eks cluster name which we have already set.

So this we are going to create the pod identity association now.

So let's go ahead and then create it.

So once created we can go ahead and also verify the same thing.

So using this command.

Okay.

So where is that I didn't list it here.

So what we'll do is we'll go to our AWS and then verify the same thing.

So here we'll go here and go to access in EKS cluster.

And here you can see this is the one.

Sorry these are IAM access entries.

Let's go down.

We'll find the pod identity associations here.

And this is the Amazon EKS ebs CSI driver role IAM role.

So we can see it here.

This is the association created in kube system namespace for service account ebs csi controller.

So that's about creating the pod identity association.

So now we can verify the add ons whatever installed on our EKS cluster.

Okay.

After that we can go ahead and then install the actual uh EBS CSI driver add on.

Okay.

So first you can go here and verify what currently exists.

Okay.

And you should see only pod identity agent ECS pod identity agent.

So now what we will do is we'll go ahead and then install the eks ebs csi add on.

So the command here is AWS ECS.

And then create add on and cluster name.

This is the cluster name.

And add on name is AWS EBS, CSI Driver and Service account role Arn is our whatever the IAM role we

just now created, Right?

So that is the one.

So let's go ahead and then install this add on.

So we can also install this add on using console also.

So you I'll show you that for you.

So we can go here and we can go to add ons.

And here get more add ons.

And here we will have something called EBS.

CSI driver will be listed here if not installed.

So that one you need to select.

And then you need to select the IAM role whatever we have created and go ahead and then install it.

So we have done that using command line just now.

So once we have installed it.

So we'll go ahead and then verify whether the add on installation is completed or not.

So you can see AWS, EBS, CSI driver add on installation is completed.

So even if you click on the add ons here, in addition to Pod Identity agent, you are going to see

Amazon EBS, CSI driver.

Now the next thing here is we are going to verify the parts for this ebs csi driver.

So cube ctl get pods from namespace cube system and grep ebs csi.

So you can see ebs csi controller related parts.

And we have three nodes running.

So if you say cube CTL get nodes.

So we have the three nodes running three worker nodes.

On those three nodes we have the three EBS csi node plugin related parts running on those three nodes

which is coming from the daemonset.

And these two are from the deployment.

So which is EBS CSI controller deployment.

So which we have seen here in the architecture diagram.

So CSI node three parts are running on three worker nodes and CSI controller which is a deployment.

Currently two parts are running.

So that's about it.

So now let's come back here.

And if you want you can also verify the daemonset here.

So let me run the Daemonset cube ctl get ds in kube system namespace and grep ebs csi.

Right.

So this is the daemonsets running.

We don't have any windows nodes.

So windows currently there is only zero.

Okay.

But for Linux nodes you can see the Daemonset is running on the Daemonset is running the pods on three

nodes and in the same line you can also verify the deployment.

So you can see it here ebs CSI controller.

Two parts are running from two minutes three seconds onwards okay, so that's what is the overall installation

of EBS CSI driver.

So in very simple terms with this installation, our ECS cluster is now fully equipped to dynamically

create EBS volumes for any workload, all handled securely through ECS pod identity.

So in the next section we will put this driver to use by provisioning persistent storage for our MySQL

Statefulset.

I'll see you in the next section.

Our next demo.

Until then, bye bye.

Thank you.


