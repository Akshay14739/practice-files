So in our retail store application sample application.

So we have five services currently present.

And in these five services we will try to take one service from them and then start understanding the

Kubernetes Foundation concepts.

So if we go here so we have something called retail store application catalog microservice.

So we are taking one microservice here.

So this is the microservice we are taking.

And then we will try to implement foundation concepts of Kubernetes using this catalog microservice

application.

So what are the foundation concepts we are going to learn.

So we will start with learning in Kubernetes.

What is a Kubernetes pod and how to use it and all those things.

And then we will also learn about Kubernetes deployment replica sets and all these things.

Then we will also learn about Kubernetes cluster IP service.

After that, we'll also learn about Kubernetes Configmap.

From there, we'll move to Kubernetes stateful sets.

And once we understand about stateful sets and mkdir in stateful sets about volumes, we'll also learn

about headless service.

So these are the concepts we are going to cover as part of section zero eight.

And in section zero nine we will focus on Kubernetes secrets and other stuff.

But for now in section zero eight we are going to focus on these important concepts which come under

Kubernetes Foundation.

So if we go to our course GitHub repository also in zero eight Kubernetes Foundation.

So we can find these sections clearly documented.

So let's go ahead and then start practically implementing all these demos.

I'll see you in the next lecture.

Until then bye bye.

Thank you

Alright, let's start by answering one very simple but powerful question in Kubernetes.

What exactly is a Kubernetes pod?

So because once we get the concept clear, everything else in Kubernetes will make perfect sense.

So let's assume two things before we start the understanding about Kubernetes pods.

So first we already have a Docker image for our application.

So that is our first assumption.

In our case we are going to use catalog microservice from our retail store sample application.

So which means we have our Docker image ready.

So that is one assumption.

So we will say these as assumptions okay assumptions.

So our prerequisites we can say.

And the second thing is we already have a EKS cluster created and ready or a a Kubernetes cluster created

and ready for us.

So these two things are the key assumptions.

Before moving on to going and then practically implementing this Kubernetes Pods concept.

So now the question is how do we actually run the Kubernetes pod?

So if you want to know that we need to understand first what happens in Docker.

So that's the important thing to start with.

So here what we will say is this is Kubernetes side.

And let's say this as Docker side.

So in Docker what happens is you have a Docker image and you go ahead and then using the docker run

command you will go ahead and then create a container.

So it immediately container gets starts and it will go ahead and then serve your application.

So which means container is a single instance of your application.

So if you want to create in Docker write more instances of your application.

So your application is something called CTS.

Or you can say that as catalog microservice CTM something whatever we want we will say CTM as the thing

for catalog microservice.

So this CTM right catalog microservice, if you want to create more instances in Docker, you need to

create more containers like this.

Now what happens in Kubernetes.

So here comes the important thing.

We can't run containers directly in Kubernetes.

So example you have a Docker image.

And if you write try to run as a container.

In Kubernetes this is not possible.

So you can't directly run containers here.

So why.

Because Kubernetes doesn't manage containers individually.

It manages a higher level abstraction called a pod.

So what exactly is this pod?

Let's understand here also you have a Docker image with you and you have something called container,

right?

So now what happens?

What is a pod?

Actually in Kubernetes Pod is the smallest deployable unit in Kubernetes.

It's like a wrapper around one or more containers.

So it's a wrapper around one or more containers.

So in Kubernetes right.

Think of it as a small box that holds your container plus a few extra things.

So inside this Kubernetes pod you have a container and you will have something called network.

You will have something called storage and identity.

So that's about Kubernetes pod.

So when Kubernetes Want to run your application?

It never runs containers directly.

It always creates a pod and that pod runs your container inside it.

So that's the key thing to remember.

So I'm going to reiterate it.

When Kubernetes wants to run your application it never runs the container directly.

It always creates a pod.

And that pod runs your container inside it.

So this way now we have discussed on a high level in comparison with Docker to Kubernetes.

Now what we will do is let's understand that where do these pods run actually.

So these pods, where are they going to run.

So that's the very important thing to understand.

So we have something called in ECS or AKS or any Kubernetes cluster.

So we will have something called worker nodes.

So let's say this as worker node one and this as worker node two in Kubernetes.

All pods are scheduled on worker nodes, so the Kubernetes Scheduler decides which node has enough resources

like CPU and memory.

And based on the availability, the pod will be scheduled on those worker nodes.

So if your cluster has two worker nodes, Kubernetes might put your first catalog pod on your node one.

So you can say this as pod and you have a container.

And this is it has put your pod one on your worker node one.

So in the same way if you have one more it will also put here something like same catalog pod part two.

And if your cluster and if this node one is completely full which means its CPU and memory is full,

right.

So the next pod may go to the node two.

So it will schedule the another pod in node two.

So in simple terms, Kubernetes automatically spreads them out.

Your pods will be distributed across Kubernetes worker nodes automatically.

It's the job of Kubernetes to take care of it.

So also let's talk about scaling and load in Kubernetes.

So now imagine your catalog service is getting a lot of traffic.

What do we do.

We simply create more pods.

So Kubernetes can easily scale from one to any number of pods based on the capacity of your worker nodes

you have provisioned.

And another important thing to understand is each pod runs an identical copy of your container.

So in Docker we said that container is the single instance of your application.

In the same way, in Kubernetes, pod is your single instance of your application, so each pod will

run an identical copy of your container.

Your catalog application.

So that's about from scaling and loading perspective.

Now we also need to understand one important concept here which is one container per pod.

So which we already discussed one container per pod.

So why is it important.

It is a very best practice.

Which means in your pod you should always run only one container and one instance of your application.

So example like so.

This is the right way of doing.

And if you take let's say this as CTM which is catalog microservice application.

So this is the container we are running.

So this is the container we are running in a pod.

Now this CTM is the container.

And this is also a CTM container in a Kubernetes pod.

So for our catalog microservice don't run two containers.

So this is container one and container two in the same single pod.

So this is a not recommended option.

So this is the recommended option.

So if you want to have one more instance of your catalog microservice then definitely you need to go

and then create a new pod with CTM container.

So this is one part and this is another part.

But this approach is not recommended at all.

This is the recommended approach.

So each pod represents one instance of your application.

So you might ask me a question.

Hey why.

Right.

Why only one container in your pod.

So you already said one or more containers can be run as a pod at the starting of this video.

Yes, but the usage is different.

Not your same application.

Containers running in same pod.

Multiple.

Okay, so we should not run multiple containers of your same application in a single pod.

But there is a concept called multi container ports.

So that is a concept which is if we if we understand that in detail.

So we will understand why means like where is the case where we need to run multi containers in a single

pod.

So let's take this as one pod.

And you are running something called c t which is your catalog microservice container inside this pod.

And what is the case where we are going to run another container in this pod wherein you can say it

as a multi container pod.

Right.

Sometimes you will see these multi container ports in your Kubernetes deployments.

That's usually for helper or sidecar container.

So you are creating here something called you call it as either helper or sidecar container.

So for example you have something called a logging agent or something like your envoy proxy, which

is uh, related to your Istio service meshes and all those things.

So these can be added as a additional container in your pod.

So they work alongside the main container.

So this is the main container.

And this is called the sidecar container.

And they share the same network and storage.

So these share the same network and storage.

So because it is sharing the same storage as catalog microservices container.

So your logging agent can get the logs from here from your storage and send it to your centralized logging

systems like CloudWatch.

Right.

So that is one way in the sense like running sidecar container.

Use cases.

So the golden rule here is for your main app.

It is recommended to have one container per pod for special helper processes.

So in your application for your catalog microservice itself you have some special use case.

Like if you want to use a sidecar containers for your Istio service mesh or whatever it is, at that

point of time, you will have multiple containers per pod based on that special use cases only, but

always one container per pod is the recommended approach.

So let's recap what we have done so far or what we understood so far.

So Kubernetes never runs containers directly.

It always runs Kubernetes pods and a pod is a smallest deployable unit, and each pod can contain one

or more containers.

Usually, one main container is the recommended option, and these pods run on worker nodes, right?

And when load increases, we add more pods for our application.

And if one node is full, Kubernetes automatically schedules new ports on another node.

And when we talk about multi container ports are for sidecar or helper patterns.

So now that you have a crystal clear understanding of what is a pod and why it exists, let's actually

go ahead and then create one.

In the next video we will write the pod manifest for our catalog microservice.

Line by line and see it come alive inside our ECS cluster.

So let's jump right in.

I'll see you in the next lecture.

Until then, bye bye.

Thank you

Alright, let's get into the demo now.

Zero eight Kubernetes Foundation.

And inside that 0801 Kubernetes Pods.

So in 0801 we have the catalog manifest folder.

And in that you can find the zero one catalog pod YAML.

So let's discuss what exactly is this YAML manifest in detail.

And then we will go ahead and then deploy.

We'll go line by line through our catalog microservices pod manifest.

And by the end of this lesson you will be able to confidently read and write any pod YAML on your own.

So let's start with the API version one as the first line in our YAML manifest.

So this tells Kubernetes which version of the API this manifest belongs to.

So every manifest starts with an API version.

So it's how Kubernetes knows which schema to use.

So now coming to the line two we have something called kind and then pod.

This specifies what kind of resource we are creating.

Here we are creating a Kubernetes pod which is the smallest deployable object in your Kubernetes cluster.

And here you can define means like based on your uh, whatever the resource we are creating, we need

to define that here.

Here it is part.

But later we will learn about deployment and a service.

And configmap are even uh, we can say like stateful set.

So like this different Kubernetes resources.

We will be defining where at this location.

Right.

Based on the kind, whatever you have defined its specification you need to define.

So kind Here we are defining is pod.

So it's equivalent details.

We are going to specify in the Pod specification.

So next we need to discuss about metadata blocks.

So this whole thing is the metadata block which gives to our pod an identity.

So we have named our pod as catalog Pod.

And we have added a label app catalog here.

So labels are super useful.

And when we are learning about deployments and services we will go in deep about this labeling thing.

But here the metadata block gives the identity to our pod.

So you define the name and you define the labels.

Now let's move on to something called which is specification.

So your Kubernetes pod specification the specs section describes what this pod will run.

So here we define a list of containers that belong to this pod.

So here you can see containers in containers.

Again this is a list item.

So why containers is a list item.

Because we already discussed right.

In a pod you can run one or more containers.

So which is you can run multi container ports also, which means instead of one you can also run two

and then three containers.

But usually we will run only one container which is our main container in the 99% of the cases.

So that's the reason it opened with hyphen means like it's a list item.

And we are creating our first container inside that.

So this container right.

So we have named it as catalog.

So we have provided its name as catalog, and it uses the official retail store image from AWS ECR repository

with version so and so.

So where we are defining our Docker image.

So this is the image you are defining here.

And Kubernetes will automatically pull this image when creating the Kubernetes pod.

So you define the pod name and also which Docker image it need to use.

After that you have another, uh, argument here, or another block which we call it as ports block.

Right.

So here, uh, this tells Kubernetes that container lesions on port 8080 using TCP protocol.

When we later expose this pod through a service, it knows where to route traffic to.

So but for now, Let's understand and be to specific.

That container listens on port 8080 and that we are defining here.

So we will learn more about this container port mapping with the service port when we are learning the

services concept.

Right.

And now another very important concept in Kubernetes which is resources.

And in that one is limits and the other is requests right.

So let's start with the requests right.

So if we define till here our core pod definition is already complete okay.

But we are also added some additional things here.

One is resources and the other is readiness probe.

So we'll discuss those also right.

So requests define the minimum guaranteed resources.

So minimum guaranteed resources that Kubernetes will reserve for this respective pod.

So here we are saying the container hundred mil cores of CPU.

That's one tenth of a core and 128MB of memory, which is guaranteed for this Kubernetes pod, right?

If that much, if that much capacity isn't available in your Kubernetes node, the pod won't will the

pod won't be scheduled on that respective worker node.

So that's about requests.

So we also have something called limits on the other hand which define the maximum resources the container

can consume.

So these are minimum and limits is nothing but maximum.

In our case we have defined 200 200ml of CPU and 256 MI of memory, even if the node has a spare capacity.

This container can't exceed that ceiling, so which means this is the maximum this pod can use.

So a worker node is of four CPU means like four core CPU with 16 GB Ram.

So let's see.

So now only if 200 M and then 256 MI is consumed.

Still it has lot of capacity.

This pod cannot consume, so this pod cannot consume whatever the spare capacity it has, it will limit

to whatever we have defined.

So why we need to define that way?

This prevents one pod from hogging all the system resources.

It keeps the cluster stable and fair.

you can imagine.

Request means reservation limits means speed limit.

It can max.

Go.

Now let's talk about another thing which is readiness probe.

So this readiness probe block right.

So defines a health check that tells Kubernetes whether the application inside the container is ready

to serve traffic.

So Kubernetes will periodically make an HTTP get request to this respective endpoint slash health on

port 8080.

If it gets a 200 okay response, the pod is marked as ready and can start receiving traffic through

services or anything fronting this respective pod.

If it fails Kubernetes or not, send any traffic until it becomes healthy again.

This ensures users never hit a half initialised or broken pod.

So in short, a readiness probe means is my app ready to serve traffic?

Later we will also learn about Liveness Probe.

But for now, we'll learn this readiness probe.

Readiness probe means is my app ready to serve traffic?

So that's about the Kubernetes YAML manifest.

In the next part we will learn this YAML.

We will apply this YAML to in our or deploy this YAML to our in our EKS cluster.

Verify that Pod runs successfully.

We will also view the pod logs and also use port forwarding to access the catalog microservices application.

And also we'll connect to this pod using kubectl exec and browse the file system inside this respective

pod.

Okay, so all those things we are going to do.

I'll see you in the next lecture.

Until then, bye bye.

Thank you

Before going and deploying the Kubernetes pod.

We also need to understand the Kubernetes API, right?

So we have already discussed the Kubernetes pod deployment YAML manifest, right.

But you might get the doubt from where we are writing that YAML manifest.

So where the complete definition is written right.

So there comes the Kubernetes documentation.

So this is the link for Kubernetes documentation.

And in the API overview section in the references in API overview section you can find the Kubernetes

API reference.

Right.

So this is the API overview.

So when you click on this Kubernetes API reference link.

So it will take you to the this page okay.

So here you will find all your API related information means like every Kubernetes resource information

we are going to find here.

So we are talking about currently pod okay so which is a Kubernetes pod and it belongs to.

So if you see Pod it is saying v1 core which means it belongs to the core APIs and its version is v1.

So if we go back to our catalog pod see API version is v1 from the core APIs.

And here you can find when you click on the show example you will find the example here.

So how to define that API version v1 kind spot.

In the metadata name you can provide your pod name, and in the pod specification, containers is a

list item and you can provide your container name, your desired name, whatever you want here.

And in the image section you provide which image you want to create the container with.

And whatever the things additional things you want to allow, you can allow.

So here is the complete specification for that okay.

So when you click on this metadata so it's object metadata information will be there.

This is the Pod specification.

So in Pod specification you have all these things you can define inside that these many arguments you

can define.

But in our case whatever is required few will be mandatory if you will be optional.

So based on whatever is required for us you can see optional optional default to false.

So like this it will be defined.

So there you can review and use based on what what needed in our requirement.

Or there is a special use case came to you and whether Kubernetes pod supports or not.

Also by reviewing this Pod specification you can understand all those things.

So this is the way you need to reference your documentation.

So let's go ahead and then deploy the Kubernetes pod in our EKS cluster and verify I'll see you in the

next lecture.

Until then bye bye.

Thank you

Alright, now that we understood our Kubernetes pod manifest, let's go ahead and deploy it in our Kubernetes

cluster.

So first thing is in your zero seven Terraform EKS cluster, you have your VPC terraform manifest and

zero to x terraform manifest.

So you need to go ahead and then create your EKS cluster if not created.

So you can run the command create cluster to also create the VPC.

And then EKS clusters using a single command.

Or you can go to zero one terraform manifest and run the command terraform init validate plan and then

apply.

And after that come back to zero to x terraform manifest and also run the command terraform init validate

plan and apply to create the EKS cluster.

So I have given here the very basic scripts.

Not that it is 100% functional, but for learning, right?

So it will go ahead and then create a VPC and after that it will go ahead and also create an ECS cluster

if not created.

And by end of the day, whatever the demos you have implemented, you want to delete the resources.

Again, you can run the command destroy cluster, which will go ahead and then first delete the ECS

cluster, and after that will also destroy the VPC.

Now our core thing here is we'll go to zero eight Kubernetes Foundation section.

And inside that 0801 Kubernetes pods.

And here we said that in catalog K8S manifest we have the zero one catalog pod dot YAML file.

So the prerequisite here is we need to first verify the Kubernetes cluster is created.

When you run the command cube CTL get nodes.

You can see all these three worker nodes are up and running in our Kubernetes cluster.

So what I will do is I'll say cube CTL apply hyphen f and zero one catalog four dot YAML.

So you should see the message now.

Pod created.

That means Kubernetes has accepted our YAML.

Scheduled a pod and is now pulling the container image from AWS ECR.

So behind the scenes, the scheduler checks which worker node has enough CPU and memory based on our

requests, and then places the pod on that node so you can run the command cube CTL get pods.

You should see your pod listed with status running and the ruddy.

In the ruddy column you can see one by one, meaning one container inside the pod is up and healthy.

If it says container creating, if the status is in the container creating state, so which is nothing,

but it is pulling the Docker image and give it a few seconds and check again.

And then it should be in the running state.

The most important command for Kubernetes pods is kubectl describe command.

And you will say kubectl describe and what you are describing, which Kubernetes resource you are describing,

which is pod resource and what is the pod name.

So we can copy the pod name from here which is catalog hyphen pod.

And this will describe this command gives a detailed report.

You will see which node it's running on, what image it pulled, the events during startup, and even

your readiness probe configuration.

So this is one of the most important troubleshooting commands you will see every day in Kubernetes kubectl

describe.

So let's see what exactly that means.

See your pod name and in which namespace it has displayed.

And what is the service account it is using.

We didn't define anything.

It is using the default and in which node it is running.

See this node.

In this respective node it is running?

What is the start time?

What is the label and what is its current status?

And what is this pod IP addresses and what is the container information.

So container ID and Docker image image information and image ID and port.

It is listening on and host port and all the information you can see here.

And the requests and then CPU limits whatever it has defined.

And this is the readiness probe configuration.

Right.

And you have given the defaults means like do HTTP get on health end point.

But additionally the timeout period and all those things it took by default whatever is present.

And in addition to that you can see the volumes, information, conditions, information.

All these are defaults and not only the information.

Whatever you are seeing here in the Kubernetes describe command for any resource, you will have something

called events.

Most of the resources.

Okay, mainly for the pods.

You will have the events here.

And this is very very important from troubleshooting point.

Why?

Because it will show you what happened actually.

So what it said is Kubernetes Scheduler successfully assigned this Kubernetes pod.

Right.

Default catalog pod means like it is default namespace.

And in that catalog pod to this Kubernetes node.

So once it is assigned to that Kubernetes node, it started pulling the Docker image.

Whatever is specified in Kubernetes pod YAML manifest.

And finally it pulled that Docker image.

And after that it created the container catalog inside that Kubernetes pod.

And also it started that catalog container.

So that's what happened on a whole.

Now we will also see the pod logs how to see that?

We need to first say cube CTL get pods.

To get the pod name.

So this is the pod name.

And we'll go here and say cube CTL logs and hyphen f and catalog pod.

So you will see something like uh uh using in-memory database running database migration etc. etc..

That means the app is initialized and ready.

So our catalog microservices, right?

By default it will use the in-memory database provided if we don't configure any external database to

it.

So there is no external database configuration provided it started using the in-memory database.

Now the next step is access our application via port forward.

So we still didn't learn about Kubernetes services concept and all those things.

So how to access our catalog microservices application deployed in the using this pod.

Right.

So that's the uh there comes the important concept called port forwarding cube CTL.

And we will say port forward, right.

So this port forwarding concept temporarily maps a local port on your local desktop or laptop to the

container port inside the ECS cluster.

So that's what this kubectl port forward command will do us.

Okay.

So we'll say kubectl port forward.

And we are using here port slash catalog port.

So we are doing this for a Kubernetes resource of type pod.

And what is the resource name catalog pod.

And on my local desktop I am using 7080 means like from my local desktop, 7080 should be mapped to

container port 8080.

So once you see the message forwarding right, so forwarding so and so, so and so, you will see the

message.

Once you see that, we can open the browser and test a few endpoints.

So from our local desktop we can access localhost colon 7080 slash few endpoints defined for our application.

Right.

So we have something called different endpoints in our microservices application which is catalog.

One is we will start with the health endpoint and health will give us a return okay.

For us.

And in the same line we have something called.

So we can copy this.

And we have something called topology endpoint.

So these are all application endpoints for catalog microservice.

And it will show us the topology endpoint.

And you can see it here I'll try to put this this way.

Right.

So this is the health endpoint which gave us okay.

And this is topology endpoint.

It is saying no database endpoint.

So it is using the persistent provider as in-memory.

So in-memory database it is using because we didn't use any external thing.

So after that we will also have something called slash catalog.

Slash products catalog.

Slash products.

And it will provide us the list of all the products defined in our catalog microservices application.

Okay.

So these are the default things provided.

So now if you go back here to our terminal here.

So these are the things in the kubectl pod uh port forward which is happening here in same line right.

So I'll try to open this right.

So let the port forwarding go.

It will go.

And in the another tab.

So we'll go ahead and then say kubectl logs hyphen f and then catalog hyphen pod.

And in the log you should see topology endpoint accessed successfully.

And you got 200 and catalog products accessed it successfully and 200.

But for the health endpoint, what happened is even if you access, that will not be logged because

when the catalog microservice application is written right.

So they have ensured that why continuous health related logs should be logged in our application.

And then they ignore that.

So that's the reason you are not seeing instead of health, if you try to put topology then you will

keep on continuously seeing that we will redeploy our thing and then verify a little later.

We'll see that for sure.

But for now, let's put it aside.

Okay.

Now we have accessed the catalog products.

Next if you want to access a specific product okay.

So you can copy this ID.

And first we'll copy this catalog products products by ID.

And this is the ID.

So I'll copy this ID slash ID.

And you can see that specific ID is shown here.

So you can see that here.

This is the ID and this is the topology information.

This is the health information.

In the same way you have something called sys okay catalog sys.

So we'll go here.

And inside catalog we will say sys I think okay.

See sys is 12 records present here okay.

And in the same way we have something called catalog tags or just tags I don't remember.

Let me try that okay.

Catalog tags okay.

In catalog tags.

So these are your tags to which your respective products were mapped.

This is application related information.

But all these APIs are accessible now using port forwarding concept which we have used here.

And this is the equivalent log in our catalog port.

We have seen all the logging inside our catalog port.

We will move on and then verify how to connect to this respective pod.

So I'll try to open a new tab here.

So in one thing we have the port forward.

And one thing we have the logging.

And I have opened another terminal tab here.

And I will use the command cube CTL exec hyphen it and catalog hyphen pod is my pod name.

Hyphen hyphen space.

Mine is a Linux container catalog pod.

So I am going to open a shell inside the running container.

So this drops us right into the containers terminal, just like sshing into a VM but much lighter.

So here you can type commands and uh, verify your application and everything inside that.

So all those things you can do.

So that's about kubectl exec command.

So instead of uh running hyphen hyphen sh you can also instead of directly connecting into the terminal

and then executing using a single command directly.

Also, we can execute here.

So you can run LHS here and it will display these things.

Or if you want E and V in your container what are the environment variables defined.

You can run this way and you can show all these are the things it is defined inside that environmental

variables from your container right now.

How to delete this part.

You just say cube CTL get pods right and you will say cube CTL delete pod catalog hyphen pod.

And this pod will get deleted.

And one thing we also need to verify is this one instead of health.

Right.

We said that we'll try to use topology and then see what happens.

Right.

So I'll try to use that okay.

So it what means like to learn about the readiness probe.

We are doing that okay.

So one more time I'm going to say.

So I'll try to stop this.

And all this stopped.

Now I'll say cube ctl apply hyphen f and then zero one catalog pod dot YAML.

So what we have used now the health check endpoint is slash topology for readiness probe.

Now if I go back here and try to verify the logs now right.

So you can see continuously the topology thing.

Right.

So this is the topology thing.

We still didn't access anything.

So we are getting it continuously.

So what exactly is happening here.

Right.

So what exactly happening here is those are from our readiness probe that we configured earlier.

Kubernetes keeps calling that endpoint to make sure the app stays healthy.

So if you use topology it is showing continuously.

So.

But in logs it becomes heavy.

Right.

So every five seconds or something it is hitting every 10s it is hitting.

So it becomes heavy.

So for that purpose, the health check endpoint, whatever we are going to use in readiness probe in

the go application, in the catalog microservices application itself, they have ensured that it will

not do any logging.

So that's what the importance of health endpoint and without any logging.

So I'll try to comment the topology here and I'll stop this and I'll go ahead and then delete our catalog

pod.

Right.

So you can also use the file to delete it.

You can give kubectl delete and then give the pod name kubectl delete pod and then pod name or delete

hyphen f and then provide the YAML file to delete that respective pod.

So this completes the demo about Kubernetes.

In the next section, we will evolve this single pod into a deployment.

So Kubernetes can handle scaling, rolling updates, and automatic restarts for us.

I'll see you in the next video.

Until then, bye bye.

Thank you

Alright.

In this lesson, we are stepping into one of the most important concept in Kubernetes, which is Kubernetes

deployment.

So you have already seen how we can run single pod in our cluster.

But in the real world we never run critical applications as individual pods.

Because pods are temporary, they can crash, restart, or disappear when the node goes down.

So let's understand what is the problem with these pods?

Before going into understanding the Kubernetes deployments, or also learn the problem with Pod and

how Kubernetes deployments solve those pods.

Okay, so we'll take the three pronged approach here.

So we'll discuss three important things.

Okay so one is so we'll start with the first one which is node.

So we'll start with this one.

Okay.

So let's take a Kubernetes cluster.

So we'll have simple cluster.

So EKS cluster right.

And you have something called a node one.

So you will say this as node one.

And you will also have something called node two.

Right.

Worker node one and then worker node two.

So you have deployed a individual Kubernetes pods p1, P2 and P3.

Okay.

So now what happens here is during means like these node events will have different things okay.

So you say a as OS upgrades nodes can go for OS upgrades or they can have some scale ins.

Okay.

And also they can have some spot interruptions.

So various events happen on these worker nodes, right?

So let's start with OS upgrades, right?

OS means like you want to upgrade these worker nodes to the latest operating system.

So at that point of time what Kubernetes does is it will schedule the ports, whatever is present in

current node to another node, right?

Provided they are managed with a some type of controller like deployment.

But when they are managed as a independent ports.

Right.

So if you are managing ports by hand, right.

So what happens is when this node goes for draining.

So when this node goes for draining means like it will delete this node and recreate a new node with

patched version.

So at that point of time Kubernetes will kill these pods, but they will never get recreated in another

node.

So that process will not happen when you manage them as pots.

So you need to manually.

So you need to manually go ahead and then create them here.

So that's what we need to do if if these nodes go into draining state okay.

So that is one disadvantage.

But now these pots get evicted and this node gets drained.

But if you are managing these pots via deployment.

So if you are managing via deployment in deployment there will be a setting called replica count.

So there you will define I at any point of time need three pods.

Okay.

Replica count will have the number of pods your application needs at any point of time.

So during that process what happens is it will ensure.

So this is the deployment controller when this node is going in assuming like we have this deployment

controller managing this P1, P2 and P3.

Right.

Three parts okay.

And now when this node is going for draining process.

Right.

For OS upgrades or scale ins or whatever spot interruptions, whatever these cases.

Okay.

Right.

At that point of time, this deployment controller will ensure that he will go ahead and schedule the

parts in another node wherever the system resources are available.

So these get created on new worker node.

So that's from node events perspective.

Now we will also see something called another thing which is a resource pressure.

So resource pressure in the sense like you might have app spikes or memory leaks in your application

okay.

Or whatever it is or disc pressures.

So whatever it is, right?

So what it does is it will evict your Kubernetes pods due to these things.

So with plane pods these means like when you are managing them as plane pods.

So there will be different different things happened example like when you have low disk.

Right.

So this is disk pressure.

Too much memory you call it as over killed.

So which means out of memory.

And then that pod gets killed.

So using all these things your pods might get killed during these events.

Also resource pressure events.

Now there also you need to manually go and then recreate the pods in different nodes if needed.

So that is another case.

And there is one more case which is high traffic periods.

Right.

So but when you talk about deployment you have the replica count.

It will go ahead and then create.

But now let's also talk about something called high traffic periods.

So in high traffic periods.

Right.

So which means like you can say like 8 a.m. to something like 6 p.m., I might have high traffic, okay.

Or for my applications or during some sale is going on like Amazon sale and it might be like run for

three days.

And these are high traffic periods.

And at this point of time, if you want to increase your number of parts, you need to do it manually

if you are running parts independently.

So as a user, you need to deploy more and more parts.

But when you do it via deployment right, you will just you either you can go and then update your replica

count to from 3 to 100 and just deploy them, and you will have all the 100 parts Created immediately.

So you don't need to maintain these 100 pod YAML files.

This is not needed in deployment, so all those will get created.

Again.

If you want to scale in.

Then you can just say back to three and then it will be done.

So this is one way.

And in addition to that we will also have something called right.

So this is your deployment.

Plus you have something called horizontal pod autoscaler.

So these two things together you can auto scale your autoscale your parts okay.

Based on based on metrics okay.

So metrics like CPU okay memory or even custom metrics.

So using custom metrics.

So deployment plus HPA together still powerful.

Why?

Because during the 8 a.m. to, let's say, 6 p.m. high traffic periods from 3 to 100 replicas, it can

go.

And from low traffic periods again, it can come down to write again three replicas.

So all these things is possible when you use Kubernetes deployment.

So that's the advantage of using deployment.

There is some other important things also available in deployment.

One is rolling updates okay.

So which means you will have a zero downtime zero downtime to your applications.

Why.

Because so example you have a application running with P1, P2 and P3 pods.

So it will only based on the configurations you have done.

Example you said only the 33% of traffic should go for Upgrade.

Okay.

It means like in a rolling update fashion.

So it will first update this P1 and means like it will recreate a new part P1, right with a latest

version.

So it will create it with P1 latest version and then it is still in the mix.

You are getting traffic to it okay.

Right.

And later once this is successful, then it will go to this one and it will create a new part here with

P2, right with the latest version.

And kill this and later it will go here and create new version here and kill this.

So now you have this is V1 version and this is V2 version.

So you have all three available.

So like this it can do the rolling updates also.

So that's the advantage of using deployment right.

So we already seen self-healing option.

Like whenever something happens to that part it is having a problem with that part when that pod is

deleted or it is hung or whatever happened.

So Kubernetes deployment will ensure that it creates a new pod in that problematic pod place.

So that's self-healing.

So we have discussed self-healing and rolling updates.

And in addition to that there will be a easy rollbacks okay.

So if you want you can immediately roll back to the older version.

So if you see anything problem you can do that easily.

And you will also have a history and audit information also available when you are doing with Kubernetes

deployments.

Right.

And all these are the features available in Kubernetes deployments, which overrides uh, using plain

pods.

So that's the importance of using deployments.

I'll see you in the next lecture with a practical implementation for these deployments.

Until then, bye bye.

Thank you

All right.

Let's go through this deployment YAML file step by step and understand every piece of it.

So before going in we are in zero eight Kubernetes Foundation.

And we are entering into the demo 0802 Kubernetes deployment.

Here in the catalog as manifest, you have the actual deployment file.

So you can see that here in 0802 you have the catalog deployment dot YAML file.

So we are going to review this in detail.

So before that so we'll go here and we'll go to the API overview.

And here you can see the deployment right.

So in this deployment right.

So if we let okay.

So if you go to this uh where is it.

So this is the deployment and it can show the example for us okay.

So this is the sample deployment.

Very simple one.

Right.

And it belongs to the group apps.

And version is v1.

Kind is deployment.

So if you go to the pod right.

It said API group is core version is V1.

And here is the pod okay.

Now here also it has said about the pods you can see right.

It is recommended that users create pods only through a controller and not directly.

So there comes the concept called deployment either using deployment job or Statefulset.

You should not create pods independently, right?

So now let's come back to deployment here.

And here is the example of this respective deployment.

And here group is apps.

And you can see all the information about those deployment spec okay.

So you can open the spec and review the spec and all those things.

So whatever is there here we are going to discuss in detail here okay.

Now let's start with these two, which is API version and then kind.

So this tells Kubernetes that we are creating a deployment object which belongs to the apps v1 API group.

The standard for all modern deployments.

So you have seen the apps group.

And in that again v1 version we are using this.

So that's about API version.

Version apps v1 and kind is deployment.

And next comes the metadata block.

So this whole thing is a metadata block.

Okay.

So now the metadata section gives our deployment a name.

Name is catalog.

And a attaches a simple label app.kubernetes.io/name equal to catalog.

And these labels are very very important because they are used to connect this deployment to its replica

sets and then pods.

Okay.

So now let's come back to spec section.

So this is the deployment spec section.

You will also have something called pod specification that comes later.

So this is deployment spec.

And here you have the pod specification.

This is pod spec.

And this top one is the at line number seven you have the deployment spec and line number 20 you see

the pod specification.

So now in the deployment specification one very most important thing which we need to focus on here

is the uh replicas okay.

We define how many replicas or copies of our pod we want.

We have just defined one.

But we can define any number so that this deployment can create that many number of Kubernetes pods.

So we will start with a single running instance of a catalog microservice.

And later we will go ahead and then scale them.

So that's about replicas.

And next we have something called strategy.

So this part comes under strategy.

Okay.

So this defines our update strategy.

So we are using the default option which is rolling update.

That means whenever we update the image or configuration Kubernetes will gradually replace old parts

with new ones with new versions.

The key setting here is max unavailable.

So that is the key setting, which ensures that a at most one pod can be available during the update

so your application stays up without any downtime.

So this max unavailable ensures that at most one pod can be unavailable during the update.

So that's the key thing.

So one only one pod can be unavailable.

So if you have three parts running right.

So like P1, P2 and P3.

It will ensure that only this part, one part will be unavailable during the upgrade.

And this will be the upgraded version.

Okay so latest version something like v2.

So once this is upgraded then it will go to the next one after this is upgraded.

So P2 v2, then it will go to the next one which is P3 v2.

So one at a time it will do so if you have 100 parts.

And if you want to do five at a time.

So you can say max unavailable as five.

So like that.

So how you want to define not only the number of parts, you can also define the percentages.

Also 33% of the traffic I want to upgrade now, etc. like that.

So now next comes the important thing which is selector labels.

So this is another important thing in Kubernetes.

It tells the deployment which parts belong to that respective deployment.

Any pod that has this same label name, right.

See the same label name will be managed by this catalog deployment.

So which means the pod.

The deployment to pod relation is based on means like how it can manage these pods is based on these

labels.

Whatever you are defining that means those are very very important.

So based on that only deployment can manage those pods.

So now next comes the important thing which is pod specification template.

This is the second spec which we talked about.

So which means from here on.

So from here on you have the pod specification template.

So here it starts first and goes to this pod specification.

So this is the complete pod specification template.

So now we will start with the template here.

And in that pod template also you have the metadata section.

So we specify here metadata spec and containers in detail in this pod specification template.

So whatever we specify in this template going forward when we go down we will see all those things.

So whatever we specify inside this template from here to from line number 16 to below, that becomes

the blueprint blueprint for pods created by the deployment.

So that's the important thing to remember.

So whatever you are defining here from here on, from template to below is the configuration information

for your Kubernetes pod and whatever above is your deployment related configuration example, how many

replicas you want to create or how many, how you want to do the upgrade, rolling, upgrade or whatever

it is.

Right?

And what are the labels it need to apply for the pods?

This deployment is creating all those things we have seen on the top.

But from this pod specification template onwards, whatever we see, those are related to Kubernetes

pod.

So this is the Pod metadata information.

We have given the same labels as what we have given above.

And now we will go down and then discuss some important things here.

So we will start with in the pod specification.

The first one is security context.

So this security context ensures that any any volume mounted inside the pod are accessible by user ID

only, which is our Non-root application user which is our app user.

So root user don't have any access, so we have straight away removed that.

And in addition to that we have something called container name catalog.

So in the containers again a list item, we are creating our first container whose name is catalog.

And next comes the security context we have defined here.

So here we are applying the strict container level security.

So this part whatever you are seeing here okay.

So this part right we are applying the strict container level security.

So here what we are doing is we are dropping all Linux capabilities and running as and run as Non-root

user only and make the rule root file system read only and run as Non-root user.

And who is that user thousand user which is a app user.

So these are done to keep our Power containers safe and isolated.

So from security perspective, we are 100% safe.

So like that we are defining these things.

Why?

Because we are implementing a real world application here.

It's not just plain, uh, what we call plain simple deployment spec, whatever they have given here.

Right.

So in the example we are not doing that.

Okay.

If you see this is a simple spec, it's not the case here.

So we are focusing on implementing a real world application.

So for that purpose we are ensuring in all angles we are covering everything or not.

So here we'll come to this uh image and image pull policy.

So we have defined our image here.

So this is 1.0.0 we are using to start with.

And later we will try and use other things okay.

So here which image we are using.

We are using the retail store sample Application Catalog 1.0.0.

We are using and we are also saying that image pull policy if not present, this tells Kubernetes to

reuse the local copy if it already exists.

Avoid.

This avoids unnecessary pulls from the Docker registry or ECR Elastic Container Registry.

So this is one very important setting.

And next thing is ports which we have discussed earlier.

Also when we are discussing the Kubernetes ports.

So we are defining the container port with TCP.

And we also discussed the readiness probe.

Also when we have discussed the Kubernetes pod, the readiness probe checks the slash health endpoint

to see if the application is ready to serve traffic.

Only after this probe succeeds, it will the part starts receiving requests from the front end.

The front end might be a Kubernetes service, so it will ensure that only if this application is healthy,

then only it will allow the traffic coming from the front end.

Whatever it is, it is a Kubernetes service we can call.

We will discuss about Kubernetes service in detail in our next demo.

And in this liveness probe we will discuss now.

So this is the liveness probe we need to discuss.

And it performs the same HTTP check what we are doing for Readiness probe.

But this one is about ongoing health of your application.

So this is ongoing health okay.

So if this probe fails repeatedly so if it fails repeatedly Kubernetes will automatically restart the

container.

So it will restart the container.

So keeping our app resilient and self-healing.

So that's what it is going to happen when we use the liveness probe.

And in addition to that we also have something called resources and requests.

So which we have discussed when we are learning about Kubernetes pod in detail, the same thing, the

same Pod specification we have put here in deployment more or less.

Additionally, we have added the liveness probe and some security things here in detail.

And this completes the deployment manifest for our catalog microservice.

It's uh now next we will apply this file to or we will deploy this file in our EKS cluster and verify

how Kubernetes creates the deployments replica sets and pods.

And then we will also explore scaling, rolling updates and rollbacks, all those things in a detailed

manner.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.

All right.

Now let's deploy our catalog microservice using Kubernetes deployment.

So we'll go to 0802 Kubernetes deployments.

And in catalog manifest we have our catalog deployment dot YAML file.

So we'll say cube CTL apply hyphen f and zero one catalog deployment dot YAML.

So this command tells Kubernetes to create everything defined in that YAML the deployment the replica

set and the first part for our catalog app.

So if the apply is successful we will see something like deployment uh catalog created okay.

So now next go ahead and then verify what we have created.

So we can say cube CTL get deployment.

And it should show that deployment for us.

So whenever we are verifying the deployment we also need to verify the replica set.

So we are going to talk about this concept shortly in another 2 to 3 minutes.

But this deployment actually creates the replica set.

And replica set will go ahead and then create the pods for us in Kubernetes deployment.

So let's understand that here on a very high level.

So always the flow is like this.

So deployment to replica set replica set to pods.

So you can talk like this okay.

So uh this is the deployment and which creates a replica set.

And this replica sets takes care of creating the pods okay.

So p one and p three.

It's the responsibility of replica set to manage the number of pods defined in your replica.

If it is three replica count equal to three, then it manages this.

So deployment manages replica set and replica set manages these Parts.

Okay.

So that's the key thing here.

So we'll understand more about it when we are doing more and more things with that.

So now we'll also run the command kubectl get pods.

It is also clear there is one command you need to know which is kubectl rollout and status.

And you can provide deployment slash catalog.

So this confirms if the deployment is successful or not.

If you whatever the deployment you have done it is successfully rolled out or not.

Also you can see using rollout status command.

Now the very important command which we said in Kubernetes when we are learning about Kubernetes pod

is Kubernetes kubectl describe okay.

And we can describe our pod now.

So pod and then we will also provide our pod name.

So this is the pod name we are providing.

So we will provide the pod name.

And here we will see the series of events here in detail.

So all the pod information is present here.

So this is the part and it's a labels.

You can see them here.

And IP address of the pod.

You can see it here and all other conditions information everything.

You can see them here okay.

Successfully assigned to oh so and so or node and all that information you can see.

But important thing here is not only this.

We also need to describe the deployment.

So we'll say kubectl describe deploy and catalog is the name of the deployment.

So if you say kubectl get deploy is an alias for deployments.

So we'll say kubectl describe deploy and name of your deployment is catalog.

And when you see the description of it okay.

So if you go up here so it is the name is catalog created in namespace default.

And this is the label it is using.

And these are the selector labels defined.

And it has one desired replica, one updated total one, and that one is available.

Strategy is rolling.

Updates.

All that is defined here and conditions information so everything we can see it here.

It doesn't have any old replica set, but it created a new replica set.

This is the replica set name.

So as we see here replica set is this deployment is pointed to only replica set.

It does not have any pod information.

So it is saying from deployment controlled scaled up replica set.

And this is the replica set name from 0 to 1.

Now we'll say kubectl get replica set.

You can say or its alias is r s replica set okay.

And if you describe this replica set, describe r s and describe this replica set.

So let's describe this and see some additional information here.

So on the top this is the replica set created in default namespace.

So these are the selector labels and information about them.

Okay so it is saying app.kubernetes.io catalog and some pod template hash and and all those things.

And in annotations some additional annotations are there here.

And this replica set is controlled by the deployment catalog.

So the reference to over this means like the head for this respective replica set is catalog deployment.

And here are the pod information.

You can see it is saying that successfully create replica set controller successfully created the pod

so and so pod it created.

So you can see the relation very clearly from replica set to specific pod.

Okay so always it is deployment replica set and pod that we have seen by reviewing everything in events

of every events of deployment, replica set and even pods.

Now the next thing here is we will go ahead and then access our application using kubectl port forward.

So I'll say clear kubectl port forward.

And we have only one pod in our deployment.

So I can directly also say deployment.

Deployment slash catalog.

Or you can always go and then get your pod slash pod name here.

So what is your pod name.

So this is my pod name so I can get that and then do that.

Or if you have only one pod in your deployment right.

So it will anyway go to that pod only.

So I'll say deployment catalog and then 70808080.

So let's see if port forward works.

Let's access the health endpoint.

And it is saying okay.

So we'll just say catalog products okay.

So that should be sufficient catalog.

And then we will say products.

And if those are accessible we are good okay.

So those are accessible.

So we'll come back to our Commands here and we will remove the port forwarding.

Right.

And we'll move on to the next step which is scaling.

So we have deployed and then we have seen the rollout status.

We learned a little bit about replica sets.

And then we are now scaling the replicas.

So we will say cube CTL right.

And then scale.

And what is that.

It is a deployment.

And what is its name catalog and how many replicas we want to scale.

I can just say replicas equal to three replicas okay.

And let's say what happens okay.

So cube CTL get pods if we see right.

So instead of one pod it has created three parts.

Now the one pod which created initially is nine minutes earlier.

And now we can see two more pods created.

And if I say scale to five parts let's see what happens.

Okay.

And if I say get pods, get pod.

And you can see total five pods are running now, so if you want to bring them back to one pod and you

can see immediately only one pod will be running here.

Okay, so all other things got terminated.

So all these things using simple kubectl scale command, you can increase the number of catalog microservices

instances to any number.

So if your cluster supports hundreds of pods or thousands of pods, you can increase them accordingly.

And in addition to that, when you are scaling to example like some six pods, and if you say kubectl

get pods and hyphen o and then wide command.

So this will show us the IP address of your nodes in in which node these were scheduled.

In which worker node.

So this is the IP address of your worker node.

So the where is it.

So here node IP address will be there.

Very sorry.

This is the pod IP address.

It will give you the node name here.

Okay.

So these are the node names.

So you can see the node here.

Sorry these are pod IP addresses.

This is the node name.

So in which worker node.

So we have three worker nodes and in which worker node these were scheduled.

It will be shown when you use the command hyphen o wide.

So that's about scaling.

Again we can scale down to one replica if we want.

Or we can scale up to three replicas.

So I'll try to leave it with three replicas.

Now kubectl get pods and we are leaving it with three replicas.

Now we are going to learn some additional things here.

Now we are at the rolling update section wherein we are going to upgrade our app version.

So one from uh means like from 1.0. 0 to 1 .3.0.

So before that, if we want, we can go ahead and also grip the image version for our respective pod.

Okay.

So what I will do here is so we'll go back here clear and cube CTL get pods.

I will say and cube CTL describe pod and this is the pod name any one pod.

I will try to take it and I will say grep image.

Okay.

And let's see what happens.

It is providing the image and image version is 1.0.0.

So you can see the thing here.

Now what we are going to do is we are going to we we we are going to update that okay.

So using the command cube CTL set image deployment slash catalog.

And for the catalog a catalog container we are going to update it to 1.3.0.

So you can directly go and then run this command.

Or you can go here right.

So go to your deployment catalog and just say 3.0 and then deploy it, save it and then deploy it.

Anything is fine.

So we will take the set image option.

So here we are setting the new image for the deployment catalog and whose container name is catalog

equal to.

And this is the whole thing.

Okay.

So that is what we have provided.

So we will try to go ahead.

Before that we will see the deployment revisions.

The command here is kubectl rollout history and then history of our deployment catalog.

So that is the command.

So we'll run here and see.

And we can see the rollout history is revision one is currently present.

Now we will set the deployment to 1.3.0.

And immediately we'll go ahead and then see the rollout status deployment catalog okay.

So now it is saying that waiting for catalog rollout to finish two of three updated replicas are available.

So slowly it is updating one after the other.

So it is saying catalog rolled out successfully.

So if you run the commands get pods right.

So just 20s and 19 seconds before new parts were created.

So till here it is clear, right?

Using the set image, we have upgraded our Kubernetes image version from 1.0. 0 to 1 .3.0.

Now what happened actually, right.

It recreated a new part.

Okay, so till here it is clear, right?

But as soon as we update the image in the background, Kubernetes creates a new replica set.

So this new one is associated with new version of the app.

So let's run the command cube CTL get Rs which is replica set.

And now you see this is the old replica set which is created 15 minutes ago.

And now a new replica set is created just 61 seconds.

And all the three pods are associated with this new replica sets.

Okay, now the next thing here is.

So if you want to understand actually what happened from replica sets perspective.

So what happened is it created a new replica set, and it doesn't replace the old replica set with the

new version of parts there.

It created a new replica set and it started creating one after the other new parts in the new replica

set at the same time, it started terminating old parts in the old replica set using the rolling upgrade

option.

So one by one, new parts come up from the new replica set and old parts from the previous set are terminated.

So this is how rolling updates achieve zero downtime.

So there is no impact to the live traffic.

So traffic keeps flowing while parts are updated silently in the background.

Now another important thing here to make a note of is the old replica set is not deleted.

It is present with zero replicas so it simply kept aside, scaled down to zero replicas.

Why?

Because Kubernetes remembers it.

And that's what makes rollback so fast.

Using Kubernetes deployments.

So you have the older version of your complete configuration in older replica set.

So rollback becomes very quicker.

So when you rollback Kubernetes just switches traffic back to that previous replica set.

No need to recreate the pods from scratch.

They are already defined and ready to go in this respective old replica set.

So that's why rollback happens almost instantly.

So let's see that.

Okay so we can we have a command here.

We can go to here.

And this is a undo command we are going to have here.

So you can say kubectl rollout undo and then provide deployment slash catalog.

So you can do that now.

So we will go back to our terminal here kubectl rollout undo.

And which one we are undoing deployment slash catalog Okay, so you remember the older one is starting

with 78 and newer one is 86.

Just remember that.

Or you can say 15 minutes older.

One is old replica set and just created 61 seconds, which is created just 61 seconds before.

Is the new one something like that?

Okay, so we are saying undo and after that we will say get Rs.

Right.

And it says immediately it rolled back okay.

And all three are running from the older older replica set.

You can see it here.

And if you see the get pods and new, these pods will be already created just 23 seconds before.

So it quickly rolled back to the older version.

So the active one will be scaled up and the older one will be scaled down.

All these things based on how you want to handle them.

But the whole thing we need to understand here is so if we go back here, right.

So this deployment will create one more replica set.

So this might be V1 version and this might be V2 version.

And here you have your P1, P2 and P3.

So these changes you want to roll back or you want to come forward.

All these things are straight through because everything is managed at the replica set level.

And these deployments manage only replica sets.

So to remember every time you update your deployment, right, so you update your deployment.

Kubernetes doesn't overwrite anything, it just creates a new replica set and updates ports gradually.

If anything fails, you can instantly roll back to a known good replica set.

So this mechanism deployment, this mechanism deployment, managing multiple replica sets is what gives

Kubernetes its powerful versioning and stability.

So.

New version.

New replica set.

Rollback.

Reuse.

Old replica set.

So no downtime.

No manual cleanup.

Fully automated life cycles in short deployments.

Make application management in Kubernetes safe, predictable, and even production ready.

Go ahead and even see the rollout, history, deployment or revisions once.

Okay.

And you can see the revisions here.

Two and three are there right now.

And if you want to go back to revision two we can always try that revision two.

So we can say and revision two is our 1.3.0 version okay.

So now what happened is from so I'll try to show that okay.

So version one is 1.0.0 and two is 1.3.0.

And now a new revision created which is a rollback revision which is 1.0.0.

Now again you are doing you are saying go to revision two and let's see what happens.

Okay.

Right.

So I'll say two revision two and cube ctl get or get r s.

And it should show that the second one second replica set.

Okay.

This is the 186 one should get all the three things.

And now if you go to get pods and if you try to describe right cube CTL describe pod.

And this is any of the parts you can take it with just now created and pipe and grep image you should

have you should have 1.3.0.

You can see it here 1.3.0.

And in addition to that you can also go here and run the revision command one more time.

So where is it?

This is the rollout.

Uh, where is that rollout status?

No.

Where is that revision command?

So we'll go here and get that history rollout history command.

Sorry, it's not revision command, it's rollout history command.

And you can see three and four are present.

So now, what happened is we'll go here and this is again 1.3.0.

So when you said that roll back to this one.

It created a new revision number 4.0.

So now you have three with 1.0.0 and four with 1.3.0.

If you want to roll back to three again it is previous version, initial version.

So like that.

Which creates a fifth revision for us with 1.0.0 like that, revision keeps increasing, but versions

of our Docker image is going to be as is.

So that's about, uh, overall deployment, replica set and pods in detail.

Now we will go ahead and then clean up whatever we have created.

Right.

So delete deployment catalog or we can go here delete hyphen f.

And then zero one.

Anything is fine.

So this completes the demo about Kubernetes deployment I'll see you in the next demo.

Until then bye bye.

Thank you

All right.

Now we have learned about Catalog Service, a microservice deployment using Kubernetes deployment in

our previous demo.

Now we are going to learn about Kubernetes service cluster IP.

And why do we need this cluster IP.

Let's start with the problem first.

So what is the problem.

So first we will talk about we have a deployment which creates a replica set which creates the Kubernetes

pods for us p1, p2, P2 and P3.

Right.

So we have three pods with us.

And so this is a catalog microservice right.

Microservice.

So let us put this as in one box.

Now you have something called UI service right.

So this UI service need to talk to this catalog microservice.

So this is another service.

And this service need to talk to this catalog microservice.

So how does it need to talk.

Right.

So this UI app right.

So whatever it is if it want to if it tries to directly point to this respective pod.

Right.

Or like this you configure hard code this pod IP addresses in your UI app.

What is the problem?

The problem is these pod IPS can change at any point of time.

So which means like when you scale your pods instead of p1, p2, p3.

If you have 100 pods need to get created, you don't have those information with you.

Or if you restart your pod, or if pod is deleted and recreated every time they their IPS will get changed.

So now what is the solution here.

Right?

So the solution here is very simple.

You need to use some load balancer in between.

So say let's say initially it is some load balancer.

So what is that load balancer.

So that is we need to understand here.

So in Kubernetes there is something called Kubernetes services concept in that there is one service

which is named as cluster IP.

Why we use this cluster IP is it is for internal communication inside EKS cluster or Kubernetes cluster.

In very simple terms when we use the type as cluster IP.

So when you use the type as cluster IP, the service is available only inside the cluster.

So it is perfect for internal communication between our microservices.

So our UI app or UI service want to communicate with this catalog microservice.

and it can happen via this load balancer, which we call it as cluster IP service.

So we call it as cluster IP service.

So that is what we need to create now.

And here two questions will be there.

One is what are you going to configure in this UI app about this cluster IP service.

Right.

So you will configure this cluster IP service DNS name and port.

So that is one thing.

And second is how does this cluster IP service know the IP address of these pods.

So this is the second important question.

So now here comes the important thing how this cluster IP will know these ports information.

So when we have created our catalog deployment every pod got a selector label whatever we have defined

inside that.

So now if you go back so we need to go back here.

And if you go to this deployment right.

So here in the selector match labels you have defined something here about kubernetes.io name equal

to catalog.

So now this should be same as in your service manifest you will have something called selector.

So in deployment it is selector match labels.

And in Kubernetes service it is selector label.

If those two matches right then request will be sent to those respective pods.

So if tomorrow new catalog pods come up or old ones go down, the service automatically tracks them.

No manual updates needed because the whole thing operates via selector labels.

Whatever.

We have defined selector labels.

So using that everything works.

So the two important things we have discussed.

How does other microservice, if you have another microservice A or another microservice B, how they

communicate to this cluster IP service is using cluster IP, C, IP, DNS.

And second thing is, how does this cluster IP service send the traffic to these ports?

Is using selector match labels and selector labels.

Now let's also talk about Kubernetes service cluster ip, YAML manifest.

So we have it in detail in our zero to catalog cluster IP service.

The entire YAML manifest is ready for us in 0803 Kubernetes service demo.

So let's understand that line by line so that it will be easy for us.

Right now let's start with the API version.

And kind and kind is service.

The Kubernetes resource is of type service and it is coming from the core APIs.

Core Kubernetes API which is v1 core.

Now the next thing is metadata.

So this metadata is very important section.

So wherein under metadata we describe basic identity information of our Kubernetes resource.

First is we define the name and second is labels right.

So now you have labels here and here the purpose we are explicitly defining them and talking about them

here is this has a different usage and this has a different usage.

So let's start and understand that the label section adds metadata tags.

Here we are tagging it with this name app dot Kubernetes dot name equal to catalog, which helps us

to group and organize related resources.

Whatever you define in metadata, these labels don't affect behavior by themselves, but they are extremely

useful for filtering and also for debugging and also for linking components.

Okay, so these are the usage of those things.

Okay.

Mainly for uh for filtering purpose and then debugging purposes.

Okay.

And they are not any there for any functional use.

Okay.

Or for dashboards visually when you are implementing dashboards using some monitoring tools, how you

want to display them, these labels, whatever present in metadata will help also for dashboards okay.

So this is the usage of those things okay.

But now when it comes to selector label here.

So this has a different usage.

We will go there and then discuss in detail.

So now here comes the spec of our cluster IP service or our service okay.

And in this you can define type as cluster IP.

So there are multiple types of services available in Kubernetes.

So we have something called this is the cluster IP service we are talking about.

In addition to that we have something called node port service.

And we have load balancer service right load balancer service.

And we have something called external name service right external name service.

And finally we also have something called headless service.

So these are the services available in Kubernetes.

And currently we are dealing with this cluster IP service.

So we have defined type equal to cluster IP here.

And after that we have something called selector.

And in selector we have defined our selector label.

So the selector is one of the most important parts of the Kubernetes service definition.

Whatever we are defining it tells the service which parts it should send traffic to that is very, very

important.

So this part whatever is very, very important.

This should match our pod our deployment selector labels.

So here we are saying.

Find all parts that have the label so and so right which is catalog.

And then.

Once those pods are found the service will automatically discover those pods and keep the endpoint list

up to date.

So if a new pod is created or old one gets deleted, the service updates automatically.

You never have to worry about IP address of these pods.

So to summarize, you need to understand that the selector label whatever we are seeing here selector

label inside this Kubernetes service should match with your deployment.

Selector match labels.

So that is the thing.

Simple thing.

So if you want we can go here and then see the same thing.

If you go to your deployment here is the selector match label.

So it should match with this selector match labels.

Now next is ports.

So we can see here the ports information in detail right.

So this is your service port which this service will be exposed.

Okay.

So other microservices can communicate to this catalog service using this port.

And you have something called target port.

So this is your container port.

So this is the container port.

So on the actual catalog port and standard TCP protocol.

So that's about Kubernetes cluster IP service YAML manifest in detail I'll see you in the next lecture.

Until then bye bye.

Thank you

Let's go ahead and then clean up whatever we have created as part of this demo.

So we'll go to we can directly say cube CTL get deploy right.

And that respective deployment right.

So we'll say cube CTL delete deploy and catalog.

And in the same way we'll also delete the service.

So cube CTL get SVC and we'll say cube CTL delete SVC.

And we are going to delete that catalog service related service.

So this completes the clean up I'll see you in the next demo.

Until then bye bye.

Thank you.

Alright we are entering into the demo 0803 Kubernetes Service.

In this demo we are going to create a Kubernetes service for our catalog microservice.

The goal is to expose our catalog deployment inside the cluster so that other microservices can communicate

with this respective catalog microservice.

So we will create a cluster IP type service.

That means internal communication only.

And we will verify how Kubernetes automatically discovers parts behind the scenes using labels and endpoint

slices.

So we have all the detailed steps listed here.

So we'll go ahead and then start implementing them.

So if we go here to our Visual Studio Code we have zero one catalog deployment and zero two cluster

IP service.

So already we have reviewed the cluster IP service YAML manifest also.

So we'll go to our terminal here and we will go to zero eight Kubernetes Foundation and 0803 Kubernetes

Service.

And we have uh if already kubectl get deployed.

If already as part of previous demo we have deployed our catalog microservice deployment.

We don't need to do that now.

If not, we can do it.

So I have already deleted them from the previous demo.

So we can go ahead and then deploy them.

So first apply hyphen f.

And in catalog kit as manifest you have the zero one catalog deployment dot YAML.

So this will create a catalog deployment.

If I run the command kubectl get pods.

So you can see three pods are running for catalog.

And you can also see get R s which is the replica set and deploy which is the deployment which is catalog

deployment.

Now this deployment we need to expose internally in our EKS cluster fronting it with Kubernetes service.

So we will go ahead and then deploy this service now.

So cube CTL apply hyphen f and catalog get s Manifest and zero eight catalog cluster IP services.

So you should see a cluster IP service that is created.

And when you run the command cube CTL.

So we'll run the command cube CTL.

Get SVC.

SVC is the alias for service.

And you can see that catalog service is created.

And this is the cluster IP IP for this respective catalog service.

Now let's first run the command.

So we'll what we'll do is we'll say kubectl get pods hyphen o wide.

We'll get the IP address of each pod.

So there are three ports.

So 2.2267.14 and ten dot 141.

So these are the IP address of the pods.

So we will run the command cube CTL get endpoint slices and see what happens okay.

So get endpoint slices.

Let's run that.

And we have something called Catalog Service and Catalog Service with some dynamic name generated.

This is the endpoint slice with port 8080.

And here you can see the endpoints 226 141 and 140 which matches with our pod IP addresses.

So behind the scenes Kubernetes automatically links this catalog service right catalog service to catalog

ports using labels.

It does that through objects called endpoint slices.

And the best part here is if pods restart or scale, these endpoint slices automatically update to keep

traffic flowing smoothly.

So what we can do is we can run the command cube CTL scale right.

And deployment.

And our deployment name is catalog.

And I can say hyphen hyphen replicas equal to.

So I can say eight replica, some eight replicas I am going to provide eight.

So it said 88.

Let me we are saying eight replicas.

Right.

And now if I say get pods, so eight pods should be running.

And now if I say endpoint slices okay.

So eight pod IP addresses should be updated here.

See it is saying these plus five more were present here.

So we can also say cube CTL describe endpoint slices and provide this name and describe that endpoint

slice okay.

And now if you go down here right.

So this is the catalog service.

So and so this is the endpoint slice and port is 8080.

And every endpoint every pod information pod it is referencing to and its IP addresses pod it is referencing

to and its IP address so everything you can see them in detail.

So all the eight parts, you can see them in detail here.

So that's the advantage of using Kubernetes cluster IP service fronting our Kubernetes deployment.

And so it can automatically get the pod IP addresses using selector labels.

So the next thing right.

So we have successfully tested this part.

Next thing is let's test the connectivity from inside the cluster right.

So inside the cluster only we will try to test it.

So how we are going to test it.

So we will spin up a simple curl simple curl image as a Kubernetes pod inside EKS cluster.

And from inside itself we are going to run the curl command to test it.

So let's understand that a little bit.

So we'll go up here right.

So let's say like this is our.

EKS cluster.

Okay.

Right.

And you have your Kubernetes pods here and you have your.

So you can say p1, p2, P3 pods.

And you have your C IP service which is cluster IP service here.

Okay.

And these pods again connecting to the deployment and all these things replica set.

Those things are not needed.

For now.

The focus is on CIP service.

So what we will do is we will create one more pod here.

We call this as curl pod okay.

So because we are saying that cluster IP services are is nothing but exposing this Kubernetes deployment

as a service.

Okay.

So that you can communicate with these applications via this service.

So that's what we said, right?

So for that purpose we will connect to this pod.

And we will do the curl to this SIP service and our catalog application endpoints.

Catalog endpoints should be accessible, something like Health and Topology and catalog uh products.

And all those things should work for us when we do the curl from this one.

So that's what we are going to do now.

So let's go back here into the terminal.

And what we will do is we'll run the command cube CTL.

Or we can go to our here we have the command ready.

So we'll just go ahead and then run it.

So this is the command.

So cube CTL run.

And this is the uh test.

And name means like the container name is test.

And image we are using is curl images slash curl.

And we are going to connect it with S-h.

So we will run that command.

So it will take a little bit of time because first it need to download that curl Uh, call Docker image

and then run it.

Okay, so now it has run and we are inside that.

And now we will run the command catalog service colon 8080 and then topology.

So that is what we are going to run because our catalog service name is also that one only see Catalog

Service and 880 even if you try and say here right.

So if we say kubectl get svc, you can see its name is catalog service and port is 8080.

So we'll do the same thing curl HTTP and catalog hyphen service colon 8080 and slash.

First we will try and see health endpoint okay.

It is good.

We will also run the command topology and it is accessible.

You can see right in the same way if you want.

We can also say catalog slash products.

And all the product information is present here.

And if we want we can also say catalog size to well, okay.

Or we can say catalog tags and all the tags information is displayed here.

So it is working as expected.

So no issue with that.

We'll also try something else right.

Which is nothing but DNS resolution check.

So verify Kubernetes DNS.

What is the DNS for this cluster IP service inside Kubernetes cluster okay, so every Kubernetes service

automatically gets a DNS entry inside the cluster, primarily the cluster IP service.

So that we can do that using simple DNS test.

So we'll go here and copy that command.

So kubectl run and the name of the container is DNS test.

And we are going to use BusyBox 1.28 which has the DNS tools available inside that.

Which means Nslookup is going to work for us when we use that BusyBox 1.28.

Latest versions of BusyBox doesn't have that.

So this is very important to know.

So we'll go here, copy the command and run that DNS test.

Uh doc Kubernetes pod.

And we are in that Kubernetes pod now and we will say Nslookup catalog service.

So we'll try to paste that.

And this is the full DNS name of your catalog service which is catalog service dot dot dot cluster dot

local.

This proves that Kubernetes DNS can resolve the service name right the service name to its cluster IP.

No need to remember any internal IP addresses.

So you can see the address is 172 2023 118, which should match with the cluster IP, IP 172 2023 118

All right.

Before going and understanding about Kubernetes config maps, we also need to understand why do we need

this config map in our catalog microservices.

So a little bit more about catalog microservice will give us a complete picture of what we are going

to implement.

So this is a catalog microservice.

And this microservice right.

Currently whenever we are deploying it and accessing it, we are able to access the records.

Which means when you say catalog slash products, you are getting the output.

Or when you are saying catalog slash tags, you are getting the tags information.

So how we are getting that information.

So that's the key point here.

So this catalog API has two options okay.

So one option is in-memory database.

So when we don't point any external database it by default uses this in-memory database.

So when catalog API application is initializing which means when it is starting it will populate its

in-memory database with the default records inside that.

So only when we are accessing the catalog APIs with products or tags, we are able to see the data on

our browser.

So at that point of time it is using in-memory database.

Right.

So either it is a in-memory database or external database.

So we said that it is option one.

So what is the external database or what is the option two.

Option two is external database and external database supported is MySQL Mysqldb.

So either it is in-memory or external database.

This catalog API need database configs, so it need dbconfig to connect to those databases.

It might be in-memory or external.

So what are those things?

One is it needs the DB endpoint right.

And next is DB schema right.

And next is db username and four db password.

So like this it need all this information to connect.

So these things like any application when we build right.

Any standard application microservice application or any application we will externalize these configuration

when building the application itself so that we will be able to pass those values XT from external sources

so that this code, whatever we are building right, can be used in multiple environments like dev,

staging and prod.

So that is the standard practice.

So for that purpose right.

So in this catalog application also there might be something here which states that we need to pass

those values.

The best common way usually we do for applications right is using environment variables.

Environment variables in our operating system.

We pass these values to these applications.

So that's the common way.

So if we go to this respective application.

So if we go to this catalog application so you can go to Stack simplify retail store sample app AWS

or which I forked from AWS, uh, GitHub repository.

Or you can directly go to this, uh, retail store sample app.

So any link is fine.

Okay.

So you can go to retail in AWS containers.

You will find this retail store sample app.

So inside these things right.

So inside retail store sample app if we go to the source location and in source location if we go to

the catalog right.

So this is the catalog application.

So here you are going to have the complete information about this catalog application.

And in that the configuration information you can see the details here.

So let me discuss those details.

So this is the configuration information we have.

So what is that exactly.

They are saying.

So in for this catalog microservice.

Right.

So for this catalog microservice persistent provider right.

Either you want to use in-memory or MySQL.

So that is one thing they are talking about.

And they are saying that default is in-memory.

So that's what they are trying to explain.

Means like in catalog microservice application, they have set some default values for persistent configuration.

And in that default is in-memory.

And for from endpoint perspective for in-memory it is it relies it it will be present in catalog API

itself right in the same container.

So there is no specific endpoint for that.

Programmatically they are initializing that database.

So it is going to be empty.

But when we are using the MySQL database.

So when we use the MySQL database our options are one is use statefulsets in Statefulsets.

And second is maybe AWS or MySQL DB.

So in these things we have the options.

So at that point of time we will have the database endpoint with Statefulsets Statefulsets fronted with

Kubernetes service.

So that service information or either the RDS Mysqldb endpoint.

So any of these things is the possibility.

Right.

So that should be our database endpoint when we use MySQL and database name.

So which is nothing but your schema name.

They are using catalog DB.

Either it is Statefulsets or RDS db.

We can use the same name.

And when coming to database user they have used catalog user.

We can use the same name, but the best practice is not to use underscores.

We might remove that if required.

Okay right.

And database password for default they didn't set anything which is for in-memory.

But in our case we will set something and database connection timeout.

They have given five.

We will leave it to defaults.

So these are the environment variables when you pass or when you set this in your container with values.

Right.

So when you set these environmental variables in your container with values then automatically your

catalog microservice application will pick those information and accordingly it will connect to that

respective database.

And these are the defaults.

And if we want we can change based on whatever the values we want to define for these things okay.

So all the values we can define and then change it.

So that's the key thing about environmental variables why we need them for a specific application.

Because of we want them to have externalized.

So that this application can point to any type of databases like dev, QA and then staging any Environments.

Now let's move on to the core Kubernetes Configmap concept.

So we already know these are the configurations for our catalog microservice.

So you have a Kubernetes deployment.

So you can say this as K8S deployment right.

So in this deployment in the env section you can define these things user and password right.

And endpoint and some value.

So everything you can define inside this deployment.

In that case what is the problem when you directly define this configuration values in your Kubernetes

deployment.

Hard coding there.

What will be the problem is you cannot reuse deployment template okay this deployment template across

environments.

So that is the problem you can you cannot reuse for across environments.

So the best option is not to hard code them.

So hard coding is not a recommended option.

So if that is the case then we definitely need to externalize them.

So we need an option to externalize.

So we are externalizing our DB configuration information in Kubernetes deployment.

So in that case there comes the important Kubernetes resource which is Kit's config map.

So there comes the concept.

So a config map is a simple key.

And then value store managed by Kubernetes.

It lets you separate your application code from environment specific Information.

So now you have the config map with all the db values.

So example like user and some username password and some password.

Okay.

And endpoint.

Okay.

So you have the dev endpoint.

Okay.

Or you and also you have something called dbschema and dev DB like this.

You have all the information.

So like this you can create dev cm means like dev config map q a config map and any config map here

and you can reference based on your environment.

You can reference those config maps in your Kubernetes deployment.

So your pods can then consume those values either as environment variables or even if you want to mount

them as a mount them as files in your container.

You can also do so config map you can use as environment variables.

Or second is you can mount it as files in your container.

So mount as files.

So all these options are available from config map.

Let's also review the config map now.

So this is the sample config map we are going to define for our catalog microservice.

So whatever we have specified here right.

So these are the things all these are the environment variables.

So which catalog microservice can understand them.

So it can understand these values.

So for that purpose in this is the standard block which is apiversion v1 and kind is config map.

So if you go here and in the APIs right.

So these are the workload APIs.

And you can see the service APIs.

So previously we have learned about the service also.

Right.

So that's the service API.

So you can see all that information here.

And you can see config and storage APIs.

So in this config and storage APIs you can see the config map here.

And in config map.

So this is the config map.

And here are the information about it.

So API version and which belongs to v1 and kind is config map.

And either you can provide binary data or simple data.

Also you can provide or immutable.

So all the information is present.

Here we are using the data object.

Here data contains configuration data.

Each key must consists of alpha numeric values with non UTF eight byte sequences must use the binary

data field.

The key stored in data must not overlap with the keys in the binary data field.

So it's it has provided its own schema information here.

So all that is too technical for us.

We will go here and directly go ahead and then review that.

So we said that this is part of core API.

So it is plain simple V1 and kind is config map.

And here in the metadata you have defined the name of your config map, which is catalog, and in the

data section you have defined your actual configurations.

Actual configs in key value pairs.

So in key value pairs.

So catalog persistent provider.

In this demo we are using in-memory.

In the next demo when we go to Statefulsets there we will use the Statefulset fronting Kubernetes service

okay.

Right.

So that means endpoint will not be empty.

And it is going to be a stateful set fronting service from endpoint perspective provider.

In this demo we are using in-memory.

In the next demo we will use MySQL.

Okay.

In the same way Dbname we are using all the defaults, whatever we have, whatever it is shown here.

So if you go here all the defaults we are trying to put it and then we will connect to that part.

Okay?

So whatever the pod it creates will connect to that pod and run the env, uh, related command and see

whether all these things are set or not.

We are going to see which means we have a config map, and we are going to reference that in our deployment

and we are going to deploy it.

So we'll go ahead and then deploy that okay.

So we will deploy that and we will connect to the pod.

So as a user I will connect to this pod and run the env command and verify if these things are set which

means like whatever defined in config map catalog is present in our pod or not.

I am going to verify.

So all these information.

So that's what we are going to do as part of this demo.

I'll see you in the next lecture with this practical demo.

Until then bye bye.

Thank you

Before going to our actual demo.

We also need to see something here, right?

So the question here is how the Kubernetes deployment will know about this Kubernetes config map.

So which means you need to reference this config map in your Kubernetes deployment.

So how you are going to do that.

So what we do is this is the config map name catalog in our Kubernetes deployment in containers section.

So where this environment variables need to be set in our catalog containers, we'll go to that container

and we will say E from.

So for the catalog container we are going to set the environment variables from config map reference

and name of the config map is catalog.

So this is the thing we are doing.

So again I'm going to reiterate that.

So for this catalog container.

So whatever we are talking.

Right.

So it is going to get the environment from environment variables from config map reference whose name

is catalog.

So that's what we are going to reference.

Now let's get into the demo.

So we are in zero eight Kubernetes Foundation and we are in 0804 Kubernetes config map.

And here we are going to review both config map and our Kubernetes catalog deployment where it has the

env from config map reference catalog.

Right.

So when we reference that what happens is it loads all the key value pairs from the config map and inject

them.

So when it is creating a pod, it will inject them as environment variables into your container inside

that pod.

So it's that simple.

No need to define each variable one by one in your deployment file.

So you need to define env and provide your key value pairs here.

Okay, so that part is not needed.

Just reference the catalog config map.

So that's the thing.

So now let's go back to our Visual Studio Code.

And here in our 080403 is the catalog config map.

So this is the one which we have reviewed.

And in the zero one catalog deployment you can see the environment reference here right at line 2526

and 27 for the catalog container.

Now let's go to our terminal here and go to the specific section.

So which is that section.

Come out of this and go to zero eight Kubernetes Foundation.

And in Kubernetes Foundation we'll go to 0804 Kubernetes config map.

And I'll say cube ctl apply hyphen f.

And I'm going to deploy all the Kubernetes manifest present in this folder which is deployment cluster

IP service and then config map.

So let's wait for these things to get created.

Now let's run the command cube CTL get pods and you should see your pod is running here.

And you can also see cube CTL get cm.

CM is the alias for Configmap.

You should see your catalog Configmap is created.

So if you want you can describe CM and catalog config.

Configmap can be described.

And here you can see all the values for it okay.

Persistent connection, timeout five db, name, catalog db etc. etc..

Right now we'll also say cube CTL get pods right.

And we will also say cube CTL exec, hyphen id and this is the pod name.

Right.

So this is the pod name.

And we will say env as the command.

All environment variables from the catalog container inside this pod will be printed here as part of

this command.

So you can see all these are the things now if you want only the retail ones we will try to grip it

with.

So we'll say first clear and we'll use the same command and we'll say grip retail.

Right.

So whatever we have set from config map those will be displayed.

So catalog user timeout five catalog db endpoint and password.

We have put it as empty and provider is in-memory.

So you can compare them here.

So whatever you have set from here those things were present here.

So that's about this confirms that our deployment successfully pulled configurations from the config

map.

So finally we have implemented the config map demo.

Now let's clean up everything with one simple command cube ctl delete hyphen f and catalog k test manifest

folder.

This will remove the deployment service and configmap created for this demo.

I'll see you in the next demo.

Until then, bye bye.

Thank you

All right before going and understanding about Kubernetes.

Statefulsets.

Let's understand the problem first, right?

And then how that problem will be a problem when you use Kubernetes deployment and how it will be solved

using Kubernetes.

Statefulsets.

We are going to see that in a practical way.

So let's say a very simple and standard example is MySQL database.

And this MySQL database.

Usually we set up in production in Multi-master and multi slave mode.

But here for convenience we will create a single master with multi slave option.

So let's understand what exactly that means.

So let's assume you have a simple MySQL master server created.

So this is a master server.

And you also have my SQL slave.

So we'll say this as.

One.

And we'll also have one more which is my SQL slave two.

Right.

Now what is the difference between master and slave means masters will accept.

Right.

Right.

And these slaves means like when.

When your application.

Right.

When it can write to your master server and it can read the data from your master database, but from

slaves, only you can read the data.

So that's the first thing.

So we have set up the master first.

And then we have also created two slave servers.

Now the important thing is we need to enable the replication between these things, which means we will

clone the master server data to slave one.

and then enable continuous replication.

So we will enable the replication from master server to slave one server in the same way for MySQL slave

two.

Also, we will clone the data from master server and then enable continuous replication.

So we are enabling replication for both the slaves from master.

So that's what we will do.

So this is the base usual setup for any MySQL database.

When you are doing it on your VMs or in your hardware servers or whatever, the physical servers, the

standard way of implementing it now.

So these servers, if it need to get if these servers need to be in continuous sync.

So if these slave servers need to be in continuous sync with the data, whatever is present in master

server, they need to know your master server, host name or IP address.

Let's say master server hostname is m1 and slave is s1 and S2.

So this host means like the slave always need to know the master host information which is M1 right.

So let's say this as M1.

So always they need to know that information.

So this is the ideal setup when you implement a MySQL master slave master slave instances with a single

master and multi slave setup right in.

If we have multi-master, we are going to have one more master setup.

And again like slave we are going to set up that master means like take the data from one master and

then initialize the data there and then enable the replication.

But it also can take read write.

So that's the difference of it.

But always the second master will be in the standby mode only, and only the primary master will take

the read writes.

But that's a different concept.

We really don't need to get into it, but we understood.

Right.

So this is the base concept of MySQL master slave implementation.

But now let's see how this how this need to be means, like how we are going to implement this using

Kubernetes deployments concept and what all problems we are going to face with that.

So if we see Kubernetes deployment for MySQL database the same setup using Kubernetes deployment, then

what will be the case here.

Right.

So when we have set up these things right.

So whatever the m1, S1 and S2 we have the order.

So we said first we set up m1 then S1 and then S2.

So we have set this up in order.

So now if you go with Kubernetes deployment right.

So when you deploy these things when you start a Kubernetes deployment with three replicas, all three

will get created same time, created same time.

So that's the big problem.

So Kubernetes deployment is not suitable for this type of implementation.

So right.

So that is one key important thing.

Now second thing second thing is we already know that these slaves need the master information.

So master information means MySQL master related MySQL master pod.

You consider these as part one, part two and part three when you are doing them via Kubernetes deployment.

So these part two and part three needs the part one related exact hostname, which means it should be

designated as something some hostname and that hostname needed so that continuous replication works

without any issues.

Is that possible with Kubernetes deployment?

No.

Why?

Because it generates when you create a Kubernetes deployment pod names are pod names are dynamic.

Dynamic in nature.

So that's not possible.

So pod names are not static and they are dynamic.

So for this setup whatever we are talking about single master with multiple slaves for this deployment.

Right.

Kubernetes deployment is not suitable.

So for somehow you have took the current pod name pod one and then configure your slave one and slave

two to point to this part one in some way.

Okay.

Something you have done and then you have made it.

But what happens when you are master server, right?

So master pod crashes, right?

So when Kubernetes deployment.

Right.

So this Kubernetes deployment recreates the pod recreates the pod at that time.

Again, part name is dynamic, part name is dynamic means again it is going to have a different name.

This can be pod X or pod Y, whatever it is.

And these slaves will lose the name of the master pod.

And again the entire thing is broken.

So Kubernetes deployments are not suitable for this type of application.

So what are these type of applications.

These are stateful applications.

They need to have the state and whatever we have done so far right.

Microservices and all those things those are stateless applications.

So for them it worked.

Kubernetes deployment worked without any issues.

But for these type of applications like MySQL and Read this right.

Are PostgreSQL right for all these things?

Right.

So these are stateful.

And then these needs a different concept which is called stateful set.

So we are going to understand Statefulset now.

Now for the same MySQL application let's talk how stateful set can be useful here.

So first thing is like in statefulsets pods created in sequential order sequential order.

So whatever may be your stateful set name.

So based on that name example, your stateful statefulset name is MySQL right.

So your first pod name will be starts with zero.

And your second pod name will be MySQL one.

And your third pod name will be MySQL two.

So this ordering right.

So this ordering will be present in a sequential order in stateful sets.

So that's the reason it will be suitable for this type of applications like single master with means

like for all stateful applications it is a good uh, resource to use from Kubernetes perspective.

So you can see here this part becomes MySQL zero and this part becomes MySQL one.

And this part becomes MySQL two.

So it assigns the unique ordinal index for us.

Each pod gets unique name.

And we can definitely rely on these names.

So why we can rely on these names.

Because when it crashes for some reason.

Example this pod is getting crashed which is master server.

When it recreates, when it recreates, it gets the same name.

So my SQL zero, it gets the same name.

There is no change in the name.

So which means these slaves can point to the master one without any issues because its name is always

MySQL zero.

So due to that they can always point to that MySQL one.

Now there is one more thing we need to know.

Not only when the pod means like when the pods get created, they start in the sequential order like

first zero will be created and once it is ready, then one will be created and then two in the same

line.

This is the create thing, right?

So when creating right when they are deleting also.

Right.

So when the pods are deleting the order will be first it will delete the last one which is two.

Then it will delete the MySQL one.

And finally it will delete the MySQL zero, which means it goes in the reverse order during the deletion.

Which means when you delete the stateful set, it deletes in the reverse order.

So that's about Kubernetes Statefulset.

Key important concepts.

When we are discussing about Kubernetes Statefulset, we also need to discuss about one very important

concept which is headless service.

So this is very, very important concept which we need to discuss and what is its importance.

Also we are going to discuss now.

So so far whatever we have discussed here is only a concept.

We are also going to review the entire YAML manifest for Statefulset in next lectures.

But before that we will focus on also learning about headless service in our next lecture.

I'll see you in the next lecture.

Until then, bye bye.

Thank you

All right.

Before going and understanding about headless service concept, we need to also think about another

angle like why not use Kubernetes cluster IP service?

So far for internal communication fronting any deployment, we started using cluster IP services.

So why not use the same thing for this stateful set.

So these are the three statefulset parts which are m1, s1 and then s2 MySQL zero, MySQL one and then

MySQL two.

So let's say we are going to have one cluster IP service fronting them and whose name is something like

my SQL.

Right.

And this is the cluster IP service.

And we have something called our catalog app.

Right.

So which is sitting here and then uh accessing this SIP service in our catalog app, We have pointed

it to the CIP service, our our database end point.

Right.

So now what happens.

So that's the key thing to understand.

So any service name you know that our service name will be if your service name is MySQL it is going

to be MySQL dot right.

Default which is present in default namespace dot sec dot cluster dot local SVC is the resource and

this is the cluster domain.

And this is namespace.

So this is namespace.

This is resource.

This is cluster domain.

So this is the complete full name.

And this is the actual service name.

Right.

So this is not important currently.

We'll put that aside but we'll focus on this now okay.

This is this CIP service will do a load balancing actually.

Correct.

Right.

So it can send the request to all these ports.

So now MySQL zero, which is M1, which allows both writes and then reads, and s1 and S2, which is

slave one.

And slave two only allows reads.

So now your catalog app sent a write request and that request went to either S1 or S2.

Your write request will fail here, so any write request will fail here.

So which means your app is in failure.

State your data you are losing.

So which means cluster IP service is a wrong option to choose for stateful sets provided you have multiple

pods here.

So one, two and then three.

But if you have only one master so only you are maintaining one master here.

In that case you can definitely use cluster IP service, but not when you have master and then multiple

slaves and all these things in picture.

Then cluster IP service is always a wrong option.

So that's one thing.

And next thing is can we point to directly our pod IP addresses.

So this catalog app right.

So can we point it to this pod IP address.

Let's say ten .0.2. 24 something is your pod IP address your master pod IP address.

Can we point like this?

No.

Why?

Because if something happens to this respective pod and then it crashes and restarts this pod IP changes

to something else.

So this might be ten .0.2. 36.

So obviously pod IP pointing not recommended.

So that's also gone.

Now what next.

Right.

So we will also have something called pod DNS name.

So can we point to.

Into pod DNS name, right?

Pod DNS name.

It's also same thing, right?

When pod gets restarted.

Then pod DNS name also will change.

So how will the pod DNS name look like?

So let's see that once.

Okay.

So it will be like ten.

Hyphen zero.

Hyphen two.

Hyphen 24 dot.

Default.

In default namespace dot service.

The resource pod and dot cluster domain which is cluster dot local.

So this also will change when your pod IP changes.

So this also not an option.

So all the options are closed which means from catalog app I don't have any option directly to point

to my MySQL master pod.

So there is no other options available for us with cluster IP service or direct pod IP or with pod DNS

name.

So we need to have an alternate option now.

And let's go ahead and then discuss that alternate option.

And that alternate option here is headless service.

So let's have a headless service here whose name is going to be.

My psql hyphen h something like that.

So this is the headless service name.

So we will put this headless service.

And then we'll also have the catalog app here.

And which points to our headless service.

And from here we have these lines.

So now let's understand when we use the headless service what will happen.

So the first and foremost thing what will happen is for the headless service.

So we are going to have for headless service the primary thing is no load balancing.

so it will not load balance the request.

So which means when the request comes to headless service, it will either send to MySQL one zero or

MySQL one or MySQL two.

That's not the case.

So no load balancing in headless service.

So that's one thing.

And second thing is so it creates.

So if you go here it creates DNS entries at pod level at pod level.

So you can reach pods directly.

So what exactly that means.

What what exactly.

It creates a DNS entries for pod level or for each pod it creates a DNS entry.

What exactly that means right.

So let's understand that here.

So what it does is it will create pod name dot.

Headless service.

The DNS name looks this way.

Headless service dot namespace.

Dot.

Service name.

Dot.

Cluster name means like cluster domain.

Dot.

Example.

Something like this.

Okay.

So now here this is the sample name.

It is going to provide for us.

So let's see the same names for each pod how it generates okay.

So we'll start with this MySQL zero.

So first thing is pod name.

So you can see that here we said that it is going to be the pod name.

So let's uh come back here and this is what IT format it is going to generate.

So we'll, we'll write the same thing for these things okay.

For each pod.

So I'll say MySQL hyphen zero is my pod name.

Right.

And then dot we have something called headless service.

So that is what we have said here, right?

So this is the headless service.

Okay.

Now we will go back and then do that here.

Okay.

So MySQL dot headless service is my SQL which is the headless service name.

And dot namespace is default namespace dot svc which is the resource name, which is service and cluster

domain is cluster dot local.

That's all.

So this is the name for this pod okay.

So I can try to put this in two lines okay.

So this is the name for this part.

And in the same way for this part the name is going to be with MySQL one okay.

And for this pod it is going to be MySQL two.

So let's put that here.

And now you can see it here.

You can this catalog app.

Right.

So now you are uh now this catalog app here, we can come here.

And directly we can point to this part using this DNS name.

Right.

So always for rights Catalog can request to this respective master server without any issues.

And you have some other apps for only reading.

So some app two and some app three.

And they only do the database reads.

So for them you can directly point them directly to these read servers.

Okay.

So you can point them because you have their own dedicated DNS names and how that is possible.

That is possible because you are using the headless service.

So that's the important thing here.

So this is the advantage of using headless service.

So pod name dot headless service dot namespace dot svc dot cluster domain.

So that is the key thing to remember the format.

Whatever we have written here.

Now you might ask the question how to create the headless service.

So that is the key important thing.

It is same as cluster IP service.

But there is one thing which changes the whole picture that is in your cluster IP service.

If you put an additional argument right or additional key value pair saying cluster IP, none.

So when you put this, this becomes your headless service.

That is all in your cluster IP service.

If you add this cluster IP colon none then it becomes the headless service.

So that's about the headless service.

So now you might ask me a question.

So we have discussed here pointing pointing the slaves to M1 how we are going to do so that we are going

to do using this via headless service only.

So we will deploy the headless service.

And using that headless service only, these slaves should be able to communicate to master.

And this master gets the unique name because of this headless service.

Not the pod name or not the pod DNS, but a headless service DNS for that respective pod will be the

pointing element for service to service communication or port to pod communication how you want to implement.

So that's about in Kubernetes.

Statefulsets the importance of headless service.

So in our next lecture we are going to go ahead and review the Statefulsets YAML manifest.

And also Kubernetes headless service YAML manifest.

I'll see you in the next lecture.

Until then bye bye.

Thank you.

And before that another important thing.

So we need to tie means like in your Kubernetes stateful set right in your Kubernetes stateful set there

will be a service name argument, right?

So there will be a service name argument.

So our service name key value pair or whatever it is right there.

You are going to point to your MySQL h which means how you are statefulset and headless service is tied

is in your statefulset you are going to have the service name and you provide your headless service

so that those two are tied together.

Because in our cluster there might be many headless services available, but our specific catalog statefulset

how it need to connect to how it need to be in line with the headless service.

What we have created for this respective statefulset is there will be something called service name

in your Kubernetes statefulset manifest, and there you will give this MySQL or whatever the name of

your headless service.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you

All right, let's review the Kubernetes statefulset YAML manifest Statefulsets.

Also, the entire template is going to look like same as deployment with minor changes.

Let's discuss them primarily.

The kind is going to be Statefulset.

And another one.

The major difference is the service name which we specify more or less.

Rest all is going to be as is as deployment.

Now let's start with the review.

So these two are primarily the API version is apps v1 and kind Kubernetes resource is Statefulset.

In the metadata section we have the name of our Statefulset is catalog hyphen MySQL.

And we have also added lot of labels here like name is catalog, instance is catalog and component is

MySQL and owner is retail store sample.

These are standard things, not much important.

Labels are optional here just for filtering and stuff.

But whenever we define a stateful set or a deployment.

Whatever we specify in selector match labels is the important labels which we need to be careful of.

Okay.

And if we go to the Statefulset spec we have replicas.

We said create one replica and here is the important one which is service name.

So this is the key important one which we need to know.

So this actually ties the stateful set with our headless service.

What we create which is in your statefulset you are going to specify service name and your headless

service name.

So my headless service name is going to be catalog hyphen MySQL.

So that I have specified here.

So that's about a service name.

And in the selector match labels we have defined all the things whatever we have defined in labels also.

But important thing here is this one.

So for all the manifests, whatever we see going forward we will find this name instance owner common.

But in the component thing whatever you see the differentiation component.

This this component is a MySQL component.

So we have set it as MySQL.

And if you talk about if we go back to our Visual Studio Code and if you go to any deployment.

Right.

So this is a deployment.

And for this we have set it as component as service.

Service is nothing but it's a microservice right.

So we just set it as catalog service.

So component is service for this.

So its own cluster IP service also will have the same thing which means service.

So that's the thing.

So rest all is going to be same catalog catalog retail store.

But this is the differentiator item which this service checks for.

So in the selector labels whatever you have is service.

And it will check for the pods matching with the service.

In addition to catalog catalog retail store sample it will also check for service.

And this is the catalog microservice.

And for that you have the service.

So all the requests coming to this catalog service will be sent to this catalog, uh, deployment related

parts Because of this, whatever we have defined here selector match label service.

So that's the key thing to remember in the same line for these things.

Also, we have given the selector match label as my SQL remaining.

All are going to be same now if we go here in the stateful set.

So this is the pod template.

In Pod template in metadata we have the standard labels what we have seen above.

So and this is the pod template specification and container name.

We are saying MySQL and we are using the MySQL 8.0 as the Docker image.

And if that image is not present locally then pull that Docker image.

And we have defined our environment variables here which is MySQL root password.

So this is the password we have defined name is this one.

You need to set the environment variable as is here how it is so that this instance can be initialized

properly.

So MySQL root password and value is my secret PW and MySQL database is catalog DB and MySQL user is

catalog user and MySQL password is db 101.

And even these environment variables whatever.

Here defined, we can also create a config map like MySQL mysql config map and define them there.

And you can use env from config map reference right config map ref and provide your config map name

right.

So all those things you can do if you want.

Okay so here it is with environment variables we have defined.

But as an assignment you can try this.

Okay.

So you can create a new config map.

And that config map you can reference here in this template okay.

And here are the ports.

So name of the port is MySQL.

This container port this MySQL database container port is going to be 3306.

And protocol it is going to use is TCP.

So that's about Statefulset.

And some more important things in stateful set are we are going to in our pod.

We are going to define a data volume of type mkdir and then mount that to our container.

So mount that to our container at var lib SQL means like in this location in my container.

In this location I am going to have the actual database files present.

Database actual files will be present at this location.

So currently we are using the volume as mkdir.

So volumes we are using is mkdir, which means mkdir is a ephemeral volume.

Ephemeral volumes are temporary.

When you are stateful set is deleted your data is gone.

So then why are you using ephemeral volumes?

Here we are implementing a production grade or real world implementation.

So here comes the answer for that.

So we have introduced headless service concept Stateful set concept as part of this demo.

So I really don't want to introduce one more concept here, which is persistent volumes.

So what I have done is later we will learn the Kubernetes secrets, and after Kubernetes secrets we

will go to the storage.

So there we will introduce the persistent disk concept using EBS CSI driver okay.

Elastic block storage persistent disks.

We are going to mount.

And then those are going to be persistent data disks.

So once we go to that section we will learn that.

But for now our focus is on key important concepts like headless service and statefulset.

And using a simple mkdir ephemeral storage as our underlying storage, we are going to do so.

That's the reason we have used simple mkdir here.

So that's about statefulset YAML manifest.

In our next lecture we will focus on reviewing the headless service YAML manifest.

I'll see you in the next lecture.

Until then, bye bye.

Thank you

Alright, let's discuss the complete YAML for Kubernetes headless service.

So as usual.

So we have the API version v1 and kind is service.

We are going to create a Kubernetes service.

And in metadata we have provided the name of this headless service is catalog MySQL.

So this name should match with the name we have given in service name here.

Okay.

So this is the service name which is matching which is catalog MySQL.

So matching is good.

And next thing is labels.

These are for filtering and uh discovery and for also dashboards and other stuff.

So standard labels which we are using for all the Kubernetes resources.

So we can put that aside.

But the important thing here is the spec section.

And there we need to discuss about this cluster IP.

None.

So let's understand this why we have defined that way.

So the key line here is cluster IP.

None.

So that single line tells Kubernetes hey don't assign any virtual cluster IP for this service.

So earlier it used to be type equal to cluster IP.

And now that changed to cluster IP equal to none.

So we are telling that we don't need any IP address for this catalog MySQL service.

Instead this becomes a headless service.

And what does that headless service mean.

So this headless service allows each part to be reached using stable ness name.

So stable DNS name.

So you will have something called catalog hyphen.

MySQL is your uh your stateful set name.

So here you will have zero one and then two if you have three replicas created like this.

If you have five replicas then three and then four.

So like that this name is going to be common.

It will increment accordingly.

And next.

So that's about cluster IP.

None.

So we have defined the ports.

This service port is going to be 3306 and target port.

We just said MySQL.

Or you can also give 3306.

Both are same okay.

So you can also give 3306 which is your container port okay.

Container port on your stateful set, whatever the pod it creates and name of this port is MySQL.

And here comes the important thing which is selector labels.

So these are very very important.

So whatever the request coming to the service if it need to be sent to the pods created using Statefulset.

These are very important.

So these should whatever we are seeing here, these should match with the selector match labels.

So I have put it here clearly right.

So these should match with the selector match labels present in your Kubernetes stateful set.

Right.

So that's the important thing.

And in these things again internally, another important thing is these might be common in many of the

components okay.

But for to identify the catalog MySQL service is sending traffic to statefulset pods.

Exactly.

This is the differentiator.

So the component is the differentiator.

If both are same, then whatever the pods created by the Statefulset requests coming to the service

can be sent there.

So that's the important thing.

So in short this headless service doesn't load balance traffic.

Instead it provides direct DNS based access to each pod managed by the Statefulset.

It's what gives every MySQL pod a predictable, stable network identity in our Kubernetes clusters.

So which is critical when we start dealing with replication or even cluster awareness later.

Let's go ahead and then practically implement this overall demo and understand it in detail.

I'll see you in the next lecture.

Until then, bye bye.

Thank you

Welcome back.

We are entering into the demo 0805 Kubernetes Stateful set.

So we have reviewed all the things related to statefulset all the manifests.

Now we will go ahead and then deploy those manifests.

So we'll go to our Visual Studio Code here.

And we are in 0805.

So let me close all these things.

And in 0805 we can see in catalog manifest we have the deployment YAML cluster IP service Configmap

for our deployment.

And there we have the information about our persistent provider is MySQL and catalog persistent endpoint

is catalog MySQL colon 3306 which is exactly your headless service name catalog MySQL.

So that is not the right way to point.

If you want to point to exactly the master server, we need to give the complete master server endpoint,

but let's see what will happen.

Okay, so why?

Because we are only creating one pod here.

So ideally should not be an issue replica.

Okay.

So that's the reason we really don't need to worry about that.

Uh, persistent endpoint and Dbname catalog DB and user.

We are saying catalog user and persistent password is DB 101.

So this should match with your statefulset details here.

So if you go here DB name is catalog DB and user is catalog user and password is Kalian DB 101.

So those are matching.

So which means for our application to get initialized and get connected to the database there should

not be any issue.

And this is the headless service related information.

So all these things we are going to deploy and then verify now.

So we will go here.

And we will also go to our documentation here.

And this is the step.

Step four apply all manifests.

So we'll go to zero eight Kubernetes Foundation and inside Kubernetes Foundation we are in 0805 Kubernetes

Statefulset.

And here we have the folder catalog manifest.

So we'll say cube ctl apply hyphen f and deploy this Kubernetes manifests.

So let's wait for all the resources to get created.

And after that we will run the command cube CTL get Statefulset.

So you can also say SS.

Mostly it should be the alias for it or S TS should be the alias I don't remember.

Yeah STS is the Statefulset alias and it is saying catalog MySQL created.

And we'll also say get pods hyphen.

Oh and then wide.

And it should create a Statefulset pod.

So this is the catalog MySQL pod is getting created.

it has created and this is the IP address.

And on this node it is scheduled.

And let's wait for that and run more one more time.

Run the command get pods and you can see catalog is also running now which is our catalog service related

part, our microservice related pod.

So this is coming from our catalog deployment and this is from our stateful set.

So you can see catalog hyphen MySQL hyphen zero.

Right now what we will do is we'll say kubectl logs hyphen f.

And this is the pod you need to get the logs from which is our catalog microservice pod we for catalog

microservice.

Also we have only one pod.

You can also say simply deploy slash catalog also.

So you can get the logs for your catalog pod.

And you can see it here it is saying using MySQL database catalog MySQL 3306 running database migration.

Database migration is also complete.

so it didn't see any issue.

It has worked as expected.

Now what we will do is we will first go ahead and then do the DNS testing.

So we want our pots right.

Pots DNS name using headless service.

So for that we can run a BusyBox 1.28 image.

So we'll go here and we will get this image right.

And then we will run this image right.

And then inside that right.

So we have connected.

We have created a BusyBox 1.2 DNS test.

Image means like we have created a pod.

And inside that pod we'll run the command and lookup catalog hyphen MySQL.

That is our headless service name.

And let's see what happens.

So it is saying that uh, it is resolving to the some IP address here.

So what is that IP address?

We will see ten 002.52.

Okay.

So now we'll go to another tab.

So I'll say clear.

So this is all terminal.

So I'll say clear and I'll say cube CTL get pods.

And let's see the IP address of that pod okay.

So hyphen o and then wide.

And it should be our ten zero 1210 zero 1252 which is our catalog MySQL zero pod.

It is resolving to the pod.

Exactly.

So this ten zero 1252 is the pod name.

And this is the DNS name which is catalog hyphen MySQL.

Hyphen zero dot catalog MySQL dot default dot svc dot cluster dot local.

So if you go back here whatever we have discussed.

So let me go up okay Pod name dot headless service dot namespace dot sec dot cluster domain thing.

Okay so cluster domain dot local.

So this is the pod name catalog hyphen MySQL zero.

And this is the headless service name.

And this is the default uh uh namespace SVC cluster dot local.

So when we run the Nslookup for our headless service, it is giving the catalog MySQL related, uh,

pod related DNS entry in detail.

So this DNS entry, we can go ahead and then update in production grade implementations for read and

writes.

If you want you can go to your config map here for your catalog application to point to this.

So you can point this like this.

And then you can point to so you can say like this and I'll try to copy this And we will take this and

point to this directly if we want.

Okay.

Instead of giving it to the headless service, we can directly point to it.

So if we want we can test this also.

Okay.

So we'll click on exit right.

And kubectl delete hyphen f catalog k test manifest.

And let's delete everything.

Okay.

So we have pointed specifically to the master server here instead of the headless service.

So this is the headless service.

And this is the exact uh um pod okay.

So we will redeploy again cube ctl apply hyphen f and catalog k et s manifest I have saved this file

so which is good.

And I will deploy this okay.

And now let's see all those things.

What happened okay.

So we'll say cube CTL get pods and catalog pod should be MySQL pod should be creating first.

So let's wait for it to get created.

And after the MySQL pod is ready then catalog pod will go into the running state.

So let's wait for it.

So it took finally three restarts to come online, but finally it is online now.

So if you see cube CTL get pods and you have it running.

So we'll also say cube CTL and then logs hyphen f.

And we'll also verify this catalog pod logs okay.

Catalog microservice logs.

So let's see using MySQL database connection.

So and so running database migration.

And this is successful.

So it has very we'll focus on ordered pod creation using scale up and then scale down all these things

we are going to learn I'll see you in the next lecture.

Until then bye bye.

Thank you

Now let's go ahead and then test the stateful set ordered prod creation using scale up option using

cube CTL scale.

So we'll run the command.

So we'll try to copy this and run the command here.

And it will scale your ports.

If I say cube CTL get parts and hyphen w watch the parts.

So it should first create the one pod.

And then after that MySQL two port.

So zero is already there.

So six seconds before it has created MySQL one pod and five seconds before it has created MySQL two

pod.

So all these things right.

This sequential creation ensures stability, especially for databases that depend on ordered startup

like master replica systems.

So if you want to also scale down.

So we can just say replicas to one.

And here what happens.

Let's see okay so we said replicas to one and we will watch them.

So the deletion order if we observe.

So first it is going to terminate the second one okay.

So catalog MySQL two first and then it will terminate the one okay.

So MySQL hyphen one.

So only catalog catalog MySQL zero remains.

That's the ordered termination behavior of stateful sets.

So they scale down gracefully exactly in the opposite order or reverse order which is two and then one.

So that is about scale up and then scale down and understand the ordered graceful creation and ordered

graceful, uh, scale down.

Okay.

Now let's also see something else.

So if we run the command cube CTL get pods right.

So you have your catalog MySQL running currently.

Okay.

And if you try to delete the pod what will happen.

Again.

It will create the same pod with the same name.

So that's the thing we want to know right.

So whether it will create or not.

Right.

So we can say cube CTL delete pod and we'll say catalog hyphen MySQL hyphen zero.

Right.

And let's wait for it to get deleted.

And now if you say get pods stateful set will go ahead and then recreate the pod again just three seconds

before it has recreated the pod.

So now one important thing you need to know here.

Okay, so we have successfully tested that when we create the when we delete the pods or parts get crashed

automatically in Statefulsets, they come back online with the same pod names.

So that thing we have tested.

But one thing happened here earlier when this catalog deployment started, right?

It has said that it has migrated all the scripts, right?

So if you go to kubectl logs hyphen f and then if you see these logs right.

So if you see the logs of this respective catalog application.

It said that running database migration database migration complete.

So when you start the catalog application, it has some data inside it that it has moved to the database.

But now you have restarted this database means like you have terminated this database.

It recreated a new instance.

You don't have the data because you are using the empty dir here.

When we go to the later advanced sections, we are going to use the EBS persistent storage here.

But currently we are using only empty dir, which means whatever the data migration happened, right?

So that is gone now.

So what we need to do, what we need to try to restart okay.

So kubectl rollout restart.

And if I restart our deployment catalog then again it will try to uh do the stuff okay.

So if we go here to get pods and it will create a new pod here and when creating it it will go ahead

and then also uh run the database scripts again if those are not present in the database.

So this is a temporary fix, which we have done in this use case because of we are using the empty dir.

So now if I say logs hyphen f and if I try to see the catalog part new part and it should show me using

MySQL running database migration, it is complete.

So nothing but catalog related basic data which we access via browser, get products and all those things.

It has rerun those things in the new database, whatever it has created after our deletion, what we

have done to test whether it creates the same pod name or not.

So that's the one thing which we need to know.

And another very important thing which we need to know here.

Right?

So if we go back here in our uh, this one.

Right.

So we said that MySQL master slave and then slave, all these things.

Right.

So before we move on to the next step, we need to understand one thing clearly about this Replication.

So when we scale MySQL to multiple replicas, Kubernetes simply creates independent MySQL servers.

It doesn't automatically replicate data between them.

It's not the Kubernetes responsibility or it's not the Kubernetes task itself.

Right?

Why?

Because it is internal to that respective application.

So MySQL you need to treat it as one application which you are deploying in Kubernetes.

So how it replicates, what are its configuration things and all those things are irrelevant to Kubernetes.

Its job is as a stateful set.

I will go ahead and create a new pod when the replicas are added there.

So that's all.

If you want to implement true master replica replication, we need to configure it manually using custom

scripts.

Or you can go to Bitnami MySQL.

There you have the custom scripts available.

Using that we can do okay.

And so remember Statefulset provides identity and stability.

But replication logic is up to us.

It's not only for MySQL, but for any of the stateful application, right?

So it might be Redis or it might be PostgreSQL.

So anything.

So all these things, it's that app specific okay.

Not Kubernetes specific.

It is app specific only.

So that logic Kubernetes will not handle.

So we have just now increased our replicas to zero to our 1 to 3.

So why the data is not present in slave one or slave two.

It will not be present.

So that's what I am trying to tell here.

Now we will go ahead and then verify the database connection inside the cluster.

We can connect to the database and then also verify all the catalog tables and all those things created

or not.

So we'll go here and we'll go to this one.

And we have the command ready.

Right.

So here we will have the command.

So this is the command cube CTL run.

And we are running it as a MySQL client.

As our pod and image we are using 8.0 and we are connecting to catalog hyphen MySQL which is our headless

service and user is catalog underscore user and password.

We will give now okay so let me run this command.

Now we will say clear and we'll also provide the password colon 101.

And we are inside the DB.

So we'll say show schemas and we can see catalog DB is there.

We'll say use catalog DB.

And here we will say show tables.

And we have the thing.

So we say select star from products.

And we can see all the items.

Here are select star from tags.

And we can see the tags information here.

So that's about it.

So connecting to the database and then verifying the things.

And finally we can also access.

So if you go here cube CTL get deploy.

And also we can see get SVC.

We have our catalog service also fronting this catalog deployment.

So this is the catalog service.

So we will put forward to this catalog service and then access our application and also test it.

So why do we need to test it.

Because we can verify the topology endpoint in the topology endpoint.

Whether it is showing MySQL as our endpoint or not.

Also we can verify.

So we'll say cube CTL port port forward.

And we will say SVC slash catalog hyphen service and 7080 colon 8080 is my catalog service port okay.

And 7080 is my local desktop port.

And we will go to the browser here.

Right.

So we can go here and we will go here.

and we will say we can say localhost 7070 health and it is accessible.

And we will also access a topology endpoint.

So we will go here and let me move this aside.

And here itself we can go and then try that topology endpoint topology endpoint.

And we can see here database endpoint.

Very clearly it is showing catalog MySQL zero dot catalog MySQL dot so and so so and so and persistent

provider is MySQL.

So we have successfully implemented this.

And if we want we can also go to catalog slash products.

And it is accessible.

So these are the products.

And if you want we can also access tags.

And that is the tags thing.

And if you want we can also say catalog size of our catalog.

We can also display that.

So like this we can verify our application.

So now let's go back here and clean up whatever we have implemented so far.

So we'll say cube CTL, delete hyphen f and catalog cat as manifest from 0805 Kubernetes Statefulsets

demo.

So this should clean up the deployment cluster IP service config map statefulset and also headless service

whatever we have created.

So now let's recap quickly what we have learned as part of this demo.

Right.

So we have deployed MySQL as a stateful set.

We used a headless service for stable DNS.

So we observed ordered creation and reverse deletion of our MySQL databases as parts under stateful

set.

So we connected to MySQL using a client pod and verified our catalog app connecting through Kubernetes

service or not.

All in all, we have learned Statefulset and headless service concept in detail, so I'll see you in

the next demo.

Until then, bye bye.

Thank you

Hi everyone.

Welcome back.

In this video we are going to talk about Kubernetes secrets.

So one of the most important topics for securing your applications inside the Kubernetes cluster.

So now so far we have used Configmaps to store our application configuration like database names endpoints,

providers, username, passwords, time out information, everything right.

But there's one big problem with Configmaps.

They are not meant for sensitive data.

So that's where Kubernetes secrets come in.

So secrets are used to store things like database credentials, API tokens and also TLS certificates

in a secure and encoded form.

So Kubernetes automatically keeps key, keeps secrets in base64 encoded format.

And it restricts direct view viewing of these Kubernetes secrets using Rbac permissions.

So in simple terms, you can put your database username and password in this Kubernetes secret.

and then you can enable the Rbac permissions role based access control permissions to this secret.

So only the people who have access to the cluster at administrative level can access those secrets.

So that way you have sensitive sensitive information is safely stored in the secret and it is base64

encoded.

And also access control policies are applied to ensure that the secret is safe in your Kubernetes cluster.

In this demo, we will take our existing catalog microservice and move its MySQL username and password,

which we have defined in Configmap, and also which we have hard coded in our Statefulset.

So both those things we are going to move into the Kubernetes secret.

And once we move that to the Kubernetes secret, we are going to reference that secret into Kubernetes

deployment and also Kubernetes statefulset so that both deployment and statefulset can use these credentials

without ever exposing them in plain text.

All right, let's jump in and start implementing Kubernetes secrets for our catalog microservice.

I'll see you in the next lecture.

Until then, bye bye.


Welcome back.

We are entering into the demo zero nine Kubernetes Secrets, and in that section we are entering into

0901 Kubernetes secrets.

So all the Kubernetes manifests and also all the steps are documented here in this respective section

for this respective demo.

Now we need to also create a Kubernetes secret file catalog MySQL secrets dot YAML which is zero six

catalog MySQL secrets dot YAML.

If we go to our Visual Studio Code so we can find that here.

So this is the file we have created in 0901 new file rest all as is coming from our previous demo,

which is 0805.

And we'll make the changes wherever required.

Example like in config map will make the changes in Kubernetes deployment, will make the changes and

also in Statefulset will make the changes.

So let's discuss all those things in detail.

So we'll go back here.

So let's go ahead and then first create our Kubernetes secret thing.

Okay.

So we'll go here and then discuss our Kubernetes secrets YAML manifest.

So here you can see API version is v1 which means it belongs to the core uh core APIs of Kubernetes

and Kubernetes.

Resource type is secret.

And in the metadata section you can see name of this Kubernetes secret is catalog DB.

And in this in the data section we are adding two values.

One is retail catalog.

Persistent user is the key and value is whatever we have provided here.

In the same way retail catalog persistent persistence password.

And this is the value.

So what are exactly these values.

These are nothing but base64 encoded values.

So if we go back here right you can go to base64 encode Kalyan DB 101 is the password when I click on

encode I get this value.

So this is the value we have placed it here a to FC.

Whatever we are seeing.

Right.

In the same way for catalog underscore user also.

So we have catalog underscore user as the username.

So we'll try to encode it.

And you can see this is the thing we have okay.

So or we can use directly catalog as the user underscore is not a recommended thing.

So we'll try to use catalog as the user name here okay.

So I'll change that okay.

Underscore is not recommended I so now this is the value.

So we'll go here and update that here in our secrets.

So when you use catalog as the username.

So this is the catalog persistent user value.

And this is the value for your password which is Kalyan DB 101.

So both are base64 encoded.

So that's about uh Kubernetes secret stuff.

And in config map.

We need to comment this persistent user and persistent password.

So we are going to comment them in our current Kubernetes config map so we can see it here.

So those things were commented.

Okay.

And the next thing is we will go to the deployment in deployment right.

So we have something called already env from and env from.

And we all we are already referencing the config map reference okay.

In same line we will say secret reference and name of that secret is catalog hyphen DB.

So you can see it here.

So this secret name is catalog hyphen db.

So that is what we are referring it.

Secret reference and name is catalog hyphen db.

So that is what we are going to do.

The change in zero one catalog deployment.

So you can also reference that here.

We will go here.

And you can see this is the part we have added in env from.

So we will get the environment variables from secret reference named catalog DB.

And next thing is we'll also talk about stateful set in stateful set.

So we are going to have the environment variables actually.

So which means we have the MySQL root password and MySQL database information in the same way.

We also need to get the MySQL user and MySQL password.

So those details we need.

So for that purpose we will say value from secret key reference and name of that secret is catalog DB.

And in that key name is retail catalog persistent User.

And whose value will be added for this MySQL user means like when it is deployed for MySQL user.

Value will be coming from the value present in secret in that secret.

For this key, whatever the value is there that you are going to get.

So if you see this is the one.

So where is that catalog secrets.

So this is the one we are going to get.

And in the same line for MySQL password.

Also we will say value from secret key reference catalog DB.

And the key is retail catalog persistent password.

So whatever the value present in catalog DB secret, that value will be given to this MySQL password.

So you can also see for statefulset how it has been configured here.

So we will also see that.

So you can see here name is MySQL user.

And this is the part we are talking about value from secret key reference name of the secret is catalog

DB and key for which we need the value is this one.

So that way we can configure these things.

So now once we have configured them we can go ahead and then deploy and then verify.

Let's go back to our terminal here.

And we will go to zero nine Kubernetes Secrets.

And in that 0900901 Kubernetes secrets.

And you have everything here.

So we'll say cube ctl apply hyphen f and then catalog k et s manifest.

So this should create our Kubernetes deployment cluster IP service Configmap Kubernetes secret and Kubernetes

Statefulset.

And also Kubernetes headless service.

So let's wait for all these things to get created.

So once these are created we'll focus on Get Secret.

And this is the secret it created.

If we want we can describe this secret also describe secret and catalog hyphen db.

And you can see this is the secret and its value is 11 bytes.

Password and user is seven bytes.

Now we will run the command cube CTL get pods and let's wait for just now MySQL database came up.

Let's wait for it.

Even the catalog pod will come online shortly.

Let's run one more time, get pods and you can see Catalog Pod is running after 60s.

Okay, So now we'll say cube CTL logs, hyphen f and catalog pod logs.

We are verifying and it looks good.

So now what we will do is we'll say kubectl port forward.

And we are going to use catalog service.

Right.

So we'll say kubectl get svc.

And we can see here the catalog service name.

So we'll say kubectl port forward and we will say svc slash catalog hyphen service.

And we will say 70808080.

And once the port forward is ready then we will go back here.

And we have all the links present here at the end so we can access these things.

So this is the topology link.

So we can access it here topology link and see if it is accessible.

It is accessible.

And now we'll also change something.

We'll change this to catalog products and if it is accessible, we are good.

Okay.

And all our products are displayed.

Which means after making the Kubernetes secret change, also our application didn't break.

So we have successfully implemented the Kubernetes basic secrets.

Okay, but these secrets are not 100% safe.

We are just what we are doing is we are just implementing a basic security, and we can still provide

Rbac access control for those secrets so that no one will get access to them.

Only core administrators can get access to them.

But still, you can use some advanced features in AWS, like using the AWS Secrets Manager, and then

implement it so that it becomes a production ready application.

So for that purpose, what we will do is in next two demos, we will focus on implementing it.

I'll see you in the next demo.

Until then, bye bye.

Thank you.


