All right, everyone, before we dive into opentelemetry and key concepts of it are even the dot operator

and all those things, let's understand a very high level about the concept called observability, right?

So observability is your ability to understand what's happening inside your application, something

like our retail store application and also looking into the outputs of our application.

So I said the word here outputs.

So you have a retail store application.

And here you are talking about some outputs here with which you want to understand what is happening

in your application.

So here we need to understand something called outputs are nothing but these three things traces, metrics,

and then logs.

So observability has three key pillars which are called these three things traces metrics and then logs.

So now let's understand one after the other.

So let's start with the metrics pillar.

Okay.

Right.

So metrics are numbers over time.

Think of them as your monitoring dashboard gauges.

Okay.

Right.

So example like CPU usage are like request rate or response times like that or some error rate percentage.

So like this.

All this information we can collect using metrics.

So now this metrics what happens is using these metrics they tell you what is happening and when it

is happening.

So example like my API response time jumped from hundred milliseconds to 10s at so and so time.

So example like some 3 p.m..

So now you know there's a problem.

But metrics don't tell why that problem happened.

So metrics will tell what and then when only.

So next comes the other pillar which is logs.

So logs are event records.

So time stamped messages about what happened to your application.

Example like in your log itself it will say error database connection timed out or something like a

warning saying that retrying the connection attempt one of three.

Something like that.

It keeps telling something about in your logs.

So logs tell you what went wrong.

Right.

So that's the important thing.

What went wrong in a specific component will be told by the logs.

So the database connection timed out at something around 3:23 a.m. or PM, whatever it is.

Okay.

Right.

So good.

But if you have ten microservices logs, don't tell you the complete full story.

So if you see our current retail store application itself, it has something called a catalog service,

card service, checkout service.

And even you have something called order service and then UI service.

So only the specific logs saying that database timed out at so-and-so time.

So that doesn't give the complete picture of what happened actually.

Right.

So now here comes the another important pillar which is faces, right?

Traces are the journey of a request through your entire system.

So a single user request might travel through your, uh, ingress gateway.

And then from there it will go to your UI service.

From there it will go to your catalog service to display the products.

And then from there it will go to the cart service.

When you click on Add to Cart.

From there again when it went to the catalog service, which means it need to get the data from your

RDS MySQL database and show it on your UI service, right?

In the same way, when it goes to the CART service, it need to add that to the cart.

At the same time, your DynamoDB table will be triggered where this this respective cart information

will be temporarily stored in the DynamoDB table.

So like this.

It's a complete flow, right?

Next checkout will happen and then it will store temporarily in the Redis cache.

And then after that, it will move to the order screen.

After the payment is completed, a order ID is generated and stored in the PostgreSQL database.

And from there again it will also store in the SQS queue.

So all that is a complete flow right?

So now this traces will have the complete path for you.

So that's about traces.

So so even in this trace you will have something called how long each step took.

And even it will have something called where it failed.

Also we are going to get.

So now the request whatever we have sent might have uh, usually it's response time is like only ten

milliseconds or something, but it took around a huge amount of like five seconds or 10s.

So by reviewing the complete trace, we will understand actually what happened.

So traces tell you where and why.

So that's the job of traces.

So bringing it all together.

So in our real world application, if our application is slow with only metrics, we will know what

and when response time is.

Means like it says, response time is two seconds.

And you know what and when, but not why.

And when you go to the logs, right?

So you will go ahead and then see what went wrong.

Okay.

So example like a database query timeout in a some XYZ service maybe like checkout service.

So now oh but you don't know why it went wrong.

And then you can go to the traces.

And in traces the timeout happened because of something like a checkout service.

Made some 50 database calls instead of one.

And due to that we have seen the slow response times.

So actual root cause can be found and their equivalent information you can find in these two places

to correlate everything.

So in simple terms, metrics will alert you, logs will give you details and traces show you the complete

journey.

So together they give you the complete observability.

Now in our case in this demo we are going to focus on all the three things which is traces, metrics

and then logs.

And now if you go for traces we will use the AWS x ray okay.

So the logs the traces will be sent to AWS x ray.

Or you can also call it as has CloudWatch traces.

Okay, right.

That's about the AWS product we are going to use.

And now for metrics we are going to focus on AMP and AMG two AWS products.

So Amazon managed Prometheus.

So the metrics will be sent to Amazon managed Prometheus.

And this we will we are going to add in our Amazon managed Grafana as data source and visualize our

databases.

So pretty very important setup and mostly asked in interviews and everything.

So that's the reason we are using this.

We can even send it to CloudWatch Container Insights or even direct CloudWatch metrics also.

But this is the most important thing to be known and most generalized approach also.

So for that purpose for metrics, we are going to use AMP and AMG and for logs we will use CloudWatch

logs okay.

Now so these three things right we are going to use three AWS products to implement.

Okay.

But the important thing is how are we going to collect these logs and send to these three products.

Right.

So there comes the important thing which is a dot.

Opentelemetry collectors okay.

So AWS distro for Opentelemetry.

And we implement the Opentelemetry collectors to collect the logs from our applications, collect these

three things from our applications and then send it to these destinations.

So that's what we are going to do in this demo.

So in our next lecture let's understand what is opentelemetry.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right.

Now that we understood about observability in our previous lecture, let's understand our answer.

The next logical question how do I actually collect traces and metrics from my applications.

Right.

So the answer here is open telemetry.

So what exactly is this open telemetry.

So let's understand that in detail.

So open telemetry is an open source framework for collecting observability data traces metrics and logs

from your applications.

But more importantly this open telemetry solves a huge problem in the industry today which is in in

the biggest problem, what we are seeing is vendor lock in, right.

So that it primarily solves.

So we have made the entire observability concept as open source.

Now we need to follow these open standards so that all the vendors also adapting to these open standards,

so that we will be able to save lot of money in not changing any of our application code.

So let's understand that okay.

So let's say you are building a microservices application and you want to implement observability for

that.

So now without opentelemetry what happens.

So this is I just said ten microservices here.

But it might be 100 microservices or even 200 microservices based on your application.

Right.

So app with ten microservices.

So you want to implement traces metrics and then logs.

So now what you actually do is on that particular time when you are implementing this application,

right?

You felt that for traces jogger SDK is good and jogger application is good.

So I will use this SDK means like I will write my ten.

Are these hundred applications microservice applications.

Uh to use this SDK, which means in my code for these hundred microservices, I have used this SDK in

the same way for metrics, I have used this Prometheus client library and I have implemented everything

is working fine, so all the traces will be sent to jogger SDK and via jogger SDK means like it will

instrument all the traces and send to this jogger app, and all the metrics will be sent to this Prometheus,

uh, application.

And there I will query them using Grafana and then see the dashboard.

So everything is working for me.

But over a period of six months or even one year, I felt that this is not good for my thing.

Okay?

And I want to change.

Or a global policy in my organization changed that.

You should use only AWS.

Something like that.

Okay.

Something happened in my organization and they want to change it.

So now they want to change to traces for AWS x ray logs and metrics for AWS CloudWatch okay.

So they want to do that okay.

So it's a organization standard.

They changed it everything.

So know all these things are not needed open source or something.

We don't need to manage.

Or even if you want we can.

We also need to use AMP and AMG which means Amazon managed Prometheus and Amazon managed Grafana.

So that's the standard.

It changed okay.

Now what is the effort level here.

Right.

So the effort level is huge here okay.

So why.

Because now for these things to work with x ray then you need to have the x ray SDK right.

So that's the important thing.

Maybe Prometheus if it is AMG or m amp.

It might work.

Okay.

This one.

Okay.

But the thing is, if it is cloud watch, then you need to use again CW agent or something.

Fluentbit whatever is needed here, you need to put them primarily this SDK need to be changed to x

ray SDK for this.

Okay, so you need x ray SDK to point to this AWS x ray for your traces.

So if it is ten microservices, one level of effort, if it is 100 microservices, another level of

development effort is needed here.

So this is like means like your application is vendor locked.

So it is pointing to this because of this respect to SDK.

And you have no option today other than changing your application to move to other observability infra.

So that's the current problem we are facing today.

So now what happened is this is before Otel Open telemetry.

Now with the hotel, what happens?

Okay, with open telemetry, what happens?

Okay, so with open telemetry.

So you will have the Autel SDK which supports multiple vendors.

So v1 v2 v3.

So it's a open standard.

So open standard in the sense everyone who is operating in this space.

Either it might be AWS or Datadog or Azure or whoever is the people who are providing the observability

solutions.

They need to follow this open standard.

So when they follow that, which means you can you can have the option of switching from one vendor

to another vendor quickly by just making some configuration changes.

So let's say this is vendor one and this is vendor two AWS is vendor one.

See this is customized one.

So this is vendor one.

This is vendor two.

Just say like this okay.

Now here your application will be built with Otel SDK.

Right.

And which means there is and this one Otel supports multiple vendors.

You really don't need to go and then change this, right.

So whatever you have implemented for your application.

So now what you do is and here another thing is there will be something called Otel collector which

will collect your traces, metrics and logs.

And from there you configure the exporters where you want to send it exporters in the sense to which

endpoint or which destination you want to send it.

Which means here everything is common.

Till here everything is good, okay.

But from here on it at some point of time means like same trace you can send to Jaguar and same trace

you can send to x ray.

So you can even implement multiple observability infra solutions with this approach also.

So here example like now you have all your data example traces in your Otel collector.

And using exporter one it can send to this Jageer.

And then using exporter two it can send to x ray.

In the same way if you talk about metrics also using exporter one it can send to Prometheus.

And then using exporter two it can send to AWS CloudWatch metrics.

So like this we have multiple.

It opens to a lot of multiple options okay.

Which means now in future like after one year you are planning to switch to some other new infra, new

observability infra.

Then it is just configure another exporter here and then point it here.

Right.

If it is provided in the supported list of your opentelemetry that's all.

Okay.

There are no changes from your application.

No changes in your SDK, no change only in your collector A configuration.

You add this and this is what it is simple YAML file okay.

And then you deploy that and just deploy in your Kubernetes.

And this Otel collector pods will get restarted due to that.

Other than that nothing.

Okay.

And then a new observability, uh, infra will be available for you where it is collecting all the things

related to your applications directly in this new infra.

So that's the key thing here.

So that's the key thing we need to remember that Opentelemetry solves the major problem which is vendor

lock in.

And it simplifies lots of things in a configuration way.

Means like instead of going and changing the code, everything is configurable, right?

So it makes it like just making some changes in YAML and then deploying it.

So that's about opentelemetry.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

In this lecture we are going to focus on something called a dot which is nothing but AWS distro for

Opentelemetry.

Right.

So now in previous lectures we understood about observability and opentelemetry.

So Opentelemetry is a open source thing.

You can directly download and then install the Opentelemetry directly.

But if it is in AWS, AWS provides it has a directly.

Add on.

So which AWS manages.

So you can directly use it okay.

And this one it supports collecting traces and collecting traces and collecting metrics and correlating

traces and metrics and all those things so you can send it to X-ray, CloudWatch, Amazon Managed Services

for Prometheus and any other partner monitoring solutions.

Also, you can send your information using this AWS distro for Opentelemetry.

So in our case.

So this a dot right.

What actually it does is it is an ECS add on right.

It will install the it will install the a dot means like Opentelemetry operator.

Right.

And using that operator only we will focus on creating the collector.

So this operator is nothing, but it is the brain that watches for your collector definitions and then

creates them.

So that's the thing of operators job.

Okay.

So using ECS add on it only deploys the a dot operator.

So let's understand that in a architecture diagram model okay.

Right.

So if you see here right.

So this I have took it from our otel documentation of a dot.

Right now you will install your Our hotel operator means, like, uh, whatever the hotel controller

manager has deployment, it will be installed and its metrics service will be installed and its webhook

service.

So using EKS add on, using EKS add on, we are going to install this okay.

Using EKS add on this operator will be installed.

And now let's talk about the collector okay.

So one component here is operator.

So that we just now discussed okay.

So operator is nothing but the brain that watches for your collector definitions and creates them.

So the important thing is it's only a administrative thing.

But the key thing which does the job is otel collector.

So here is the otel collector.

So now you need to understand one thing here otel collectors you can deploy as a deployment or a a stateful

set, or even you can deploy it as a sidecar component in your application pod, or you can deploy it

as a daemonset.

So in these many ways, you can deploy your Otel collector.

Okay, so the Adot collector or Otel collector is the data pipeline that receives processes and exports

telemetry data traces and then metrics.

So it is deployed in four different ways.

So one is we'll start with this sidecar one which is pod level collection.

So runs alongside your application container in the same pod and collects traces metrics from that specific

application.

So this is needed when which when each app needs a custom collection configuration.

At that point of time you can use this sidecar.

And we also have something called Daemonset.

Okay.

So this is the Daemonset mode.

So which runs one collector per Kubernetes node.

When you run it as daemon, set mode.

So it collects traces metrics from all ports on that specific node.

So you are doing the node level collection.

So this is pod level collection.

So this is node level collection.

So centralized collection per node.

Mostly we use for metrics even for logs.

Also we use this approach.

Okay.

And when you talk about collector as a deployments this is a Kubernetes deployment.

So here runs as a regular Kubernetes deployment acts as a centralized gateway for all traces or metrics.

And that is one way of deployment.

And we have one more which is persistent collection, which means using Kubernetes stateful sets.

So runs with persistent volumes attached to it.

Okay.

So each collector instance has a stable identity and then storage.

So when you need a buffering or queuing in your telemetry data at that point of time.

You can use the stateful set approach.

Okay.

But all in all we are going to use one as deployment and one as daemon set.

Okay.

So these two things we are going to use in our use cases.

Okay.

Now let's understand the flow here.

Okay.

Right.

So you will install the operator.

And then you will define your collector configuration and then deploy your even the collector right.

And then what happens is once you deploy these collectors right in your application also you will enable

the opentelemetry right.

So in your helm charts, if you remember when we are implementing the helm charts in the default values,

you will find Opentelemetry should be enabled or not.

So we will enable that.

That means in your Kubernetes deployment there will be an annotation related to Opentelemetry which

you are going to enable that.

Okay.

As part of helm chart changes we will do that in our upcoming lectures okay.

But the key thing is you will enable that.

And when you redeploy your applications or restart your pods, then it will start collecting the data.

So this example you have deployed this collectors deployment and this collectors deployment from your

application pods.

It will start collecting the data provided.

Your application pods exposes the metrics endpoints.

So when you enable that annotation related to Opentelemetry, then it will be able to scrape the metrics

from these respective pods.

So now once you scrape the metrics and then from uh, all those information, then those information

will be processed inside this collector.

So how you want to process the data means like you want to add additional labels to the data or whatever

you want.

You can do the processing in your collector.

And once you do that processing then you will export that data.

So you can see you are exporting the data here okay.

And you can export to the destinations whatever you can define in the export configuration in your collector.

Config.yaml.

Okay.

Right.

So what you will do, you can export them to Prometheus or you can export them to CloudWatch.

You are specific metrics I am talking.

So metrics you can expose to, metrics you can expose to Prometheus and even even CloudWatch metrics.

Also you can explore export means like the metrics can be sent to CloudWatch also and traces you are

sending it to x ray.

So like this.

So you can send everything to those respective destinations.

So key thing here you need to understand here is one is operator who is called the manager who creates

and manages the collectors okay.

And who is the collector.

Collector is nothing but the data pipeline.

So he is the data pipeline which receives the telemetry data and then processes them and exports to

AWS.

That's the architecture.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

In this lecture we are going to review the folder structure for our section 20.

So now in section 20 we have 20 zero 120 zero 220 zero three.

And then 20 zero four.

And in 20 zero one we are going to have the ECS cluster environment, which means like create VPC,

create ECS cluster, and also create Terraform manifests and deploy Terraform manifest related to Carpenter.

And then we will also have the Opentelemetry project here.

Okay.

So in this Opentelemetry project we will install the a dot x add on and other add ons which are required

for us like cert Manager, Kube State Metrics and Prometheus node exporter.

So all the open telemetry related configurations we will implement in this zero five open telemetry

Terraform manifest.

So in addition to installing the add ons, we will also do some more things which we are going to review

shortly.

But here is the folder structure.

So in 20 0101 EKS cluster environment we are going to create the VPC plus EKS cluster plus karpenter

plus opentelemetry.

So all these things are present here.

So these are the five things.

So you can once you review these opentelemetry terraform manifests.

So one after the other you can go into each folder and run terraform commands.

Or you can run this script create cluster with Carpenter and Opentelemetry to create everything.

So as usual you need to ensure that your S3 bucket for Terraform state storage should be updated in

these respective C1 versions and remote state files wherever it is there.

So zero one is primarily related to EKS cluster environment in 2001, and zero two is for retail store

app environment.

So for the retail store app environment one is AWS Data plane which we need to install.

And second is our helm charts which we have learned in section 19.

So here what I have done is in section 19 we have implemented the pod disruption budgets and then topology

spread constraints and all those things we have implemented.

So as part of that we used to get three parts per service which is close to like 15 parts it used to

generate and it used to create 6 to 7 uh T3 small instances.

Okay.

So which is a huge cost.

So here I have ensured that in this we are going to have high cost, which means production grade and

low cost, which is simple one part per service to deploy and then test.

So for testing opentelemetry, we don't need the production grade 15 parts to be running right in each

availability zone.

So for that purpose, we have also put low cost, low cost related helm values, which means we will

disable the pod disruption budget.

And then this topology spread constraints everything there and ensure that only one pod will be created

for each microservice.

So I have updated those things and put it when we go there.

We are anyway going to review them.

So that's about section 21 wherein 20 zero one, which means we will create our EKS cluster environment

and we'll create our app environment and be ready in the EKS cluster environment here in 20 zero one.

This is the one which we need.

We are going to have the new one.

Okay.

And inside this zero two we are going to have the low cost related helm values which we are going to

review.

So that's about 20 zero one project.

Now let's come to the 20 zero two project which is Opentelemetry traces.

So in this opentelemetry traces we will have this a dot collector traces dot YAML file wherein we will

deploy the traces collector and then also deploy the instrumentation related configuration and then

test the traces which means from our application retail store application.

Okay.

When we access our retail store application, when the request passes to all the five services.

Right.

So it will go to our catalog and from there one second.

So I'll try to remove this.

Yeah, it will go to catalog from there.

It will go to carts from there.

Again.

It will from UI okay.

From UI ingress.

So this is the path right.

So this whole path catalog carts and then checkout.

And from there it will go to orders and then orders database.

Right.

Postgres all that this complete tracing path we are going to review in AWS okay.

So we will have the uh retail store application.

And then you will have the A dot collector, right.

Which will collect the traces and send to AWS x ray, which is nothing but AWS traces.

This whole thing we are going to review.

That's 20 zero two.

And once we are done we can uninstall these things if we want to.

Okay.

Right.

And then we'll move to the next thing that is opentelemetry logs.

So in opentelemetry logs also we will try to use the A dot collector to send the logs for our applications

from our RCA applications, from ECS cluster, a dot collector, and CloudWatch logs.

So like this.

We will also test that okay, so that's about Opentelemetry logs.

So we will deploy the collector and verify.

So when when we deploy this collector and then traces already our application is deployed.

Then what we are going to do.

We just need to restart our application so that these changes will be implemented.

That's all.

Okay.

So we'll restart the application for traces once and logs once so that all the pods will uh all the

pods, whatever is restarted, which means retail store application.

Then this collectors will be able to scrape the metrics from those respective pods.

Okay.

So that's only just restarting.

So that's the reason in 20 zero one itself we are deploying everything and putting ready.

So now you have learned about traces and then logs.

Then you will move to something called Opentelemetry metrics.

Okay.

So this is another interesting thing.

Okay.

So here we will install this collector right.

So this collector from our retail store application and also from our Kubernetes cluster right.

We will send the metrics right to a which is Amazon managed Prometheus.

And then we will also have the AMG which is Amazon managed Grafana to view the dashboards okay.

But only one thing we need to remember is this particular demo.

Just watch it okay.

Why?

Because to use this AMG, we need to enable the identity service and create a user.

And that user should be associated with this AMG Per user, it is going to cost dollar nine.

Okay so till here.

Okay.

But this part don't do.

Okay.

Unless you want to experience it first hand.

Okay.

If not you can watch whatever I am doing.

So this is going to cost us.

So for that purpose I am again giving the cost alert here that this part is optional.

Okay.

Right now that's about metrics.

Metrics.

Also we are doing it in a production grade, which means from uh, Kubernetes clusters to AMP to AMG,

the complete flow we are going to implement.

So that's about the folder structure and project complete 20 observability Opentelemetry project, which

we are going to implement.

So let's go ahead and then implement 20 zero one in our next lecture.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

We are entering into the section 20, and in that we are entering into the section 20 01X environment

with a dot AWS distro for Opentelemetry.

So you can see we'll have the EKS cluster environment and retail store app environment here.

So now the idea here is we we need to go ahead.

And then in zero one EKS cluster environment we need to review our Opentelemetry terraform manifest.

So let's go back here and let's understand what all we are going to install as part of this demo, primarily

from EKS add ons perspective.

So earlier all these are the EKS add ons we are using in our EKS cluster.

So if you see as part of section 0902 we have added pod identity agent okay, ten zero one EBS CSI driver

and in 15 external DNS and in section 19 Metrics server.

So like this.

Some things which we have also installed using helm, Load Balancer, Controller, Secrets Store, CSI

driver, and ACP.

Now as part of section 20, which means section 20 zero one, you can say right.

So we are going to install cert manager, which is a prerequisite for Adot.

We will install CERT manager.

Then we'll also install AWS distro for Opentelemetry.

And then we'll also install the kube state metrics.

And we'll also install the Prometheus node exporter.

So these are the EKS add ons we are going to install as part of our 20 zero one Terraform project.

Okay.

So now let's go ahead go down and then review that project related folder structure okay.

So as usual in zero five open or open telemetry Terraform manifest will have the C1 and C2 variables

C3 zero one VPC, remote EKS remote data sources and locals, and Terraform, TFR and Helm and Kubernetes

providers.

So these are standard stuff.

Okay.

So from our previous sections right.

So we don't need to worry about them.

Those are standard.

Only thing is in your C1 versions.

And here C3 zero one and C3 zero two, you need to ensure that your AWS S3 bucket is provided your bucket

name is updated.

Okay, here comes the important things one is C6 01A dot collector IAM role and IAM policy and pod identity

association.

Okay so standard our stuff related to Pi okay.

So for the a dot collector part to access the AWS services.

So we need that IAM role IAM policy and Pod identity association.

So to install the dot x add on prerequisite is cert manager add on.

So we'll install cert manager add on.

Then we'll install the A dot thing.

And then for getting the complete metrics we'll install the kube state metrics and Prometheus node exporter

related these things.

Okay.

So these two add ons also we will install.

And then we also need some Kubernetes related cluster role and cluster role binding permissions, which

we will add them as part of this C609.

So that completes the main A dot stuff.

Okay.

So in this project a dot related things are from C601 to C609.

But in addition to that we will also add something more which is as part of 20 zero four section we

are going to have the A plus A plus metrics right.

So this demo okay.

So which means metrics will be sent to Prometheus.

And from there we will view them using Amazon managed Grafana.

So for that purpose when we are creating this open Terraform Terraform manifest itself, we will create

the Prometheus Workspace.

And we'll also create a Grafana, uh, workspace also.

And when creating it.

It's Grafana related IAM policy and IAM role also we will create okay.

So these are the.

This is the complete Opentelemetry terraform manifest which we are going to create.

Now we will go ahead and then review the Terraform code on a high level.

So we already learned.

Core Terraform in multiple sections.

Uh when during the start of this course we have a dedicated, uh set of resources where we learned all

the Terraform basics.

So it's easy to just review the code and understand what we have implemented.

Okay, so let's go back to our Visual Studio Code.

And we are in 20 observability Opentelemetry.

And in 2001 in 0 one EKS cluster we have the zero five open Opentelemetry terraform manifest.

Okay.

So before going there I just want to show you something very important okay.

In zero to X.

Terraform manifests.

Okay.

If you go to the terraform.tf, you can see the cluster version is 1.33.

So in the previous demos we have used 1.34.

But here a dot a dot supports means like as on today a dot x add on is provided till 1.33 only.

So that's the reason in this observability and opentelemetry section, we have downgraded our cluster

version to 1.33.

As on today, 1.34 is the latest, and we have downgraded one version because a dot x add on is available

till 1.33 only as on today.

Okay, in future when you are implementing it, you can cross check that and then upgrade your cluster

version accordingly.

Okay, that's another important thing to remember.

In our next lecture we'll go ahead and then review all these open telemetry Terraform manifests.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

Now let's go ahead and then actually review our open telemetry terraform manifest whatever we have created.

So we are in 20 observability open telemetry.

And inside that we are in 2001 ECS environment with a dot.

And in that in zero one ECS cluster environment we are in zero five open telemetry terraform manifest.

So here in C1 versions C2 variables, all these are pretty straightforward.

Okay.

So we can see all these are coming from our previous Terraform projects itself.

And C3 zero one VPC remote C3 02X remote same as earlier Terraform projects.

Only thing is in C1 versions you need to update your S3 bucket name here to store your state storage

in your account.

AWS S3 bucket.

And here also in C3 zero one and whatever the bucket name you have used, and in C3 zero two.

Also, you need to do the same thing here in data sources and locals.

These are the standard things which we have defined.

Okay.

And then in provide us helm and Kubernetes providers were defined okay in previous projects.

Also we have defined Helm and Kubernetes providers.

But most of the time we only used helm provider.

But in this section we will also use the Kubernetes provider.

In simple terms, we are going to create a Kubernetes resource in EKS cluster using Terraform.

So earlier we used to do the helm releases.

Now we will also do the Kubernetes related resource also.

Now see six complete section is related to a dot.

So let's review that C6.

Now in C6 zero one it's the a dot collector.

I role we are creating.

So this is the a dot collector IAM role okay.

And for that we already know for our Im roles we provide this a assume role policy.

And this is the assume role policy we are providing for pod identity association okay.

So pods dot x dot Amazon aws.com.

So we have defined it as a data source and calling it here and associating it as assume role policy.

In simple terms it is nothing but our trust policy for this IAM role.

Okay.

And then actual IAM policy we are defining it here.

We are going to review that for a dot collector.

So we said already said that we are going to create three types of a dot collectors right.

One is traces, second is logs and third is metrics.

Okay.

So in that line we have defined the single policy for everything okay.

Right.

Means like multiple permissions we have given for a dot collector policy.

So one is so you can see CloudWatch log permissions.

We have given CloudWatch logs right.

And CloudWatch logs read for querying or debugging.

So these two permissions we have given for this a dot collector okay.

And another thing which we have given is CloudWatch metrics permission.

So this is optional.

So if you want to use in your art collector exporter as CloudWatch for storing the metrics, then these

permissions are applicable.

In our case for storing the metrics generated in our applications via a dot we are storing in AWS managed

Prometheus, which means we need AWS managed Prometheus related permissions, which you can see here.

Okay.

And we also provided for tracing x ray permissions okay.

So x ray put trace segments put telemetry records.

So like this for x ray for tracing we have provided.

So we have seen for logs here.

And then for x ray which is tracing and for matrix.

Also we have seen the permission.

So that is our a dot collector role okay.

Now the next thing is we'll go down here and we will associate the IAM role to IAM policy to IAM role

here Im role policy attachment.

So whatever in C6 zero one IAM role is there.

Right.

So that role and this policy, whatever we have created this we have associated.

So that's about Im role and IAM policy for a dot collector.

As usual this a dot collector need to access CloudWatch or AMP or X-ray which means it need the authentication

to the AWS services.

Right.

So for that purpose we are going to use the Pod Identity Association.

So we'll provide our cluster name here.

Namespace is default and service account.

For this, a dot collector we are going to use is a dot hyphen collector, and we are also associating

the IAM role means like using pod identity association.

A dot collector pod need to access the resources and those resource permissions we have already provided

in the art collector related IAM role.

It's equivalent Arn we are passing here.

So that's about Pod identity association for art collector.

Now we already said that for art collector the prerequisite is means like for the a dot operator or

a dot plugin.

Right.

So a dot operator to be installed as x add on.

So you need to have the cert manager related x add on to be installed.

So first is we will install the cert manager related x add on.

So pretty standard which we have seen earlier for other x add ons also.

So you can query for the default version.

You can query for the latest version and whatever you want to use, you can use.

We will try to use always latest version.

So we are using that okay.

And the next thing is so we will install the cert manager add on as part of C6 zero four.

After that we will install the EKS add on a dot okay AWS distro for Opentelemetry, which will install

the operator, a dot operator or Opentelemetry operator.

So here when we are installing the dot, we know that we are using the latest version.

From here we can get the latest version and use it.

But the important thing is add on name is a dot.

That is fine, but it depends on cert manager x add on cert manager.

So once the CERT manager is installed then only we will install this AWS distro for Opentelemetry.

So those are standard things even for cert manager.

You can see the add on name here which is cert Hyphen Manager.

Now another important thing is we can define some configuration values wherein for this manager right.

So open telemetry manager or operator you can define your resources means like how much CPU it need

to request, how much memory it need to request.

And then what are the maximum limits you can provide for that a dot operator.

So those and even the replica count.

So all those information we have provided inside this configuration for this add on inside this JSON

encode thing okay.

So we will create only a dot operator replica count one.

And then we will provide the limits memory resource requests and then limits information.

And these are standard resolve conflicts and create override and resolve conflicts on update override.

So those are standard things for all x add ons.

We are putting okay standard outputs we have defined.

So we have seen the cert manager add on and then x add on.

So we'll also have the ex add on which is Prometheus node exporter.

Also we will install and one more we will install which is kube state metrics.

Okay.

So these two are the add ons.

Also we are going to install.

So same standard ex add on related steps okay.

Now you might ask me the question why do we need this Prometheus node exporter.

And then kube state metrics.

Right.

So why these add ons are required for us.

Is that a dot add on.

Not sufficient.

It is sufficient for collecting the metrics or collecting the traces and all those things.

But what metrics it need to collect.

That's the key thing right.

So what does kube state metrics do.

That's the key question here okay.

Kube state metrics.

It simply watches your Kubernetes API and exposes the state of your cluster objects as Prometheus metrics.

So that's what kube state metrics will do in simple terms.

What does it monitor is Kubernetes deployments.

Means like how many replicas are there, how many are ready.

All those things.

Or it will also monitor the Kubernetes pods.

What's their status whether they are in pending or running or failed state all those information.

It will also monitor the nodes stating like how many nodes are there, what is their capacity, etc.

and it will also monitor your Kubernetes services configmaps secrets, everything in your cluster.

So it uh, so why do we need this kube state metrics.

So without kube state metrics, we can simply say we are flying blind on our EKS cluster.

So you can't answer the questions like how many ports are crashing or is my deployment at desired replica.

So all those information will be there in bottom line.

Simply it turns Kubernetes State into observable metrics so essential for cluster monitoring.

Now that's about kube state metrics.

But how about Prometheus node exporter EKS add on.

Right.

So this is again a completely different purpose.

So it's key thing is to it runs as a daemonset one pod per node in your EKS cluster and exposes OS and

hardware level metrics for each node.

So what does it monitor?

It monitors your CPU, memory, disk, network and system.

Your X worker node related CPU, memory, disk network and system load.

Average file descriptors, context switches, everything it monitors.

Okay.

So now kube state metrics is for your Kubernetes layer, which means pods, deployment services, config

maps and node exporter is your infrastructure layer, which means CPU, Ram, disk.

So bottom line we need both kube state metrics which tells you what's happening in Kubernetes, and

node exporter which tells you what's happening on actual EKS or worker nodes.

Actual missions.

Okay.

So that's about these two add ons.

So that's the reason we need them.

So we are installing those two things.

And in addition to that we are also installing creating the cluster role and cluster role binding actually

for our service account or collector.

So we are creating first the service account or collector here.

And for that service account.

So you can see here when we are creating the service account resource Kubernetes service account and

name is collector.

In default namespace we are creating.

So now you are creating a Kubernetes resource in your Kubernetes cluster.

then how does this resource work in your ECS cluster?

How the Terraform will connect and then create this resource.

So there comes the important thing so far whatever we are defining here.

So this using this Kubernetes provider information, it will be able to securely connect to your EKS

cluster and deploy Kubernetes resources.

So now because of that it will go ahead and then create first the service account.

And it will also create a cluster uh role here.

And that cluster role is going to have the following permissions for core Kubernetes resources and for

apps resources and any extensions if we have those resources and also the metrics endpoint.

So for all these things we are going to provide the uh access okay.

And then now you have the cluster role with all the permissions.

And you have the service account a dot collector.

So how do you bind them together using the cluster role binding.

So this is your cluster role and this is your service account.

These two things you will bind using Kubernetes cluster role binding.

So this part also we are trying to complete this inside Terraform itself.

Why.

Because at our actual traces logs and AMP will only focus on important things which is how to define

your a dot collector there and if any instrumentation for traces.

If you want to define those things you will focus on all other dependent things.

We are trying to create them using Terraform itself.

So all the dependent resources are all prerequisites.

We are completing using Opentelemetry.

Terraform manifests zero five okay.

Right.

So that's about a dot related things.

In our next lecture we will focus on some more C7 and then C8 things.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


So till here we have completed the art collector related prerequisite configuration C6 012609.

But we also need to focus on something called Prometheus Workspace and Grafana Workspace.

Okay, so those things are required when we go to 20 zero four section okay.

Right.

So for that purpose we are creating both Prometheus and Grafana using Terraform itself.

So that in 2004 when we deploy our art collector with Prometheus related configurations.

So it will start collecting the metrics from our EKS cluster and start sending those metrics to Prometheus.

And we can use the identity center user that need to be associated with Grafana at that time.

Only that Identity Center uses dollar nine will be charged.

Okay, so but as usual, whatever the pricing is there for workspace and then Grafana, the very minimal

pricing.

Those things will be applicable here.

But that bigger amount the dollar nine.

When we associate that user to Grafana, then only it will be charged.

So as a prerequisite, we are going to just go ahead and create those workspaces first.

And when we go to that 2004 demo, I will show you how that data is flowing from our, uh, via a dot

collector to Prometheus.

And after that, it's your choice to go ahead and then create a identity center user and associate with

Grafana or not and spend the dollar nine or not.

But my suggestion, if you see that and understand that should be sufficient initially.

Okay.

But if we are keen in understanding and then doing some more exploration explorations in Grafana, then

it is good to do that or a open source version of Grafana, you can take it and then install on EKS

cluster, and then associate or add the data source of Prometheus in that Grafana and then explore Grafana

features, which is of free of cost.

But here we are implementing a production grid, which means we need to use AMP and then AMG.

So for that purpose we will use in that line only.

So now if you see C7 amp Prometheus workspace it is going to be simple okay.

Resource AWS Prometheus Workspace and provide the name of your Prometheus workspace.

And then tags and then define their outputs.

That's all okay so pretty standard Prometheus workspace.

And when we go to Grafana for Grafana we need the IAM policy and then IAM role.

So why we need that.

Because Grafana need to go ahead and then read the data from Amazon managed Prometheus.

So for that purpose these are the permissions required for it.

So these are related to Prometheus policy okay IAM policy which we are saying it as Prometheus policy,

where we are giving in this policy permissions to read the data from means like get the data from our

Prometheus workspace.

And the second IAM policy we are defining is s policy.

So for Grafana to publish SNS notifications.

So in Grafana not only you can create dashboards and view the metrics everything, but you can also

define alerts.

So those alerts where it need to go again that need to go to SNS right.

So SNS notifications in AWS.

So you can say this as AWS SNS notification.

So there it need to go.

So for that purpose you will also provide that information okay.

So you are sending those things to grow SNS.

So this respective Grafana needs permission.

So you have given those things.

So now the next thing is we are also going to use the AWS X-ray read only access.

So if if if you want to have traces also to be reviewed in your Grafana.

So X-ray read only access okay.

So you can, uh correlate your data.

You can also get the traces data also on Grafana to visualize them or whatever.

Right.

So for that purpose you are also giving the x ray read only access and you'll create an Im role here.

And in that IAM role assume role is going to be Grafana Amazon AWS.

So now you can see here this is the AMG IAM role.

With this as the trust policy we have created a IAM role, AMG service role we have created.

And for that we are attaching the three policies.

One is AMG Prometheus policy, AMG, SNS policy and X-ray read only policy.

So those three things we will associate to that IAM role.

And when we go to the C8 zero three Grafana.

Right.

So here we will focus on creating the Grafana workspace.

So name is going to be local dot cluster underscore name hyphen AMG.

And this is the description and account access type will say current account and authentication provider.

We are using AWS SSO which is AWS Identity Center.

You will also have options for this for uh um, like uh Saml also you will have so we can go here and

then see that resource if you want.

Okay.

So we'll go here and open this and open the Grafana workspace.

And here you can see the options okay.

Where is it authentication type AWS SSO are Saml or even both.

You can use okay.

And uh now we will come back here and we will move on to the next thing.

Okay.

Permission type.

We are using customer managed and enroll a right.

So we are saying aws iam role dot iam iam role dot r and whatever we have created here.

Okay, so these are the permissions we are giving so that we are associating here and data sources for

this Grafana you can use Prometheus CloudWatch and then x ray.

So we have given x ray permissions and even Prometheus permissions currently.

Right.

So if you want to add x ray as the data source you can also do that.

Currently we will do only Prometheus and create a sample dashboard and then review it for notification

purposes we will use the SNS okay.

And here you have some additional workspace configurations like plugin admin Enabledtrue and Unified

Alerting enabled.

True.

So these two things also we have enabled.

So that's about Grafana workspace.

Now in this these two settings are very important settings.

So let's understand them on a high level Right.

So this when we enable plugin admin enabled equal to two, it enables plugin management within the Grafana

UI.

So it allows workspace administrators to install, update and remove Grafana plugins directly from the

Grafana interface.

So plugins are nothing but simple Grafana extensions that adds visualization types.

Data sources are even applications.

So it's it's in simple terms it's very useful to enable that.

Okay.

And another one is only workspace admins can manage these plugins and not all users from security perspective.

And AWS maintains a list of approved plugins for security also.

So now so it's a useful setting.

So we have enabled it.

And another latest feature is unified alerting.

So actually what it does is it enables Grafana unified alerting system.

The modern alerting architecture.

So it replaces the legacy alerting system with a more powerful multi source alerting framework.

So for that purpose it is recommended to even enable that.

So what will it do is it provides us multidimensional alerts.

And it can also temporarily silence the alerts.

Mute mute the alerts.

Your system is in the maintenance period and you want all the alerts to be muted.

That also is there inside that even you can do the alert grouping and multiple notification channels

per alert.

You can define and even alert routing based on labels you can do.

And also you can do the integration with AWS SNS in your config.

So these two are very important settings.

So that's the reason we have enabled it.

and AMG workspace default outputs.

Also some outputs we have defined so standard terraform.tf vars.

Uh so that's all okay.

So these are the things which uh we have a reviewed C1 to C8 zero three AMG Grafana.

So now we will go ahead and then create the EKS cluster and then review the things okay.

Right.

So this create EKS cluster with the Carpenter and Opentelemetry.

So here now in addition to earlier we have this script Till Carpenter.

Now we are going to also add additional thing here in this script which is install Opentelemetry using

Terraform.

So it will go to that folder and run terraform init and Terraform apply auto approve.

That's all.

Or if you want to go manually you can go to every project zero one VPC terraform manifest run terraform

init apply auto approve after that zero two.

After that zero three.

And in zero four, you need to configure your kubeconfig and deploy these three things using YAML.

Okay.

Kubectl apply hyphen f and then zero five.

Again you need to run your terraform init and apply auto approve commands okay.

And another important thing is as our number of things number of uh uh basic uh add ons are getting

increased, you need to install the cert manager add on and Prometheus node exporter add on, add a

dot add on and also kube state metrics add on earlier.

You already have EBS, CSI add on running metrics add on running.

So if you see here right.

So if you go to the picture here so let me go here.

So all these add ons need to run on your EKS cluster right.

So which means uh you are basic EKS cluster.

Earlier it used to be right.

So if you go to the Terraform TF here, it used to be T3 dot small.

Okay, but now what I am doing is to support all these add ons.

I am trying to make them T3 large.

Okay, so our our servers are going to be for this respective A dot demo.

We will use the T3 large this and the next demo, even DevOps ci CD.

Also we will ensure we will use the T3 large.

So that is another change.

I just want to show you now we will go here and we will go to section 20 observability Opentelemetry.

And in that 20 zero one EKS cluster with a dot.

And in that we will go inside zero one eks cluster.

And here you will find the dot create cluster with carpenter and Opentelemetry.

So now I will run this command and this should create our EKS cluster.

Right.

And then also install our Carpenter related Terraform manifest VPC and also open Terraform manifests.

So before that, as these zero five opentelemetry is just now created.

If you want, you can also run Terraform init and then also verify validate plan.

Okay so this is a Pre-tested configuration.

So that's the reason I can run the script as is.

If you change anything then it is better to go inside each folder and create separately which means

like VPC.

Once created then go to ECS.

After that it is created, go to Karpenter, then deploy Karpenter node class node pools and then go

to Opentelemetry.

So one after the other you go.

If you are, you have changed anything and then if you want, if you don't want to run it via script,

anything is fine.

So this will take another, uh, five close to 10 to 12 minutes to create everything.

So in our next lecture, we will focus on moving to the next step, which is uh, retail store app environment.

So once that is ready, then we will move to the traces.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you.


We can see here our X environment creation is successful.

And now we can see VPC, X cluster, Carpenter and even EC2 node class on demand node pool, spot node

pool and Opentelemetry.

Everything got installed successfully.

Only thing we need to verify is what we added newly is these 19 resources related to our Opentelemetry

and all created successfully, and their information also provided here in outputs.

Now we will go ahead and when we run the kubectl get nodes, also all our three nodes are running where

our current EKS add ons are installed.

So we'll go to our AWS console and go to our cluster here.

And in the cluster.

If you see the version is 1.33 as on today and in the add ons.

Right.

So you can see metrics server Prometheus node exporter kube state metrics Amazon Export identity agent

and external DNS.

Also go to the page two.

Here we can see AWS distro for Opentelemetry and Amazon EBS, CSI driver.

So all these things are running without any issues now.

So now we will go back here and we can first verify the Prometheus related workspace created or not.

And it is created.

This is our Prometheus workspace.

And in Grafana also we can see our Grafana workspace created or not.

It also created okay.

Now the next thing is we'll move on to our application installation okay.

So we'll go to our Visual Studio Code here.

And this is done zero one ECS cluster environment is done.

So I'll try to minimize that.

And we'll go to zero two now which is retail store app environment in that first is we need to create

our data plane.

So let's do that now.

So we'll come out of zero one 01X cluster and we'll go to zero to retail store app environment.

And in that we'll go and then create our retail store AWS Data Plane.

So you can go inside this zero one retail store AWS data plane.

And you can go inside this Terraform manifest and you can run the Terraform init and Terraform apply

auto approve commands.

Or you can run the create AWS data plane.sh here directly.

So I am inside data plane and I'll go ahead and then create the data plane.

So in meantime when the data plane is getting created we'll go ahead and then review this in zero two

retail store app helm which means our application with helm charts here we have high cost retail store

helm charts which means it creates each pod in each availability zone.

We have three availability zones.

So three pods will be created per application which is close to We have five services here.

So 15 parts will get created which is going to cost us money.

So for that purpose again I have also created a low cost retail store Helm charts.

Okay.

So these are high cost.

So wherein you can deploy 03V100 means which is related to your without using the secrets manager.

And zero five is with using the secrets manager.

So now if you come back to low cost here right.

Uh which doesn't need secrets manager.

And then uh here these in this if you review the values for this low cost retail store helm.

If you take any of the value cut here.

Okay.

Right.

So if you go down here right.

So in the auto scaling you are saying minimum replicas one okay.

And nodes selector.

Also we have used the spot earlier in the high cost.

You will see this as on demand okay.

But here we have used Users part.

And in addition to that topology spread constraints.

Okay.

So we can see here schedule anyway for hostname and for topology zone.

Also we said schedule anyway.

But if you compare that with the high cost one right.

So if you see the this one right.

So here instead of schedule.

Anyway it used to be do not schedule.

So that fix also we have done so which means it doesn't matter now.

So topology spread constraints doesn't matter now.

So we are only creating one pod.

And that pod will be created in any availability zone okay.

And pod disruption budget.

Also we have made it false.

You can see it here.

We have made it false.

We didn't enable PDB and now only change as part of those are the changes related to low cost and high

cost.

But whatever this last item is there right.

So this is the one enabled for both high cost and low cost, which is nothing.

But we are enabling our applications with open telemetry, right?

So what exactly that means?

Open telemetry enabled equal to true.

And provide the instrumentation value.

Default instrumentation okay.

Now this will be present in every file here okay.

So if you go to the end of this in every file this is going to be there in all the values files okay.

So now what exactly that means is the thing which we need to understand.

Right.

Even in high cost or low cost we are trying to enable the open telemetry.

Right.

So if you want to understand that we need to go to our retail store related helm charts in 19.

We have a source code here, downloaded retail store chart updates.

You can take anything.

So we'll take charts V 2.0 or charts v 1.0.

And we'll go to chart chart here.

Right.

And in the default values if you see here by default open telemetry is false.

Instrumentation is empty.

Now we have enabled that okay.

Open telemetry enabled.

And instrumentation value.

We have put default instrumentation.

So what exactly that means is if you go to the templates and go to the template YAML file.

And if you go down here right.

So.

In your pod template.

Right.

So in your pod in your Kubernetes deployment for your thing in the pod template annotation sections,

you will say that values dot open telemetry enabled and values dot open telemetry instrumentation.

You have a value right?

Then put that value in values dot open telemetry instrumentation here.

Okay.

So at this point.

So if you provide default instrumentation there in the instrumentation thing here.

So let me just go here and try to open this value card.

can't.

And if you provide this default instrumentation here, which means you are going and providing that

value default instrumentation here, what exactly is this?

This is the annotation instrumentation.opentelemetry.io/inject SDK.

So our applications okay which is card catalog and all these things, they are coded in such a way that

Opentelemetry related libraries are already enabled in those applications.

So means like that SDK is enabled.

So when you say inject SDK and if you provide the default instrumentation value, we will discuss that

default instrumentation value when we are reviewing the traces thing okay.

So we have that file here and we will review that okay.

So if you go here to 20 zero two and this is the default instrumentation we are talking okay.

So that value when you provide here right.

So that value when you provide here this annotation will be added in your Kubernetes deployment.

So this is the annotation which will be added instrumentation.opentelemetry.io/ingest SDK.

And then the default instrumentation value.

This annotation will be enabled in your Kubernetes deployment.

Now you might ask me the question when this annotation is added to our Kubernetes deployment, whether

it is a cloud service or catalog service, what happens.

Right.

So what happens is the Opentelemetry SDK gets automatically added to your application container at startup.

So what does that mean right.

So your app will start automatically sending traces to AWS X-ray provided if your default instrumentation

is configured and deployed and you are your collector is deployed.

Okay.

So now your HTTP request traced automatically, your database calls traced automatically and calls to

other services traced automatically.

So you don't need to modify your application code.

You don't need to rebuild your Docker image.

You just add this one annotation and observability gets turned on for your app.

Okay, so so that's about this setting whatever we are doing in our carts file okay.

So now if we go here so these are the changes we are going to do.

So let me minimize this one okay.

Section 19 we don't need it.

So if you go to our uh low cost related helm values you have values cart values, catalog values, checkout

everywhere you will see this setting which is Opentelemetry enabled.

Okay.

Now we can go ahead and then deploy our application if our data plane is ready for us.

Okay, so our data plane is ready here.

And one thing you need to ensure is you need to update your.

If it is cart service then you need to update your DynamoDB.

It is us West two don't need.

But in catalog you need to update, uh, your, uh DB endpoint if it is checkout.

Okay.

So you need to update your Redis endpoint.

And if it is, uh, orders, you need to update your Postgres related endpoint before running this.

Okay.

So I am going to use zero five which is with Secrets Manager.

Okay.

So we will go back here to our terminal.

And my data plane is already created.

Now we'll come out of that and we have zero to retail store app helm AWS data plane.

And in that we'll go to zero to low cost retail store helm.

And then I will use 05V to install remote helm charts.

Okay, so now first we have created our EKS cluster environment and then we have created our data plane.

Now we are deploying our application also.

Once these are done in our next demo we will focus on deploying the A dot collector and default instrumentation

and then focus on also testing the traces, whether a dot collector is collecting the traces and sending

to our AWS x ray, and also see the end to end flow on AWS x ray in trace map.

Okay, so let's wait for these services to get installed.

So when these services are getting installed you can see the kubectl get node claims.

And it should uh request the node for a c spot instance.

It is getting requested.

And soon those will be ready.

And then our, uh, all the parts will get created.

Okay, so if we run here also, it will be creating only one pod for us.

Okay, we said in HPA, only one pod is needed.

So it should create only one pod for us.

Okay.

So if I say cube ctl get HPA.

Also, we should see it's getting ready.

Okay.

Only minimum parts is one here.

And replicas also one.

So let's wait for all these things to get deployed and ready.

We can see here all the services got deployed and it's secret provider classes.

And everything is created.

And pods should be slowly coming up online.

Let me go here and run get pods and few things are containers are getting created, so let's wait for

them to get ready.

Okay, let's check now again one more time.

And if the pods are running.

We can go to our terminal here and go to EC2.

And in EC2, if our load balancer is up we'll go ahead and then access our application.

It is active.

So we will come back here kubectl get ingress and just check if our application is accessible.

And if needed we'll try to do one transaction here.

Okay.

Means like we'll create one order.

So if everything is working then we'll go to the next demo which is traces.

Okay.

We'll check it.

This is good.

It's moving.

Okay.

We'll click on purchase and.

it worked.

And we'll also go here and also provide topology.

And our application is healthy.

This is my database endpoint DynamoDB Redis cluster.

And this is my database endpoint which is PostgreSQL.

And this is SQS okay.

Right.

So this is good.

So now we will go back here and in our next lecture we will focus on learning about Opentelemetry traces

okay.

So we have enabled everything.

So one thing if we want to check we'll say kubectl get deploy.

And we'll take any deployment like cards and hyphen and then YAML.

And here we will go here to annotation section.

And in annotation section you can see we have added this Mitre dot, IO slash, inject SDK and default

instrumentation when that is added.

These things added automatically.

Okay.

And now if you want you can also check for catalog.

Okay.

And verify the same thing in catalog.

Also if we go here.

See this is added and these things added automatically.

And now we'll also check if we want.

We can also check for one more which is checkout okay.

And even checkout will verify.

Okay.

So we can see here it is added and these things added automatically.

Prometheus path port and scraping.

Everything is added now.

So now you can check for UI and also for another catalog or whatever.

Right.

Missing one which is UI and orders.

So I'll see you in the next demo which is Opentelemetry traces.

Until then, bye bye.

Thank you.


All right, everyone, welcome to section 20 zero one, which is our observability with Opentelemetry.

Send traces from your application to AWS X-ray via Opentelemetry collector.

Okay, so by the end of this session you are going to see this picture in AWS X-ray.

So let's understand that a little bit in detail.

So we are going to understand how traces flow from our application to X-ray.

So this complete path we are going to understand in detail.

We'll also learn about how to configure the Adot collector with receivers processors and exporters.

So those things we are going to discuss in our next lecture in detail.

But how to configure a dot collector is another key important thing here.

And that we are going to learn in detail.

And next thing is we'll also learn about one is configuration of a dot collector.

Second is filtering of data.

So that is very important.

So we have defined multiple filters here.

So why we have defined those filters is this catalog or cards or checkout will have lot of health check

things okay.

So those health check URIs we don't need to send those traces to a dot collector to AWS X-ray.

Right.

So it costs lots of money okay.

So we have added exclusive filtering.

So those things also we are going to review.

And in addition to that we are going to see how our entire microservices architecture is visualized

in AWS X-ray.

That is what we are discussing about this one okay.

So we'll see everything in detail okay.

So now let's understand this flow on a high level first okay.

So we will first go ahead and then enable Opentelemetry in our helm charts right in our custom values

file for all these applications.

So that's our first step.

And then we will go ahead and then configure the A dot collector which is a huge file okay.

So which we are going to review shortly.

This is the A dot collector traces.

And this whole thing we are going to review in our next lecture.

And it is it looks big okay.

But it is very simple.

We are going to review it in detail.

So then we will move on and then also understand about instrumentation configuration.

So here we will have that information and even that we are going to review.

And from there we will go ahead and then deploy the collector and deploy the instrumentation.

And we will restart our application.

And then so this is step one, step two and then step three.

And then finally we will access our application via our load balancer URL and complete the complete

flow like till creating the order, and then see whether the traces are coming to AWS X-ray or not.

In addition to X-ray, we will also send the logs to our a dot collector logs section means like same

traces will also send to a dot collector logs.

Okay.

So that what happens is we will enable this collector in the debug mode.

So we can also verify the logs in the.

We can also verify the traces in our a dot collector logs also.

That is just for debug purpose to see whether the traces are going or not, etc. and in production it

is not recommended.

So the one exporter is AWS X-ray and the second exporter is dot collector logs which is debug.

Okay.

So these things we are going to also look into.

Let's go ahead and then understand about the complete uh Adot collector related configuration in our

next lecture.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


Before going and reviewing the art collector related configuration.

So let's see this trace details picture on a high level.

What we are going to get at the end of this demo.

Right.

So as a client you are accessing your ingress service and then request first goes to the UI service.

And from UI the request will be sent to the catalog service.

And then all the products will be displayed on your UI.

And once that is done right, so you will go ahead and then add one product.

And then at that point of time this cart service will be triggered.

So once you add that right to the cart then the checkout process will be triggered.

So this is one this is two and then checkout process will be triggered.

And then when on the checkout when you click on purchase right checkout will call the orders.

Okay.

So it will generate means like it will call the order service.

And then your order ID will be generated at the same time.

So if you see here the products will be brought from the database which is my SQL here.

Okay.

And in the same way and then displayed on the UI here and in the same line, when we talk about orders

and when the order ID is generated that will be stored in your database here, which is PostgreSQL.

So this is the high level flow.

So in the application what happened I don't know.

But here additionally orders also should put your order information in SQS queue.

So that is missing.

And here also you should have the DynamoDB DynamoDB uh record edition here.

Okay so this is missing.

And here we will have something called reddis.

Okay, so this call also is missing here.

Okay.

So it's how they have implemented it okay.

The application.

So we are not going deep.

But to some part most of the things are working here okay.

Right.

So I don't know much about the application code in detail.

What is written inside.

So this is the thing.

So they have used inject SDK.

So that's the reason we are seeing most of the things are working.

So these things are still not working.

So means like from carts to DynamoDB.

We didn't see this.

And from checkout to Reddis we didn't see this.

And from orders to SQS also we didn't see this, but all these things are also happening in our application.

Okay, that said, let's move on to our important thing which is a dot collector configuration.

So here important things.

First part we are going to review following things okay.

So which is one is which deployment mode.

You want to run your a dot collector application.

Either it might be a deployment Daemonset Statefulset or sidecar container in a pod.

So we are going to use the deployment mode for traces.

So this will be like a centralized collector for collecting the traces from the applications which are

opentelemetry enabled.

Okay.

So if we talk about Daemonsets you can use for logs to get the logs from each pod in the each node.

Okay.

So like that.

So for deployment for traces you can use deployment mode.

And we are using that.

And when you are using in the when you are using the deployment mode as Kubernetes deployment.

Right.

So you also need to ensure that how many replicas of your collector should be created.

So this is for learning purpose.

We will create one replica means like one pod will be created and this deployment should run with which

service account?

That is very important, right?

So we have created a a dot collector service account as part of zero five Opentelemetry.

Uh, Terraform manifests.

Okay.

So there we created this service account.

So we will provide that service account.

So why this service account?

This service account with the help of Pia will be able to access logs, uh, x ray for traces and also

for Prometheus which is a p.

So all these permissions we have provided as part of this opentelemetry, uh, Terraform manifest project.

So Prometheus amp x ray and then logs all these permissions are there uh means like IAM role and IAM

policy we created.

And then with Pia association with this service account we have done so.

We will provide that a dot collector service account here.

So this resources is nothing but you are defining for this respective dot collector.

What is your request.

And then limits okay.

So how much system resources it need to take.

Okay memory and then CPU.

So those things also you can define here okay.

So these are the things we define in deployment mode.

So now let's come back here and then review that okay quickly.

So we are in 01. collector traces.

And if you come here this is the API version.

And this is the kind is the resource is custom resource which is Opentelemetry collector.

And here name is a dot traces.

And in default namespace we are creating it.

And in spec right.

You see the mode is deployment, replica is one and service account is a dot collector.

And these are the resources we have defined.

So whatever we have discussed just now.

So these things deployment mode replica service account and resources.

So those things we are seeing here in our YAML manifest for Opentelemetry collector.

Okay.

Now comes the next thing.

Okay.

So we'll minimize these things.

And then one after the other we will discuss okay.

Right.

So this is our complete config information for our Opentelemetry collector in that we already said that

we are going to have receivers processors exporters and then extensions and then service pipelines okay.

So let's understand that a little bit here.

So in config you have receivers okay.

And then you have processors.

And then you have exporters and then exporters.

And then you also have extensions and then service.

So let's understand them one after the other.

So now first talk about what are these four things actually?

Receivers processors exporters and then extensions.

So in receivers you define how the data will come into your a dot collector.

And then processors are nothing but how you are going to transform your data.

And exporters are nothing but how you how your data goes out means like where your data goes out.

You define here.

So those are the exporters.

So now let's start with one after the other which is first we'll start with the receivers okay.

So in receivers we have something called uh receivers.

Define how the collector accepts telemetry data from application.

So we are using the what LP resource what LP receiver which stands for Open Telemetry Protocol.

So LP is the standard protocol for open telemetry.

So it supports actually two things.

One is.

And second is HTTP.

And both will have the ports.

This is for 317.

And then this is for 318.

So gRPC is a binary protocol which is more efficient.

And HTTP is a Rest like protocol easier to debug.

So those are the things present in our uh, related thing.

So we have five microservices that send traces to this collector using O2.

So this collector lesions on specific ports 4317 and then four three uh one eight.

And there the application will send those information.

So what we do is using the instrumentation.

So if you go to the instrumentation part.

So if you go to the receivers part so this is the thing ot LP protocols.

One is gRPC and one is HTTP right.

So you configure your receiver in your A dot collector but your respective application.

How will it know that it need to send to this collector and this port that you define in your instrumentation.

So if you go to the instrumentation part right.

So you can see here exporter and endpoint.

And you say a dot traces collector colon 4318.

In your instrumentation you define that this a dot hyphen trace hyphen collector is your traces collector

related uh service name.

So you can see here a dot hyphen traces if you put it the name here, when it is getting created with

hyphen collector, it will get created.

And with 4318 port from from our applications, we are going to send it because this instrumentation

name is already present in our Kubernetes deployments of our application in the annotation, which we

have already discussed right in each of the Kubernetes deployment of these helm charts.

When you enable Opentelemetry enabled and in the values.

Also, if you go here and go to the values here, right?

So if you go to any of the values so you provide this default instrumentation information.

So which means this instrumentation is referred in your Kubernetes deployment.

And there you are saying that my exporter endpoint for sending the traces is a dot traces collector

with 4318.

Then the information will come to this OTT receiver.

I hope the complete flow is clear for you.

Right.

So now we will come back here.

So that's about the receiver.

So now the next thing is we'll go to the processors okay.

So processors transform filter and enrich telemetry data as it flows through the collector.

Okay, a collector.

So the order also matters here.

Very very important.

Data flows through processors sequentially.

So we have defined four processors in our application.

Okay.

So for our a dot collector application we have defined four processors okay.

So now let's start with the memory limiter processor okay.

So this memory limiter processor prevents the collector from using a dot collector application from

using too much memory and crashing with out of memory errors.

So it keeps checking memory usage every five seconds.

If memory exceeds five MB starts dropping data, so allows temporary spikes up to 640 MB.

So that's the configuration we have done.

So let me go here and then show you right in the processor memory limiter check interval is five seconds

and limit M is five to L, and it can spike additional 128 MB if required.

Okay, so these are the things we have defined so that our application, a dot collector will not go

into out of memory issue.

And then it will automatically, uh, fail or crash that application.

Okay.

So for that purpose in application, in the sense a dot collector, it cannot crash a dot collector,

right.

So for that purpose we have defined the memory limiter okay.

Now the next thing right.

So this is like this memory limiter is like a safety valve.

If your application suddenly sends a flood of traces, the memory limiter protects the collector from

crashing.

So we set the limit as phi to mm, which matches our resource requests.

Okay.

So when you go here and see the resource requests also so you can see request we are default we are

requesting is 512 which is mapping with our limit MIB here also.

Okay.

Now the next one we are going to discuss which is.

So this is done.

We are moving to the filter out health check traces okay.

So this is another thing which is very important right.

So the purpose we have defined this.

So this for this application we have exclusively designed.

The reason is when I am testing this use case and this demo, huge amount of traces are continuously

bombarding our x ray system.

And that is going to cost us money first thing.

And we will lose the actual importance of our use case itself.

Why do we need the health check related traces sent to the, uh, AWS X-ray?

Right.

So we don't need it.

Okay.

So for that purpose, what we have done is like whatever the Kubernetes liveness and readiness probes,

which runs every 10s.

We have excluded AWS load balancer health checks.

We have excluded are all the five services which are having hundreds of traces per minute, whatever.

Every five seconds they will be running for their health check.

So those also we have filtered out.

So the filter looks complex, but when you read through it it is going to be super simple.

Okay.

So if you come to this filter here right.

So let me come back here so you can see.

Right.

So in the traces any spans with the actuator health path this should be removed which is related to

our spring boot and ELB health check should be removed okay.

And anything with Kubernetes probes and Java services should be removed.

Any slash health endpoint should be ignored.

Okay.

And uh, with uh, slash health exact path or anything coming with URL dot path equal to slash health

also should be ignored and any Kubernetes probes need to be removed.

Load balancer health checks should be removed, uh, with the HTTP user agent.

So here if you see it is user-agent dot original and here it is HTTP dot user agent.

In the same way Nestjs dot callback should be removed and then express middleware for health endpoint.

So these are some of the things which when I am reviewing the things in our art collector logs, if

I find still traces are coming, traces are coming one after the other.

I have troubleshooted and then added those things so that now you have more very minimal traces coming

related to these health checks.

So 80 to 90% I have filtered out so that your use case will be simple when you are testing also, and

it is also recommended to do.

Why do we need to send all these things unnecessarily to the x ray system.

Right.

So that's what we have done in these Filtering process.

Okay, so not only these things based on your based on your requirements in your organization, there

are specific use cases.

These type of requests should not be traced.

Something okay.

So those things also you can ignore them here if you want okay.

So that's about filtering thing okay.

Now let's come back here.

And next is K8S attributes processor.

So you have implemented memory limiter filtering.

You have implemented all these things goes in a sequential order.

So that is very important sequential order one after the other.

So now we'll talk about the K8S attribute processor okay.

So what is the purpose of this k eight attributes processor.

It enriches okay.

Or it enriches the traces with Kubernetes metadata.

So we can correlate traces with pods deployments and then namespaces.

Okay.

So when a trace comes in from a pod.

The k attribute processor looks up the pod in the Kubernetes API extracts the metadata.

Like pod name, namespace and deployment name and adds these attributes.

Attributes to the trace span.

Okay.

So that whatever the things sent to your x ray, you have the complete correlation present.

Okay.

So if you go back here, if you see this respective let me show you that processor.

K attributes processor.

Right.

So here you can see it is going to extract the Kubernetes namespace name.

And then Kubernetes deployment name Kubernetes pod name Uuid and then name okay.

And then the now we know that these are the things these are 1234, five are the things it is going

to extract.

But then what is this pod association actually?

The processor needs to figure out which pod sent the trace.

Right.

So it need to query the Kubernetes via Kubernetes API.

It need to query those pods and get the data right.

So what it tries three methods in order is.

So it will, uh, check for the pod IP using the pod IP, which is most reliable matches the source

IP of your trace.

Okay.

So means like in your trace, you will have the IP address using that IP address.

It feels like it has a pod IP.

And then, uh, query your pod using that IP address.

And then if that is successful, then it will get all this information from that respective pod okay.

Right.

And if by any chance, if the if that is not working, then it will go for the pod Uuid and then get

the information.

And even if that is not working, even it can use the network connection metadata also.

So primary fallback and another fallback.

So like that you will define these things okay.

And additionally you have something called auth type okay.

So you are going to use the service account here means the collector uses its Kubernetes Service account

to query the Kubernetes API server for Pod metadata.

So that's the reason if you remember we have given lot of permissions for our service account also.

So if you go to your zero one eks cluster, zero five opentelemetry terraform manifest.

And if you go here to C6 098 cluster role and role binding for your service account.

We have also provided some additional permissions for this service account so that it can query your

Kubernetes pods and get the necessary data right.

So that's about the uh, That's let me come back here.

And that's about the service account part.

And you also said that pass through false means only extract the metadata we specified.

Don't pass through all pod labels and annotations, which could be huge data which you are sending to

AWS X-ray.

So that's about uh, Kubernetes attributes processor.

So with this processor, when you when we view traces in AWS X-ray, we can see which pod generated

the trace, which deployment it belongs to and which namespace it's in.

Okay.

So this is critical for debugging.

So you can correlate X-ray traces with your kubectl logs also if you want to okay.

So this is about Kubernetes attributes processor.

So we have one more which is a batch processor also.

So this is the batch processor.

So this is another very, very important processor for from a performance perspective.

So let's understand that in detail.

Right.

So the purpose of this batch processes processor is to group traces into batches.

So you it generates some ten traces or 20 traces.

You group them into batches before sending it to x ray for efficiency and performance purposes.

So let's understand without this batch processor what will happen.

So this is not present.

So what happens is collector sends one means like collector is getting traces from all five applications.

You have the collector here.

And what it will do is collector sends one trace at a time to your x ray.

So this is x ray.

So one trace at a time it keeps sending.

So each send requires a new Https connection.

So this from collector to x ray.

Every time means, like every trace, one trace at a time when you send.

So it is like every time it's a new Https connection to a x ray, and it's a high overhead and slow

performance.

But with batching what happens is collector groups example, you can say 50 traces, right?

It will group 50 traces together and send them to x ray okay.

With a single one API call okay.

So from our a dot collector okay.

So from collector it will send these 50 traces with single API call.

So which means it is much more efficient.

So send batch size we will define here.

So if you go here to our batch processor you can see you are saying send batch size 50.

And you are also saying timeout is if you don't reach 50 traces in 10s.

If we don't reach 50 traces in 10s.

Send whatever we have.

So that's what we are doing here.

We are defining batch with timeout as 10s and send batch size as 50.

So that's about batch processor.

So now we have discussed about receivers processors and then processors with all the things which need

to be executed in sequential order.

Now let's talk about the exporters also.

Right.

So exporters define where the telemetry data goes after processing.

So you have the data in your collector.

It is processed now where you need to send it is the key thing okay.

So you are sending it to AWS X-ray here.

So this is the AWS X-ray exporter sends traces to AWS X-ray.

So you will define AWS X-ray related information here, which is in which region of AWS X-ray you want

to send it.

Okay.

So you can go here and go to exporters.

And you can see you have defined the AWS X-ray exporter here and US East one as the region you said

it.

One thing you need to remember is if you have means like you are service account with which Adot collector

is running.

If it has permission related to X-ray, put trace segments and X-ray put telemetry records.

If those permissions are there, then only those will be sent to X-ray.

So where did you define that.

So if you go here to our C6 zero one C6 028 collector IAM policy.

And if you go down here so you have something related to X-ray permission see put trace segments and

put telemetry records okay.

So if these are there then only the whole process will work.

means like this x ray exporter can send can send the can send the traces to x ray write.

It can write those traces inside the x ray system okay.

Right.

And next is we have set some additional things here.

Indexed attributes.

This is another super important step which you need to understand.

So indexed attributes are searchable in x ray.

By indexing these attributes we can filter traces by namespace uh or by deployment or by Pod or by HTTP

status code like 500 or HTTP method like delete.

So like this you can query.

So without indexing these attributes exists in trace but you cannot search on them.

So that's very important.

Indexing the attributes.

Whatever you need for search in x ray system okay.

And the charges for this is present and it is minimal.

Okay.

X ray charges extra for these indexed attributes, which means like a half a dollar per million traces

per attribute they are going to charge.

So if we have defined these five things right.

So five attributes.

So for 1 million traces that's exactly $2.5 extra.

So it's worth for search capability.

Because when we are searching only we know how we need to means like uh we can actually troubleshoot

the issue.

So for that purpose based on your requirement, what all things you need to, uh, enable the indexing,

those things you can define in index attributes section.

Okay.

And we have also defined one more exporter which is debug exporter okay.

So this is for uh in your a dot collector right.

You will get these traces additionally.

So this is an optional one, right?

You don't need it, but just for learning purpose I have put it okay.

So you can define here.

But when we are using it there you can uncomment it also.

So this is only for learning and troubleshooting in production.

You should remove this.

It creates a lot of log output.

Logs are expensive to store so you don't need it.

Once things are working, just disable it and then redeploy your collector.

Okay.

So this debug exporter is great for verify traces are flowing or not.

See exactly what data collector is receiving.

So you can also review those things in detail and understand the trace structure.

Also what is the trace structure it is sending.

Also you can see okay now the next thing is extensions.

So we'll also discuss about extensions now.

So we have let's go ahead and then do this in our next lecture.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


Now let's understand what are extensions actually.

And inside that we have defined the health check.

Let's also understand that okay.

So extensions add extra functionality to our a dot collector beyond data processing okay.

So now we have defined health check as an extension here.

So this health check extension exposes an HTTP endpoint that Kubernetes can use for liveness probe in

your collect.

When is is your collector running or not or readiness probe.

Is the collector ready to accept data.

So that's we have implemented here.

So the health check is available at 1313 port actually.

So now if you come back here so this is the extension we have added it.

So this is the health check okay.

Now let's move on to the next thing which is.

So when you define this, which means you are telling the a dot collector, right?

Custom uh c r this is the Opentelemetry custom resource.

So you are telling when it is creating the deployment.

Adot collector Adot traces collector.

So you need to ensure that it need to add the liveness and then readiness probes for us.

Okay.

So I'm just going to show you quickly that okay so I already deployed the collector to show you this

okay.

In our next lecture anyway we are going to deploy in the sense when we go to the demo section.

But this is the A dot collector right.

So a dot traces collector.

So this is the one.

So when you define it as a dot hyphen traces.

Here we also discussed about the name.

Right.

So when you define as a dot hyphen traces with hyphen collector it will create the Kubernetes deployment.

So you can say kubectl get deploy and a dot hyphen traces hyphen collector and hyphen and then YAML

and then review that actually.

So you can see it here.

It has defined the readiness probe here.

One.

Three.

133 port.

And in the same way you can see it here.

It also defined the liveness probe also.

So these are the things it defined.

Okay.

Now let's come back to our application.

Go down and move to the next thing which is a service section we are in currently.

Now okay.

Right in service section.

So actually what is this config service.

So we need to understand that first okay.

The service section is where means like is where we wire everything together into processing pipelines.

So whatever you have defined on the top here right.

So these things okay config receivers processors exporters and extensions.

So we are going to wire them together in the service pipeline, which is called the actual processing

pipeline.

And this service section has currently two parts.

One is extensions and second is actual pipeline, which is the data processing pipeline.

So in extensions you will define the whatever the health check you have defined.

So here in extensions you have given the name as health underscore check and endpoint.

And then you provided this.

So in extensions you define this health check here.

That's all okay.

So which means that extensions is enabled until and unless you add this here it is not enabled only

a config item in your traces thing.

It's not enabled in the same way.

Now in the pipelines we are going to discuss additional things okay.

Right.

So what is this config service pipeline.

Right.

So this pipeline is is the key thing which defines the complete data flow from receiver to processor,

processor to exporter.

So this complete thing which will be defined inside this pipeline.

So now if you go up here right.

So if you if you if you go here slowly.

Right.

So this is the thing.

So here we have something called traces.

So think of pipeline as a factory assembly line.

We get the raw materials like come into our factory.

So like receivers.

And then we pass through the workstations means like we process that and then put that ready.

And then finally we create a finished product.

And exporters are nothing.

But we send that.

We export that product wherever it is needed.

Okay.

Finished product.

So in the same line, if we talk about the traces here under traces, you will define the receivers

processors and exporters.

So this is the actual pipeline data pipeline which will take action.

Okay.

So the above, whatever you have defined till here above is just the configuration information.

So you have defined something above.

But if you don't use under these receivers okay you have only one receiver.

That is fine.

But in processors you have memory limiter.

And then you have filtering.

And then you have something called uh Kubernetes attributes processor.

So like this you have and then batch processing these four things in this processor if you define only

memory limiter, Kubernetes attributes processor and batch processing only, these will be executed

even though this filtering is present in your configuration.

If you don't use it in the pipeline, it is of no use.

Okay, so now if you go here and then review those things so you can see in intra for pipelines.

In pipelines.

Also you will have traces, metrics and logs.

Okay.

So currently we are defining the Traces pipeline.

And there receivers is which we have defined.

And processors we have defined whatever the name you have defined here.

Okay.

So you can see memory limiter filter slash health check.

And then we also define test attributes and batch.

So those things we have provided here.

So those are the processors.

And in exporters you said AWS X-ray and debug and in production.

After you test this your application and then logs are flowing and etcetera etcetera.

You can just comment this and then redeploy this.

A dot collector traces dot YAML so that this will be disabled.

So even though it is defined here, it will be disabled because you have disabled it in the actual data

pipeline.

So that's what we need to understand here by looking at this pipeline.

Just take a sample request and then understand what is happening.

Okay.

Right.

So UI service means like when a user accesses your application.

So UI services when you that that request will come to the UI service.

And from catalog service the products will be displayed for the user on the browser.

Right.

So which means UI service is triggered and catalog service is triggered.

So UI and catalog services will send the trace to your a dot traces collector with 4318 port.

So to this receiver.

And then immediately receiver accepts the trace.

And then it will go ahead and then start processing that data.

So it will that data will pass through the memory limiter.

And it if it will check the memory usage and say okay then it will move to this filter health check

related filter.

And it will check for any of the things, whatever is related to health checks in the trace.

And then if it is not there, then it will through to the next step which is k eight as attributes processor.

So there it will lookup for the Pod metadata, adds the pod name.

And then whatever the metadata we have defined here right.

So that metadata namespace deployment Uuid node everything it will uh add and then it will move to the

next thing batch processor.

So this batch processor groups it with 49 other traces.

If it is present.

If it is not present it will wait for 10s.

And the timeout is 10s.

And after the timeout it will anyway send that trace to your AWS X-ray system.

How it will send it will move to the next step here and here.

AWS X-ray exports the batch to X-ray system.

In addition to that, the same trace will be also sent to your art collector logs.

So what exactly that means?

A dot collector logs means simple.

Okay, so cube ctl get pods and uh, we will say cube CTL logs hyphen f and then a dot pod.

This is the pod running right.

So a dot traces collector.

So it will send that traces here actually.

So that is the thing.

So I have just restarted things to show you okay.

So this is span 45.

So I'll show you one one span.

We can verify okay.

So this is some span zero right.

And here you can see the trace ID and this is the parent id id.

And this is simple middleware JSON parser.

And then this is the HTTP route uh route slash and express name.

So and so so and so so like this.

You can see the traces in our a dot collector logs.

Also we are seeing cube CTL logs hyphen and then a dot collector pod.

And if I run this so you can see here all these are the logs uh present in your a dot collector okay.

So if you want we can also access our application and then test it.

Currently we are explaining it.

I am trying to show you that okay.

But actually we can see this means like in the later next section also.

But I'm just trying to show you.

So these are some of the things which I am unable to filter or I still didn't go and then filter these

things okay.

But earlier it used to be hundreds of things continuously used to flow.

So maximum things I tried and then filtered out before accessing the application itself.

Some traces are coming, right?

That's because of the, uh, 80 to 90 things.

8% of, uh, health checks and everything we have filtered and still some more are there so that we

can see right what is happening.

So for that purpose, okay, so that's about, uh, this one.

If we want we can also access our application.

So cube CTL get ingress and access our application and then test it.

Actually test it in the sense view it okay.

This actual demo you are going to see in our next lectures after learning about instrumentation, but

I am just showing it here on a high level for you.

Okay, so now I will go back here and access our application.

Okay.

And Parallely, we can see the traces here.

Okay.

Let's wait for it.

We are accessing it and we are accessing it and we are accessing it.

Click on continue so you can see.

Right.

So till here it is fine.

So this is the span zero catalog product ID right.

And then you can see the start time end time.

And then HTTP method and hostname is catalog server.

And then this is the IP address.

Right.

And all the information.

So which HTTP target is this.

And HTTP version status code everything you can see right.

So in addition to AWS X-ray we are also logging it here.

That's what we need to understand.

And in production, you just need to comment this and then redeploy your collector.

Just kubectl apply hyphen f and then a dot collector traces dot YAML file.

And then your logging will be disabled.

That's all.

Okay.

So we have we have completely reviewed the a dot collector related configuration step by step.

So end to end we have a reviewed now.

And we can see all these things deployments replica service accounts resources config.

Now we know what are receivers processors exporters extensions service pipelines everything we understood

in detail.

So when we go to our metrics and logs also at that there just if we review the YAML file that should

be sufficient for us okay.

So in our next lecture we'll go ahead and then understand about the instrumentation related YAML file.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, everyone, in the previous section, we configured the a dot collector, the central processing

pipeline that receives traces from our applications.

So but there is a critical question which is what we didn't answer.

Okay.

So how do our applications actually generate and send traces to that collector.

So that's what we are going to cover now.

So we are going to go to look at the instrumentation configuration.

The piece that automatically instruments our application with telemetry open telemetry.

So this is where the actual magic happens okay.

So with this single YAML file we can instrument all five five microservices which are Java Node.js go

without touching a single line of application code.

So in our open Opentelemetry.

In our helm charts.

Right in our helm custom values.

We have already reviewed the Opentelemetry enabled part.

And there in the instrumentation area you have said we are using the default instrumentation.

That is nothing but this file we are referencing.

So let's start with reviewing this file now okay.

Right.

So we have something called API version.

This is the version.

And this is the kind which is the custom resource.

This is a custom resource.

And this name of it is default instrumentation.

And in namespace default it is currently deployed means like we are going to deploy it.

So now let's understand about this important environment.

So what we are doing we are setting these environment variables.

So where these environment variables will be set in your Kubernetes parts of your applications.

Example retail store application.

You have catalog UI right.

So I have already deployed our zero one.

If you go here in our 20 zero to Opentelemetry traces these two files okay.

In zero one Opentelemetry we have our a, a dot collector traces and instrumentation.

I have already deployed them to show you okay so just see them once okay.

So kubectl to deploy them it is just simple right.

Hyphen F01 and then hyphen F02.

When you run these things those will get deployed actually.

So those are already deployed I want to show you where these environments were set okay.

So we'll say cube CTL get pods right.

And then here in your any of the pod okay.

Right.

So we will say copy cube CTL right.

Exec hyphen it will say cube CTL exec hyphen it and provide your pod name Hyphen, hyphen and e and

v and we can say grep.

And you can see these environment variables.

Whatever you are seeing here.

Right.

Which are starting with Otel okay.

Otel all these things.

So we will grep for Otel.

Right.

And if instrumentation is enabled, so all those things will be set.

So you can see all these things okay.

So the first one currently which we are reviewing is Otel SDK disabled.

So let's review that now okay.

So this env section whatever we are seeing here right Otel SDK disabled which is a global environmental

variable that are injected into every instrumented container okay.

So these environment variables configure the Opentelemetry SDK behavior.

So this SDK Otel SDK disabled.

Its purpose is to enable or disable the Opentelemetry SDK.

So we have set here to false.

Okay.

Open telemetry hotel SDK.

Disabled false means you are enabling the open telemetry for your applications.

Okay.

So this is a global setting.

If you want to disable then just make it as true and then deploy it.

And your open telemetry will be disabled completely.

Okay.

So this is one option.

Okay.

And another uh environmental variable is Otel exporter protocol.

So the purpose of this is defines which protocol to use when sending traces to a dot collector.

So we have multiple options available.

One is G and the other is HTTP.

And the other is this one okay.

So this one two and then three.

So this third one we are using currently which is OT over HTTP with Protobuf encoding.

So we are using that.

So we chose HTTP over with protobuf because HTTP is simpler easier to debug with.

Curl works through most proxies okay.

And protobuf is efficient binary encoding small payloads than JSON and even wide compatibility, which

means works with most of the Opentelemetry backends so it's not as efficient.

This gRPC is super efficient and this http JSON is super problematic.

Okay, so this is super efficient.

This is super problematic.

So we chose a middle option okay little balanced option which is HTTP slash protobuf okay.

So now the next thing is we are moving on to a hotel resource providers AWS enabled.

So this is another important section okay.

So another thing.

So the purpose of this is it enables automatic AWS resource detection to enrich traces with cloud metadata.

So it can have it can get the uh EKS cluster information cluster Arn.

And then it will also query the EC2 instance metadata to get the instance ID instance type availability

zone.

It can also get the AWS region AWS account ID.

So all these things can be automatically added.

And then why do we need to add these things for regional analysis, for troubleshooting and even cost

tracking purposes in AWS this will be helpful.

So this also we have added it here okay.

And next is important one which is auto traces exporter okay.

So this is the important one.

So we have options here for otel traces exporter.

So we have one is OT LP which we have configured.

And second, you can send traces to Zipkin and you can send to.

And then we also have something called none.

Okay.

And then currently we are using OTC.

That is the thing which we are using.

So it's the Opentelemetry standard.

Our A dot collector express here.

It's a vendor neutral and even future proof configuration when we are using OTC.

So this setting tells the application send traces using OTC protocol to the end point, whatever we

have specified inside this instrumentation.

So you can see this is the end point we have specified.

So to that using OTC protocol it is going to send it okay.

Now the next thing OTC matrix exporter.

We are putting as none we are not.

We already decided that we are going to have one collector for, uh, traces.

And for our learning purposes.

Second one for logs and one for metrics.

So which means we are going to disable metrics and logs inside this collector configuration.

So that's the reason those were none okay.

And now we have something called exporter also.

Right.

So now let's go ahead and then see the exporter configuration.

So where we are sending to our a dot traces collector 4318 port.

So what is this a dot traces collector.

So if you say cube CTL get SVC right.

So you will find that a dross dot traces collector Kubernetes cluster IP service with 4317 and 4318,

which will keep sending the traffic to our actual a dot collector parts, which is nothing but traces.

Collector pod.

This is the one.

Okay, So we are configuring our endpoint that endpoint.

So this instrumentation will collect the actual traces means like it will enable the applications to

send traces and where it will send the traces using protocol.

And to this collector it will send the traces.

And in addition to that we have something called propagators and baggage.

So these are additional things okay.

How you want to define the trace okay.

And what all things you want to use propagators define how trace context is passed between services

okay.

Right.

Uh means like you are going from service one to service two, service two to service three, and from

service one to service two.

When you are going, uh, gather the service one related context data.

And then S2 also will read that context data.

And from there means like from service to service, how it need to maintain context.

So all those things you need to enable, right?

So for that purpose we are enabling trace context.

Okay.

So we can go still deeper and understand it might take another 15 20 minutes to understand these things

in detail.

But on a high level these are like simply when one service calls another service propagators ensure

that trace context.

The trace ID span ID sampling decisions is passed in the HTTP headers.

So for that purpose, propagators will be very much helpful.

And in that we are using something called trace context which is a W3C trace context standard.

Whatever we are using here.

So this is the universal standard for passing trace context okay.

It ensures opentelemetry services can trace through even non opentelemetry services.

Traces work across different vendors like AWS, Google, Datadog, etc. and this is future proof because

it is following the W3C standard.

So that's the important thing to remember.

And next we also need to understand about this baggage.

Okay.

So this baggage allows you to pass custom key value pairs through the trace.

Something like a user ID or tenant ID right.

Or some feature flags etc. etc. so you can track those things also.

Okay, so every service in the trace can read these values from this baggage and then add them as span

attributes.

Use them for conditional logic if they want or log them for correlation.

So like this you can define these things okay.

So so these are primarily powerful for user tracking knows which user's request is slow etc. etc. or

even for tenant isolation in multi-tenant systems or for a B testing track, which variant the user

saw, etc. etc. we can use that.

Okay, but we should be careful when we are using baggage because it's not encrypted.

Don't put secrets here okay?

And another important thing is performance.

It adds super overhead.

So each service receives and forwards it.

It's a baggage right.

Every service that that will receive and forwards to the next service.

So that's another big thing.

So keeping it small is one important thing.

So that's about propagators.

And we have one more which is sampler okay.

Currently this sampler we are using always on.

So what is a sampler right.

So sampler is nothing but which a sampler decides which traces to keep and which to drop.

So in high traffic application you might have millions of requests per day.

Storing every trace is expensive and really not needed for non-critical Applications.

Okay, so most traces are identical example successful guest get requests, etc. etc..

So this sampling this sampler right allows you to reduce costs and store only few traces in your x ray

and only keep important traces.

What are needed and also only for statistical accuracy.

Also you can implement that means like sample enough to understand your application behavior.

You don't need to have all the 100% of your application to be sampled.

So currently for our learning purpose, we have said always on why because we are testing it.

Okay.

So for we are going to test only couple of samples right?

Which means 2 to 3 times we will access our application.

Those things we want here okay.

But for learning and initial setup that is fine okay.

But to understand the patterns or for getting the complete visibility or, uh, for low traffic apps.

All this is good, but for heavy traffic traffic apps.

So we have multiple options, okay.

In that in sampling we have the options like right.

Trace ratio is one type of sample okay.

And here you will give the argument as 0.1.

So when you provide this trace ID ratio and argument as 0.1 which means you are sampling only 10% of

your traces randomly.

So it's cost effective for high traffic applications.

So instead of 0.1, if you give 0.05 as the argument, which means only 5% of application and traffic,

you are sampling, okay.

And there are other types also this is trace ID ratio.

But you can have something called a type equal to right.

Something called parent.

Trace id ratio.

So that is another one.

Okay.

Here also you can give again argument is equal to 0.1 or whatever right 10%.

So combination of parent based and ratio sampling most common in production okay.

So these different different options we have and those things based on our requirement we need to choose.

So that's about the complete instrumentation related configuration.

So in our next lecture we will go ahead and then deploy the dot collector and this instrumentation and

then test end to end okay.

So whatever I have deployed I'm going to delete it again in the next lecture.

We'll go ahead and then deploy them okay.

Right.

So I'll see you in the next lecture.

Until then.

Bye bye.

Thank you.


Welcome back.

Now let's go ahead and then deploy our a dot traces collector and then verify the things how it is working.

So now we will go back to our terminal here.

And we will go to section 20 zero two.

So we'll go to 20 zero two.

And here we have zero one open telemetry traces.

And inside this we have the dot collector traces.

And then a dot instrumentation for traces.

Okay.

So we will first deploy our 01. collector traces.

So we can see here our dot collector is deployed.

And now let's go ahead and then run the command cube CTL get pods.

And we should see our dot traces.

Collector should be running.

Okay.

So we can also say cube CTL.

Get telemetry collector so that whatever we have deployed a dot traces, it should be present in the

mode it is deployed is deployment mode and it is currently running and it is managed collector, which

means it is managed by our a dot operator x add on whatever we have installed as part of zero five Opentelemetry

terraform manifest.

So if we say here kubectl get deploy.

So we should see the deployment related to a dot traces collector.

Now if we say describe deploy and a dot traces collector.

And if you see on the top the labels here right.

So if you see here there will be one label saying managed by Opentelemetry.

Operator.

So if you say kubectl get and it will list the namespaces in that opentelemetry operator system.

So actually we really didn't check X add on, which is a dot AWS distro for Opentelemetry where it is

installed from Kubernetes.

Resource perspective means like.

We have seen that using the console in our EKS cluster, right in AWS console, but here we didn't see.

So now we will see that this is the Opentelemetry namespace.

So we'll say kubectl and then in namespace opentelemetry operator system.

And I'll copy this.

And inside that we'll say get deploy.

And you should see the Opentelemetry operator related deployment running.

So this operator is the thing currently which is managing our collector.

Okay.

So first we if we want we can also verify its logs also.

So we'll say kubectl hyphen n and then logs hyphen f.

And this is the pod of opentelemetry operator.

We'll see the collector parts shortly.

These are the info logs.

We didn't see anything related to any issues, but here we are seeing some fail to select Opentelemetry

instrumentation for this pod, etc. etc. this is UI pod because we still didn't deploy our instrumentation.

Okay, so we'll deploy them shortly.

Those are related to that.

Okay.

So let's uh, go for the collector logs now.

So Opentelemetry operator we have seen now we'll say kubectl get pods.

And in the default namespace itself we have the a dot traces collector.

So we'll say kubectl logs hyphen f.

And this is the traces collector.

And here are the logs okay.

So all our info related only okay.

So nothing any issues or anything we are seeing here.

Okay.

Uh it is saying memory limiter configured exporter components and then uh starting extensions and extension

is starting etc. etc..

Okay.

Standard log okay.

So we'll leave that now.

And important thing is now we need to deploy our A dot instrumentation traces as soon as we deploy this

and restart our retail app, which means like retail app means all the five services inside that retail

app.

Then the traces from our application will start flowing via collector to AWS X-ray.

So let's deploy this first okay.

Zero to a dot instrumentation traces okay.

Right.

And now if we say cube CTL get instrumentation we should see it should be deployed now default instrumentation.

Now the next thing is we need to go ahead and then restart our app.

So we have the restart script here in our 20 zero two Opentelemetry traces which is just kubectl rollout

restart catalog restart cards restart checkout orders, UI, etc..

Okay.

Right.

So if in which we can directly say restart and if these apps are deployed in some other namespace,

you need to provide that namespace.

If not, it will take from the default namespace.

Okay.

Anyway, these are deployed in default namespace.

We can directly say Dot restart retail app, and now it will restart our catalog cards and then checkout

orders and then UI.

Okay.

And now if you see cube CTL get parts right.

And those should be uh start starting now.

Okay.

So you can see all these are coming up online.

Now you remember in uh when we are discussing about our a dot collector traces in the exporter thing.

We have exported two things.

Okay.

So one is two AWS X-ray and also debug verbosity with detailed, which means it will also send the logs

to our a dot collector okay.

So cube CTL.

Cube CTL get pods.

And you should see the dot collector logs now.

So you can select this pod right.

And cube CTL logs hyphen f and a dot traces collector.

Let's see.

See now you can see spans are coming right.

So select public Flyway schema history for orders DB.

Okay.

So like this you are seeing some uh what we call okay.

Uh, traces already.

This is span eight okay.

Like this span seven.

So it is coming.

Okay.

As we have removed lot of monitoring.

So whatever we have done here, filtering.

So it is going slow.

But if we didn't have implemented these filters right.

Monitoring filters here.

Health and then Prometheus readiness or readiness liveness probes everything.

Then here it would have been like like bumping the logs like anything.

And even it is sending to x ray also in the same way okay.

So 70 to 80% of unnecessary data we have filtered using those filters.

Okay, now what we will do is we have seen the logs here on a high level.

We leave this like this and then we will go here and then we will go to CloudWatch.

So let's go to cloud CloudWatch right.

And in CloudWatch right.

So we will go to traces here okay.

So if you go down here in application signals you will have the traces here okay.

So here we will get the traces.

See for catalog and all these things okay.

So these are all just the traces currently coming in okay.

So two one right.

So these are the current traces we are getting.

And with this URL address and etc. etc. we are getting them okay.

But our thing is we need to access our application okay.

So this is our actual application, so I'll try to access the application now again.

Right.

I have accessed it.

And then we'll click on refresh one more time and then we'll click on Add to Cart.

So we are implementing the complete use case here okay.

Start equipment requisition.

Now it went to checkout and we are in the checkout currently.

And we'll click on purchase.

And we should see the orders okay.

Right.

So order also created.

So this is the order.

Now we'll come to the traces here.

Right.

Where is it.

So this is the one.

And try to run query.

And we should see the things here okay.

So this is the one we will try to run one more time.

And it should be with the K8S okay.

So this is the one you can see here right URL K8S.

That is our domain name k test default UI something like that.

So we'll go here and try to take this, uh, specific trace.

And we should see the information here.

Okay.

We will try to minimize this.

And.

We have till catalog.

Maybe batch is little delayed.

So.

But you can see client UI catalog and then catalog database and cards.

And then here you can see checkout.

We should also see the orders shortly.

Run query and we'll take the latest one okay.

And we should have everything now mostly okay.

So we'll try to minimize this.

And.

We can see that here completely.

This is the trace.

Let me maximize it.

And you can see client UI.

And it went to checkout.

It went to catalog.

And then after that it went to cards.

It went to checkout.

And then from checkout it went to orders and orders database also.

And these are the icons if you want to see metrics.

So you can click on metrics.

And you can see that average 4.12 seconds happened in UI and then 3.78 seconds.

So like this one 29 milliseconds for carts and three milliseconds for catalog and 3.76 seconds for orders.

So like this you can see all these information okay.

Right in detail.

So this is the trace information.

So let me minimize that and come back here.

And here you can see the complete segments timeline also.

So you can see the UI the following get get post delete happened.

And these are the things okay.

And how much duration it took.

And what is the response code okay.

In the same way for carts.

What happened for catalog.

What happened okay.

Right.

So and what is the thing it is calling card slash cart slash.

So and so catalogue.

Catalogue products something like this okay.

And for checkout.

what is happening.

And for orders.

What is happening?

Everything.

You can see them here in detail.

And for any trace.

Also, if you want to go deeper and see raw data, you can click on this and then even see the raw data.

Also we can also click on go to Trace Map and you can see the complete trace information here.

If you want to see in list view we can click on list and then view and then open any of these things.

Okay so any of these nodes and review the stuff also here.

And in addition to that in the trace map itself in the map view itself.

Self.

If you click on any of these items here, right.

So let me try to minimize.

So if you click on any of the nodes like UI node they its equivalent metrics also will be displayed.

And response time durations everything will be displayed.

This is UI related things.

And in the same way if you want for catalog or cards you can get those information also.

So even you can see the connection information and everything you can see.

You can see traces and then you can analyze those traces and lots of things we can do.

So we our core goal is using a dot, whether we are able to get the traces to our x ray or not, we

need to verify.

And then we have verified that okay.

So now we will go back and then clean up whatever we have implemented.

So we have successfully implemented it and verified.

So now we can go ahead and then delete the instrumentation and also delete the traces cluster.

So these are continuously running.

So it's not good to have this running for us okay.

It will cost us right.

Unnecessarily.

We are just testing it okay.

So if you go to trace map traces here and run query right.

So we have lots of things going on in last five minutes.

Only two traces Okay.

Why?

Because we have minimized the stuff.

Means like we have filtered lots of stuff, so nothing is there.

Okay, so it is fine if you want to have it running also.

That is fine.

Why?

Because I have filtered all the unnecessary things and put earlier.

It used to be huge amount of things continuously coming because of those health check URLs.

Okay, but now this is fine.

Okay so in last five minutes we are seeing only two traces came in.

Okay.

We didn't access our application in last five minutes before that we have done.

And if you go here to last 15 minutes and run the query, we can see 96 traces in last 15 minutes.

And here is our application.

Last we have accessed.

This is the one.

Okay.

So this is the one we have accessed.

So that's fine if you want to have it running or my my way is like why should we run it?

Okay.

So we know we learned it.

So we will clean it up.

In our next demo we will focus on opentelemetry logs or metrics, whatever it is.

And there we are creating a dedicated, uh, dedicated art collector.

So we really don't need to worry about this.

Okay, I'll say cube ctl delete hyphen f.

And uh, we need to go to zero one open telemetry traces and cube CTL delete hyphen f and zero to a

dot instrumentation traces and hyphen f and 01A dot collector traces.

So this should remove the whatever we have deployed related to traces collector.

If we say cube CTL get pods.

Also, we should see only our five things cards, catalog, checkout orders and then UI.

So this completes the learning of a dot traces okay.

So this complete path whatever we are supposed to implement, we have uh implemented successfully and

then tested it.

Okay.

So in our next lecture we will focus on a dot logs.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, everyone, in previous demo, we have focused on sending open telemetry using open telemetry,

sending our application traces to AWS X-ray so that we have done as part of section 2020 zero two.

Now, as part of section 20 zero three.

Right, we are going to focus on sending our application logs, which is like catalog cards, checkout

and orders and UI.

So these application logs we are going to send to AWS CloudWatch using a dot collector.

So we are going to deploy the dot Opentelemetry collector specifically for logs, which will collect

the logs for our retail store application and send it to AWS CloudWatch.

And for that again so here we need to understand few important things.

So if we go here to section 2003, you have the open telemetry logs, uh, related uh, YAML manifest

here.

So which is the open telemetry collector related YAML manifest.

So this YAML deploys an art collector specifically for collecting container logs from your EKS cluster

and sending them to CloudWatch logs.

So here you will see just only our retail store application.

But it's a overall for EKS cluster.

So mostly we are primarily focusing on our retail store application.

So in the picture I have uh depicted only retail store application.

So now let's see few important things as part of this okay.

So the first and foremost thing which we need to understand about this art collector, which is logs

dot collector is deployment mode.

So that is the one which we need to understand in this deployment mode for logs, we are going to use

Daemonset.

Okay.

So this is critical for logs collection.

So why right.

So unlike traces and metrics we usually use Kubernetes deployment there.

But for logs it requires a daemonset because container logs are returned to the node's local file system

which is Kubernetes worker nodes local file system at path air.

And then pods.

So at this location we will find the logs actually.

So each node has its own logs.

So with daemonset mode so we can collect right.

So one collector pod per node will get created.

So if we have three Kubernetes nodes right.

So if we have three Kubernetes nodes so on each of these nodes, one collector pod will be created.

So one a dot collector pod will be created on each of these nodes.

Okay, so the reason is it need to collect the logs from that respective node only.

So each pod, whatever the collector pod created there.

Right.

So it reads the logs from its local node.

So no network transfer of raw logs between nodes is needed in that case right.

And it is efficient and scalable considering all those things.

The effective way for transferring the logs from our Kubernetes nodes to our AWS CloudWatch, right,

is using Daemonset mode.

So the Adot collector service account.

So this part is completed, which is Daemonset mode.

So as usual we will have the same service account and resources which we have already discussed in our

previous demo also.

Right.

So we will provide our art collector service account here and also define the resources.

So those things we can come back here and review those things.

So this is the API version and this is the API version we are using.

And kind is Opentelemetry collector and name is a dot logs and namespace is default and mode is Daemonset.

And service account we are using is a dot collector right.

And now we will discuss this env pod security, context tolerations and resources.

Again, it is same as our previous section which means like for this respective pod, whatever it creates

using this Daemonset.

Right.

So these are the system resources.

You are defining requests and then limits.

So let's talk about a little bit about the pod security context.

So this is very important setting.

So here when we say pod security context with run as user zero and run as group zero means uh which

is nothing.

But you are saying that run as a root user.

So why do we need a root user here?

Container logs are at the path where log pods.

So which have restricted permissions only root can read them.

So without this collector would get permission denied errors.

Okay.

So there might be a security concern you might be feeling.

But for collection of logs it's necessary.

The pod is read only and has minimal attack surface.

That said using this is not an issue.

Okay.

And another is pod security policy.

So we have also defined the pod security policy which is completed.

And next is Tolerations.

So this is another important setting right.

So this is like these tolerations ensure the collector pods run on every node even nodes with taints

also.

So without these tolerations, the collector wouldn't run on the nodes and you miss some logs.

So for that purpose, these tolerations are very important.

So you said here something called operator equal to exists means tolerate all taints.

This pod runs everywhere.

So that's another important thing.

So we'll also have something called volume mounts and volumes.

Another important setting accessing the host logs means like where this collector pod is scheduled.

This will be scheduled on every Kubernetes worker node in your cluster.

So when it is scheduled right it mounts the that respective nodes where log pods okay.

So that respective nodes where log pods to your respective a dot collector pod.

And so that you will be able to access that Kubernetes worker nodes var log pods related location in

your a dot collector pod.

So as the logs are present in your art collector pod, then it can be collected and then sent to the

AWS CloudWatch.

Okay, so one important setting here is read only true.

So which means the collector only reads logs never writes.

So this is a safety measure.

And what logs it is reading from the host path.

It is reading from that respective Kubernetes worker node.

So that's about all these settings like pod security, context tolerations and volume and volume nodes.

Additionally, we have also defined a environment variable to get the node name okay.

So just to get your specific node name where we can use it in the rest of our YAML wherever it is required.

So if we go here.

So if you want to create a log stream with each node separately, then that will be helpful.

So for that purpose we have added this environment variable.

But ideally it is not needed okay.

So optional case okay.

So we have put it there okay.

And we never used also down the line.

So that's the thing.

So now if we go to the config section as usual we are going to have receivers processors exporters and

service pipelines.

So in receivers we have something called file log.

So this is the file log.

So we are getting the log files and receiving them as file logs.

So we are saying file log.

And we are saying include these files.

So you can see here var log ports star star star dot log and excluding.

You are also excluding any a dot collector logs.

And also you are excluding any cube system logs.

Also, we are also excluding the kube system logs because it will reduce noise from our system pods

also.

And we have also added one setting here start underscore et equal to end.

So it says that only collect new logs from this point forward and don't backfill historical logs.

So this prevents or overwhelming CloudWatch on start up of the collector.

So that's the important thing here.

So that's about the file log receiver.

So let's go ahead and then take that.

Now we need to discuss about processors.

We have memory limiter uh Kubernetes attributes processor and batch processor.

So let's discuss them in detail.

So if we go to the memory limiter.

So we are clearly saying here that we have defined a memory limiter stating that which will prevent

the collector from using more than In 400 MB of memory.

If it reaches this limit, it drops logs rather than crashing.

The A dot collector.

And next is we have something called Kubernetes attributes processor.

After this we have attributes processor.

And this is the magic which means like we add to our logs.

So raw log files don't have the context.

They are just text.

So this processor adds the namespace pod name and container name.

So now once this processing is complete in your CloudWatch you can have the full Kubernetes context

for filtering and searching.

So that's about one more processor.

And finally you have something called batch processor which will group logs into batches before sending

to CloudWatch.

So reduces the API calls and which also improves the efficiency.

So that's about batch processor.

So now the next thing is exporters.

So in exporters we have something called AWS CloudWatch logs.

So here we have AWS CloudWatch logs and region you are providing and log group name you are providing

and log stream name you are providing.

So in which region you want to send you will decide here and in to which group you will define here.

And also you will define the log stream name.

So you can send to a single stream like this.

Or per node means like from each node you have three worker nodes.

You can have three worker nodes names created in your CloudWatch.

And there for each node you can get the logs accordingly also, but recommended is single stream all

logs from all nodes in one stream, which will be simple, easy to query and also works well.

Works very well for small and medium clusters.

Okay, So that's about the exporters.

And finally we have the pipeline thing.

So this pipeline thing in pipeline we have something called logs.

And in logs we will define whatever the receivers we have defined processors and then exporters.

So let's go ahead and then see that now.

So this is the logs pipeline and receivers.

We have something called file log receiver we have defined here.

So this is the file log receiver.

And then we have something called in processors.

We need to arrange them in the order memory limiter after memory limiter Kubernetes attributes processor.

And after that batch and after that we also have in exporters AWS CloudWatch logs.

So that's about the a dot collector logs right.

So we have reviewed everything in detail.

In our next lecture we are going to focus on deploying this and testing it in detail.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

We are in 20 observability open telemetry section.

And we are entering into the demo 23 open telemetry logs.

And as part of Architectural Review section in our previous lecture, we have already reviewed these

pictures in detail.

Okay.

Now we are going to go ahead to this Open Telemetry Logs section in 2003 and deploy our a dot collector

logs.

So one thing I have made the change inside this dot collector logs is I am planning to use the log stream

name as hyphen v4.

This is one change, and the second change I have done here is in the zero three node pools part, right?

So for the carpenter parts instead of micro, small, medium, all these things I am trying to use from

only use medium and large so that it will be easy for us when we are testing these things.

Okay.

So if it is a small uh VM instance, it need to fit in all the means like it also need to host the retail

store application related parts.

And this a dot collector pods.

And it might end up in having these Adot collector pods in the pending state.

So either you can use medium or large and if it is very good still, if you use only large and x large

like that, you can increase the sizes.

Okay.

So for testing purposes here okay.

Now to support means like on that specific node itself you need to have your retail store application

pod.

In addition to that it also need to have this art collector pod because you are deploying it as a daemon

set.

It need to be present there on that node.

So for that purpose, we have made this change and it is uh, it will solve the pending pod issue when

you deploy the art collector.

If no system resources are available, it will be in the pending state and it's not good.

So for that purpose, this change and then I have already deployed the zeroth node pool spot.

After making this change in your case, when you create the cluster itself, this will be present for

both on demand and spot.

Also, it will start using the medium large.

So even for spot also it is medium large.

So you should not face any issue.

But I am trying to explain what actually we have changed in our 2001 configuration.

Now if you go to 2003 opentelemetry logs and here we are using v4.

So now let's come back to the terminal here and let's start come out of our previous 20 zero to Opentelemetry

traces.

And we are in 20 observability Opentelemetry.

And as part of previous demo we have uninstalled only traces collector and traces instrumentation things.

We didn't uninstall our application or anything.

If you run the command cube CTL get pods.

So we should see that our cards catalog checkout everything is working fine.

Okay, so they are running okay.

So what we will do is so we'll go here and go to 20 zero three opentelemetry logs.

And here you have the zero one Opentelemetry logs.

And inside that you have the actual YAML file which is a dot collector logs dot YAML.

Okay, so even for the requests also we have took only very less.

Here I have also adjusted this.

Also CPU request is 50 and memory is 128 mi only.

Okay let me deploy this collector pod okay.

And let's see what happens.

Kubectl kubectl get pods.

And let's see whether the collector pod is running okay.

So it is running.

So three are other nodes which are our cluster nodes.

And now one is our karpenter node.

So if we run the command kubectl get node claims we should have one karpenter node t2 medium with this

IP address.

Right.

So in this a dot collector pod is running or not we need to verify kubectl get parts.

Hyphen o wide, which is ten zero ten 175.

Okay, so in that it is running or not, we need to verify.

Okay.

Ten zero ten.

So here this is the, uh, Cuba Carpenter node.

And in that a dot log spot is a dot logs.

Collector pod is running.

So not an issue.

And if you want to get pods hyphen o wide also when you run.

Right.

So even these things also uh UI orders checkout catalog cards all are running in that T2 medium file

only right in T2 medium node only, which is our uh, carpenter node.

Okay.

Now what we are going to do here is so we have successfully deployed the collector, get open telemetry

collector.

And we should see the telemetry collector is running.

And if you want you can say get DS which is daemonsets okay.

And you can see a dot logs collector for are the desired parts it need to run.

Currently it is running.

For and for are ready up to date and then available.

And if you want to also verify the logs, you can also do that.

You can say get pods and kubectl logs hyphen f and this will say kubectl get pods hyphen o wide and

we will run uh will run the logs for the pod which is present on 175 where which is our karpenter node.

Okay.

So where that pod is scheduled, those logs okay.

We will say kubectl logs hyphen f.

And this is the thing.

So these are the logs of our a dot logs collector okay.

Now let's come out of this.

Let me clear this and we'll go to CloudWatch and inside CloudWatch.

So we will go here to logs thing okay.

So in logs in log management.

So we'll open log management and logs inside two things.

Okay.

In log management we have the AWS, EKS retail EKS demo one slash application.

And inside that we should see this retail dev EKS demo one v4 stream.

Okay so these are the logs currently present.

So far we didn't access our application.

So what we will do is we'll go here get ingress right.

And we'll try to access our application using our ingress endpoint.

Let me copy this and we will go here and access our application and do a one full transaction which

means we'll add to cart.

And we will start equipment requisition and checkout process will be triggered.

And we are in the checkout service.

And when we click on purchase orders and order service also will be triggered.

So one full transaction we have completed.

And then we'll try to refresh okay.

And all the logs came in here.

So there are a lot of logs came in here.

And if you want you can open and see what are these logs and all those things.

Okay.

So that is one way okay.

Or you can go to Log Insights.

And then here you select your log group which is AWS ECS retail dev ECS demo one slash application.

And here you can run some queries here.

So this is one sample query.

So show all logs okay.

So if you want it can show.

And if you want to limit it to only top 10,000 logs.

Also you can do that.

We ours is only a very simple one.

So we are trying to fetch the logs and there are some logs came in here.

So let's run this again.

And we should see some around close to 26 records or 27 records showing here.

And out of these things we can try one command here, one query here, which is like search for catalog

related things.

Okay.

This will filter for slash catalog slash okay.

Right.

And we will copy this and run this query once and then see what happens.

So we'll go here and put this query and check for any catalog related logs came into our CloudWatch

or not.

So it is saying close to 24 records came related to this.

And you can see those information here okay so this is the log stream v4.

And this is the message body okay.

So catalog products and this is the product information and all that.

You can see it here.

In the same way you can see the logs related to catalog here all the catalog related logs it is showing

here for that respective service okay.

So like this.

The core goal here is to show you that whether the logs from our retail store application is coming

to AWS CloudWatch, and that is done using a dot collector for logs or not.

So we have successfully demonstrated that.

And if like by any chance, if it is taking some time or logs are not coming.

So if you can also restart your retail store application means like all the five kubectl pods will get

restarted.

So this is the script right to restart them.

So rollout restart catalog card checkout orders.

You can also do that if you want.

So now this is about Opentelemetry logs okay.

Now we can go ahead and then clean up whatever the logs collector we have deployed here.

In our next demo we will focus on Metrics collector which will send the metrics for our application

to our AMP which is Amazon managed Prometheus.

And from there we will also query the data using AMG, Amazon managed Grafana.

We will visualize these metrics.

So I'll see you in the next demo.

Until then, bye bye.

Thank you.


Welcome back.

In demo 20 zero two we learned about using Opentelemetry.

Sending our application traces to AWS X-ray.

And as part of demo 23, we have learned about sending our application logs to AWS CloudWatch using

a dot collector for logs again with Opentelemetry.

Now in this demo we are going to focus on in 2004, creating a dot collector for metrics, which will

collect our metrics from our application and send it to Amazon managed Prometheus instance.

And from there we will create a Amazon managed Grafana instance and then add this.

Amazon managed Prometheus as a data source.

And then we as a user, we will be able to go to Amazon managed Grafana UI and view our dashboards configure

and view all our metrics in a dashboard.

And for this.

Additionally, in addition to Amazon managed Grafana, we also need to enable the IAM Identity Center

service I Identity Center service in AWS.

Okay.

And this will be the key thing for accessing the Grafana.

So we will create the users in this IAM identity center.

And after that using those users will be associated to this.

Amazon managed Grafana.

So that using a user, whatever you have created in AWS, IAM Identity Center, you will be able to

access the Amazon managed Grafana using browser and browse our dashboards or create dashboards.

Create alerts, everything we are going to do with that thing.

That said, as part of this demo, we already know that as part of 20 zero 120 zero one when we are

creating the Terraform project for Opentelemetry, we have ensured that we have created the AMP and

AMG instances.

So if we go back to our Terraform project here, right.

So if we go to our 20 zero one and in 20 0101 EKS cluster environment, and inside that if we go to

zero five opentelemetry terraform manifest.

So you can see here AMP Prometheus workspace is created and AMG right.

Grafana workspace is also created.

So if we want we can go to our application here and also verify that okay.

So if we say AMP Amazon Prometheus open a new tab.

And we'll also say AMG and open in new tab.

So this is the Amazon manager for Prometheus.

And here in all workspaces.

So you can see this workspace.

Right.

And in addition to that you can also see the Grafana workspace also created for us.

So these are the things ready for us.

And we need to focus on enabling the identity center, creating the user and whatever the user you create

there you need to enable.

That means like it's mandatory that that user will have that two factor authentication.

So you need to install a authenticator app in your mobile and then enable those stuff.

Also when creating the user here in Identity Center.

Right.

And then we also need to configure this Amazon managed Grafana after logging into it.

Amazon managed Prometheus as the data source in Grafana.

So these are the things we need to do.

And we will import one standard dashboard and then see if whatever the data flowed from our applications,

right.

Not only from retail store applications.

The scrape configs we define in our YAML file.

So we are going to have the complete cluster metrics.

Also not just only this retail store.

Okay.

So entire EKS cluster metrics.

We are going to send it to Amazon managed Prometheus.

And we are going to deploy the dashboard to see all the metrics also.

So for for convenience purpose, note that in addition to this retail store application, we are also

going to have all the EKS cluster metrics also sent to this a dot collector for metrics collector okay.

Which means this metrics collector will collect entire EKS cluster metrics.

So that's about on a very high level what we are going to do.

In our next lecture, we will focus on understanding about the complete cluster info cluster collector

information, which means the Adot collector which we are going to create for metrics and that we are

going to review in detail.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

This is our art collector configuration for Prometheus Metrics.

So it's going to scrape metrics from all of our Kubernetes workloads and send them to Amazon Prometheus.

So you can see that YAML manifest.

Come here okay.

In 2004 Opentelemetry amp AMG.

And in zero one Opentelemetry metrics you have this complete a dot collector Prometheus full K test

cluster related configuration.

So this we are going to review it on a high level now right.

So as usual we are going to have the deployment mode.

And that we are going to use the Kubernetes deployment okay.

And we are going to have number of replicas how many we need to create.

And the pods created the art collector whatever it creates.

Right.

So with which service account you need to run so that service account information we will provide.

And again this service account we already know that we need to create an IAM role IAM policy so that

they will be able to access our respective amp.

Amazon managed Prometheus to write the metrics to that.

So those permissions, which we have already seen in our Terraform project here, if we go to our this

120 zero one and zero one ECS cluster, Terraform manifests.

And here you will find the A dot collector IAM policy.

And here in this you can find the permissions related to Amazon managed Prometheus thing.

And in addition to that we also need to know one more thing before going and discussing that YAML.

So we have also installed additional add ons.

Okay.

So to send the metrics means like to to get the metrics for our cluster.

One is kube state metrics and node level metrics.

For Prometheus to get we have added the add on Prometheus node exporter metrics also.

So in addition to O x add on a dot and cert manager which are defaults, we have also added Prometheus

node exporter metrics and kube state metrics.

So these two add ons.

Also if we go to our uh if we go to our application here.

Right.

So if we go to our EKS cluster we can find them already installed.

So let me go to the cluster here and let's see them before going and reviewing the YAML.

So if we go to the add ons here.

So we should find the metrics server external dns x pod, identity agent cert manager Prometheus node

exporter.

And in addition to that kube state metrics AWS distro for opentelemetry.

So these are the things important things.

One is AWS distro for opentelemetry kube state metrics.

And one another one is cert manager and Prometheus node exporter in addition to export identity agent.

And one more is metric server.

So everything is present in our EKS cluster now.

Now we will also define the resources and additional environment variables.

So let's see all of these things in our YAML manifest now.

Right.

So we will go here.

And this is the 1 in 20 zero four.

So we can see the A dot metrics Prometheus collector will get created.

And then mode is deployment replicas.

Currently we are using one in production.

When you are implementing you can add more okay so that's not an issue.

And the service account we are using is a dot collector and using x pod identity Pi concept.

We are using this service account with the Im role and IAM policy created in 2001 project.

Right.

So Terraform project for Opentelemetry and these are the resources we have defined CPU 300 memory Phi

12.

And in addition to that we are also given the maximum limits also for CPU and memory.

And these are the environment variables we have defined here.

Okay.

So like cluster name this is the one region.

This is the one.

And we are also getting the node name information.

So these things below at the configs we are going to use them anyway.

So we will discuss that okay.

And next thing is so we have reviewed these things to LP first.

So this is for application that push metrics using Opentelemetry SDK.

So most of our metrics will come from Prometheus scraping.

But this is available as if needed as an additional option.

But important thing is Prometheus receiver.

So this Prometheus config plays the important thing for us.

Okay, so here in this Prometheus configuration right.

So if we go here right.

So you have something called uh we have the scrape interval scrape timeout and external labels like

cluster name.

So whatever we have defined in environments here.

So this is the one okay.

So these three are the global configuration information we have defined.

So how often to scrape each target is defined in this scrape interval.

And max time to wait is the scrape timeout.

And we also defined the external labels.

So every metric gets labeled with our cluster name now.

So these external labels are critical if you are running Multiple ECS clusters and sending that ECS

cluster related metrics to a single AMP instance.

Amazon managed Prometheus, right?

So when we even if you send it to separate, Amazon managed Prometheus.

And if you are using a single Amazon managed Grafana instance as a workspace, right?

So there also when you query in Grafana, you can filter by cluster name.

So that's the reason these external labels are very very important.

And the next thing is scrape configs right.

So this is where you are key scrape configuration will be defined.

Okay.

So we have defined nine different scrape jobs which are standard Prometheus jobs.

Wherever you go you will find these standard ones each discovering different types of targets.

Okay.

So let's see like if you see here this is one scrape target right.

Means like one scrape job we have defined here for Kubernetes API servers.

Let's talk about this on a high level.

Before going to other sections in this YAML file.

Okay.

Right.

So the purpose of this respective Kubernetes API server's job is to scrape metrics from the Kubernetes

API server itself.

So it will scrape the cluster level metrics like request rates, latency, and resource counts related

to your kube API server.

Okay.

So now we have provided the job name as Kubernetes API server right?

So this name appears as the job label on all metrics scraped by this respective scrape configuration.

Okay.

Right.

And here we have also provided the bearer token file.

So which is for authentication.

So uses the service account token mounted in the Prometheus part to authenticate with the API server,

and this token is automatically injected by the Kubernetes into every pod.

So that's about bearer token file.

And we also have something called Kubernetes SD config.

And under that you have provided role endpoints.

Right.

So this is like service discovery uses Kubernetes service discovery to find targets SD means service

discovery right.

So you are saying role endpoints means discovers all Kubernetes endpoint resources which represent the

actual IP addresses backing services.

So all those things it will discover this is more dynamic than hard coding IPS.

Or if API server pods move, Prometheus automatically discovers the new locations.

So that's the Kubernetes SD configs.

And we also have something called reliable configs.

So here comes the filtering part.

Okay so this is another important config here.

Okay.

So keep only API server and remove any other endpoints okay.

So it will first initially it will discover all the endpoints service endpoints.

But here you are saying you are filtering saying that only keep Kubernetes API server endpoint.

So this uh reliable rule filters discovered endpoints to keep only the Kubernetes API server related

service.

So if you go here right to the terminal and if I say kubectl get svc.

So just run this a little bit and see.

Right.

So there is a service named Kubernetes is of type cluster IP and it's IP address.

So in our default namespace we have this service okay.

So what exactly this service does, right?

So this service is the API server cube API server related service.

And we are from all the services present in default namespace of your Kubernetes cluster.

Right.

So you are trying to get this one.

The job information means like you are trying to get.

This means like you're trying to filter that okay using this reliable config.

So you are saying action keep only keep targets matching the regex whatever we have defined here okay.

So drop everything else in the regex you have defined is matches only the namespace, which is default

namespace and service name is Kubernetes and also port.

You should always use the https port, so that is the regex you have defined.

And in addition to that you have also defined the source labels.

Combines three metadata labels with semicolons okay one is namespace service name and endpoint port

name.

So only the Kubernetes API server endpoint is scraped.

All other endpoints are dropped as part of this.

Now we have also defined the scheme here which is Https.

So you are saying that use secure connection Https secure connection to scrape the API server.

In addition, you have also defined the TLS config file C.assert and insecure skip verify.

True.

Okay, so the path to our certificate file we have given cert means like path to the certification authority

cert used to verify the API server certificate.

And in addition to that you said insecure skip verified rules.

It skips hostname verification accepts certificate even if CNR San doesn't match.

Also it will accept because of this setting.

So this is common in Kubernetes because the API server set might be for, uh, Kubernetes dot default

dot SVC might, but access it via IP address.

Okay.

That's the key thing.

So this is one scrape configuration.

So like this you have another scrape scrape configuration for Kubernetes nodes.

Okay.

Before that.

So this what exactly this Kubernetes API servers will try to scrape is API server request total API

server request duration seconds means like API latency.

And also if uh.

Additionally it might also get the z request duration seconds which is z performance.

So the underlying database where the configuration will be stored for Kubernetes is z database.

And this API server, uh request duration with that respect to z backend DB.

Also it will try and get those information.

So like that.

Now we have another job called Kubernetes node nodes.

So the purpose of this respective Kubernetes nodes job is to scrape kubelet metrics from every Kubernetes

worker node.

So node level resource usage, pod counts, etc. we can get using this Kubernetes nodes.

Okay.

So you can go and then understand in detail.

So everything like if we try and explain this uh big file it will take huge amount of time.

But these are standard ones which wherever you go for Prometheus, these are the things out of the box

available.

And we just need to copy paste and start using them.

Okay.

Right.

If any changes needed slight things will be needed.

Okay.

And this is one more.

So we'll try to see what are those jobs on a high level and move on okay.

So one more.

We have something here called Kubernetes nodes.

See advisor okay.

So this Kubernetes nodes see advisor right.

So the purpose of it is scrapes.

See advisor metrics from cubelet.

So that is nothing but detailed container level CPU memory network file system metrics will be scraped

from using this respective job.

Okay.

And we also have something called Kubernetes service endpoints.

Right.

So the purpose of this service endpoints is scrapes metrics from application pods that expose via services

okay.

And that's about Kubernetes service endpoints.

So we also have something called Kubernetes service endpoints okay.

So this is one more uh job right.

But it's key important.

The purpose of this job is it is same as previous job Kubernetes service endpoints.

But for services that need less frequent Scraping.

We will use this one.

Okay.

So here you can find the scrape interval a little different every five minutes.

It will do the scrape interval.

But above one Kubernetes end points.

If you see it's scrape interval right.

So you can find it here.

If it is not defined in the job which means we need to go up here and see the default global scrape

interval which is 30s.

For our service endpoints it is 30s.

And for service endpoints slow right.

It we have explicitly defined it is every five minutes in the same line.

We have one job related to Prometheus.

Pushgateway discovers Prometheus pushgateway services for scraping metrics from batch jobs.

It's purpose is that okay?

So now in addition to that, we have something called job number seven, which is Kubernetes Services,

which probes services using black box exporter path.

It checks the HTTP endpoints for availability, latency, SSL certificate, and validity.

So like that we have one more for Kubernetes ports job.

Also this is the important one okay.

Right.

So this the purpose is to scrape metrics directly from the pods not via services using Pod annotations.

So this is one more.

Okay.

So here the important thing is Prometheus.

Uh it will check for these labels Prometheus io slash scrape io slash scheme and then our slash path.

And, uh, if, if already present, it will consider for that port whatever is there.

If not, it will override these things.

But the whole thing is it is going to scrape the pod metrics directly instead of already we have defined

for services.

And we also defined for the pods.

Okay.

So wherever these labels are present.

Prometheus.

Aeolus.

Scrape.

Right.

Equal to true.

That will be taken into consideration in our application for our retail store application.

It is not at the service level and it is at the pod level.

Means like we have defined it in our Kubernetes deployment.

So that which means those things will be applicable and metrics collected using this respective Kubernetes

pods job.

Okay.

In this setting, in this, in this Kubernetes pods itself, in this job itself, one important thing

here is action drop.

Okay.

So this what it does is it drops the non running pods okay.

So means like if the pod status is running pain means like we will have different pod statuses right

running pending failed etc..

So it removes targets matching the rejects.

So if the pod statuses are in pending succeeded failed are completed.

So those things will be dropped.

So it reduces unnecessary scrape attempts and then errors.

So that's the key thing.

And the thing whatever I am talking about here.

Right.

So the annotations right.

So these annotations we will go ahead and then see that now shortly to our application.

So let's go ahead and then see those pod level annotations for this scrape configuration.

So we'll run the command cube CTL get pods.

And we have our card catalog checkout everything running.

And we'll go here and go to any of the pod right.

So we'll say get pods and we'll take this cards right.

And hyphen o YAML.

So let's get the YAML output for this respective pod.

And on the top right if you go here to the annotations section Right.

So you see that here?

prometheus.io path is this one.

Port is 8080 and scrape is true.

So this is the important thing here right?

So if scrape true which means for this pod related metrics will be collected and sent to the respective

Prometheus instance.

Okay.

Right.

Now let's come back here.

And uh, so that's what is the Kubernetes pods thing.

In the same line you have something called Kubernetes pods slo.

Also here the scrape interval is five minutes earlier.

For these things it is only 30s right.

And that 30s comes from the global setting.

Whatever we have done at the config here.

So these are the nine scrape configs we have defined as part of Prometheus.

Okay.

Right.

And uh, to reiterate those things.

One is Kubernetes API servers, one is for Kubernetes nodes and for Kubernetes API servers.

The target is API server, and for Kubernetes nodes it is for Kubelet.

And another one is Kubernetes nodes Si advisor job, which is again the target is Si advisor in the

Kubelet.

And we also have something called Kubernetes service endpoints, job and service endpoints slow job.

And one more is Prometheus Pushgateway.

And another one is Kubernetes services with probe.

Right.

And another one is Kubernetes pods and Kubernetes pods.

Slow.

So these are the nine jobs we have defined.

And if you want more and if you want to collect some other information also you can collect accordingly.

Now let's come back to the after Prometheus config.

This is completed we need to go to the processors okay.

So in processors we have defined multiple processors here.

So one is batch processor right.

And the other is memory limiter and resource detection X and adds overrides resource attributes.

So these are the processors we have.

So let's discuss one after the other here.

So we'll start with the batch processor.

So this batch processor uh will send the data in batches every 60s okay.

And batch size is 1000 matrix okay.

So that's the batch processor.

This is defined purely for the efficiency.

Instead of calling always for every metric.

If you are calling the API to send the metrics or push the metrics into AMP.

So it's a problematic thing.

It might even create, uh, slowness and performance issues also for AMP.

Right.

And but this is the efficient way you can give every 60s you send a batch size of 1000.

So that is mainly to improvise the efficiency.

Okay.

So that's uh, we send one batch for 1000 metrics instead of 1000 API calls.

It is going to be a single API call.

So that's the efficiency we are going to implement right using batching.

And we also have something called memory limiter.

So here you are limiting your uh a dot collector pod memory to 5 to 12 MB.

And you are also giving a spike limit which is 128 MB.

Allow temporary spike up to 138 MB.

Okay, so this prevents the collector from crashing due to memory pressure.

It will drop metrics before out of memory.

Right.

So that's the reason it is important to define the memory limiter.

And when we are defining the pipeline this should be our first one to define.

So what I am talking here is.

So if you go to the pipeline here right.

So memory limiter should be the first one in the processes.

How you define here is not the key thing.

But when you define in the pipeline the order should be important.

And next is resource detection.

So this is another one okay.

So this enriches your metrics with environment variables whatever you have defined on the top.

And you are which is like cluster name and AWS region.

Whatever you have defined on the top right.

Those things.

And it will also add the EKS cluster info in your metrics and your EC2 instance, metadata in your metric

and your system hostname OS info also in your metrics.

Okay, right.

So that's about every metric will have attributes like cluster dot name.

And this makes filtering easy in your Grafana it becomes super easy right.

And you also have these resource attributes.

Okay.

Inside this only.

So we are also going to add in the processors this resource attributes thing where we add the cluster

name and also the deployment dot environment okay.

Currently we are using the value as development.

In your case if it is a different environment you can provide that information here like production

or staging.

So all these will help in filtering the things easily for us.

Okay so that's about the processors thing.

And we have something called next is export us.

So this is means like here in exporters we have one is Prometheus remote write and the other is debug.

So the first thing is Prometheus remote write.

So here you need to define your workspace.

Okay.

So when you go here right.

So we need to update this anywhere.

So we will go there and then get that information.

So we'll go to our cluster.

So this is our cluster.

And where is our Prometheus node.

Uh node exporter add on ID and Prometheus.

See this is the AMP workspace ID.

So when you have created your EKS cluster the project five which is your Opentelemetry project.

Right.

So there you get this AMP workspace ID if you want you can go.

You are in 2001.

You can go to zero one EKS cluster environment.

And in that you can go to zero five Opentelemetry terraform manifest and run the command Terraform output.

And it should provide the output for you.

There you can get the amp workspace ID also.

So you can see here this is the workspace ID.

So let me copy this and also update that here okay.

So this is the workspace ID we need to update.

Okay.

So we are going to export the metrics to this respective amp Amazon managed Prometheus instance.

Okay.

And then we also use the authentication here which is AWS sig for authentication.

So auth is SIG for authentication right.

So the collector signs requests using the IAM role attached to its service account via pod identity.

So no API keys no passwords just IAM permissions.

And it is much more secure okay.

And after exporters we have one more exporter which is debug in your art collector logs.

You also get the same metrics whatever it is writing to the Prometheus okay.

Right.

But this is optional one.

And in production you should comment this.

You should not use it.

You need to comment in the pipeline.

So if you comment here it is not going to write.

Okay.

If you enable it, it will write.

Okay.

So if you if you uncomment it here in your pipeline, it will start writing.

If you comment it, it will not write.

It's our choice.

Okay.

So I have defined it for your you can redeploy your collector with enabling this option and then also

see those things in our logs.

Okay.

So I will try to put it on for some time.

Right.

So and later I will try to comment it okay.

So I don't want to bombard my cluster.

So that's the reason I commented it earlier.

But just for learning purpose we will try to use it.

Okay so that's about the debug thing.

And now the next thing is extension section right.

So this we have discussed when we are discussing our uh traces.

Also we have health check endpoint for liveness and readiness probes and P p r o f, which is a go profiling

endpoint for performance debugging.

And we have z pages endpoint internal metrics about our Adot collector itself.

And we have the important one which is Sigv4 Sigv4 auth which is AWS authentication extension used by

our exporter.

So whatever we have defined here the same thing is used here.

Sigv4 auth.

Here you are defining region as US east one and service is a P.

Yes.

Okay.

So this sigv4 extension is what makes the Prometheus remote write exporter work with AWS authentication.

So that's about the extensions.

Now let's go to our next thing which is.

So now these extensions thing is over in pipeline we'll define the extensions and in pipelines In metrics

section, we will define these things which are receivers processors and then exporters.

So let's see those things.

So in extensions we have defined these things line health check Pro f z pages and seek for VR.

And in pipelines this is the metrics pipeline.

So here we are sending it right.

So this pipeline is from our A dot two.

We are sending it to AWS means like AMP right.

So that is the one.

So in receivers we are using the Prometheus receiver.

We are not using any OTC here.

That is important.

And in processors the order is important.

First is memory limiter.

Next is resource detection.

Next is resource.

Next is finally batching batch and then sent to our AMP and exporters.

You can see Prometheus remote write is the exporter we have defined.

And one more is debug.

So these are the things we have defined.

And one very, very important thing is you need to ensure that you update your workspace ID here before

deploying this Prometheus thing okay.

This Prometheus collector.

So this completes the review of the complete YAML.

So we didn't go through each and every scrape config in detail.

But if you want you can review those things okay.

Right.

So means like what it does and all those things nowadays we have uh all the GPT tools where we can copy

and get the line by line explanation also if needed.

Okay.

So but most of the important things we have discussed, mainly the kube API server and pods and then

pods, low nodes and nodes, and then also some important jobs we have already discussed.

Okay, so in our next lecture we will focus on deploying and verifying these things.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

We are in 20 observability open telemetry.

And we are entering into the section 2004 open telemetry AMP and then AMG.

So here we have already reviewed the architectural flow.

And we have also reviewed the complete YAML file in detail.

So now the prerequisites here are we need to enable the AWS IAM Identity Center in your AWS cloud account.

So let's complete that first okay.

Right.

So we will search for an identity center.

And if it is not enabled in your account it will ask for enablement.

Go ahead and then enable it.

So after enablement it looks this way okay.

Right now let's complete this complete IAM identity center configuration.

A little later first we will focus on deploying our a dot metrics collector and then see our metrics

are sent to the Amazon managed Prometheus.

Okay.

So first we will focus on that as a priority.

So let's come back here.

And this is the prerequisite for enabling the Identity center.

And we need to do that.

But we'll do that a little later.

So it's just go there and then enable it.

It's going to be simple straightforward process.

And once it is enabled we will see all other steps a little later.

Now let's come back.

You should have your EKS cluster running.

And as part of section 21 we have already implemented that.

So EKS cluster like vpc eks Carpenter.

Carpenter and then Opentelemetry.

Everything is created.

And after that in zero to retail store app you have first the zero one retail store AWS data plane.

So your data plane should be created.

And after that you should also deploy your application.

So all these thing is already completed.

So if you see get pods also you can see all our application pods are also running.

So let's wait for them.

So these are things are running.

So now if you want you can also go here and clear cube CTL.

Get Ingress and get our ingress service name here.

Right.

And then access it via browser and also verify our application.

So here we will access our application and then verify if it is working or not.

So before moving to the next steps okay.

We are adding it to the cart.

And then we are going through the checkout process.

And we'll go ahead and then click on purchase.

And our order ID should be created and everything should be working.

So order ID created.

And if we want we can also click on this and say topology right topology endpoint and see catalog is

all the carts is checkout orders.

Everything is healthy means like our application is up and running, so we'll go to the next step here.

We have already reviewed the metrics a dot collector YAML file.

And in that YAML file we need to update our workspace ID which is our Prometheus workspace ID.

So if we go here right.

So in our 20 zero four in Opentelemetry metrics here in our exporters.

Right.

So here in the exporters here you need to update your workspace ID so you can go to our AWS here and

go to Amazon Prometheus.

And this is the workspace ID.

You can directly copy and then paste it.

Or you can go to your zero five okay.

So this is your in.

If you go here right in 20 01X environment with a dot in 01X cluster environment and in zero five opentelemetry

terraform manifest.

If you run the command terraform output you will get the amp related Workspace ID also whatever we have

created as part of this Terraform project.

So this is the one we need to update.

Okay.

So we will update it in our respective Prometheus full cluster dot YAML file.

And once that is done we will go ahead and then deploy the a dot matrix connector.

So let's go ahead and then deploy it.

So we'll go to the terminal here.

And we'll come out of the previous logs terminal.

And we are in 20 observability Opentelemetry.

We will go to 20 zero for Opentelemetry amp AMC and cube CTL.

Apply hyphen F01 opentelemetry metrics and 018. collector Prometheus full cluster dot YAML.

So this should create a cube CTL get pods and it should create a metrics collector for us.

And it is getting it is currently running.

So if you want we can also verify the logs also.

Let's wait for it.

Kubectl describe pod.

And this is the metrics Prometheus collector related pod currently running.

So let me open that and created OTC container.

This is fine.

Startup flow.

That is fine.

We'll go ahead and get pods and let's wait for it.

And it is running.

So now if you see logs hyphen F and you provide that one right.

So you can see metrics started already flowing.

So all these are metrics whichever is going in.

So you can see these are sending it to the AMP.

So because we have enabled this right in our collector.

So this debug thing is enabled continuously.

Our log is getting updated with all the metrics.

It is sending it to the AMP.

So if we want we can comment this and redeploy.

Okay.

So apply hyphen nav and then zero one and then 01A dot collector if you want to.

So I'll leave it as is.

Okay.

Right.

I have commented it back in your case or I'll try to uncomment it.

In your case you comment or uncomment at your convenience.

Okay, now let's go back and there is one more script here which is verify amp metrics.

So let's review that on a high level first okay.

So this is the verify AMP metrics.

So it will do some things okay.

You need to update your workspace ID here.

So we'll get our workspace ID from here.

So this is my workspace ID right.

And we will update here.

And if you want you can run these commands.

Also whatever we are saying like Opentelemetry collector all these things you can run.

We have already verified the logs.

Now we are updating the workspace ID in our verify AMP metrics.

So it will just verify first like basic connectivity to AMP.

It will do using AWS Curl.

And it will also discover all scrape jobs whatever are running and discover retail store services.

And it will check for total unique metrics count and all these things.

And it will provide those information.

Pretty straightforward.

Okay.

We will go here.

And this is the one right.

So this is the terminal.

We have the verify AMP metrics.

And so after updating the R workspace ID nine e which should match with our nine EEC here we should

go ahead and then run the command verify AMP metrics.

And it will do a basic connectivity test and all other AWS curl commands whatever is defined here okay.

So these are the scrape jobs currently defined Otel collector API servers nodes, node C advisor parts

and service endpoints.

And these are the things currently scraping.

Now we said that all metrics should be sent.

So in addition to our our our cards catalog checkout we will other things also currently going into

orders and then UI.

These are retail but other things whatever is present in our cluster also those are also the scrape

jobs currently sent to the amp.

And these are the things okay.

So total unique metrics we can see here.

1259 and these are the scrape jobs right.

Which we have defined.

And these are the services which are currently the metrics are sent to our AMP and all tests completed.

So that's about verifying whatever we have deployed the Adot collector.

In our next lecture we will focus on Amazon managed Grafana thing.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

We have successfully completed the part where we have deployed our art collector for metrics and verified

that if the metrics are flowing to the Adot via Adot collector for metrics to Amazon managed Prometheus.

So now the next step is enabling this identity center, creating a user in that.

And then that user should be associated with Amazon managed Grafana.

And then log into Amazon managed Grafana and then configure this Amazon managed Prometheus workspace

as a data source, and then also import the standard dashboard and verify our metrics.

So these are the things currently pending.

So let's go ahead and then do them.

Now one important thing we need to understand here which is very very important is when we add a user

in Identity Center and associate that user with Grafana.

It costs dollar nine per user.

So when you associate that user with Grafana, it costs dollar nine per user.

So if you don't want to waste this money, or then ensure that you just watch this video and don't try

to do this.

Okay.

Right.

So that's the important thing to remember.

Now we already created the Amazon managed Grafana as part of zero five open telemetry Terraform manifest.

Okay.

So now we need to create a user in identity service.

So let's go back to here right.

And then we will go to our identity center.

And if it is not enabled it will pop up for enablement.

And you provide all the information in the user section we can add a user like this, something like

Kalyan Reddy.

And you can use the password.

Send an email to this user with the password setup instructions.

I'll give some email ID here and then provide it.

And.

At the rate gmail.com.

And provide my Kalyan Reddy.

And then we will go ahead and then click on next.

And just give some group here and click on next.

And click on Add User.

And a user is created for me.

But currently it doesn't have any, uh, multi-factor authentication devices.

So we also need to configure that.

We also need to set the password for that user.

In the emails.

You can see here.

This is the invitation I have got.

And then I'll click on Accept invitation.

And then it should open.

This way a portal page will be displayed.

I need to provide a password here.

So I will provide the password now.

So I'll provide the password.

Now the password is set and it is redirecting me.

And I will provide the password information here.

Username we can get from here and provide that information.

Ready.

Click on next and provide the password.

We will provide the password here.

Click on login.

And it is asking for the MFA device.

So we can use the authenticator app or built in authenticator or security key.

So I'll try to use the authenticator app.

So I'll click on next.

I am using the Azure Authenticator here.

So install either Google Authenticator or Duo or App on your mobile device and then scan this QR code.

Okay, so anything is fine.

So in your mobile phone, whatever you are using that one, you can try and use it.

Okay.

And I have scanned it and now I will provide my authenticator code which will be displayed on my mobile

and click on assign MFA.

And that is done.

So MFA configuration is also completed here.

And we will be able to log into our portal.

Currently no apps should be present here okay.

So this is the ID and there are no applications associated with it.

So that's about uh, creating a user in identity service and having that two factor authentication enabled.

Okay.

So let's understand first why do we need this identity service.

And then in that why do we need to you create a user and all those things okay.

So you might be thinking that why can't we use the regular AWS I right?

So let's understand that why we need to use IAM identity center for Amazon managed Grafana and not regular

IAM.

So let's understand that.

So we'll we'll get a clarity on that okay.

So AWS IAM Identity Center formerly known as AWS Single Sign on or AWS SSO is centralized identity management

service.

So regular IAM users are designed for programmatic access like CLI, API calls, SDKs.

So they use access keys and secret keys.

But this IAM identity center users are designed for human access okay.

So like web consoles applications and services like Grafana.

So they use the username password and multi-factor authentication.

These are the key things for Identity Center.

Okay.

So IAM Identity Center provides single sign on across multiple AWS accounts.

And it also provides a centralized user management.

And in addition to that we can also integrate the enterprise identity providers like Active Directory,

Okta or even AD.

So all these things also integrated with this AWS identity service.

Okay, so it's the modern way AWS wants you to manage the human users.

Okay.

So then why does the Amazon managed Grafana require it?

Okay, so here is the key thing Amazon managed Grafana does not support traditional IAM users for authentication

AWS IAM users for Authentication.

So when you access the Grafana web console, you must authenticate using one of these methods.

One is using your AWS Identity Center users.

Or second is you should use some Saml based identity providers like Okta or Adi.

So for that purpose, we need to enable this identity center and then create a user.

And that user need to be associated in your Grafana.

So now let's come back here.

And the next step here is Amazon managed Grafana associate the user.

So we'll go back here and we'll go to the Amazon managed Grafana.

So this is the workspaces.

This is Grafana right.

So Amazon managed Grafana.

And here this is the Grafana thing.

Add new user or group.

So I'll click on this.

And I have the Kalyan Reddy the new one I just now created assign users and groups.

So as soon as I assign this user right, so this immediately it will charge dollar nine.

Okay.

So that's the important thing to remember.

So now I got charged with dollar nine per user.

So this user and now after.

So you need to be careful if you want to assign and then review.

Complete the full flow.

Do it or else stop here before assigning itself okay.

Now for this user it's in the viewer permissions.

But we need to update to make him as admin.

Okay, so now I am making this user as admin in Grafana for my Grafana.

So that's about uh, associating the user.

Also in our next lecture we will go ahead and then log into AMG and add Prometheus endpoints and import

a dashboard and verify the metrics.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Login to AMG and add Prometheus endpoint.

So let's go back here and then we will go to our workspace here.

And this is our Grafana workspace URL.

You can try to open it.

And as the session is already present here for our portal let me click on sign in with AWS IAM Identity

Center.

And ideally it should log in automatically okay.

If not if I try to log out here.

Right.

So if I try to sign out this one and I'll try to also sign out this one right.

And try to log in again okay.

Right.

So this is my Grafana workspace URL and sign in with AWS IAM Identity Center.

And here I need to provide my user ID which is Kalyan Reddy.

And then next the password I need to give.

And once I give the password, it will ask me for a MFA code.

And I need to go to my authenticator app on my mobile and provide that MFA code.

So which is I have provided it and I am signing in, and this should take me to the Grafana login page.

And here we can go to data sources in connections data sources add the data source.

And this is the Prometheus.

And we'll click on Prometheus and provide.

I will leave it to Prometheus and provide our Prometheus server URL.

We can provide our Prometheus endpoint here and add it.

Or we can go to apps here, go to AWS Data Sources and Amazon Managed Service for Prometheus.

And here you can select your region which is US East North Virginia.

And here you can see nine EEC.

You are you are Prometheus endpoint is displayed directly so you can click on Add Data source.

Okay.

Right.

And this should be added in your data sources okay.

Now if you go to data sources you can see our data source here.

So this is the one which we have created just now.

So this is a dummy one.

We can see here our Prometheus data source which is our Amazon managed Prometheus data source is created.

Here we can see nine E.

And this is our Prometheus workspace which is ending with nine e.

Okay.

Right now the next thing is we'll go to the dashboards here and click on import a dashboard.

And here we'll provide the ID as 15661 as the I'd.

And then click on load.

And this is the Kubernetes dashboard.

And in folder dashboard folder we are putting it.

And here we will select the Prometheus data source which is our nine EC data source and click on import.

Right.

And it should get the data slowly now.

So you can see it's loading all the data.

So you can see it is saying workloads 25 total pods 53 total nodes four nodes are there.

And this is the memory usage and everything it is showing in detail.

So node wise information and all the things.

Okay.

So you can see additional information here namespaces.

And then also pod resource detail information.

Everything you can get here in this dashboard.

So like this.

There are multiple other dashboards are available which you can search in open source community and

get them and use it.

Or you can build from scratch based on the needs okay.

So this is for our UI service average CPU 1%.

So this memory is 54% and total CPU 0.512.

Like this for orders cards.

So like this you can get the information for every uh.

application.

Okay.

Every container, you get the information also.

So all the graphs, you can see it in detail.

So what we have tested.

Right.

So we have tested everything in detail that.

So if we go back here right.

So our our application metrics sent to our Amazon managed Prometheus using the a dot collector for metrics

and then using Amazon managed Grafana with the identity center user, we are able to access and visualize

the dashboard.

So we are able to test, uh, everything.

The end to end flow is tested from here on how you want to build your dashboards and then what you want

to do.

Everything is again a configurable item.

So more dashboards you want to build and more alerting things you want to implement.

Or if you want to go and then change some scrape configs.

Everything you can do.

You have the whole setup here.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Let's go ahead and then clean up the things, whatever we have created.

So we have deployed the a dot collector.

So let's go ahead and then cube CTL.

We are in 2004.

And in zero one Opentelemetry metrics we have the dot collector related configuration.

So we'll go ahead and then delete that opentelemetry collector okay.

And after that if you want to delete everything right.

So remove the retail app remove the data plane and then remove the EKS cluster.

We can run these things okay.

So we can go to come out of this and go to 20 zero one.

And inside that we'll go to zero to retail app environment.

And inside that retail store app helm AWS data plane.

So here we will have low cost and high cost.

We have deployed the low cost one.

So we can go here and then uninstall our retail apps in the same line.

You can delete the data plane and you can delete the cluster.

Also go to that respective folders and then delete it.

So we might use the data plane and then cluster these two things in our next demo.

So I am not deleting it.

But for today your demo is done.

And then you don't want to do anything.

Uh, and tomorrow you want to resume the other section.

Then you can go ahead and then execute these things also.

So I'll see you in the next demo.

Until then, bye bye.

Thank you.


