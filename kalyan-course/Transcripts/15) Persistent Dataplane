All right.

Welcome to this new section on retail store application with persistent data plane running on Kubernetes

cluster.

In this section we will bring everything together our microservices, our databases and our helm knowledge,

whichever we have learned in previous demos.

So why we are combining all these things to deploy a fully self-hosted, persistent data plane inside

our Kubernetes cluster.

So let's take a look at this diagram on the screen and understand a little bit more about what we are

planning to do here.

You can see our retail store application running entirely inside the Kubernetes cluster.

So here each microservice like catalog, cart checkout and orders is deployed right here.

And all their persistent layers like Mysqldb, DynamoDB, Redis and even the PostgreSQL and RabbitMQ.

So all these things are also self-hosted within the same EKS cluster.

This is what we call the persistent data plane running on Kubernetes cluster.

So no external managed services yet.

Everything is running locally inside EKS to help us simulate a setup without extra AWS cost.

Now look at the second diagram here.

Right.

So if we go down so we have something called I have split these things and then depicted in a different

way.

So we can see here this is our persistent data plane right.

And this persistent data plane currently as part of this demo we are implementing on EKS cluster.

And this whole thing we are deploying using helm.

Okay.

So you can see we will have the ingress service.

We'll have the UI application.

And then we'll have the catalog service with its own underlying database which is MySQL cart.

Microservice with DynamoDB.

Checkout with.

Reddis.

Orders with.

With PostgreSQL.

And in addition to that we will also have RabbitMQ for our.

Messaging.

So all these things we are going to deploy using our helm charts.

And another important thing here is in future when we go to sections like 14 zero one and then 14 zero

two.

So there we are going to run the persistent data plane using AWS services like AWS, RDS, MySQL, AWS

DynamoDB, AWS ElastiCache, AWS, RDS, PostgreSQL, and AWS Simple Queue Service, which is SQS.

So this we are going to do in this section demos.

Currently we are going to focus on these things which we are going to implement using helm.

So let's go ahead and then implement this demo.

I'll see you in the next lecture.

Until then bye bye.

Thank you.


Alright, we are in our course GitHub repository and we are entering into the section 12 which is helm

in that currently we are working on the demo which is 1205 Helm Store, Helm Retail Store.

And here as part of First Step, we are going to download and review the helm charts for all our microservices

like catalog, cart, checkout, etc. and to download these things we have a download and Untar helm

chart.

So means like download and Untar this helm chart script we have created.

So we'll go ahead and then uh, implement that means like we'll download these charts first.

So we'll go back to our terminal here and we will enter into section 12 helm.

And in that we have something called 12 zero five Helm Retail Store.

And here you have retail store charts okay.

So that is the folder we need to go in retail store charts.

And there you will have the script download and Untar helm charts.

So this will use the helm pull command to download the charts present in OCI repository and then also

untar those helm charts.

So currently in this folder nothing is there.

So first it will create a chart directory charts directory.

And inside that it will download all these things okay.

So we'll go ahead and then download the helm chart.

So when we have the source code of those helm charts in our local desktop, it will be easy to review

for that purpose.

We are trying to download.

And after that we'll start reviewing the catalog microservice first.

So we can see here it has downloaded all these helm charts.

So if we go to here if we go here we have the charts folder.

And in charts folder you have all these microservices related charts downloaded helm charts.

So we can also see them from our Visual Studio Code.

So these are the things downloaded and in retail store apps.

So this is the retail store apps folder.

So here you will have all the custom helm values file for each microservice example.

If you take values hyphen catalog dot YAML.

So from the default values what catalog chart is having?

On top of that, what we want to override, we are going to define in these custom values.

So for each and every microservice we have those things.

And here you can see you can use this script to install these things okay.

So you can install the charts using this install retail apps and uninstall retail apps if we want.

Okay.

Or we can manually install it using these commands okay.

Helm install catalog and provide the chart URL and provide version and your custom values file.

So like that it's our choice okay.

So for easy up and down I just added these things.

But these are the important things okay.

These five things.

Custom values are the important things for each helm chart.

In our next lecture we will focus on learning about catalog values hyphen catalog YAML in detail what

is happening in combination with its own equivalent helm chart, which is catalog, helm chart and in

their templates and then helpers dot TPL file.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

Let's go ahead and then review this values hyphen catalog YAML.

In that we have something here called MySQL and create equal to true.

So by setting create equal to true we are telling helm to deploy a MySQL database instance inside our

EKS cluster along with cat catalog microservice itself.

So helm will automatically create the MySQL deployment and its service cluster IP service and link the

catalog application to it internally.

So if by default it should be false and means like uh, why?

Because it might be using in-memory or anything.

So we can go to values dot YAML for catalog and then see that what exactly is defined here.

So this is the retail store sample catalog chart.

And here these are the default values dot YAML.

And here you can see replica count is one.

And this is the image repository you need to take and tag if not defined means default, it need to

take the chart version and chart version will be present here.

1.3.0 right.

And now you can see you can go down here.

We'll go to the MySQL stuff.

Currently we are reviewing that right.

So here is the thing.

By default it is false.

And in app dot persistence by default the provider it is saying is in-memory and endpoint is empty and

database is catalog.

So these are the information it has by default okay.

So now what we are going to do is so we will try to go here.

This is the values catalog dot YAML.

Right.

Just put it as space.

And then now you can see MySQL create.

True.

So what exactly is happening.

We'll go to templates and review those things.

So you have something called MySQL Statefulset template and MySQL service template.

So let's go to MySQL Statefulset.

So the whole template whatever we have here Right.

So this is the template which is present in a if loop.

It is saying that if values dot MySQL, dot create means like if it is true then only create the stateful

set, which means you said here true.

So which means this will execute and stateful set will get created.

And if you make it to false then your stateful set will not be created.

Which means you are not going to create any MySQL database instance in your EKS cluster.

So that's the way we need to understand.

Same will be there for even mysql service if values dot mysql dot create.

So you can see values is the values file mysql dot create is true.

Then create this service also.

So that's about MySQL service and then MySQL stateful set okay.

Now we'll also review some additional things here which are app persistent and then secret things okay.

So in app dot persistence you are saying provider as my SQL documentation of this retail store sample

application and in the source section right, you will have something called catalog, right?

So this is the catalog application.

And if you go down here it is saying persistent provider by default is in-memory which we are seeing

here.

Okay.

So where is it earlier in the values dot YAML.

You can see it here.

By default it is in-memory configured okay.

And database endpoint empty.

You can see that database name.

You can give catalog or catalog DB anything.

Okay.

So here it is defined as catalog okay.

Now know this is the name of the database okay.

Name.

And then which is catalog DB and user.

You can define whatever you want.

Okay.

So they said catalog user.

Here we have in the values YAML it is catalog and password.

They have put empty.

So these are some default values for that.

Okay.

But currently we are discussing about app dot persistence provider in provider.

What are the things available in memory or MySQL?

So we are selecting currently which is MySQL right.

So when we are selecting MySQL means we are planning to create a MySQL database instance as a stateful

set in our Kubernetes cluster.

So that's what it mean.

Okay.

And when you put endpoint empty means you don't you are not creating or you are not connecting your

catalog application to any of the external RDS MySQL databases.

So if you have a external RDS MySQL database, so you can give that information here, you can put this

false provider is MySQL but endpoint information you can provide here okay.

So that's the thing database name we are putting as catalog.

Now let's go ahead and then also review the catalogs catalog microservices Configmap here.

So in this config map you can see if values dot config map create is true.

If you go to the default values YAML, you will find that at the top itself that where is it?

You will find somewhere C config map by default is create true only.

And if you want to give a custom name, you can give or let the system take care of it.

So now let's come back to config map here.

And then it says that retail catalog persistent provider.

It is getting from our values dot app dot persistent dot provider which means from here app dot persistence

dot provider.

And if it is MySQL if equals MySQL.

If your persistence provider is MySQL then also set these two things.

It is saying okay right means like these two things comes into the consideration okay.

And you see this is the retail catalog persistent end point okay.

So this is the end point.

But it is not saying that it will take it from values.

Dot app dot persistent provider.

It is saying that it will take from catalog dot mysql endpoint.

So we are going to discuss that a little bit later after discussing this.

So Dbname you are getting directly from the values dot YAML where you say values dot app, dot persistence

dot database.

So from here this part is clear.

Only this one we are stuck now which is catalog MySQL endpoint.

So let's go ahead and then review that now.

So we'll go to help us dot TPL here in this helpers dot TPL.

So this is the thing catalog dot MySQL endpoint.

So whatever we have seen just now right.

So in let me just give some space here like this okay.

Now we can go to config map here right in config map.

You said that uh include catalog MySQL endpoint.

So you are including a function here catalog MySQL endpoint.

And in this, what exactly is the thing written?

So it says that if values dot MySQL dot create, which means if values dot MySQL dot create is true,

right?

Then you pick the information.

As you can pick the information from values.

Dot means like you get the catalog dot MySQL full name that will be your service name.

Catalog dot MySQL full name and append it with values dot mysql dot service port.

So that's the common thing okay so values dot mysql dot service port will be defined in your values

dot YAML okay.

So if you go down here this is the one service values dot where is it MySQL dot service dot port.

So this is the MySQL dot service dot port.

So this is the one it is talking about.

So the second part is clear for us.

And the first part is it is saying catalog dot MySQL dot full name.

So if you go to this service name here right this is the service name.

So you can see Catalog dot MySQL dot full name.

So this is the service name.

You have it here.

So let's take that.

It is saying so when MySQL dot create is true currently it is defined in our custom thing as true.

It will get the information from here directly and set in our config map.

So the end point becomes our cluster IP service MySQL cluster IP service.

So that's about config map in relation with these informations provider endpoint database okay now for

your application right.

So if you want to also set the credentials.

So in your values dot YAML.

Right.

So in your values dot YAML you can also in default values dot YAML.

Also you can see this information okay.

So if you want to create a Kubernetes secret by default it is true.

It is creating it okay.

And name is catalog DB means like Kubernetes secret name, username is catalog and password is empty.

So we'll talk about this empty password later.

But let's come back here.

So we said that yes, we need a Kubernetes secret get created.

And whose name of the Kubernetes secret is catalog db and its username is my db admin and password is

DB 101.

So if you go to the secrets template here.

So if you go down here this is the secret template.

If values dot app dot persistence dot secret dot create okay so this is the one app dot persistence

dot secret dot create is true right?

Then you create the values that app dot persistence dot secret dot name.

So this is the one name of your secret.

It is coming from our values dot YAML and username.

We we we bought it from our username only and here.

But the password it is saying that again include catalog dot persistent dot password.

So let's see that.

So you have set something here.

Why.

Why don't we use this directly.

Why is it referencing to some other function from helpers dot TPL.

So let's go there okay.

And understand catalog dot persistence dot password.

So that's what it is said that okay.

So it is saying that if not empty right.

Which means your values dot app dot persistent dot secret dot password.

So if you don't set this if you don't provide your password here and if you try to put it as empty which

means like this empty, then only it goes and then generates a password for us here using this okay,

get or generate pass and it will automatically generate some password.

But if it is already present then it will just base64 encode that password.

So we have something called Db1 zero one.

So when it is creating the Kubernetes secret it will base64 encode that password.

So that's what it is going to do.

Same is happening for username also.

Right.

It is going to base64 encode that password.

So that's about Kubernetes secret which we have seen here.

And if you go to the Statefulset template, they are going to reference our secret here C value from

secret reference values dot app, persistent secret dot name.

And from that you get the username and then password.

Okay so from this secret you extract this information.

So those things you are already doing standard Statefulset which we have implemented in our previous

demos.

Right.

It's the same thing, but it is templatized in the helm chart with all these things okay with if include

and all those things.

So you can review those things for additional knowledge.

Okay.

But the key things, whatever is required here, we have discussed those things in detail which means

like one is values hyphen catalog dot YAML, which defines all our custom inputs for MySQL creation,

database name, and even credentials.

We have also discussed about the Configmap template, which uses those values to create environment

variables for our catalog container or catalog pod.

And the other thing which we have discussed is the Kubernetes secret template, which stores our database

username and password securely.

In addition to that, we have also discussed the helper functions.

So one is catalog MySQL endpoint and the other is catalog persistence password.

So which makes the deployment dynamic and repeatable.

Together these templates make the catalog microservice self-contained secure.

And we can go ahead and then practically deploy that remaining things.

You can go ahead and then review.

So all these things are pretty standard.

You will understand easily.

But whatever is important I try to explain.

Welcome back.

We are in 1205 Helm retail store section.

And now we are going to go ahead and then manually install the catalog helm chart.

So helm install catalog and provide your helm chart and version and then values hyphen catalog dot YAML.

And we should be in the directory retail store apps.

So let's go there and then first install the catalog microservice.

So we'll go to tool helm and 12 zero five helm retail store.

And inside that we'll go to retail apps.

And there you will find all the custom values.

So we are installing one after the other helm charts here.

So now we'll run the command.

And let's go ahead and then install that catalog micro.

It's unable to get the helm chart.

So let's first authenticate.

So we'll go to oh we'll run the command AWS ECR get login password.

This command to authenticate to Amazon Public ECR.

This token is valid for 12 hours okay.

So now let's complete this first, and after that we'll go ahead and then run the helm install command

for catalog microservice.

So let's run this and it should work now.

So let's wait for it to install the catalog microservice.

We can see catalog microservice deployed successfully.

So we'll say helm list.

And it should show that in our default namespace catalog is deployed.

Okay.

Now we will run the command helm status catalog and hyphen.

Hyphen show resources.

And this should show us all the resources it installed as part of this release.

Okay.

So we can see it has deployed the Configmap and catalog MySQL cluster IP Service and Catalog cluster,

IP Service and Catalog deployment and catalog related part.

And this is catalog MySQL Statefulset and its equivalent part.

And we can also see the Catalog Service account and catalog DB secret.

So here secret directly we are using from our values dot YAML.

Whatever the credentials we have provided.

So if we go here in our values catalog YAML.

So these are the things currently it is present these helm charts currently not integrated with secret

store CSI driver.

So currently we are not using all those things okay.

So we are trying to install using helm all these charts and then see whether everything is working or

not.

And if you want you can still optimize this and take it to the next level.

And we already have the source for these charts for everything here.

So accordingly we can modify.

Now let's go back here and also run the command cube CTL get pods and let's see whether they are running.

So catalog MySQL Statefulset is running and catalog microservice pod also running.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


In our previous lecture, we have successfully installed the catalog microservice helm chart.

Now let's go ahead and then install the chart microservice now.

So which will install the cart microservice and its DynamoDB endpoint.

So let's go ahead and then do that now.

So we'll go back to our Visual Studio Code here.

And here in retail store apps we have the values called dot YAML.

And here you can see we have the the custom values file present.

And here we have defined that app dot persistence.

And then provider is DynamoDB.

And in DynamoDB table name is items and create table.

True.

And whether we need to create a local DynamoDB or not we are saying it here.

Local DynamoDB means self-hosted DynamoDB, which is a Kubernetes deployment in EKS cluster, which

is a DynamoDB Local.

So which when we say DynamoDB create true, it is going to create a local DynamoDB.

Okay.

Which means it will create on a Kubernetes deployment.

It will create a pod for us okay.

So these are the default values.

So all these things are as is.

And here you can see if we go down here is the thing.

So default persistent provider is in-memory.

And we are changing it to DynamoDB and tables items.

And if it is not created we are saying create the table also.

Right.

And here you can see DynamoDB create false which means you are using in-memory in the default value.

So you don't need a local DynamoDB that is in-memory setting, but you are saying it here.

Provider is DynamoDB, which means we need to create a DynamoDB.

So we said true in our custom values and provided in image information as is, and service information.

Also as these rest all we left, it defaults.

So that's about values hyphen code, dot YAML.

And if you want you can go inside templates and browse all these templates and then review them.

Service accounts, config map deployment DynamoDB deployment and DynamoDB service.

So all these things you can review.

So if you see the DynamoDB deployment also if and equals to DynamoDB which means like dot values, dot

app, dot persistent, dot provider and dot values DynamoDB create.

So if both things are there, which means this is DynamoDB.

And also this is true, then only means like these.

These two, this and this are present.

Then only create this deployment.

It is saying okay.

So which means it is saying that if the here you can see persistent provider, if the persistent provider

is DynamoDB and DynamoDB create true, then only create this deployment whatever is defined here.

So like that, everything is written so you can review them and understand in detail.

If you want to go in detail of this helm chart, more or less, all these helm charts are pretty standard.

And if we read one, if we understand one thing more or less, all these things are going to be in a

similar way.

Okay.

Now let's go back and then install this cart helm chart.

So helm install and then cart and provide your chart related path and version.

And then hyphen f values cart YAML which is our custom value.

So we'll go back here.

And in the terminal we are in retail store apps.

And we will install the clear clear.

And we will install this cart helm chart.

Now our cart microservice will be installed.

So we can see here cart microservice is deployed.

So we can run the command helm list and we can see that CART is also deployed now.

So we will also say helm and then status cart and hyphen.

Hyphen show resources.

So this is the important command.

So what all resources deployed as part of this cart helm release we are going to see them okay.

We can see it here.

It has created a service account.

It has created a config map.

It has created a DynamoDB cluster IP service and also carts cluster IP service.

And it also created a DynamoDB.

And now it is creating the deployment.

So if we run the command cube CTL get parts, their parts are also we can see them here okay.

Get parts.

We should see the carts DynamoDB and carts part.

All are in running state.

So this completes the deployment of carts microservice.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Alright.

In previous lecture we have successfully deployed the carts microservice.

Now we are going to focus on checkout microservice.

Okay, so now let's go back to our Visual Studio Code.

And now we are going to review the custom values file for values hyphen checkout dot YAML.

So this is the file defined here.

And in this is that if you go to retail store sample checkout chart and in values dot YAML you can see

this is the default one.

Okay.

So pretty standard same as other custom other default values file.

But here important things are app persistence provider is in-memory.

And in Redis endpoint it is empty which means we are not using any AWS related Redis endpoint.

So if you use ElastiCache or something you can define that here.

So even in our previous uh carts also.

Right.

So if you go to carts default values right.

It is provider is in-memory in the same way.

Here also you can see provider is in memory for even the, uh, redis related means like checkout related

default values.

And we will customize that to app persistence provider, Redis and Redis endpoint.

So we are not using any of the cloud provider here like Reddis external source.

Like we are not running any elastic AWS Elastic Cache or something like that.

So we are putting it as empty and in end points we have additional end points.

Checkout also need to communicate with the order service.

So you can see that here okay.

So see the communication here in detail.

So checkout need to communicate to order service.

So for that purpose we also need to provide the orders end point.

So this is the default orders.

End point orders .4. sec.

Default is the namespace and svc is the service name and cluster dot local is your cluster domain okay.

So this is the complete endpoint information.

So when you deploy the orders it's end point is going to look like this.

So that's about end points.

And now in addition to that you also have something called Redis create false in the default values.

So when you make it as true it is going to go ahead and then download the reddis Docker image and then

create a pod for us using the Kubernetes Statefulset or deployment.

We will see that here.

Okay.

So it's a it's also a deployment you can see.

Right.

So it's also a deployment.

So let's come back here and we are going to say that Redis create true and image information we have

provided.

And it is also going to create a cluster IP service.

These things we can leave as is or remove them.

Why.

Because those are already present in defaults here.

So we really don't need to define them here okay.

So I'll try to empty it so they are already present from the default values.

From there it is going to take okay.

So that's about checkout service.

Now we can go back here and then deploy the checkout microservice helm chart.

So values hyphen checkout YAML.

And this is the checkout chart okay.

So let me copy this and go to the terminal and clear.

And let's go ahead and then deploy this.

So this should create a deployment for us.

And also as usual our checkout related deployment which will create a checkout related parts also.

So if we run the command helm list we should see that checkout should be deployed now.

So you can see checkout is deployed.

Now.

What we are going to do is help status out and hyphen.

Hyphen show resources.

And this should show us all the checkout related resources it has created.

Okay.

So now you can see checkout related pod and checkout redis pod.

Both are running and in deployments checkout and checkout Redis.

So because you said here in your custom checkout values, Redis true, it has downloaded this Redis

and then installed it okay.

And here you can see checkout redis service and checkout as usual your checkout microservice.

And this is the config map.

And these are the secrets okay.

So if you want you can also So cube CTL get cm.

And this is the checkout uh related thing.

So you can say cube CTL get cm and then checkout and hyphen or YAML.

And you can see in the checkout what exactly is present.

It said that it has the order endpoint.

It has the Reddis as the provider and Reddis checkout hyphen Reddis colon 6379 as the reddis URL.

So if you say kubectl get svc, you should see checkout hyphen reddis there see checkout hyphen reddis.

It is connecting to this respective cluster IP service with 6379 port.

Okay, and if you want you can also run the command cube CTL get.

And earlier we have seen another CM which is catalog.

Also you can review catalog hyphen YAML.

And in that what all things were set there.

So this is the DB and this is the endpoint catalog MySQL and provider is MySQL.

So if you say get SVC this endpoint catalog MySQL 3306.

See catalogue.

MySQL is a cluster IP service with 3306.

So which means we are trying to show that these microservices are pointing to the self-hosted persistence

data layer in your EKS cluster itself.

Okay.

In the same way, if you go and then also review the cart cards also kubectl get cm and then cart carts

hyphen YAML.

Here you will find the information about.

So this is the DynamoDB create true and then table name is items, provider is DynamoDB.

And here the endpoint is cart hyphen carts hyphen DynamoDB.

So if you say get SVC and you should see that cart hyphen cart DynamoDB with 8000 as the cluster IP

service present here.

So that's about checkout service and also other services.

What all is present in Configmap and then how they are pointing.

I will see you in the next lecture.

Until then, bye bye.

Thank you.


Alright.

In previous lecture we have learned about checkout microservice and its equivalent Redis Kubernetes

deployment.

Now we are going to focus on orders microservice, its persistent database PostgreSQL and also its messaging

service which is RabbitMQ Statefulset.

So let's go back to our Visual Studio Code.

And here we have the values orders values orders dot YAML file.

In app dot persistence we have something called Postgres.

And in app dot messaging we have something called RabbitMQ.

So provider is Postgres and database name is orders.

And if you want to create a Kubernetes secret you are saying secret create true.

And then name of the secret is orders db and username is my db admin.

And then password is db 101.

So this will create a Postgres database.

And then additional means like these are the information you need to provide to your Postgres, which

means you need to create a database.

And these are your username and password.

And to create the PostgreSQL.

So we'll say PostgreSQL create true.

And then these are the image information.

And this is the cluster IP service it is going to create.

And these are the persistent volume information which is ten GB of uh volume with read write once.

So if you go back to templates here you can see Postgres service Postgres statefulset.

So you can see values dot postgres dot create.

So values dot postgres dot create equal to true.

Then only create this statefulset and Postgres service is there.

And in addition to that this is the Postgres related, uh, Kubernetes secret.

Now if we go back here, we have something called messaging app, dot messaging and provider is RabbitMQ.

And in addresses you have provided orders hyphen RabbitMQ, dot default dot svc cluster dot local which

it is going to get created now and it's equivalent service.

So which is 5672.

You can see right.

So this is the service.

It is going to get created okay.

Here is the cluster IP service information what it is going to create for RabbitMQ.

And now if you come back here and it will also create a secret with this username and then password.

Name of the secret is orders RabbitMQ.

And if you go down whether RabbitMQ need to be created or not you say create true and it's image information

and cluster IP service information and data means like the size of the data.

It need to use ten GB and access mode is read write once.

Now if you come back here to your RabbitMQ secret.

So this is the thing values dot app dot messaging RabbitMQ secret create.

If it is true then only if you go here.

So this is the one secret create equal to true values dot messaging dot rabbitmq dot secret values dot

app dot messaging dot rabbitmq dot secret under app.

It is there right app dot messaging dot rabbitmq dot secret dot create equal to true.

Then create this secret okay and RabbitMQ cluster IP service which is values dot rabbitmq dot create.

So which you can see it here okay values dot RabbitMQ dot create is true.

Then only create this service in the same way RabbitMQ Statefulset values dot RabbitMQ dot create.

You can see if it is true then only create this stateful set.

So all these resources will get created in that line.

So and Configmap also you can see values dot Configmap dot create which will be by default true in our

default values dot YAML.

So you can go down here and configmap create true okay.

And you have your Kubernetes deployment which is our orders deployment.

This this is the orders deployment.

And you will also have orders service okay.

So this is the service.

And you have the service account related template everything.

You can see it here.

So that's about on a very high level about order service.

It is going to create RabbitMQ related stateful set and also its cluster IP service.

And in addition to that it also going to create PostgreSQL Statefulset and its service and orders microservice

related Kubernetes deployment and service.

For both PostgreSQL it is going to create a Kubernetes secret.

And for RabbitMQ also it is going to create a Kubernetes secret.

And in addition to that, we are also going to have the Configmap and service account which are standard

things.

So now let's go back here and also deploy the orders helm chart okay.

So we'll go back to the terminal here.

And let's go ahead and then deploy this.

We can see here we have deployed the helm chart for orders.

So if we run the command helm list we can see that here orders is deployed.

So let's wait for the list to get displayed.

And we can see orders is displayed.

Now we'll go here and we will run the command helm status orders and hyphen hyphen show resources.

So this is very important command.

Now let's see all the resources whatever created as part of this orders release.

So we can see it here from the top.

It has created a deployment which is orders, which is orders, microservice, core application.

And in the parts you can see orders, application and orders PostgreSQL and orders RabbitMQ.

So all these three parts are running.

So this is orders PostgreSQL Statefulset and this is orders RabbitMQ Statefulset.

And this is the orders Kubernetes service account and orders RabbitMQ Kubernetes secret and orders DB

which is our PostgreSQL Kubernetes secret.

And this is the orders Configmap.

And here orders PostgreSQL cluster IP service orders RabbitMQ cluster IP service and orders cluster

IP service.

So all these things created with a single helm chart install.

Okay.

Now we'll also go ahead and then run the command cube CTL get cm orders hyphen YAML, which means we

are reviewing the orders config map and here messaging provider is RabbitMQ and this is the address

RabbitMQ address.

Orders.

Hyphen RabbitMQ colon 5672.

And this is the persistence endpoint orders PostgreSQL information.

And this is the orders persistent name.

And this is the provider.

And if you go here and see the cube CTL get svc you should see orders RabbitMQ 567 to service orders

hyphen RabbitMQ 567 to service.

And in the same way you will have orders PostgreSQL 5432 service orders PostgreSQL 5432 service.

So that's about, uh, order service.

In our next lecture we will focus on the UI.

Okay, I'll see you in the next lecture.

Until then, bye bye.

Thank you.


All right.

In previous lecture, we learned about orders microservice and deployed the orders helm chart.

Now we are also going to deploy the UI helm chart, wherein it will install the UI application and also

create the ingress service for us.

And in addition to that, in this UI custom values, we will also point that UI application to all these

microservices which are called endpoints.

So for all these endpoints, the configuration will be updated inside this UI application.

Custom values dot YAML.

So let's go ahead and then review that now.

So we can see it here.

This is the values ui dot YAML.

And here app dot theme.

We have given green and in endpoints we are giving catalog dot catalog dot default cluster and cart

hyphen cast default SVC cluster local eight, and another one is checkout and another one is order.

So if you go to the terminal here and if you say kubectl get svc right, so you can see the cart service

cart hyphen carts.

So this is the one.

And for catalog this is the catalog service.

You can see catalog here okay.

And in the same way for checkout checkout service.

And you can see that here checkout okay.

In the same way orders.

Also you can see it here.

So this is the default one okay.

In default namespace you are pointing to the service.

And this is the cluster domain cluster dot local.

So these are the endpoints we have defined.

So if you go to the values dot YAML and default values dot YAML.

And if you go here in app dot endpoints all these things were commented.

And you have given a empty map here.

Empty map means it will use the in-memory service which means the entire application, whatever.

This entire retail store application, when you don't provide any endpoints, it will take it from means

like the pre-built static application, it will take into consideration complete in-memory application.

So that's what we have tested in our 12 zero to helm custom values.

So here you didn't define any any endpoints.

So it took the default application which is in-memory application.

And everything is going to work all the services and everything.

But that is a complete static version.

Now here we are deploying it as a uh, multiple endpoints, multiple applications, multiple pods.

So here in single pod itself, everything works in 1202.

But here you are going to have separate cart application, separate catalog microservice, separate

checkout and separate orders in addition to separate microservices underlying their their persistent

data plane also split, all running separately, and you are pointing from your UI application to these

microservices.

Okay, so let's do that now in values UI.

This is the part we have completed and this we have already seen earlier.

Also, it is going to create an internet facing ingress load balancer which is nothing but our application

load balancer in AWS.

So if you want you can also review your default values dot YAML for your UI chart.

And there are additional settings also available inside that.

So if you want chart to be enabled then you need to do these things means like provide your information

related to OpenAPI based URL or bedrock related region.

All those information.

These are additional advanced things which are not related to our Kubernetes, its features in your

retail store application so we can ignore them.

Our key thing is app dot theme and app dot Plot end points.

So those two things anyway we have defined here app dot theme is green and app dot end points are these

things.

Now if you want to review the templates also you can go here and then review them.

You will have the config map and if values dot Configmap create it will create this.

And here you have all the information and you have the.

This is the UI deployment and this is the UI ingress and additional other things also present.

This is the UI uh service and UI service account.

So that lot of things available here we are focusing on ingress and then deployment and then service.

So these are the things we are going to use now.

So let's go back here and go ahead and then deploy our UI service now.

So we'll go here and run the command helm install UI and then provide the UI chart and version and hyphen

F values.

Ensure that you are in retail store hyphen apps folder where your values UI dot YAML is present.

So let's wait for this helm chart to get installed or deployed.

Now we can see our helm UI chart is deployed so we can run the command helm list.

And it should see.

Now it should show all our five things cart catalog, checkout orders and UI.

Everything is deployed now so we can also run the helm command, helm status, UI, hyphen, hyphen

show resources to see what all it created.

Okay, now you should see that you have the deployment and service and ingress.

So all those things see service account config map cluster IP service deployment and UI related deployment

pod and also ingress service.

So everything got created.

So before deploying this UI right.

So before deploying this UI you need to ensure that you have your load balancer controller installed.

So get pods, hyphen n and then kube system.

Okay, so you should see that AWS load balancer controller is installed.

So if you go here so this is installed.

So if you go back here that is which section.

So it is your section 11.

So your load balancer 1101 if it is installed or not.

If it is not installed ingress service will not get created and these things will not come.

So this address value will not come.

And even if you go to this one also right.

If you go to EC2, also your load balancer will not get created.

So that's very important.

You need to ensure that your load balancer controller is installed.

So let's go to Load balancers here.

And you can see here it is getting created now.

Now if you want we can run the command cube CTL get pods And you should have your UI pod orders.

Pod.

Check out Pod Catalog pod and then cart carts pod.

Okay, so those are the default applications UI, microservice orders, microservice checkout microservice.

And then this is your catalog and this is your carts and underlying persistent things.

This is DynamoDB.

This is MySQL.

This is reddish.

This is Postgres and this is RabbitMQ.

So all are running now.

So we don't have any issues with them.

Now the next thing is kubectl get ingress right.

And you should see your load balancer is created or not status of it.

You can see it here and it is created.

And if you go back here you are also using IP okay target type IP which we have learned when we are

learning the 11 zero two section means like uh about Target type IP and then instance.

So when you use the IP it will create a cluster.

IP service means like you need to create a cluster IP service and that you directly.

You will get the pod information in your ingress service, which means if you go to your target groups

here, your UI pod will be added here directly.

Okay, instead of node, port service and all the nodes.

So now you can see it here.

Ten zero 12 239 okay.

So if I say cube CTL get pods hyphen o white.

And for the UI pod right.

So you can see ui pod ten zero 12 239 in our ingress service target groups.

You see that now.

And another thing to see here is so if you go back here right where is it.

So this is the one.

So you have seen the ingress service right.

And from ingress service it is pointing to.

So I'll change the color here from ingress Service it is pointing to UI application and from UI application,

right.

So if you see the config map of this UI cube CTL get cm, hyphen, oh get cm and ui and hyphen oh,

and then YAML ui config map when we see it has all the endpoints card catalog, checkout and then orders.

So which means we are pointing from UI to catalog cart checkout and then order.

So all this is pointing okay.

But one thing is as your load balancer is active now right cube CTL get ingress and take the address

information here DLB, alb, dns name.

You can say ah Kubernetes ingress service address and go to the browser and then paste that okay.

And your application should be accessible.

And in meantime you can also say topology right.

And this is the important one.

Now if you see it here catalog and you can see its end point is healthy cards and its end point is healthy

and catalog its persistent provider is MySQL and its database end point is catalog MySQL 3306.

And for cards also you can see end point and persistent provider is DynamoDB and DynamoDB.

Table is items and this is the end point.

So all these are your cluster IP services okay.

And this is your checkout service.

And then this is the persistent provider Redis.

And this is the Reddis database end point.

And this is the Postgres order service which is healthy.

And this is the order service end point.

And this is its PostgreSQL persistent provider and its database end point and messaging provider RabbitMQ

and its database end point.

Now if you access your application also right.

So it is present here.

So the all these things, whatever you are seeing when you click on explore and all these things, these

are coming from your catalog application.

So the one which is running here in this catalog service right at this end point and pointing to this

database from there these information is displayed.

And when you click on Add to Cart then the request is going to this one okay carts okay.

And then from there when you click on checkout process right.

So when you click on this checkout process the request will go to this checkout service.

You can see it here right.

So this is the checkout service.

And after that when you click on like this continue.

And when you click on purchase the request will go to the order service and order will get generated.

And the information the order information will be present inside this database.

This completes the demo about.

This completes the demo about retail store application with persistent data plane running on AWS EKS

cluster, which is 1205.

Now we can go ahead and then undeploy these things.

Okay, so let's go to our retail store here apps.

And here you can go here and click on uninstall.

And then all these things will be uninstalled.

Or if you want you can run these commands manually and also uninstall which is uninstall retail apps

you have here right.

Helm uninstall UI helm uninstall orders.

So like this.

You can even uninstall them manually.

And this script is for installing it okay.

So we can see here all helm installs Uninstalls completed.

And if we run the command helm list, we should see that nothing is present related to helm releases

in our default namespace.

So this completes our demo.

Let's also see how the install script is working okay.

So we'll install the retail apps using this respective thing.

Okay.

So which means like we also have means like to install all these things.

We have written the script so that first it will install the catalog service sleep for 10s.

Then it will install the cart service and then sleep for 10s.

Like this checkout orders and then UI service.

And after that we can go ahead and also verify whether after creating the ingress and if the ingress

is in active state here, then we can go ahead and then verify our application also.

So all those things we can do that after this complete thing okay.

So we are just testing it okay.

So whatever the install script we have whether it is working or not.

So it's a quick test.

Another two minutes.

We can see here all helm installs are completed.

So if we run the command helm list you can see all those helm releases installed using script.

And if we want we can also run the command cube, CTL get pods and all the application pods should be

running now.

So which is a cart and then catalog checkout orders etc..

We'll also verify SVC which are all our cluster IP services.

And we will also verify get and our ingress address information should be present here.

And if we go back here and then refresh our ingress service should be in the provisioning state.

Once it is active we can go ahead and then access our application.

So we just now installed everything via simple script okay.

Earlier we learned one after the other.

Each value custom values, dot YAML everything.

Now we are testing it via script also.

And in addition to that when accessing your application right.

So you can also verify your application logs okay.

So like catalog microservice log checkout microservice log orders, service logs or logs, hyphen f,

and these are only single parts.

You can directly say deploy slash orders or you can directly.

Now if you want, you can also give the pod name.

So this is your catalog pod like this.

You can have your checkout pod.

So this is your checkout pod I'm just showing it for simply checkout pod here.

And these are the things okay.

So it has generated some log here for the checkout part.

And once this is ready right.

Once it is provisioned we can see the logs there okay.

When we are accessing the checkout service you can see here our load balancer is active now.

And you can select this okay DNS name from here.

Or you can go here and run the command cube CTL.

Get ingress and copy the address from here okay.

So this is the address.

So let me copy and go here and access it via browser.

And once our application is accessible, when we click on that checkout service after adding to cart

and going next.

So this is your application when I click on add and now see right.

So far no logs.

It is in the cart one product start equipment requisition and see it's called the checkout service.

And after that when I click on continue see another time it has called the checkout service.

And now we will again click on continue.

And this is another time it has called the Checkout service.

And in the same line if you want you can also verify kubectl get pods.

And you can also see the order service logs.

Also kubectl logs hyphen f.

And then this is the order spot.

And let's see what happens here.

Anything comes or not okay.

So this is the thing.

Now when I click on purchase right it should call the order service.

Checkout will call the order service and see.

Now you got some information here.

Okay.

So attempting to connect and then this is your something RabbitMQ.

And it is trying to put this respective order also in your messaging service.

In the messaging service it has put there.

Okay.

So that's about the whole process.

Okay.

So you can also view your catalog log and all those things also.

And I have also added something here like for few services.

Right.

Uh default logging is like and means like not present.

So if you want to enable extensive logging also for CART microservice and for UI microservice, uh,

you can try those things.

Those are spring boot related applications.

So you can enable them.

So for that purpose also I have added additional steps not needed.

It's not that we need to go and then understand the entire application.

If we understand how to install this application via helm that's more than enough okay.

But if you want to go deep and then debug your application and understand more, you can also try all

these options.

So now let's go ahead and then uninstall whatever we have installed.

Okay.

So we'll come back here and we'll say uninstall retail apps.

So this completes our helm demo wherein we have discussed in in detail about our retail store application

with persistent data plane running on EKS cluster.

Okay.

Persistent data plane running on EKS cluster.

So in 14 02I remember right.

So there will be another demo wherein in 14 zero two we will focus on persistent data plane.

Right.

So this persistent data plane is going to be this one okay.

RDS, PostgreSQL, Elastic Cache, DynamoDB and then RDS MySQL.

So I'll see you in the next demo.

Until then bye bye.

Thank you.


All right.

In previous lecture, we learned about orders microservice and deployed the orders helm chart.

Now we are also going to deploy the UI helm chart, wherein it will install the UI application and also

create the ingress service for us.

And in addition to that, in this UI custom values, we will also point that UI application to all these

microservices which are called endpoints.

So for all these endpoints, the configuration will be updated inside this UI application.

Custom values dot YAML.

So let's go ahead and then review that now.

So we can see it here.

This is the values ui dot YAML.

And here app dot theme.

We have given green and in endpoints we are giving catalog dot catalog dot default cluster and cart

hyphen cast default SVC cluster local eight, and another one is checkout and another one is order.

So if you go to the terminal here and if you say kubectl get svc right, so you can see the cart service

cart hyphen carts.

So this is the one.

And for catalog this is the catalog service.

You can see catalog here okay.

And in the same way for checkout checkout service.

And you can see that here checkout okay.

In the same way orders.

Also you can see it here.

So this is the default one okay.

In default namespace you are pointing to the service.

And this is the cluster domain cluster dot local.

So these are the endpoints we have defined.

So if you go to the values dot YAML and default values dot YAML.

And if you go here in app dot endpoints all these things were commented.

And you have given a empty map here.

Empty map means it will use the in-memory service which means the entire application, whatever.

This entire retail store application, when you don't provide any endpoints, it will take it from means

like the pre-built static application, it will take into consideration complete in-memory application.

So that's what we have tested in our 12 zero to helm custom values.

So here you didn't define any any endpoints.

So it took the default application which is in-memory application.

And everything is going to work all the services and everything.

But that is a complete static version.

Now here we are deploying it as a uh, multiple endpoints, multiple applications, multiple pods.

So here in single pod itself, everything works in 1202.

But here you are going to have separate cart application, separate catalog microservice, separate

checkout and separate orders in addition to separate microservices underlying their their persistent

data plane also split, all running separately, and you are pointing from your UI application to these

microservices.

Okay, so let's do that now in values UI.

This is the part we have completed and this we have already seen earlier.

Also, it is going to create an internet facing ingress load balancer which is nothing but our application

load balancer in AWS.

So if you want you can also review your default values dot YAML for your UI chart.

And there are additional settings also available inside that.

So if you want chart to be enabled then you need to do these things means like provide your information

related to OpenAPI based URL or bedrock related region.

All those information.

These are additional advanced things which are not related to our Kubernetes, its features in your

retail store application so we can ignore them.

Our key thing is app dot theme and app dot Plot end points.

So those two things anyway we have defined here app dot theme is green and app dot end points are these

things.

Now if you want to review the templates also you can go here and then review them.

You will have the config map and if values dot Configmap create it will create this.

And here you have all the information and you have the.

This is the UI deployment and this is the UI ingress and additional other things also present.

This is the UI uh service and UI service account.

So that lot of things available here we are focusing on ingress and then deployment and then service.

So these are the things we are going to use now.

So let's go back here and go ahead and then deploy our UI service now.

So we'll go here and run the command helm install UI and then provide the UI chart and version and hyphen

F values.

Ensure that you are in retail store hyphen apps folder where your values UI dot YAML is present.

So let's wait for this helm chart to get installed or deployed.

Now we can see our helm UI chart is deployed so we can run the command helm list.

And it should see.

Now it should show all our five things cart catalog, checkout orders and UI.

Everything is deployed now so we can also run the helm command, helm status, UI, hyphen, hyphen

show resources to see what all it created.

Okay, now you should see that you have the deployment and service and ingress.

So all those things see service account config map cluster IP service deployment and UI related deployment

pod and also ingress service.

So everything got created.

So before deploying this UI right.

So before deploying this UI you need to ensure that you have your load balancer controller installed.

So get pods, hyphen n and then kube system.

Okay, so you should see that AWS load balancer controller is installed.

So if you go here so this is installed.

So if you go back here that is which section.

So it is your section 11.

So your load balancer 1101 if it is installed or not.

If it is not installed ingress service will not get created and these things will not come.

So this address value will not come.

And even if you go to this one also right.

If you go to EC2, also your load balancer will not get created.

So that's very important.

You need to ensure that your load balancer controller is installed.

So let's go to Load balancers here.

And you can see here it is getting created now.

Now if you want we can run the command cube CTL get pods And you should have your UI pod orders.

Pod.

Check out Pod Catalog pod and then cart carts pod.

Okay, so those are the default applications UI, microservice orders, microservice checkout microservice.

And then this is your catalog and this is your carts and underlying persistent things.

This is DynamoDB.

This is MySQL.

This is reddish.

This is Postgres and this is RabbitMQ.

So all are running now.

So we don't have any issues with them.

Now the next thing is kubectl get ingress right.

And you should see your load balancer is created or not status of it.

You can see it here and it is created.

And if you go back here you are also using IP okay target type IP which we have learned when we are

learning the 11 zero two section means like uh about Target type IP and then instance.

So when you use the IP it will create a cluster.

IP service means like you need to create a cluster IP service and that you directly.

You will get the pod information in your ingress service, which means if you go to your target groups

here, your UI pod will be added here directly.

Okay, instead of node, port service and all the nodes.

So now you can see it here.

Ten zero 12 239 okay.

So if I say cube CTL get pods hyphen o white.

And for the UI pod right.

So you can see ui pod ten zero 12 239 in our ingress service target groups.

You see that now.

And another thing to see here is so if you go back here right where is it.

So this is the one.

So you have seen the ingress service right.

And from ingress service it is pointing to.

So I'll change the color here from ingress Service it is pointing to UI application and from UI application,

right.

So if you see the config map of this UI cube CTL get cm, hyphen, oh get cm and ui and hyphen oh,

and then YAML ui config map when we see it has all the endpoints card catalog, checkout and then orders.

So which means we are pointing from UI to catalog cart checkout and then order.

So all this is pointing okay.

But one thing is as your load balancer is active now right cube CTL get ingress and take the address

information here DLB, alb, dns name.

You can say ah Kubernetes ingress service address and go to the browser and then paste that okay.

And your application should be accessible.

And in meantime you can also say topology right.

And this is the important one.

Now if you see it here catalog and you can see its end point is healthy cards and its end point is healthy

and catalog its persistent provider is MySQL and its database end point is catalog MySQL 3306.

And for cards also you can see end point and persistent provider is DynamoDB and DynamoDB.

Table is items and this is the end point.

So all these are your cluster IP services okay.

And this is your checkout service.

And then this is the persistent provider Redis.

And this is the Reddis database end point.

And this is the Postgres order service which is healthy.

And this is the order service end point.

And this is its PostgreSQL persistent provider and its database end point and messaging provider RabbitMQ

and its database end point.

Now if you access your application also right.

So it is present here.

So the all these things, whatever you are seeing when you click on explore and all these things, these

are coming from your catalog application.

So the one which is running here in this catalog service right at this end point and pointing to this

database from there these information is displayed.

And when you click on Add to Cart then the request is going to this one okay carts okay.

And then from there when you click on checkout process right.

So when you click on this checkout process the request will go to this checkout service.

You can see it here right.

So this is the checkout service.

And after that when you click on like this continue.

And when you click on purchase the request will go to the order service and order will get generated.

And the information the order information will be present inside this database.

This completes the demo about.

This completes the demo about retail store application with persistent data plane running on AWS EKS

cluster, which is 1205.

Now we can go ahead and then undeploy these things.

Okay, so let's go to our retail store here apps.

And here you can go here and click on uninstall.

And then all these things will be uninstalled.

Or if you want you can run these commands manually and also uninstall which is uninstall retail apps

you have here right.

Helm uninstall UI helm uninstall orders.

So like this.

You can even uninstall them manually.

And this script is for installing it okay.

So we can see here all helm installs Uninstalls completed.

And if we run the command helm list, we should see that nothing is present related to helm releases

in our default namespace.

So this completes our demo.

Let's also see how the install script is working okay.

So we'll install the retail apps using this respective thing.

Okay.

So which means like we also have means like to install all these things.

We have written the script so that first it will install the catalog service sleep for 10s.

Then it will install the cart service and then sleep for 10s.

Like this checkout orders and then UI service.

And after that we can go ahead and also verify whether after creating the ingress and if the ingress

is in active state here, then we can go ahead and then verify our application also.

So all those things we can do that after this complete thing okay.

So we are just testing it okay.

So whatever the install script we have whether it is working or not.

So it's a quick test.

Another two minutes.

We can see here all helm installs are completed.

So if we run the command helm list you can see all those helm releases installed using script.

And if we want we can also run the command cube, CTL get pods and all the application pods should be

running now.

So which is a cart and then catalog checkout orders etc..

We'll also verify SVC which are all our cluster IP services.

And we will also verify get and our ingress address information should be present here.

And if we go back here and then refresh our ingress service should be in the provisioning state.

Once it is active we can go ahead and then access our application.

So we just now installed everything via simple script okay.

Earlier we learned one after the other.

Each value custom values, dot YAML everything.

Now we are testing it via script also.

And in addition to that when accessing your application right.

So you can also verify your application logs okay.

So like catalog microservice log checkout microservice log orders, service logs or logs, hyphen f,

and these are only single parts.

You can directly say deploy slash orders or you can directly.

Now if you want, you can also give the pod name.

So this is your catalog pod like this.

You can have your checkout pod.

So this is your checkout pod I'm just showing it for simply checkout pod here.

And these are the things okay.

So it has generated some log here for the checkout part.

And once this is ready right.

Once it is provisioned we can see the logs there okay.

When we are accessing the checkout service you can see here our load balancer is active now.

And you can select this okay DNS name from here.

Or you can go here and run the command cube CTL.

Get ingress and copy the address from here okay.

So this is the address.

So let me copy and go here and access it via browser.

And once our application is accessible, when we click on that checkout service after adding to cart

and going next.

So this is your application when I click on add and now see right.

So far no logs.

It is in the cart one product start equipment requisition and see it's called the checkout service.

And after that when I click on continue see another time it has called the checkout service.

And now we will again click on continue.

And this is another time it has called the Checkout service.

And in the same line if you want you can also verify kubectl get pods.

And you can also see the order service logs.

Also kubectl logs hyphen f.

And then this is the order spot.

And let's see what happens here.

Anything comes or not okay.

So this is the thing.

Now when I click on purchase right it should call the order service.

Checkout will call the order service and see.

Now you got some information here.

Okay.

So attempting to connect and then this is your something RabbitMQ.

And it is trying to put this respective order also in your messaging service.

In the messaging service it has put there.

Okay.

So that's about the whole process.

Okay.

So you can also view your catalog log and all those things also.

And I have also added something here like for few services.

Right.

Uh default logging is like and means like not present.

So if you want to enable extensive logging also for CART microservice and for UI microservice, uh,

you can try those things.

Those are spring boot related applications.

So you can enable them.

So for that purpose also I have added additional steps not needed.

It's not that we need to go and then understand the entire application.

If we understand how to install this application via helm that's more than enough okay.

But if you want to go deep and then debug your application and understand more, you can also try all

these options.

So now let's go ahead and then uninstall whatever we have installed.

Okay.

So we'll come back here and we'll say uninstall retail apps.

So this completes our helm demo wherein we have discussed in in detail about our retail store application

with persistent data plane running on EKS cluster.

Okay.

Persistent data plane running on EKS cluster.

So in 14 02I remember right.

So there will be another demo wherein in 14 zero two we will focus on persistent data plane.

Right.

So this persistent data plane is going to be this one okay.

RDS, PostgreSQL, Elastic Cache, DynamoDB and then RDS MySQL.

So I'll see you in the next demo.

Until then bye bye.

Thank you.


Welcome back.

As part of section zero seven we have created the.

Plus x everything via Terraform.

Right.

So this whole architecture, whatever we have built on section zero seven we slowly improvised that.

Right.

So we improvised that by manual provisioning the following things like as part of section 0902 we have

installed the pod identity agent.

After that, in 0903 we have installed the secrets store CSI driver using helm, and also AWS secrets

and Configuration provider also using helm.

So these things we have installed using her okay.

And all these things manually by running the helm commands.

Right.

And from there on we moved to the next section which is section ten wherein we have installed the EBS

CSI driver add on, which is a ECS add on, and that also we have done manually only.

So we have done manual provisioning, only using AWS commands, etc. etc..

Okay, even the IAM roles.

Whatever is required.

Everything we created manually using AWS CLI commands.

Okay.

Now that's about ebs csi driver.

Then we moved on to also installing the AWS Load Balancer controller.

Even this we have installed using helm.

And it's before that we created the IAM policy role.

And also x P I pod identity agent Association and Helm install.

So these four things we have done again using manual only right.

So now all these things when you see manually, that is fine.

Okay, so till section 12 when we are learning about helm, that is fine.

So we might go to the advanced sections of this course.

And at that point of time.

So today you have learned helm okay.

And tomorrow you want to learn some other concept.

So let's go here as part of section 1401 and 1402.

We are planning to have the persistent data plane using AWS cloud, which means you are going to install

all these things, right.

So in addition to that, to test that you also need the ingress service also.

So we need to deploy the ingress.

And till section 12 you might have done learning and implementation and then you have deleted the cluster.

So let's assume that way okay.

So you have deleted the cluster.

Now what happens.

And the next day you came in and at at that point of section 14 you want to learn something about the

data plane and section 15, section 16 etc. you want to learn.

So if you want to create a cluster, then you need to execute demo zero seven.

And if you want the load balancer controller then you need to execute 11 zero one.

And if you want the secret store CSI driver then you want to execute 0903.

So like this you want to do all those demos.

And then only then you come to section 14 demo.

So like this it becomes typical means like again in section 15 you are learning something okay.

And there also you need ingress load balancer and that you are planning to do on some other day.

Even that becomes learning becomes tough here.

And in addition to that, learning how to automate these things using Terraform right with a single

click Terraform apply auto approve and then you have your EKS cluster.

And in addition to that you have your node group created.

And after that you have your secrets store CSI driver create installed.

Right.

And you have your EBS CSI installed and you have your LBC installed just with Terraform apply or to

approve.

So this is super powerful.

So that's the reason we are automating all these things via Terraform.

So there are two reasons we said okay.

One is our learning becomes easy.

Second is it's very important to learn right.

Which means like it's powerful.

Okay.

So you are you are building almost the production grade cluster with Terraform in a by writing your

own code.

Okay.

Not that something like so if you, if you, if you go to Terraform modules right you will find your

module.

And this module almost will do everything okay.

Everything means like lot of features will be available inside that.

But that code, if we go and then review it is so tough.

One thing.

Okay.

And second, it changes frequently.

Changes frequently.

Why it changes.

Because it is open source and we are not building from scratch.

It might have in-house bugs.

Okay.

And if you use in your prod grade clusters.

Right.

Out of 100%, 99% chances, it will have outage on some day during the upgrade process or something.

Something because that code is not your code.

Okay.

And it changes frequently.

Why?

Because every time new features coming, they keep updating.

So all those things happens and you end up in a problem there.

So for that purpose, writing everything from the scratch.

And then if you want to change these things to your own Terraform modules and then call them, you can

always do that.

So when we are learning about Terraform, we already converted our VPC code to a custom Terraform module

in the same line.

Whatever you want, you can implement them also.

So that said, now in section 13 we are going to automate all these things with a means like we are

going to automate all these things so that when we run this section 13 related Terraform manifests,

all these things are available in our ECS cluster out of the box.

Okay.

So let's go ahead and then practically implement it.

And before implementing it we also need to know one thing, which is we can go to our zero seven ECS

cluster Terraform ECS cluster and go to zero eight ECS, terraform manifest and then destroy your cluster.

Whatever you have created means like Terraform, destroy auto approve and you can go to zero one VPC,

Terraform, manifest and destroy whatever you have created.

So no references of VPC and then ECS related things after the section 12 demos.

Why?

Because in section 13 we are going to create freshly the VPC and then ECS cluster with all the add ons

so far whatever we have learned.

So now if you want you can go here and go to zero seven right zero seven ECS cluster and go to zero

to ex Terraform manifest and run the command Terraform destroy auto approve.

So once your ECS cluster and node group is deleted, you can also go to zero one right.

And then also run the command Terraform destroy auto approve to remove the VPC also.

So these things completed before starting section 13.

So that's very important.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, let's get into the section 13 now which is Terraform EKS cluster with add ons.

So there are no changes to the zero one VPC Terraform manifest.

But in zero to x terraform manifest we have added additional files here tf files to install all the

add ons.

So let's look at them okay.

So if we go down here right.

So earlier we have the zero one VPC terraform manifest which is same as our section seven.

No changes in zero one VPC.

But if we go to zero to x terraform manifest we have the file still see ten okay.

But now if you come to our section 13 we have something from C7 to C16.

So here from C11 to C16 we are going to install the EKS related add ons okay.

So now if you come back here as part of C11, C12 and then C13, we will install the EKS pod identity

agent add on and also define the Helm and Kubernetes providers for subsequent add ons, and also define

a common IAM role policy for pod identity, right?

So that's about 11 to 12 and then 13.

And in C-14 we will install the load balancer controller.

And in C15 we will install the Amazon EBS, CSI driver ECS add on.

And as part of C-16 we will focus on installing the secret store CSI driver.

So now let's go ahead and then start with C11 pod identity agent X add on.

So we'll go here and the code is pre-written.

And then I'm going to explain them in detail.

So earlier if we have seen when we are learning about Terraform right.

So from here zero six and then zero seven.

So we have learned lots of Terraform concepts in detail.

So we really don't need to go and then do a live template writing for these things.

So we need to understand why that resource is used.

And then how that resource is populated means like the values for that resource, how the data is populated,

we are going to review.

So that clears most of the things.

So if you have completed our zero six and then zero seven sections which are written in Terraform itself,

then this will be easy for you to follow.

So we are in C11 pod identity agent X add on.

So let's walk through how we deploy the ECS pod identity agent as an ECS add on using Terraform.

So we start with two data sources here okay.

So both of type AWS ECS add on version.

And the first one is called Pia default, which fetches the default add on version that's compatible

with our clusters Kubernetes version.

So it will fetch the default version of ECS pod identity agent, which matches with our Kubernetes version.

And the second one is Pia latest, which includes most recent true that makes Terraform automatically

grab the latest available version supported by our EKS cluster.

So that's about the data sources we are going to define.

And once we have that info we move on to the add on resource itself which is AWS EKS add on.

And then here the local name for this resource in Terraform we are going to put it as pod identity.

So here we add a dependency on our EKS node group just to make sure the cluster nodes are up and running

before we install this pod identity agent.

Because this pod identity agent creates the pods, and those pods need to run on some EKS worker nodes.

So if you are EKS node group is up and running, then only you can deploy this pod identity agent so

that those pods can run on those respective nodes.

Okay.

So the add on name we are going to give it as EKS pod identity agent.

So this is the ECS pod identity agent which we are going to install.

So it's not a desired name.

That is the name we need to provide.

Exactly.

And we plug in the versions dynamically from the latest whatever we have defined here Pia latest.

So that is the one add on version.

We are going to take the latest available version supported for our x x cluster version.

And to make deployment more reliable right we use these two settings resolve conflicts on create is

overwrite and resolve.

Conflicts on update is also overwrite.

So that simply tells Terraform if there's already an older or conflicting configuration, just replace

it cleanly with the latest one.

So we add these things.

And now finally we also output the values which are default add on versions, and also latest add on

versions, and Arn and also ID.

So that's about installing the EKS add on pod identity agent.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


All right.

As part of C 12 Helm and Kubernetes providers, we will define our helm and then Kubernetes providers

here.

So we will start with the EKS cluster auth data source here.

So here we are using the data AWS eks cluster auth to fetch a temporary authentication token for our

EKS cluster.

So using the cluster name.

So we are just providing our cluster name.

And this data source will fetch that temporary authentication token for us.

And now this token acts like a short lived Kubeconfig credential.

And it's automatically refreshed by Terraform whenever needed.

Next we configure our helm provider block okay.

So in this helm provider block so this is the provider and helm we will have a Kubernetes block inside

that Kubernetes section.

So that includes three key pieces.

Okay.

So one is cluster endpoint.

So what is our cluster endpoint.

We can get that from our AWS eks cluster main and then endpoint.

So if you go to our C ten x outputs so here you have the cluster endpoint.

So you can get that from there.

And the second is cluster key a certificate.

So that also you can get from our cluster information.

So you can see the cluster C certificate also defined as the output here okay.

So this is the one we are trying to get it and send it to the helm provider okay.

And when we are providing that we are also doing a base64 decode of that respective certificate.

Okay.

And in addition to that we are also going to get the token from here.

Data dot aws eks cluster auth cluster dot token.

So this token acts like a short lived kubeconfig credentials.

Okay, so these three together.

So whatever we have defined host cluster see a certificate and token.

So these three together allow Terraform to connect securely to the ECS control plane and manage helm

releases directly inside the cluster.

So which means when you run the commands helm install, helm upgrade, helm list, whatever the helm

commands, you are running from your CLI wherever you have installed.

Right?

So those things only if you configure your kubeconfig it is going to work.

So when we have run that on our local terminal.

So when we run the command helm list are these things your kubeconfig should be configured.

So that's the thing.

So currently when I run the helm list there is no kube configuration in my terminal.

So it is failing okay.

In the same line if you don't configure this provider and then go ahead and then install Helm release

using Terraform, that will not go and then deploy in your EKS cluster.

So to deploy that respective helm release whatever you are going to define here in next sections like

example, load balancer, controller, helm, release.

This is one thing.

So if you want to deploy this helm release means like if you want to run the it's equivalent to helm

install or Helm upgrade command.

So if you want to run that then definitely you need to define this so that helm can go ahead and then

connect to your EKS cluster using these information and then deploy the helm releases.

So that's very important here.

And next thing is we have also defined the Kubernetes provider here.

So using the same connection details host cluster CI certificate and token.

So now even though in these configurations we didn't create any of the Kubernetes resources as Terraform

configs.

So what that means actually Kubernetes resources as Terraform configs means.

So earlier when you define your Kubernetes resource.

So if you go here.

So if you go anywhere here to the top okay.

So if you go to any of these things Kubernetes ingress okay Https.

And this is let's say like this config map.

This is a Kubernetes resource defined in YAML format.

But if you want to define that in Terraform format.

So if you go here to the browser and say Kubernetes config map Terraform.

Right.

And then it will give you the Terraform resource of that.

Okay.

So this is the example usage.

So this is the config map you can define in Terraform.

Also not only in YAML you can define a config map in Terraform language also.

But to deploy this to your cluster, you need this provider.

So for helm releases you need helm provider.

And for Kubernetes resources of Terraform means like of defined in Terraform language you need Kubernetes

provider.

So in these things we really didn't use any of the things related to Kubernetes provider means like

raw Kubernetes resources.

We didn't define anything.

But usually I want to tell you that you can define means like if you want to write your Kubernetes configs

in Terraform language, you need this to specify that for you.

I have defined that here for us till here if you define that is sufficient.

So that's about Helm and Kubernetes providers.

And we also have something called pod identity.

Assume role in C 13 okay.

So let's understand this in detail now.

So let's define the AWS IAM Policy document that allows our exports to assume an Im role through DoD

identity.

So this one, you will have the default data source available and that data source with the content

inside that we have defined.

So if you go and then search for this okay.

So if you search for this right so you will get that respective resource see data source AWS IAM policy

document.

So this is the basic one okay.

So example usage.

So here you are defining whatever is required for us.

This is nothing but our trust policy whatever we have defined in our previous demos.

So if you go to 11 zero one so you can see the trust policy here, this is the trust policy you have

defined.

Or if you go to storage also.

Right.

So you in CSI driver install, you have defined the trust policy.

Or even if you go to secrets uh, in 0903.

Also you can find the trust policy.

So that trust policy we are defining here in Terraform.

That's it.

Nothing more than that.

Okay.

So that's what we are defining.

So whenever we have we need to attach this trust policy to any IAM role.

We will call this data source and then attach data dot AWS IAM policy document dot dot dot JSON okay.

So like this.

You can see it here in any of the policy.

If you want to reference that you can reference that as a JSON.

Okay.

Even we are going to reference that here in our IAM policy and role.

And you can see that here right in IAM role like IAM role data dot AWS IAM policy document dot assume

role dot JSON.

So like this we are going to reference this respective trust policy when we are creating the IAM roles

for our each add on LBC or EBC, CSI or secret store wherever it is required.

So that is about pod identity.

Assume role.

So I'll see you in the next lecture.

Until then, bye bye.

Thank you.


All right.

In this section we are going to deploy the AWS Load Balancer controller.

The key component that connects your Kubernetes services and ingress resources with AWS application

load balancers and network load balancers.

So it runs inside the cluster and automatically provisions Albs and Nlbs.

Whenever your Kubernetes workloads request them.

And this time we will set it up entirely using Terraform with proper IAM roles, Pod identity associations

and also helm installation.

So all in one go.

So whatever we have manually implemented as part of 1101 load balancer controller install.

So whatever we have done there now in these files okay C14 012 C14 zero four we are going to automate

that.

So let's start with the C14 zero one.

Here we are using a Terraform HTTP data source to fetch the latest IAM policy JSON directly from the

official load balancer controller GitHub repository.

So if you see the IAM policy, whatever.

If you try to access this in browser, right.

So you will get the IAM policy permission means like the permissions whatever defined for load balancer

controller.

Everything will be present inside that from our AWS Load Balancer controller GitHub repository.

Always we get the latest version.

So this one using HTTP data source in Terraform we are getting it okay.

So this makes sure means like whatever the approach we are following right.

This make sure we always use the most recent and recommended permissions.

So no manual JSON file.

No version drift will be there when we follow this approach.

So we are creating a data source with LBC.

IAM policy related information.

So that's about C14 zero one LBC, IAM policy data source.

And we are setting that request header is going to be JSON.

And if you output this the entire policy will be displayed when you run the Terraform output after apply.

But it is a very big thing.

I have commented it, but when you want to troubleshoot it and if something is not working and if you

want to see the entire policy, then you can uncomment and then uh, see it in the Terraform outputs.

Okay.

So once you fetch this IAM policy we go to creating the LBC related IAM policy and IAM roles okay.

So we define the LBC IAM policy here okay.

So this is the LBC IAM policy wherein we are getting the data dot http dot lbc IAM policy dot response

body.

So if you go here this is the one.

So the response body whatever you are getting here.

So this what this entire content whatever you have seen in the browser.

Right.

So this one you can get it using this data source HTTP with dot response body so that you are passing

it to the LBC IAM policy.

So you create the IAM policy here with all those permissions, whatever you are getting using this HTTP

data source.

And once you create that LBC IAM policy.

Right.

So you go and then create a IAM role.

And when you are creating the IAM role right.

So you also need to provide your assume role policy.

So here comes from our earlier pod identity document.

That one that allows the experts to assume this Im role through the Pod identity agent.

So if you go here so this is the one data dot aws IAM policy document dot Assumerole dot JSON.

So that's what you are providing.

So the trust policy for your IAM role.

LBC IAM role.

You are providing it here.

And these are the tags.

So you have created a IAM role with the trust policy.

And you have also created a IAM policy.

Now you need to attach your IAM policy.

Whatever you have created here and the IAM role this one.

So for that you will use the resource aws IAM Policy attachment.

So you will provide your policy Arn and your role name and then you will attach them.

So that's about IAM policy IAM role and trust policy for your load balancer controller.

So that part is completed.

Now we will move on to the next very very important thing which is pod identity association.

So you have created your IAM role right.

And now you need to implement the Pod Identity Association.

So this links our EKS cluster, the AWS Load Balancer Controller Service account inside the kube system

namespace, and the IAM role we just defined.

So with this association in place, the controller pod, the AWS Load Balancer controller pod automatically

assumes the IAM role and gains the exact AWS permissions it needs, whatever defined in the IAM policy.

Okay, so now if you see here resource aws eks pod Identity Association.

This is for our LBC.

And we provide our cluster name here.

And in kube system namespace only we install our load balancer controller.

And our load balancer controller name is going to be AWS Load Balancer Hyphen control.

So this is a standard name we have provided for AWS Load Balancer controller.

So you have the namespace service account and the role Arn which we have created in our C14 zero two.

So this is the role Arn.

We have provided here in Pod identity agent.

So once the Pod Identity Association is created, if you want you can output that identity association

a.

And finally we move to the helm release for load balancer controller.

Okay.

So here the most important thing is depends on meta argument.

We discussed this when we are learning the Terraform basics.

Okay.

So this helm release need to be deployed only after these things were completed.

So for example this LBC IAM role is created only after that deploy this helm release resource.

And in addition to that you should also have your Kubernetes node group, private node group is created

and ready and your pod identity association is completed.

And also your pod identity agent is installed.

So all these things, we are putting it as a dependency so that this helm release, whatever it deploys

the AWS load balancer controller.

So that will work effectively after only these things are available.

Okay.

So if not it might go into a stale release also.

So for that purpose we need to ensure and define.

In Terraform we have the option to define them okay in a sequential order.

So this helm release will be deployed.

Load balancer controller will be deployed or this resource will get created only after these things

were deployed.

Okay.

Or these things were present.

These resources were created and present in Terraform.

And name of this respective helm release is AWS Load Balancer controller.

And the charts.

These are the charts and this is the chart name repository is this one.

And this is the chart name.

This release name.

You can give anything okay.

Right.

Your choice okay.

You can give dot dollar flower brackets.

Open local dot name hyphen AWS load balancer controller.

Also you can give okay And in which namespace we are deploying it in kube system namespace.

Okay.

So here we are not specifying any version in production.

It is recommended to use the specific version you are going to baseline.

Okay.

So here we are using the latest version only.

And in addition to that we are setting weight equal to true weight for resources to become ready.

And then give it around 600 seconds timeout.

And also we are defining cleanup on fail true for a clean rollback if something goes wrong.

So all those things we have defined and inside the set block.

Inside the set block we configure all the helm parameters.

So if you go to the manual stuff also in 11 zero one right in the Readme, if you go here in the helm

install.

So we have defined lots of things here.

So if you go down here we have in helm install.

You set that you provide your cluster name region your VPC id Service account true.

And what is your service account name?

So everything you have defined okay in the same line.

So how do you define these set things.

So these set things in Terraform.

We are going to see it here now in C14 zero four.

So you will open this set equal to.

And here you have the list of items.

So all these are the each one is one list item.

So first is create service account via helm.

We are making it true and create service account name means like name of your service account.

You are providing it AWS load balancer controller.

And uh if you have any Irca annotation you can add it okay here.

Okay.

As we are using Pia, this annotation is not required.

IAM roles for service account is not required.

So I have removed that Irca is not needed.

We are using the pod identity agent and the next thing is cluster name.

We can get that using AWS eks cluster, dot main.id or main dot name also.

Right.

So we got the cluster name and we need the vpc id.

So vpc id we already have the definition here C3 remote state.

And here you can get the VPC ID in this project to using this data dot remote state Terraform remote

state dot VPC dot outputs dot vpc id.

So now let's come back here to Lbcl.

And you got your VPC ID also.

And in which region you already have the where dot AWS region.

So that's about your helm release how to define it okay.

And if you want you can also define the helm metadata.

Complete helm release dot load balancer controller dot metadata.

So you are complete metadata, how it is installed and whatever it will come.

So earlier when you are installing it, after installing the helm chart, it will show you it is deployed

and then so and so so and so right.

So all those things will be displayed using this output.

So that's about the load balancer controller related Terraform configs.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right.

Now let's go ahead and then discuss about C15 files, which is EBS, CSI driver installation which is

a ECS add on.

Okay.

So in we are going to set up the Amazon EBS CSI driver, a key add on that enables your Kubernetes workloads

to dynamically provision EBS volumes for your persistent storage.

So we handle this setup entirely through Terraform now in three stages.

Okay.

One is IAM role creation.

C15 zero one and Pod identity association creation which is C15 zero two.

And then also in C15 zero three we are going to install the EKS add on whose name is ebs csi driver.

Okay so let's start with the C15 zero one ebs, csi IAM policy and then a role for ebs CSI driver.

We will have a managed policies okay.

Which is Amazon EBS, CSI driver policy okay.

So by default we'll have that policy available.

So we don't need to create any policy object here.

So we can directly create a EBS, CSI IAM role and associate it with the Assumerole policy which is

our trust policy which we have seen earlier also.

Right, which we have defined in our C11 pod identity agent in in our C13 pod identity assume role.

Right.

So that is the one data dot aws IAM policy document, dot Assumerole dot JSON.

And then create a ebs, csi IAM role.

After that, attach the IAM role.

This is the IAM role and this is the policy Arn which is a service role policy by default available.

And then you are going to attach that.

So now this IAM role has the permissions of Amazon EBS CSI driver policy whatever defined inside that.

So that's about IAM role and IAM role policy attachment.

If you want you can output that IAM role.

And also.

And now the next thing is create the Pod identity association okay.

So this is your EBS csi pod identity association.

You provide your cluster name.

This EBS CSI driver will be installed in the kube system namespace.

So you provide that namespace.

And your service account is going to be ebs csi controller SCA.

So this is the default one which creates.

So we provide that service account name, Kubernetes service account name and role Arn.

We just now created here right.

So this is the one okay.

So where is it.

So this is the one aws iam role ebs iam role dot e.

So that you can provide it here.

So that's about pod identity association resource creation.

If you want you can define its association output also.

Okay, now the next thing is ebs csi x add on.

Okay.

So how we have done for pod identity agent.

In the same line we can also define the data source X add on version for Amazon AWS, ebs, csi driver.

Also, what is the default version available for our Kubernetes version means like CSI driver, default

version, EBS, CSI driver default version, and what is the most recent version?

We can define those data sources here and use whatever we want to use.

And here is the x add on ebs CSI.

And these are the this is the depends on meta argument.

We are saying that first IAM role should be created Pod identity association for EBS, CSI should be

completed and pod identity agent should be installed.

And also private means like node group, private node group also should be created and ready.

So this is a standard one so that you can deploy this EBS csi driver which creates the controller means

like EBS, csi controller, and also it will also create a Daemonset.

Also all these things uh, as part of EBS, CSI.

So for that purpose this depends on is mandated here.

Now the cluster name we can get it using this and add on name.

This is the standard default name okay AWS ebs CSI driver which we are installing and add on version.

We are using the CSI latest okay for this Kubernetes version and service account role Arn, which we

have defined here.

So this is the role Arn.

We are providing it here okay.

That is the role uh and resolve conflicts and create and update.

We are putting it as usual overwrite.

And these are the standard tags.

So when we deploy these three files your EBS CSI driver will be installed automatically using Terraform.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Alright as part of C-16 zero one and C-16 zero two, we are going to install the secret store CSI driver

and the AWS Secrets and Configuration Provider, both directly using helm.

So you might remember back in section 0903, we used the secrets store CSI driver to mount secrets as

volumes inside our pods, so that worked great.

But now we are going to explore the second method called Sync Secret.

So if you go to 0903.

So here we have installed our, uh, if you go to the helm install command for secrets store CSI driver

okay.

Right.

We just plainly installed this helm okay.

And then we went to the catalog integration and in the catalog kit as manifest.

Okay.

So if you go to this catalog deployment right.

So we are mounting the secrets here as volume.

So we mount it and then we extract them from the mount.

Okay.

And then set them as environment variables and then use it even in catalog deployment or even in catalog

statefulset.

See, you are mounting that secret store CSI driver secret provider class and then extracting the information

and then using it.

So that's what we have done earlier okay.

But now with Sync Secret enabled.

So what I am trying to tell you here is in 1601 in the parameters.

Right.

So in set parameters we are going to use this additional option.

And with that what happens is instead of mounting the secret files inside the container, the CSI driver

actually syncs your AWS Secrets Manager objects into your native Kubernetes secrets.

So that means your pods can use them.

Just like any Kubernetes secret.

No volume mounts, no file reads, and much simpler Configuration.

So that option also we are going to try as part of this means like so if you put this option here,

what happens is if you go to your 14 zero two and then retail store, and if you go to the microservices

and catalog deployment here, okay, right here you will not see any volume mounts here related to secrets

store CSI driver and secret provider class related information.

Here you just reference your secret here as a catalog hyphen DB.

So what happens is when you enable this setting.

So if you come back to our C 16 zero one, when you enable this sync secret dot enabled setting, it

is going to go ahead.

And then in catalog deployment it is going to it is going to create a catalog DB with your secret information.

What is your username and password.

And that secret, if you reference it here that's all that is sufficient.

So that configuration you are going to provide inside this respective secret provider class, saying

that you create a catalog DB secret with this key and then value from our username means like in your

AWS secret managers object and password.

Also, you get from our AWS Secrets Manager object and then set it as the password with this key name.

Okay.

So and then it will create a catalog DB secret for us.

So when we implement 14 zero two you will understand it in detail.

But I am just giving the high level overview here only okay.

So we tried two options.

So we have a greater satisfaction here.

Just if we go with only the mount option which we learned here in 0904, then there will be a missing

feeling.

We have other additional advanced options.

Why didn't we use it.

So for that purpose I have also used that in 1402.

When we go to that section, we will understand it in detail.

But here we are discussing this because why this setting Kalyan has put when doing this helm release.

So for that purpose, I am trying to explain that.

Okay, so now let's come back to this secret store CSI driver installation and here helm release.

And this is the secret store CSI driver.

And it depends on first pod identity agent should be installed and your private nodes also should be

installed.

Okay.

So that's one thing.

So this is optional for this okay.

Pod identity is not there.

Also.

That is fine for secret store CSI driver when we are installing here okay.

Why.

Because here we are not using any IAM roles or IAM policies as part of installation.

Those IAM roles and policies are needed only when your catalog pod or your orders pod need to connect

to this secret store, which means that will be part of your application and not part of your infrastructure.

Okay, so whenever you are installing load balancer controller, you have defined your IAM role and

IAM policy because that load balancer controller to run and access the AWS resources to create AWS Load

Balancer or something, you need that.

So for that purpose we have created here or for even the EBS, CSI also, but for Secrets store CSI,

this one is actually accessed by our applications.

Okay.

So means like our application need to get the secret, which means that application needs the service

account and IAM role and necessary permissions to get that information.

So that.

So here I am role policy.

All those things became the application part and not the infra part of secrets store CSI driver.

So that's the reason we are only installing these helms for secrets store CSI and ACP both here okay.

And we will do the we will define the IAM roles and IAM policies and pod identity associations for these

things.

When we reach the 1401 retail store AWS Data plane section.

Okay.

So there we will discuss it in detail.

Here we are installing these things only okay.

Now this is the name of your helm.

Release CSI secret store and repository.

You are getting it from the Kubernetes.

Six.

Okay, so this is a standard Kubernetes sync project, and chart name is this one.

You are installing it in the kube system namespace.

And this part we have discussed in detail sync secret Enabledtrue.

Right.

And we have also put additional things where it is equal to true timeout is equal to 600.

And then clean up on fail equal to true.

So to safely remove all the resources whatever installed if it fails okay.

And timeout you are also giving 600 and wait.

Also you are saying to true which means wait till all the things are deployed.

Okay, so that's about this secret store CSI driver related helm release.

And now we also need to discuss about another helm release, which is AWS Secrets and Configuration

Provider.

So for these things also private node should be deployed.

And you are also saying secrets store CSI driver which is we defined in your 1601 is already installed.

Then only install this secret provider helm release.

Okay, so that's the thing.

And pod identity is optional, but I have added it here.

Okay.

And next is, uh, even you can say it as mandatory only.

Why?

Because these things we are using only the pod identity approach to access the resources on AWS.

So it's let's put it as mandatory only for us.

Okay.

So it depends on AWS even it is not there still fine.

But it's always good to define the, uh, explicit dependency so that we have defined it in a orderly

manner.

Okay.

So that's fine.

Now let's move on to the name of this.

We are saying it as secrets provider AWS and repository.

So this is a CP means like AWS related one.

So you will get it from our AWS related repository.

And name of this chart is this one.

And we are going to install that in kube system namespace.

And one more thing we are going to define here is already we have installed as part of 1601 our secrets

store CSI driver, so we will disable that explicitly.

Why?

Because if you don't set this by default, ACP will install that okay.

So for that purpose we are disabling it here.

And standard wait for all parts to become ready.

And then timeout and clean up on fail are defined here.

And it's equivalent output.

So that's about secret store CSI helm install and secret store csi ACP install.

So this completes the ECS Terraform manifest with add ons.

One is LBC, one is EBS and the other is secret store CSI.

And also another one is pod identity agent C11.

So these are the things we are going to deploy as part of this new Terraform project.

Okay.

So we have added them from C11 to C 16.

In our next lecture we will go ahead and then create them and then also verify whether these pods are

running or not.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, let's go back to section 13.

And here you have the zero one VPC Terraform manifest.

And zero to x terraform manifest with add ons.

Okay.

So you can go to zero one VPC Terraform manifest.

You can run the command terraform init.

And then also run the command terraform validate plan apply and then auto approve.

After that you can also go to zero to x Terraform manifest with add ons there.

Also you can run init plan apply and then auto approve.

So before that as usual you know that we need to verify our C1 versions and you update your bucket information

here in C1 dot in zero one VPC terraform manifest and also in zero to x Terraform manifest.

Also ensure that your bucket name, your bucket name where Terraform state should be stored is in addition

to bucket updated in C1 versions TF in zero to x.

Terraform manifest with add ons in C3 remote state.

Also ensure that your bucket name is added here where your VPC Terraform tf state file is present created.

Okay, so now I will directly run the Terraform apply and auto approve for VPC.

I didn't change anything there, so we really don't need to worry about anything there, right?

So if you want you can run the plan validate all those things I have run the apply auto approve.

We can see here 18 of our VPC resources got created successfully.

So we will go to zero to.

We are in our 13 Terraform EKS cluster with add ons.

And we are in zero to x Terraform manifest with add ons.

And here we are going to run the command terraform init.

And after that we'll also run the command validate plan and then apply auto approve.

So why we are running validate is we have made a lot of changes, lot of things we have implemented

right from C11 to C16.

If there are any errors or anything, also we will know that okay.

So we'll run the commands, validate and also plan now.

So we'll run the command Terraform validate.

You can see here configuration is valid.

We will also run the command Terraform plan and wait for the plan to be printed for us.

We can see it here 33 resources.

It is going to add.

And all the things outputs.

Everything looks good.

So I'll run the command terraform apply and then auto approve.

So let's wait for the EKS cluster to get created.

One thing I have done here is I have also ensured that in Terraform I am using the P3 medium from now

on.

Okay, so when we go to the advanced sections again, if we need a better uh instance type needed again,

we'll also upgrade from T3.

Small.

We have moved to T3 medium currently, but if needed we will also upgrade some more.

Okay.

Currently ECS cluster is getting created.

After that it is also going to create the node group and after that our add ons things will come into

the picture.

So we have defined the explicit dependencies.

Meta argument depends on in our resources because not only during the creation okay, but during the

destroy.

Also when we run the terraform destroy auto approve also.

Right.

So at that point of time, also, if they were not destroyed in a proper order, then it will be a difficult

thing.

Okay, some things will be deleted, some things will be not deleted.

So for that purpose, for everything we have defined in a orderly manner.

So after this, create this, after this, create this.

So we have effectively used the depends on meta argument.

We can see here our ECS cluster created successfully.

So if we go up here right.

So all the resources first ECS cluster got created, and after that ECS node group got created.

And after that ECS add on pod identity got installed.

And then after that EBS, CSI driver and load balancer controller and also secret store CSI driver,

all these things got installed successfully.

So now this is the CSI add on Arn and all these things.

But if you go down here this is the secret provider related helm metadata.

Okay.

So it's when it is deployed and all the information you can see right.

This is load balancer controller related metadata.

And here you can also see additional information about our ECS cluster.

This is the to configure kubectl we will use this command to configure it.

Let me copy it and run it.

And now our Kubeconfig should be configured.

Now if I run the command kubectl get nodes so we can see that our three nodes are running.

And now we will start with verifying our driver.

So we'll say get pods hyphen n and kube system okay.

And we should see the load balancer controller running.

And we should see the EBS CSI controller running and EBS csi node related Daemonset related nodes means

like Daemonset related pods are also running and ECS pod identity agent running, and also CSI secrets

store CSI driver running and add ACP also running so we can see everything.

Whatever we have installed is running without any issues.

So this completes the section 13 which is automating the installation of EKS add ons and necessary things

like load balancer, controller, CSI controllers, whatever is required using Terraform.

So I'll see you in the next demo.

Until then, bye bye.

Thank you.


Welcome back as part of section 1102 and 1103 and 12 zero five, we have implemented our retail store

application with persistent data plane running on Kubernetes cluster, which means your underlying persistent

systems like Mysqldb, DynamoDB, Redis and then RabbitMQ and PostgreSQL.

So these things are running on EKS cluster itself.

Okay.

So now and in addition to that, when you talk about 11 zero two and 11 zero three, you have used the

YAML manifest.

And 1205 you have implemented everything with helm.

Okay.

Now we need to understand something here.

So how do we need to offload these things to AWS.

So that's that part we are going to work on now.

So all these things we will replace these things with the AWS services as part of sections 1401 and

1402, which means catalog is going to get a AWS, Ards, MySQL database, and cart is going to get

the AWS DynamoDB and for checkout service, we are going to have the AWS ElastiCache, and for order

service, we are going to have the AWS, RDS, PostgreSQL, and in addition to that, for RabbitMQ replacement,

we are going to use the AWS Simple Queue service SQS service for messaging.

So these are the things we are going to do as part of this section okay.

Section 14 and in section 14 zero one we are going to build the data plane first.

And in 14 zero two we are going to deploy our application.

So let's understand that okay so here in simple terms retail store application with persistent data

plane running on AWS cloud okay.

So let's see what exactly we are going to do.

So we are going to create a AWS persistent data plane which is 14 underscore zero one section.

So here we are going to create RDS, MySQL DynamoDB, ElastiCache, PostgreSQL and SQS.

All these things provisioned by Terraform.

And then once this 1401, all our persistent data plane for our retail store application is created

and ready using Terraform.

Then we will go ahead and then deploy means like update the necessary endpoints of RDS, DynamoDB,

ElastiCache, all these things, whatever is there.

We are going to update these YAML manifests and then deploy all of these things means like the retail

store application complete application, we are going to deploy and access it via browser and then verify.

So that's what we are going to do.

So as part of 1401 we are going to provision our data plane using Terraform.

And as part of 1402 we are going to deploy our Kubernetes YAML manifest for all the five services and

also the ingress service and then test it.

So this is what we are going to do as part of section 14.

So let's go ahead and then implement these things in two phases 1401 and 1402.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

Let's enter into the section 14.

And in that we are entering into the section 1401 retail store AWS Data Plane.

And here we are going to go ahead and then review our project structure.

So we are supposed to create all these services complete this persistent data plane.

We are going to build using Terraform.

So we can even manually create it.

But it takes a lot of time.

And for these things if we miss any settings again it is going to be a big issue.

So we have automated this entire AWS persistent data plane for retail store application using Terraform.

So let's review the complete Terraform code now step by step okay.

So we will go here.

And in 14 retail store microservices with AWS data plane.

In 1401 we have the data plane.

And in 14 1402 we have the microservices with data plane.

So now here we have given the project number as zero three.

Why?

Because zero one Terraform project is VPC and zero two Terraform project is ECS.

Terraform manifest with add ons and zero three Terraform project.

In our course is going to be AWS data plane Terraform manifest.

Okay, so that's the reason we have given the number for this project as zero three.

And we have lot of files here okay.

So let's understand how we have categorized them okay.

So we'll go down here.

The folder structure starts this way.

Let's go ahead and then review these things on a high level right.

So we will start with in zero three AWS data plane Terraform manifest.

We'll have some common base files like C1 versions dot standard right.

And we define variables.

And we we want to access both VPC from project one and also ECS from our Terraform project.

To those information when creating this data plane.

So for that purpose we have VPC remote state file.

And also we have the ECS remote state file.

And in addition to that we have also defined the standard data sources and locals which is present in

all other projects also.

So that's about base files.

And in addition to that we will have common IAM and CSI integration files.

One is pod identity assume role.

So which which is nothing but our trust policy which we have seen in our x zero to project with add

ons whatever we have done there.

Also you have seen that file okay.

And this is simple IAM policy right for secrets store CSI wherein we need to get the secrets and describe

secrets right.

So that related permissions will be present here.

We will review that once we go there, but on a high level, I am going through here.

Okay.

And in addition to that, for catalog, we have the catalog related things present here.

And in the same way for cart microservice it's equivalent information means like those for cart microservice.

What all the things needed.

Those things are present here in catalog.

It is going to create a MySQL DB instance.

If you see here in the same way for cart, it is going to create a DynamoDB table but necessary permissions,

all those things we will create.

So that's the reason we will have these many files here okay.

In the same way for checkout microservice also we will create the AWS Elastic Cache.

And that related things are present here.

And for orders microservice we are going to create PostgreSQL db instance and also SQS queue.

So those related information also present here.

Now let's go ahead and then review these files one after the other, which means we will start and then

first review the common base files okay.

So we will go here.

And in C one versions dot f.

Very very important thing here is update your Terraform.

Update your S3 bucket where you want to store your Terraform state files.

So I have given the key here as retail persistent endpoints.

And this will be my Terraform state file in this folder.

It is going to create okay right.

And update your bucket without fail.

So these are standard C one dot f.

You have AWS provider.

And all these things are defined as is which we have seen in our previous Terraform demos.

And here again standard variables AWS region, environment name business division and tags.

No changes in that okay.

From previous demos okay.

And in C3 VPC remote also you if you go to zero to EKS cluster.

Also it is the same file Okay, to access the VPC information here, you need to update your S3 bucket.

That is very very important.

So using Terraform remote state we are going to access the zero one VPC project.

Terraform projects information here VPC ID or subnet IDs.

Private subnet IDs are public subnet IDs in the same way in C3 zero to X remote state.

So here we are going to access the x related Terraform state which means zero to x project.

Whatever you are seeing that related projects information we are going to access like cluster name,

cluster ID, cluster security group cluster certificate authority data, whatever is needed here.

So those things we are going to access.

So ensure that you need to update your bucket name here where your ex terraform state file is present.

So for places three places you need to update C C1 versions here and in C3 zero one also here your bucket

name and in C3 zero two also.

And next is we have something called data sources and locals.

So our standard thing from our previous sections whatever is coming we are using the same way.

Okay.

So anything extra also those are simple data sources.

Whatever we have defined to get a account info or current region.

And these are coming from our previous Terraform projects itself.

Okay, now C5 zero one pod identity uh assume role.

Also it is a data source aws IAM policy document, which we have even demonstrated in section 13.

Also if you see here you will find it okay.

Pod identity assume role C 13.

In the same line.

We have the same thing here pod identity assume role.

And if we go to C5 zero two.

So this is important.

So to access your secret present in AWS Secrets Manager.

You need some permissions.

Okay, so those are nothing but get secret value and then describe secret value.

So here you are creating only IAM policy okay aws IAM policy.

And you are saying the name local name for this is retail store DB secret policy.

And you are giving these two permissions for this policy wherein get secret and describe secret.

And you are also explicitly giving the permission to which resource on which resource these permissions

will be applied, which means these permissions can be applied on resource whose name is retail store

DB secret star, which means any AWS Secrets Manager secret created with this name.

Retail store DB secret 1234.

Whatever it is, those things you can if this policy is applied somewhere.

So using this policy you can get you will get these two permissions on that respective secret.

So that's the thing to remember.

Now, if we go back here and we will, we will go ahead and then review this retail store DB secret

ones.

Okay.

So we will go to the AWS console and we'll go to secrets manager.

Secrets manager.

Secrets manager.

And in that resource right.

So we will go to retail store DB secret one okay.

So this is the one which we have created.

And here if you see retrieve secret value username is my DB admin and password is DB 101.

So if you want to create one more.

So this is I created it.

But if for this demo already one is used, we can also use two if you want.

Okay so I'll try to use the one only in the demo.

I don't want to change the files but how to create a new secret.

So we'll go here and we'll say here other type of secret here right.

Other type of secret and I'll say username and then I'll say my DB admin.

And here I'll give the password and I'll say DB 101.

Okay.

And I'll leave this encryption key to default.

And here I will provide the name retail store DB secret two okay.

And if I want I can provide.

These are DB secrets for catalog and orders okay.

So for both MySQL and PostgreSQL and I'll click on next and I'll leave these two defaults.

Click on next and then go ahead and then store okay.

So this will create a new secret for us.

So ensure that you create this secret with name one okay.

So why.

Because we are referencing one and why we are not creating via Terraform I don't want these files okay.

So these values okay.

Username my db admin db EP 101 in my Terraform config files anywhere.

Okay, so ah, it don't need to be stored in my terraform state file also when it is created.

So for that purpose this is a secret.

I want to put that object separate and I created it separately via console or via AWS CLI.

And those will not be present in any of my IAC files infrastructure as code files.

So for that purpose we are creating it manually here.

Okay.

And now what we will do here is we if you see the name of the complete resource is in Secret manager

in US East one region and in the in my account ID.

So here you will get your account ID, your region and the secret and name is retail store hyphen db.

Hyphen secret star means like whatever the hyphen to we created also for that.

Also this DB secret policy is applicable means like you will be able to access the information from

even DB secret hyphen two.

Also, if you give hyphen three, hyphen four from all those things, we can get the data using this

policy.

So that's what I mean to say.

So this completes the intro section which means like we have discussed about these things which are

uh C1 to C4, we have already discussed.

So these things are completed okay.

And these two also we have discussed and then completed.

Additionally we have also created a manually using AWS console.

We have created Secret Manager Secret okay.

So which we have created just now okay.

We have created that secret okay.

So this we have done manually okay.

So let's remember that okay.

Now we'll go in our next lecture we'll focus on catalog microservice.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

In this lecture we are going to focus on catalog microservice.

So this catalog microservice we are going to review all the things whatever we need to create for MySQL

okay.

Right.

So now let's go back here and understand that.

So we'll start with the C6 zero one which is catalog RDS MySQL security group.

So let's say you have your EKS cluster okay.

And you are workloads you deploy on this EKS cluster example like your catalog workload.

And you have your RDS MySQL database here.

And you want to explicitly say that okay, right.

For this MySQL database the request should be allowed only from EKS cluster okay.

So from EKS cluster only you need to explicitly allow the traffic.

So to do that you need to create a security group.

Okay.

So you need to create a security group SG stating that you want to allow traffic only from EKS cluster.

So how we are going to do that.

So that we are going to review now.

So we will go to our Visual Studio Code and review that.

So that is C6 zero one.

So let's understand this resource now.

Okay.

Right.

So let's see this part is not needed.

So we can remove that.

This is the security group for RDS MySQL name of it.

We have provided it here.

Allow MySQL access from EKS cluster only and provide your VPC ID.

So you need to provide your VPC ID that you can do using data.

Terraform remote state dot VPC dot outputs dot vpc id.

So you can go here to see 301.

And you can see from project zero one this is your project zero one Terraform manifest.

From there you are getting your VPC ID right.

So that's clear for us.

So we'll go back here.

And next is this is the ingress rule you are defining.

So you are saying that allow MySQL from EKS cluster security group only.

So from port is 33062 port is 3306.

Protocol is TCP.

And here you need to provide your EKS cluster security group ID okay.

So from EKS cluster only then traffic will be allowed.

So if you go to zero to X and if you go to the output section right at the end right, you will have

the cluster security group ID security group associated with EKS cluster.

So from this security group ID only allow the access we are saying.

Okay, so this is the one X cluster security group ID.

So if you go to C6 zero one here data dot read terraform remote state dot x dot outputs dot x cluster

security group id.

And instead of this, if you want, you can also give source as means like instead of security group.

If you want to give source and then you can also define that source something like instead of if you

feel like this security group is complicated, then you can define the source.

And in source you can give your 1.0.0.0 slash 16, which is your complete VPC range where your EKS cluster

is present.

Even those things you can define okay, but that is not the thing.

So let's define perfectly whatever is required here, which is uh, security group EKS cluster security

group we have defined.

So that's about this one.

Okay RDS MySQL security group.

Now we also need to discuss about catalogue des mysql db subnet group.

So what exactly that means.

So this RDS MySQL instance right.

So this MySQL instance to create it it need to create somewhere right.

So in our VPC right.

So let's say this as the VPC.

So you have created public three public subnets and three private subnets.

So these are private subnets let's say okay so these are the private subnets.

Now your RDS MySQL database instance where it need to get created so that it will be secure.

It need to get created in your private subnets.

So if you want to get that created in your private subnets then you need to create your subnet group.

So that is this one.

Whatever we are talking about which is C602.

So now let's go back here and understand that in detail.

So we'll go to C602 now which is this one.

Does MySQL DB subnet group.

So AWS db subnet group RDS private.

And name of this group is local name dot RDS private subnets and some tags you have defined.

And in subnet IDs you are getting from data dot terraform remote state, dot VPC output and private

subnet IDs.

So that's what you are trying to get.

So if even if you go to the VPC also and verify the outputs you have your private subnets okay.

So where is it private subnet IDs.

So this is the one you are trying to get here okay.

so that's about C6 zero to private subnet IDs providing to create a DB subnet group.

So how many subnets are there in this private subnet IDs.

Using that DB subnet group will be created which will be used by the RDS database instance during creation.

So that's about MySQL DB subnet group.

Now let's come to C6 zero three right C6 zero three is catalog catalog ards MySQL credentials.

So let's understand that okay.

So now you already defined the secret manager secret.

So if you go here right.

So if you go to here okay.

And then if you search for secrets manager you already have manually created your secret in secrets

manager whose name is retail store DB secret one.

You don't want these values, whatever is present here to be known to anyone in IAC scripts or wherever

you want, in Terraform scripts or wherever.

So you have manually created it.

Okay, now using Terraform data sources concept, you can access that secret.

Okay, so data AWS Secrets Manager secret and local name for that is retail store secret in Terraform.

And this is the name of my secret.

So if you copy this and if you go back here and if you search for it, it is nothing but our retail

store DB secret one.

So that one we are trying to access here.

Okay.

And then we will get the information of that.

And from that we will get the secret version AWS Secrets Manager Secret version.

If you try to open the documentation for that secret version, it provides a resource to manage AWS

Secrets Manager.

Secret version, including its secret value.

So we want the secret value.

And for that purpose, we are using this resource.

Okay.

And now inside this resource AWS secret version.

Right.

So you will have something called string value.

So if you see the attribute reference and argument reference secret string specifies text data that

you want to encrypt and store in version of the secret okay.

So you are trying to access that secret and get the secret string okay.

So that's what you are doing here.

Okay.

So you can get using this data dot AWS secrets manager secret version dot retail store secret uh, secret

value dot get string.

Okay.

So when you try to access that, you will get your complete secret information.

Okay.

So in a JSON format and then using JSON decode you can decode that okay.

Now if you see it here for for which secret you are trying to get that you need to provide this ID data

dot aws secret uh means like data dot AWS secrets manager, secret dot retail store, secret.id.

So for this secret, you are planning to get the information retail store secret JSON.

And if you want, you can also print those things.

Okay, because those are sensitive values.

I don't want it to be stored anywhere.

I'll say sensitive equal to true.

And if you want for debugging purposes you can run Terraform output hyphen JSON like this and then get

the exact value also if you want.

Okay, so I have even defined them as outputs.

But I said them sensitive equal to true so that they will be masked even in the terraform outputs.

Also, in simple terms, this C6 zero three catalog rds MySQL credentials.

We are trying to access our secret manager secret and get whatever the username and password present

inside that.

So that same thing we can set for our RDS database instance.

So that's what we are doing.

Now if we go to C6 zero four this is the AWS db instance we are creating and which whose local name

in Terraform we are setting it as catalog RDS and identifier is my DB three engine is MySQL engine version

is 8.0.

Instance class is DB .3. micro allocated storage 20dB.

And when the instance is created, what should be the default database name, catalog db and username?

You will get it from whatever we have defined just now here.

So where is that.

So let me close this.

And this is the one right.

So let me just give some space here and credentials.

So now this is the credentials.

So you will say local dot retail store secret JSON dot username I think okay Local dot retail store

secret Jason dot username and password.

So for your database instance, whatever the password you are setting is, whatever is present in your

secret manager means like you are setting username as my DB admin and password as db 101 and db subnet

group name.

Right.

So this is the DB subnet group.

Just now we have created here.

Right.

That is the one we are trying to access AWS DB subnet dot RDS private dot name.

So that this DB instance will get created in those private subnets.

And what is the VPC security group IDs.

Just now we have defined one security group.

Also right to this database instance should allow traffic only from EKS cluster.

So that security group we are providing so we can provide multiple security groups.

So that's the reason it's a list item.

And these are additional settings.

Keep final snapshot.

Publicly accessible.

False.

So we are creating a secure database in private subnets and delete automated backups through Multi-az

is false.

We are not creating any multi DB instance backup retention period one.

All these are default things.

We have put it and we are also going to output the RDS db address and also RDS MySQL.

This is the security group ID.

So these two things we are going to output from this C6 zero four.

So that's about from C6 zero one to C6 zero four.

So so far we have defined the security group DB subnet group and then credentials and db instance.

So till here RDS related things are completed okay.

But important things are IAM role and Pod identity association.

So why catalog needs IAM role and then pod identity association.

So here comes the SCA which is service account.

And what?

What are we talking here?

Right.

So we are talking something like you have the catalog pod here, right.

So when you create the application right.

So you have the catalog pod and it will access your MySQL database which is your MySQL database.

So now if you want to this catalog right, this catalog pod need to access this RDS MySQL database means

it need credentials.

Right.

And where are these credentials located in AWS Secrets Manager.

Right.

In AWS Secrets Manager, these credentials are present.

So how to access them.

So there comes the important thing.

So you have catalog.

You have RDS DB.

And to access those credentials.

You have something called CSI Secrets Store CSI, and you have something called ACP.

And then finally, you have the secret manager here, right?

AWS Secrets manager.

So to access this catalog pod need to go through this and then get that information to access the RDS,

my SQL.

So to do that it needs the following things.

One is I policy.

Second is it also needs the I role.

And third is it also needs the x p I association for catalog service account for which catalog.

And it also needs the catalog service accounts.

So these four things are needed.

So let's talk about these four things now in detail, right?

So now let's come back here to our Visual Studio Code.

And first thing is IAM role we will discuss.

Okay so this is the catalog secrets IAM role and assume role policy.

We have it here right.

So this is the pod identity.

Assume role which we will access here.

So that's the thing data dot aws IAM policy document dot Assumerole dot JSON.

So IAM role is created.

No issue.

This is pretty standard.

Now when coming to IAM policy we already created IAM policy in the common base files here for secrets

store CSI to access the retail store DB secrets.

Okay, so this is the one retail store DB secret policy we have created.

So that one we need to associate with the IAM role we created.

So our IAM role is AWS IAM role dot catalog secrets dot name.

That is the IAM role and policy is AWS IAM policy dot retail store DB secret policy dot r which is coming

from our C502.

So this is the one we are getting and then doing.

So that's about Im role and policy.

So if you go here right if you go here this is completed.

Policy role is completed.

Now we have seen those things now in Visual Studio Code.

Now let's come back here and go to C6 zero six which is export identity.

So the first thing is when you want to create a pod identity association.

So you need the following.

One is service account.

So one is service account two namespace three I role right.

So now namespace it is going to be uh it is going to be default namespace.

Why.

Because catalog Pod is going to be there in default namespace service account name also catalog okay

and IAM role.

Just now we have seen right.

So that is the thing.

So now let's go here and cluster name.

We can get it from Terraform remote state.

This is the cluster name namespace default service account catalog okay.

Roll.

We have just now seen here right.

Where is it.

So this is the one see AWS IAM role dot catalog secrets dot.

So this is the one we need here in this thing AWS IAM role dot catalog secrets dot e.

So that's about catalog microservice related persistent data layer related implementation.

So in our data plane.

So SDB is one thing.

And you also want to access the secret information present in Secret Manager to access your MySQL database.

Right.

So here nowhere we have hardcoded our credentials.

We are trying to catalog Pod will go to secret Manager and then get the credentials and then access

our database.

So once these things are created.

So whenever the catalog pod is created right.

So it will go to Secrets Store CSI.

Right.

And then you also have something called ACP.

After that you have the AWS Secrets Manager okay.

So now this is catalog part.

And you have something called R s my SQL to access this database.

It need to go to this flow, get the credentials and then access your database with those credentials.

So that's what it is going to do now okay.

So this completes the catalog microservice I'll see you in the next lecture with cart microservice.

Until then bye bye.

Thank you.


Welcome back.

Let's go ahead and then discuss about Cots microservice related information now.

So for CART microservice to access the Amazon DynamoDB again we need the IAM policy and IAM role.

And also pod identity association with our uh service account for our carts microservice.

Okay.

To access the DynamoDB table and also actual DynamoDB table, which we need to create.

So let's say this is our EKS cluster and this is our carts microservice related part.

So to access this we need the following.

Again as usual, same as IAM policy with permissions to access this DynamoDB table.

And then next is IAM role right.

And then we also need our cart service account Kubernetes service account.

And also finally p association.

So these are the four things we need to discuss.

So let's go here and then review them now.

C7 zero one cart DynamoDB here okay so we have the cart DynamoDB IAM policy.

And we have given these permissions DynamoDB create table, delete table, and all the DynamoDB related

permissions.

And we also didn't say any specific resource full access to all DynamoDB resources in all regions.

So like that we have put it.

And then in addition to that, once the IAM policy is ready, now you need to associate, you also need

to create an IAM role.

So which is cart DynamoDB role.

And when creating this DynamoDB role you also need to have a assume role policy and that assume role

policy.

You can get it from your C501 pod identity assume role wherein data aws IAM policy dot Assumerole dot

JSON.

So this is IAM role created for us.

Now we need to associate this IAM policy and then IAM role Okay.

So we need to attach the IAM policy to this IAM role using AWS IAM role policy attachment.

So that's about IAM policy IAM role.

And then trust policy and IAM policy attachment to the role.

Next is cart x pod identity association.

So cart pod identity.

This is the cluster name EKS cluster name namespace where we are going to deploy cart is a cart application

is in default namespace.

And the service account for our carts application is going to be carts, carts, Kubernetes service

account and role Arn we are going to get from here.

So this is the role Arn.

We can see it here AWS IAM cart DynamoDB role dot a.

So that should be provided here.

Where is it?

This is the one.

Okay.

Cart DynamoDB role Arn.

Additionally, you can define your output for your pod identity association.

And finally, you need to create your DynamoDB table.

AWS DynamoDB table and local name for this table is items and name of this table we are going to give

is items and in billing mode this is important.

We are giving pay per request which is on demand pricing.

On demand pricing.

That means we don't have to worry about capacity units or throughput planning.

AWS scales it automatically based on demand, and the primary key for this table is the ID here.

Okay, so which uniquely identifies each shopping cart item okay.

So we have the attribute ID and type is string here.

And we also have the attribute customer ID and then type is string okay.

So ID is our partition key here.

And customer ID will be used to create the global secondary index which allows us to query items based

on the user who owns the cart.

Okay, so these are some key technical information about the core carts application.

We really don't need to worry about it.

Okay.

So this global secondary index gives us the ability to look up all cart items from a specific customer

ID.

So we are protected.

We are projecting all attributes here, which means every column is available when querying through

this respective secondary index.

Okay.

So you don't need to worry what all are these.

So we are creating a simple DynamoDB table whose name is items okay.

And whose primary key is ID.

And billing mode is pay per request.

So that's the key thing here.

We have added some uh additional tags here.

So that's about the carts microservice.

So in our next lecture we will focus on checkout related data plane uh Terraform config files.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


So let's talk about the checkout microservice.

So here we have three things.

One is this security group subnet group and cluster.

So here we don't have anything related to Pod identity association or IAM.

Roles are policies.

So all these things are not needed.

So this checkout service need to microservice need to access the AWS elastic class which is nothing

but Reddis okay.

Redis cache we can say right.

So now this Elastic Cache service okay needs a security group wherein from which services it can allow

the from which AWS services it can allow the traffic from.

So we need to allow from our EKS cluster related, uh, whatever the things deployed in that range.

So for that purpose, we will create a security group for this AWS Elastic Cache stating that only allow

the traffic from EKS cluster security group ID.

So source will be always the EKS cluster from where the traffic is originating, and then going to Elastic

Cache.

So that is one thing which we need to implement.

And second is where does this elastic cache redis need to get created.

It creates a instance and in which location it need to get created.

It need to get created in a private subnet.

So to do that, how we have done for RDS, MySQL a subnet group.

So here also we are going to create a private subnet group so that this will be created in this private

subnets.

So these two are the important things C801 and then 802.

And finally 803 is your checkout redis cluster itself.

So now let's go ahead and then review them one after the other.

So this is the checkout Redis Security group.

And here you can see the name.

And then in which VPC it is getting created.

We pass it the VPC ID using the remote state whatever we have defined earlier also.

Right.

It's the same thing okay.

So now Uh, ingress port is 6379 and two port is 6379.

Protocol is TCP, and the source from which the traffic will originate and come to the ElastiCache is

from our EKS cluster.

Security group ID allow traffic from EKS cluster security group.

So that's about the security group.

And in C8h8o2 you have the checkout Reddis subnet group.

So AWS ElastiCache subnet group we are creating and we are providing the subnet IDs as again from our

VPC outputs private subnet IDs.

So that's about C801 and C8 zero two.

Pretty straightforward ElastiCache subnet group we are creating here okay.

And next is Redis cluster.

We are creating AWS Elastic Cache cluster.

And local name for this cluster in Terraform is Checkout cluster.

And here is the cluster ID name hyphen checkout hyphen.

Redis engine we are using is reddish node type we are using is cache .3. micro.

And number of cache nodes one and port is 6379.

Here we will provide the subnet group which we have defined in C8 zero two.

So whatever we have defined here that subnet group we are providing it here.

And security group also whatever we have defined in C8 zero one that security group.

So this is the one reddish edge.

This is the one we are referencing here.

And engine version this one and parameter group name.

So these are defaults okay.

And this is the tag.

So this is the Redis cluster or Elastic cache cluster on AWS we are creating.

And we will also get the address name as a output here.

So that said we will be successfully creating the checkout means like the checkout related redis cluster

also.

So so far we have completed RDS, DB, DynamoDB, RDS, MySQL db and ElastiCache.

In our next lecture we will focus on orders microservice.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

Now let's talk about the orders microservice and then Postgres SQL here.

Okay.

Later we will discuss about Amazon SQS which is messaging service.

First we will focus on persistent DB for our orders microservice which is PostgreSQL.

So in EKS cluster you have the orders microservice and whose service account is orders.

And you need to access the RDS, PostgreSQL db, aws, RDS, PostgreSQL db.

So first thing is for this rds, PostgreSQL db right.

We need to allow traffic only from the EKS cluster.

For that we need to create this security group.

So that is the thing first thing to implement.

So this is the security group.

So that is C9 zero one.

Then this RDS PostgreSQL need to get created in some private subnets so that private subnets we create

the DB subnet group.

And finally we create our DB instance.

So now you have the instance ready and you have your orders microservices ready for you.

But to connect to this you need your DB credentials.

So these DB credentials are present in your AWS Secrets manager right.

So if that is the case then you need to as part of the Sync Secrets in Secrets Store CSI driver, it

will create a orders hyphen DB related Kubernetes secret here in ECS cluster.

So using that it will access the credentials and then access the PostgreSQL DB.

But to create this secret.

Right.

So we need to ensure that that sync secret to work we need to create the IAM policy IAM role and we

need to create a Pia association between the service account, Pia service account name space and then

IAM role.

So these are the things we need to implement.

So now let's go ahead and then review all those things which is still C9 zero five okay.

All these things are pretty straightforward which we implemented earlier.

Just we need to review them okay.

So first is orders PostgreSQL security Group okay.

So standard security group right.

And this is the VPC ID inside that.

And the PostgreSQL port is 5432.

And here is the ECS cluster security group ID which will be the source from where the traffic will be

initiated.

And in the PostgreSQL only allow the traffic from this respective security group only from ECS cluster

only.

So that's about PostgreSQL security group address.

Nothing.

Okay.

Leave to defaults and next is DB subnet group.

So AWS db subnet group for RDS PostgreSQL subnet group.

Right.

Whatever we are creating its local name reference in Terraform is this one.

And you are saying data terraform remote state dot VPC dot outputs dot private subnet IDs.

So that's about subnet group.

In this subnet only our PostgreSQL databases will get created right.

And next thing is db instance.

So this is our orders Postgres db instance.

Its identifier is this one orders Postgres db engine is Postgres.

This is the version we are using and instance class we are using this one allocated storage is 20,

max allocated storage is 100 GB and DB subnet.

Group name.

Just now we have defined in C9 zero to AWS, subnet group, dot RDS, PostgreSQL, subnet group dot,

and then you get the group name.

So that's what we have defined here.

In the same way, in C9 zero one, we have defined the security group ID, and that one we are referencing

here.

And when creating this respective DB instance default database, it will create our default schema.

It will create which is orders db and username and password.

Same as our RDS, MySQL local retail store secret JSON username.

So you can see it here C6 zero three.

You have already implemented this okay, in C6 zero three you have already implemented this.

Okay, locals dot retail store secret JSON dot username and then password.

So from here we are referencing that.

So same uh my DB admin and DB 101 is the same username and password.

Even we are setting it for Postgres SQL okay.

And port is 5432 rest.

All are the default settings.

We have put it here and we will also put the endpoint and also dbname as outputs.

Okay.

So we are going to access them accordingly.

And in C9 zero four we are going to create a IAM role whose name is orders Postgres get secrets.

And for this Get secrets IAM policy you are going to access attach is retail store DB secret policy

which is present here in our common thing.

Okay CFI zero two.

So to access the secrets these are the permissions needed.

Right.

So for that purpose.

So now let's come back here.

This is the IAM policy to role means like attaching the policy and role here okay.

And it's equivalent output.

So PostgreSQL related IAM role also created.

Finally we also need to create the Pod identity association okay.

So this is the cluster name.

We can get it from the remote state data source namespace is default service.

Account name is orders.

And just now in C9 zero four we have created the IAM role.

So aws iam role dot orders PostgreSQL get secrets dot a.

So this is the one we need to reference here.

Okay.

So we will create a pod identity association in this manner.

So this completes the this part which is PostgreSQL part okay.

So this is the PostgreSQL part which we have completed.

Now let's talk about this same orders microservice.

Right.

So this orders microservice also need to access the SQS, AWS, SQS, which is nothing but simple queue

service which is a messaging service.

Okay.

Now this also need to access.

This means it needs again the following permissions.

One is I am Policy.

Right.

And IAM role.

Right.

And then you also need to have the service account and for is Pod identity association which is p I

association.

Okay.

So these things are required to access even this SQS service.

Okay.

But one thing you need to remember here is you already created a, you already created a Pia and you

already have a service account and you already have a IAM role.

So to this IAM role, if you give the policy permissions, you create a new IAM policy to access this

SQS service.

Right?

To access this SQS service, create a new IAM policy and associate it to that IAM role.

Then this orders will be able to access this AWS SQS.

So that's the thing.

So that's what we are going to do now.

So first thing is we will create the SQS queue to create it.

Resource name is AWS SQS queue.

Local name for that respective resource in Terraform is orders SQS queue and then this is the name of

it.

And these are the message retention.

All these are the default values whatever we have provided here.

But name of my queue is retail dev orders queue okay.

Message retention we have given one day okay.

So that's about the queue.

And you will get the queue URL and q r information here.

Now let's come to the SQS IAM policy.

So in SQS IAM policy you said that these permissions you provide and to which you are providing for

this resource okay which is SQS R.

And just now whatever we have created in C9 zero six.

Right.

So for this resource.

So this is the Arn right.

So this for this resource whatever we have created okay.

So provide these permissions.

Okay.

Now this IAM policy, if we attach it to this PostgreSQL uh where is that PostgreSQL get secrets role.

So we have created a IAM role in C9 zero four.

So this is the IAM role we have created orders PostgreSQL get secrets okay.

To this role if we associate that policy.

So when this service account.

So if we come back here using this service account, when this pod is trying to communicate to SQS,

it is again the IAM role with whatever the policies we have, we have defined here okay.

So if we associate then it will be able to access the SQS.

So that is what we are going to do now.

So if we go to C9 zero seven we said that okay orders SQL policy attach.

We are attaching the policy whatever we have created here AWS IAM policy.

Orders SQL policy.

Policy Arn role.

Arn is orders PostgreSQL.

Get cigarettes dot name.

So these two things we are associating.

So that's about the order service.

So these are the things complete data plane things which we need to implement okay.

So let's come back here.

And we have also successfully completed the orders also.

Now let's come back.

So we discussed about RDS PostgreSQL and we also discussed about uh SQS.

So these things we have discussed.

Now if we go ahead and then run the command terraform init validate plan apply and auto approve, then

this persistent data plane will get created.

So that's about 14 zero one section.

So I'll see you in the next lecture for executing this Terraform commands and creating the resources.

Until then bye bye.

Thank you.


Welcome back.

Let's go ahead and then execute the Terraform commands to create this persistent data plane on AWS.

Now we'll go back to our terminal here.

And we'll go to CD.

And then section 14.

And inside that we'll go inside 1401 Retail store AWS Data plane.

And in that our Terraform project number is zero three AWS Data plane Terraform manifest.

So we'll run the command Terraform init.

And again one more time I am repeating this before running these commands.

Right.

Please ensure that in your C1 versions your S3 bucket is updated and in C3 zero one also where in which

S3 bucket you have your VPC related state file and in which S3 bucket you have your ex related state

file.

So those bucket information you need to update here.

C1, C3 zero one and then C3 zero two.

So now let's go ahead and then run the command Terraform validate.

And after that we will run the command Terraform plan.

And if no issues then we will go ahead and then run the command Terraform apply auto approve.

And we need to verify lots of things here okay.

So you need to verify your RDS MySQL is created DynamoDB is created, elastic cache is created and PostgreSQL

is created.

SQS is created.

And in addition to that, whether it also created the Piz.

So which we have discussed here.

Right.

Lot of pod identity associations we have discussed.

So all those Piz created or not we need to verify.

So let's go ahead and then run the command Terraform plan now.

So it's it's run okay.

So Terraform plan and it is saying that it is going to create 24 resources for us.

Okay.

So let's run the command Terraform apply auto approve.

It will take some time.

Wait for all these resources to get created.

We can see it here.

All the 24 resources got created successfully.

So close to 12 minutes it took to create all these resources.

Now let's go back here and we will go ahead and then review one after the other.

So we'll go back to our AWS console and start with RDS, MySQL and PostgreSQL.

So we'll go to that databases and we will verify those two things in DB instances.

We currently have two things.

One is Mydb three, which is our MySQL database instance for catalog.

And this orders Postgres DB for our orders Microservice.

So these are the things it created and ready for us.

Okay.

So you can see the my DB three and its endpoints okay.

So now we'll also verify the.

So we have successfully verified the orders postgres db and catalog Mysqldb.

Right.

So those created and next is we'll also verify the elastic cache okay.

So which is our Redis okay.

So weather for checkout uh microservice we have created the uh ready.

So let's see whether it got created or not.

So redis OS caches retail dev checkout, Redis even this got created without any issue.

And next thing is we will also verify skew simple queue service and see whether the queue also created

retail dev orders queue and even that got created successfully.

Only now pending is DynamoDB, so we'll go to DynamoDB.

DynamoDB.

And verify that DynamoDB also.

Right.

So we have the in table section items table created.

Okay.

But our carts application is not going to work because of one bug present in our application, which

is 1.3.0 current version.

Okay.

Right.

Wherein they have hard coded the database region to us West two.

So which means we need to make some changes for our application code okay.

Mainly for the carts DynamoDB.

So what are those changes in C1 versions.tf right.

So we will create one more provider AWS with alias as West two.

And we will define the region US West two.

And we'll go to cart DynamoDB table and ensure that this DynamoDB table gets created in AWS West two

only for Cod service.

Only the table that we are creating AWS DynamoDB table in West two region, because the code is hard

coded that way.

Okay.

And we are not planning to change anything code.

This is AWS provided AWS sample app, right?

So I really don't want to change it.

I already submitted a issue or bug for them, but let's see.

Okay, so this is the current fix for this okay.

Right.

So now the Cod microservice code is currently hard coded to us West two DynamoDB endpoint to ensure

compatibility without modifying application code, we are creating this DynamoDB table specifically

in US West two region using an AWS provider.

So earlier it has created an US East one.

So we have made this change.

So if we run the Terraform validate and then apply and then see what happens.

Okay.

So we'll run the command terraform validate.

And we'll also run the command terraform plan.

It should delete whatever the items table created in AWS US East one region.

And this should get created in US West two region.

So let's wait for the plan to be printed here.

So it is saying no changes because provider level changes.

It didn't find any drift from resource perspective okay.

So what we are going to do is so we are going to add one more table in US West two region right by adding

additionally here right this one.

And I'll try to say this as West two.

Right.

And I'll try to remove this okay.

So whatever we have the existing one.

So this should be removed in US east one.

And also one will be created in you in US West two region.

Okay.

Right.

So now let's run the command again.

Terraform plan.

And let's see what happens.

Okay.

Right.

It need to delete one table and create another table in different region.

See it is saying one to add and one to change.

So one it will be added in our US West two region and one will be deleted which is present in US East

one region.

So this is expected.

So I'll run the command Terraform apply auto approve.

So let's wait for these changes to take place.

Ideally at a single shot.

First time itself it should have worked.

But there is a issue in the carts application where the region is hard coded.

I want to ensure that it is fixed in the way that without changing the code.

Okay, so let's wait for the DynamoDB table to get created.

We can see here it got created.

Now we can see here one added and one destroyed.

So we'll go back here and go to DynamoDB.

And in US east North Virginia we'll go to tables and try to refresh.

And here nothing is there and we'll go to the US West two region.

Right.

And here it should be present.

The items table should be created now right.

So we can see it.

It got created successfully.

So now we have verified so from all these things.

So we can go back here and we can see we have successfully verified the RDS, MySQL DynamoDB which is

in US West two right.

And Elastic Cash, RDS, PostgreSQL and then SQL.

And in addition to that we'll also go to ECS, right ECS Kubernetes service, open it in new tab and

we'll also go to the Pod Identity Associations.

Okay.

So we'll click on this and we'll click on access.

And here you can find the pod Identity Associations.

So for catalog we have created in default namespace one Pod identity Association.

This is for getting the secrets okay.

And for orders.

Also we have created one pod identity association for getting the secrets.

Even that is present okay.

And for cats also we have created one pod identity association.

So to access the DynamoDB and for orders we also need to access the SQS service, which means its equivalent

IAM role or IAM policy is attached for the role or not.

We need to verify.

So we'll open this and we'll go to the orders related IAM roles okay.

So we'll click on here and we'll say retail and retail orders.

Postgres.

Get secrets role.

And here, not only in addition to DB secret policy, you should have orders.

SQS policy also so that it can also access the simple queue service SQS.

Also it can access.

So most of the things we have verified successfully remaining roles.

If we want we can also verify but those are already present here.

One is a catalog get secrets role and then another one is orders PostgreSQL role.

And this is cart DynamoDB role.

So these three things already created successfully.

Now in our next demo we'll focus on 14 zero two, which is nothing but pointing these applications catalog

cart checkout orders.

So all these things in 1402, we are going to point to these endpoints and then deploy and then test

it.

So I'll see you in the next demo.

Until then bye bye.

Thank you.


Welcome back.

In previous section 1401, we have already completed creating the AWS Persistent Data Plane.

So there we have implemented all this using Terraform.

Now we are going to focus on 1402 very very important section wherein we are going to integrate each

of these microservices with the AWS managed services like RDS, MySQL, PostgreSQL, ElastiCache, DynamoDB

and then SQS.

So we already know if we go to section 14 zero one and run the Terraform commands, this whole data

plane will be ready.

But when coming to retail store application we are going in 1402.

We are going to deploy the YAML manifest.

So now we need to understand how this is designed right.

So let me quickly show you how I organized this project because Organization is very important when

you are managing multiple microservices.

Folder structure plays the key role.

Let's go to our GitHub repository and we are in section 14 retail store microservices with AWS data

plane.

In that we are entering into section 14 zero to microservices with AWS data plane.

So here we are going to deploy the microservices which will be pointing to the AWS data plane.

Now let's go ahead and then review this complete folder structure.

Right.

So now we'll go here and start with the top level important thing which is zero one secret provider

class.

So this secret provider class section right contains our secret provider class definitions for catalog

db and then orders DB.

Right.

So we will use these to sync database credentials from AWS Secrets Manager into Kubernetes secrets.

So this is a very secure and very cleaner way.

Next is zero to retail store microservices.

So here we are going to have catalog cart checkout.

And we are also going to have orders and then UI.

So we are going to have each folder for each microservice under this retail store.

Microservices.

So you can see each of them separately.

Separate folders were created and their Kubernetes YAML manifests were present inside them.

So that's about retail store microservices.

After that we'll have something called.

So zero one is secret provider.

Class zero two is retail store microservices and zero three is ingress.

So where we create this ingress service to expose this application via internet and access it.

So that's zero three.

And finally here is very very important thing which is zero for verification pods okay.

So I have created dedicated verification pods for each uh, each service.

Right.

Catalog card checkout and then orders so that we can connect to those things and then verify you can

think like these verification parts are your debugging tools.

So whenever something doesn't work for you you can quickly jump into these pods, create these pods

and connect to these pods.

And from these pods connect to any of the services and then verify.

So this is a little bit of real world troubleshooting approach in production.

We need these type of tools for us to troubleshoot issues in Kubernetes.

So everything is well organized, easy to navigate and even easy to troubleshoot.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right, let's understand the Kubernetes Secret provider class for catalog in detail as part of this

lecture.

Let's start with the first thing, which is API version.

So this tells Kubernetes we are using the secret store C driver and of type version one of its custom

API.

Right.

Basically, this YAML isn't a normal regular Kubernetes resource or object.

It's a C, which means custom resource definition managed by the CSI driver.

So this secret provider class is a CRD custom resource definition, and it's kind is secret provider

class and name of this respective secret provider class we are giving as catalog DB secrets.

We are creating this in default namespace.

It's optional, so if we don't even provide namespace.

Also it gets created in the default namespace.

Let's go ahead and then review the actual specification.

So the first thing we are looking into is provider.

Provider here is set to AWS.

That means we are pulling secrets from AWS Secrets Manager using the AWS plugin.

So if you remember, in addition to Secrets Store CSI driver, we also installed the Ascp plugin which

is AWS Secrets Configuration Provider.

So this plugins responsibility is to pull the secrets from the Secrets Manager.

AWS Secrets Manager.

So we have the Secrets Store, CSI driver, ACP, and AWS Secrets Manager in AWS cloud.

So now let's get into the actual parameters here.

So we configure how AWS should be accessed and what secrets to fetch from the AWS Secrets Manager.

So if we go here this is the Secrets Manager.

So let me open that.

And inside the Secrets manager we have something one secret we have created.

So we need to look into it.

So this is the one.

Let me refresh.

So we need to go to the US East one region.

And in that we have something called retail store DB secret one.

So this is the secret name.

And here if you click on Retrieve Secret value you have the username and password with those values.

And if you want plain text this is a JSON file in JSON format.

Simple okay.

So now let's come back here right.

And in from which region means like region.

We have said as US East one, the CSI driver will pull secrets from this AWS region only.

So if your secret lives elsewhere, this must match.

Currently our secret is present in US East one.

So which means we are good.

Okay, so now let's come back here.

And we also said one important thing here use pod identity equal to true.

So this is a very important one here, which means the pod id x pod identity agent will be used as part

of this whole process.

So you have a catalog service account link to a IAM role with secret manager permission so that it will

be able to means like, we will be able to flawlessly pull the secrets from AWS Secrets Manager.

So we already created the IAM policy role and p I association and also service account catalog is mapped.

When we are doing this Pia association.

So this part we already completed right.

As part of 14 zero one itself.

We have when we are implementing the Terraform configurations for catalog microservice we have completed

these things.

Okay.

So now we are saying use pod identity true.

Which means we are going to use pod identity credentials to access your secrets from AWS in a secure

way.

Now let's come back to the objects section.

Right.

So first thing is we are giving the retail store DB secret one, which is the object name.

So we are pulling a secret named retail store DB secret one from AWS Secrets Manager.

So you can see the name here matches.

Okay, so this secret contains the MySQL user and password for the catalog service.

And you can see the object type.

We are saying Secret Manager.

So which is AWS Secrets Manager.

And here this is the important one JMS path okay.

So this JMS path slices the AWS secret JSON to extract individual keys.

So if you see here when you click on this retail retrieve secret value in plain text.

So using this JMS path you will get the username as separate and password as separate means like this

part.

And this part we are going to extract from this JSON.

Okay.

Now let's come back here.

And we said path as username and object alias as username.

So inside that secret manager JSON, take the username field value and expose it to Kubernetes with

again username.

So this is the object alias we are creating.

So in the same way we are taking password and then exposing it as again password.

Okay, so that's what we are doing.

We are trying to retrieve username to username and password to password.

Now the important thing is so this you can consider as part one.

And we also need to discuss the part two here.

So this is very important.

Okay.

So this is very important part two right.

So which is secret objects.

So this section tells the secrets store CSI driver.

Hey after pulling the AWS secrets also create a normal Kubernetes secret inside the EKS cluster.

So this part is very important okay.

So now we can see here on what name our Kubernetes secret to be created in EKS cluster.

After pulling the data from AWS Secrets Manager, we are giving the name of the secret as catalog DB.

Okay, so this is what your deployment will actually reference.

So if you go to our Kubernetes catalog deployment also here we are referencing the secret reference

as catalog DB in our catalog Kubernetes deployment file.

Okay.

Right.

So as we have provided here catalog DB we need to reference the same thing in our Kubernetes deployment

also.

And type is opaque the standard type for custom secrets.

And in data inside the data we map AWS secret fields to Kubernetes secret keys.

So that's the important thing.

So object name is username.

So we said the object alias as username right.

So this one we are putting it here.

And the key the value we are giving and then storing it in retail catalog.

Persistent user.

Okay.

And in the same way password.

Also this is the password and the key the value of that password we are storing in retail catalog persistence

password.

So in our Kubernetes deployment you are saying env from from this secret reference catalog DB you are

saying which means in your this catalog container, these two environment variables will be set with

username and password present in our AWS Secrets manager.

So whatever the username and password these are present here right.

My DB admin and DB 101.

So those will be set as environment variables for the values for these two environment variables.

So that's about Kubernetes secret provider.

Now we have reviewed the key secret provider class for catalog application.

And one more thing we need to understand for the same thing is.

So this is the catalog DB secrets, the name of your secret provider class, the same thing you are

going to reference in your catalog deployment in the in the volume section.

So you are going to add a volume of it.

So you can see AWS secrets and CSI Driver and Secrets store CSI, dot, IO.

And the secret provider class is catalog DB secrets.

So that's what we have done.

Catalog DB secrets whatever we have here.

So this is the name we have referenced here as a volume.

And then in our container right in our catalog container.

So this is for port specification spec.

And now in the catalog container you have added the volume mount slash secrets store right with this

volume.

So you can see the AWS secrets here.

So it's mounted as slash slash secret store.

Which means whenever the pod gets started, whenever the catalog pod gets started, it will get the

secrets from AWS Secrets Manager via Secrets Store, CSI driver Ascp and necessary Authentication using

Pia, all those things.

And finally, it will get those secrets and mount it at this location in your catalog pod.

That is first thing it will do.

And the second thing, what it will do is already in the secret provider class.

You also mentioned that you also need to create a catalog DB secret.

So after mounting that is first thing and second it will also create a secret.

Okay, that is catalog DB secret.

It will create.

And once that secret is created, it will reference this pod, will reference that secret and set these

environment variables and expose it to your internal application.

Whatever is there, which is your catalog application, then only your catalog application can successfully

connect to the RDS MySQL database.

So this is the overall flow for catalog.

And same applies for our uh, same applies for our this one also which one orders.

So you can see the orders also here the name you can see it here orders DB secret is the name of it.

And rest all all these things are same because we are using the same retail store DB secret one for

storing the username and password.

Same username and password for creating the RDS PostgreSQL database we are doing, which means my db

admin and db 101.

So this part is same.

Okay, only the name of it is orders DB secrets.

And in the Secret Objects section, we are creating the secret name as orders DB and here for orders

application.

What will be the environment variable it can recognize for credentials to connect to database those

information.

Retail orders.

Persistence.

Username.

Retail orders.

Persistent password.

We are setting it so the username and password present in AWS Secrets Manager will be stored inside

these things.

And if you go to the orders related.

If you go to the orders related deployment, also you will have the CSI secret volume and mount that

CSI secret volume.

So that's the thing we are going to see.

So now what happens when catalog right.

When this catalog deployment.

Right.

So when you deploy this catalog deployment because you have mounted them here.

Right.

So as a you have mounted this in your Work catalog application, so when the pod gets created, it will

trigger the secret creation, which means it will trigger the, uh, CSI driver thing, the whole path,

and then finally complete these two things.

One is mount it, and the second is create a Kubernetes secret.

Same for even orders DB also.

And another very, very important thing.

So this catalog DB is getting automatically created when our pod is getting created.

Correct.

Right.

So now you need to understand that if you undeploy this, if you undeploy means like if you delete this

Kubernetes deployment, right then your this catalog DB secret also will get deleted automatically.

So that's the importance of using.

This means like it will not statically store here.

If you remove the application aligned with that, that secret also will go away.

Now let's go ahead and then deploy the secret provider classes.

So we'll go back to our terminal here and we'll go here and we are in.

We'll come out of these things and we are here and we'll go to section 14 retail store microservices

with AWS data plane.

And we'll go to 14 zero to microservices with AWS data plane.

And we have the folder retail store kits manifest with data plane.

So in from inside this this is the zero one secret provider class.

And inside that if you verify you will have both catalog db and orders db.

So we'll say kubectl apply hyphen f and then zero one secret provider class which will create both secret

provider classes for us now okay so we'll say kubectl get secret provider class.

And both should be created now catalog db secrets and orders DB secrets.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


All right.

In previous lecture we have completed this part okay.

So we have successfully deployed this part and understood about this concept.

So one more time to reiterate about this secret store.

Right.

So one thing we need to see here, which is as part of this secret store CSI driver related features

section, right.

So we have implemented mounting the secrets to a Kubernetes pod, which is our catalog pod using a CSI

inline volume.

So this one, when did we do that.

So if we go here to our section zero nine Kubernetes Secrets, and here as part of 0904, if you go

to catalog test manifest in either Statefulset or deployment, you will find that you have created a

inline volume, right?

Secrets store CSI driver as a inline volume for your deployment or Statefulset.

And then you have a you have extracted that, you have mounted it as a volume and then extracted the

information from that mount point in Mt. Secret Store.

Whatever the data is there, you have extracted and used it as per your need.

So that is one feature which we have implemented.

And the second feature is supports sync with Kubernetes secrets.

So this is another feature supports sync with Kubernetes secrets.

And this one we have implemented just now okay in 1402.

Right.

So if you go to our any of the catalog deployment or so, you can go to catalog deployment.

And you can see here this is the secret reference catalog DB.

So two important concepts for secrets store CSI driver we have successfully implemented.

Now let's come back here okay.

Which is now the secret store thing is completed.

We will move on to the next thing okay.

So first what we will do is we will not go to all these microservices first.

So we will first go ahead and then deploy the UI service.

And in addition to that we'll also deploy the ingress service.

So these things we will deploy as the second.

Okay.

So earlier we have deployed this as the first okay D1.

And now we will deploy UI and then ingress.

So let's go back to our terminal here and then deploy them okay.

We'll first run the command cube CTL get pods and nothing should be there are get ingress and nothing

should be there.

And if you see we are in right section 14 zero two.

And inside the folder retail store k eight is manifest with data plane.

Now we will see here we will have something called zero to retail store microservices.

So we'll say cube CTL apply hyphen f zero to retail store microservices.

And inside that you have the zero five UI.

So you are deploying the UI related Kubernetes deployment.

And next thing is you will also deploy kubectl apply hyphen f you have zero three ingress.

So which is your ingress service.

Also you are going to deploy now.

So now if you run the command cube CTL get pods.

You should see your UI pod should be coming up just now.

Okay, so let's wait for it.

It will take a minute or two and then finally it will be available.

And we can also say kubectl get ingress and egress will take time right.

So if we go back to our browser also right.

So we can go here, go to our EC2 here and then sign in again.

So let me sign in.

We are in EC2 now.

We'll go to the load balancer section.

And our load balancer should be getting created now.

So we should switch the region to us East one.

And we should see our load balancer will get getting created in the process.

In provisioning state it should be okay.

So it is in the provisioning state.

So let's wait for it to get created.

Then access our application and also access the topology page.

And after that we'll move on to the next things we can see here.

Our load balancer is in the active state.

So now let's come back here.

And uh and we will say kubectl get ingress.

And this is our terminal.

So we will try to copy this and go to our browser and access it in our browser.

And we should get the 500 error.

Okay.

So topology and you can see catalog is it is 500 error.

And catalog is unhealthy.

Carts is unhealthy.

Checkout orders everything are unhealthy.

But before deploying UI we still didn't review that thing okay actually.

So we'll go here and in the UI right.

So in the UI service right.

So in the config map you are pointing to catalog carts checkout and then orders end points.

And those are currently not deployed.

So that's the reason in your topology section you can see all of these as healthy.

So UI service account deployment cluster IP.

All these are pretty standard which we have seen in our previous demos also.

Okay now let's come back here.

And in our next lecture we will focus on deploying catalog and verifying it in detail.

I'll see you in the next lecture.

Until then, bye bye.

Thank you.


Welcome back.

In this lecture we are going to focus on learning about or deploying the catalog microservice.

So let's go back to the Visual Studio Code.

And we are in zero to retail microservices.

And inside that in zero one catalog we have the catalog service account and catalog Configmap.

And we also have the catalog deployment which we have seen earlier.

Also when we are learning about secret provider class.

Right.

And then we have the catalog cluster IP service and catalog MySQL external name service.

So this in the external name service here in the external name we are going to give the our database

endpoint.

So if we go to our terminal here right.

And if we go here right.

So in our 14 zero one project you have the Terraform manifest to create the AWS data plane.

Right.

So in that zero three AWS data plane Terraform manifest.

If you run the command terraform output, it will output all the information, whatever you have provided

as part of Dataplane Terraform project.

So there you have something called catalog endpoint.

So this is your MySQL database endpoint.

So you need to update that here in your zero five catalog MySQL.

External name service and standard cluster IP deployment config map service account.

These are same as our previous things.

Okay right.

But here in the config map whatever you see catalog hyphen MySQL this is your external service name.

So this is the name.

This is your external service name catalog MySQL.

So in config map you are pointing to the external name service.

And this external name service is pointing to the RDS MySQL database End point.

So that's the thing.

And if you want, you can also go here and understand that in detail.

So you can either use external name service or you can go here and take this value here and directly

update in the config map replacing this one okay.

So you can directly put it here okay.

So if you want okay.

For catalog I have used external name service and for orders I have directly updated the endpoint here

okay.

So both are okay.

Both have their own pros and cons.

And it's our choice based on our requirements how we want to use it okay.

So but earlier it used to be always external name services only okay.

So it is a simple DNS alias.

No code or config map changes required here.

Okay.

So that's about uh external name service.

Now let's come back here.

And in catalog.

We have reviewed all these things.

Now we'll go ahead and then deploy the catalog.

So we'll say in we are here and in zero to retail store microservices.

So cube CTL apply hyphen f zero to retail store microservices in that zero one catalog.

So that is the folder structure right.

So if you go here also we can see that where is it.

In zero two you have the zero one catalog which we have just now deployed.

So if we run the command cube CTL get pods, your catalog application should be running.

Now.

One important thing which we have discussed as part of Secret provider class is whenever the catalog

pod gets created at the same time, your catalog secret catalog DB secret should get created.

Okay, see it is created catalog hyphen DB how many?

Just 33 seconds before even if you run the pod.

Also, it will be mostly at the same time.

Okay, right.

42.

And if you see secret also, it will be in the same time.

So.

Right.

So that's the thing.

Now your application is up and running.

If you say cube CTL logs hyphen f.

And then if you want you can verify your catalog application logs also okay pod logs you can verify

it is saying using MySQL database catalog hyphen MySQL colon 3306 running database migration database

migration complete.

It is saying database migration complete means in our RDS MySQL database.

We should see that.

We should see the all the files related to catalog present inside that product catalog should be present

inside that okay.

So that how you are going to verify right.

So you can go to you can connect to that RDS MySQL database and then verify.

So for that purpose what we have done is in our zero six in our zero four folder.

Right.

So here verification parts.

We have the zero one catalog MySQL client pod.

So this you will use the same service account name.

And it will use the uh thing.

And then it will try to get the usernames and then passwords whatever.

We have used the same concept, whatever we have used for the catalog deployment in the same way it

will try and do that.

Okay.

Which means whatever the things set, using either the Configmap or the secret example using MySQL username

and password it is trying to get from retail catalog persistent user and retailer retail catalog.

Persistent password from the uh, from the uh secret catalog DB.

That is one thing.

And second thing is it is also trying to get the MySQL host and MySQL db information from our Configmap

reference.

You have the catalog Configmap.

Right.

So this is the catalog Configmap.

And here you have the MySQL persistent provider.

And this is the endpoint and DB name.

So these two things it is trying to get from that Configmap okay.

All the things it is trying to get and then set the values here okay.

Environmental variable variables.

It is setting okay.

One is mysql user MySQL password MySQL host and MySQL db.

So let's go ahead and then deploy and then understand more about it.

So if we go here right.

So we can go to our zero for verification parts and deploy our catalog MySQL part.

So that's the thing we are going to do now.

So here we'll run this and in zero for verification parts.

You have that.

And if you run the command cube CTL get pods then your MySQL part should be coming online now.

So let's wait for the part to start.

And once it starts okay.

The name also you can see catalog MySQL client okay.

Now once it starts.

Right?

So if we go here, if you click on this link right.

So here you have the information in this verification parts right.

Step one is catalog to AWS RDS MySQL database connectivity verification.

So for that first you will deploy the pod and then you'll connect to that catalog MySQL client pod.

And then you will first verify your environment variables.

So whether the pod is ready.

So let's verify it is ready.

Then I'll connect to that MySQL client pod.

I am inside that MySQL client part.

If I say env there are a lot of environment variables, I just need only the MySQL related.

So you have the mysql db's catalog db password is db one zero, one user is my db admin and also host

is catalog hyphen mysql colon 3306.

So using the MySQL command, we will be able to connect to that.

I'll say MySQL hyphen h.

And then in the MySQL host you have both username both.

Your end point means like your host in this catalog hyphen MySQL.

What is present?

You have seen that your RDS endpoint is present.

So it will connect to this actually, which is nothing but your RDS MySQL database instance.

But here it also appended with port here in the MySQL host.

But for your MySQL hyphen h command you just need catalog MySQL.

So I'll copy that and you can use hyphen u user.

And what is the user.

This is the db one.

This is my db admin is the user.

So you can always say copy and then dollar and then MySQL user something like this.

You can provide the command here in detail.

So this is the command we are going to use okay.

So let me try that command.

And we are in okay.

So now if you say show schemas or are show databases.

You can see catalog DB is created.

This is our AWS RDS database.

So that is what I mean to say.

So this is our database.

If you go up.

So this is the one we are currently conducting.

So this is the one we are connected.

Now let's come back here and we will say use and catalog DB.

And inside that show tables.

And here we'll say select star from products.

If our database migration is successful.

If our application started successfully catalog application we should see the records here.

So which means the connectivity what we are talking.

So this is successful.

This is 100% successful.

We have implemented it okay.

Now this completes the catalog.

We can go ahead and then exit this respective part if you want.

And if if kubectl get pods and if you feel like you don't have sufficient nodes, worker nodes running

in your EKS cluster and it is taking more of resources, you can also delete this catalog MySQL client

if you want to.

I will leave it as it is.

So in our next lecture we will focus on cart application.

Before that we also need to verify the topology here.

Okay so we will try to refresh here.

And we can see catalog is healthy now okay.

And even if you try to refresh demo store it will not be accessible okay.

Demo store will be accessible after cart is started.

So when catalog and cart are started it might show up I think so okay.

Right.

But for now fine.

Our catalog application is healthy and this is our database endpoint and persistent provider is MySQL.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


So we have successfully installed the Ingress Service UI application catalog microservice.

Now we will focus on court microservice installation or deployment.

Okay.

So now we'll go here go down.

And this is we'll say this as D3 deployment D3 is done.

So we'll focus on cart now.

So let's go back here and we'll go to the Visual Studio Code.

And in CART service write in cart microservice.

We already discussed this earlier.

Also we have something called in the code.

They have hard coded the region to us West two okay.

So for that purpose for our cart persistent DynamoDB endpoint, we are using the US West two related

endpoint and region.

Also you are saying US West two and then default region.

Also US West two and provider is DynamoDB.

Table name is items and if if it is not created let's go ahead and then create.

So that's what we are telling here okay.

But if it is not created if it is created that is fine.

Okay.

So now if you go here and then go to our AWS, it should already be created as part of our AWS data

plane project.

Right?

So if you go here to here and if we go to in US East tour West two.

Right.

So this is US West two.

Right.

Let me go there.

And here in the we will go to DynamoDB.

And in the DynamoDB right.

We will go to the tables and items table should be already created.

And in the items table right.

You can explore items here.

So these are older things.

So let me try to delete them okay.

Now nothing is present inside this items table, it is empty.

Okay, so earlier I am testing something.

So those are there.

So this is empty now.

Now let's come back.

Right.

So let's come back here.

So this is the DynamoDB US West two endpoint and region is West two.

And here in the deployment standard deployment.

Okay.

So no changes same as earlier.

And cluster IP service and then service account.

Okay.

But if you go to the data plane layer you can see that for uh cart thing you have already created the

ECS pod identity association, IAM policy, IAM role, all those things, and even the DynamoDB table,

all these things you have created using data plane.

Okay, now you can also go here and then you verify the pod identity association and all those things.

Also if you go to ECS right.

And Elastic Kubernetes Service, these are the key connectivity items right.

So I am trying to show you those things okay.

If you go to the clusters and we'll go to US east one location where our EKS cluster is present.

And here, if you go to the access section and you can see here for the carts already a pod identity

association is created.

Okay.

So this is the DynamoDB table related IAM role also mapped.

Okay.

So which we have already done using data plane.

Now we will go here and then deploy those things.

Okay.

So we will say cube CTL apply hyphen f.

And in zero two retail store microservices zero to cart we are deploying.

Okay.

Now let's come back here.

So once we deploy this right we have the uh we can go ahead and then verify those parts.

So we have reviewed these things and we can access that topology via browser also.

So we will go here and we'll run the command cube CTL get pods.

And our carts is running right.

So carts pod is currently running.

And if you want you can verify the SVC service account, all those things.

Okay, carts related cluster IP is there and if you want you can also say cube CTL get all right and

then service account for cart should be there.

All those things.

But important thing here we need to verify is cube CTL get pods and we'll say cube CTL logs hyphen f.

And then if you want you can also verify your carts microservice carts pod related logs also.

So let's verify those things.

So this is a simple spring boot application.

And it is saying that using DynamoDB persistent and DynamoDB table name is items.

And DynamoDB endpoint is us west to whatever we said it and DynamoDB, uh, information.

All the things are like this.

Okay, it is showing.

Okay, it started completely initialized in two milliseconds.

Okay.

So some some places if you see table not found all those things ignore okay.

So let's go ahead and then test.

Then we will understand what is happening okay.

So now we will also go to the browser here and then try to refresh and then see carts is healthy.

Now if we want we can also for verification, deploy our cart DynamoDB related pod and execute these

commands also.

Okay, like connect to the DynamoDB client and verify AWS configuration and list DynamoDB table.

Describe the table and also file means like all the items, whatever is present in the table.

Also you can output them, but for DynamoDB, whatever is happening, you can verify directly via browser

itself.

Here the table items will be present.

Okay, you can see them, you can browse and explore them.

So we really don't need to go for any CLI pod for that.

Okay, so when we are accessing our application we will know that okay.

So I'll try to come back here to tables items.

And we are still here only right.

And explore items.

Let's put this this way carts is healthy.

And if I try to refresh mostly our application should be accessible.

If these two are up, our application will be accessible.

So you can see here the carts zero.

Currently if I click on Add to cart right.

So one item Aqua s GT is added in the cart okay.

Right now let's come back here to our DynamoDB okay.

And then run this okay.

Scan.

Run the scan and you can see one item in items.

You can see one item here.

And if you try to do this way so this is the item which is present currently okay.

And now let's come back.

Explore items.

Currently only one item is there.

So if you want we can go to home and we can go to this one okay.

So we will try to add one more.

So this is the second item we have added.

And when we run here we can see the second item here okay.

In the same line if we come back here and for this same phase charger Formal wear, right?

We'll try to add one more quantity.

So currently you can see the quantity here.

Only one right.

Only one for both of them.

So what we will do is we will try to add one more face charger formalwear okay.

So now it's quantity became two.

And here also when we try to run the scan in our DynamoDB table you can see the quantity is becoming

two.

So like this.

Whatever happening the entire flow you are trying to see here okay.

So if you go here right.

So as a user right as a user from the browser, I have accessed our application.

And then I'm trying to add the product catalog into the cart.

So currently three items I have added.

And those three items means like three items means one plus another item of two quantity.

Okay so I have added two different items.

And those two different item records are present in DynamoDB.

As soon as the order checkout order completes, that will be exit from the DynamoDB even that we are

going to see that later.

But when we complete, when we start the checkout orders and when we do the end to end verification.

But now it is clear for you, right?

Which means like what is happening, the flow means like the data is coming to your DynamoDB.

So catalog is up, cart is up, both are working.

We have tested them also.

And if you click on this it will call the checkout service and it will fail.

See checkout is not available.

It failed okay.

And if I go to home still those three items whatever I have added to the cart, two plus one still present.

Why they are present because we have the persistent database DynamoDB here, which is holding that information.

So that's the thing okay, for cart microservice DynamoDB is the persistent database.

Now let's go ahead and then uh focus on another checkout service in our next lecture.

So I'll see you in the next lecture.

Until then bye bye.

Thank you.


Welcome back.

In previous lecture we have completed the cart microservice.

Even deploying the cart microservice and testing the connectivity, how the data is flowing inside this

DynamoDB.

Also we have verified.

Now we will focus on checkout microservice.

So let's go ahead and then understand those things okay.

So we will go back here and we will update the end point of this checkout microservice.

So we will go to the terminal here.

And here we in the Terraform outputs in this data.

In the data plane Terraform manifest we will verify for the checkout related endpoints.

And here checkout Redis endpoint.

Also you can find it here okay.

So this is the endpoint we need to use in our config map related to checkout.

So if we go to the Visual Studio Code and in zero three this is the checkout service account and in

the config map.

Here you need to update your Reddis related endpoint.

Whatever you have created as part of your data plane.

Persistent data plane Terraform project.

Okay, so this is same for me.

So if you see here right let me copy this.

And it's the same thing one of one okay.

This is good.

Now you have the standard checkout deployment.

No changes as is okay.

And then you have the checkout cluster IP service.

So no changes.

Now we can go ahead and then even deploy this checkout service.

So let's go here right.

And we will go to our terminal here.

And we are in retail store kit as manifest with data plane.

And here you have the uh in zero to retail store microservices cube CTL apply hyphen f zero to two.

Retail store.

Microservices.

We are deploying the checkout service.

Okay.

And then it is getting created now.

So if you say cube CTL get pods our checkout service should be creating now.

So this is the one.

So I'll try to copy this and cube CTL logs hyphen f and then provide the review the logs for our checkout

service.

So this is the checkout service.

And it is currently starting.

And then it started okay.

So this is our checkout service logs.

Now if you come back here right.

And then try to refresh this.

And our checkout is healthy now.

And this is the end point retail checkout.

And then this whole thing is our database end point.

And even you can see that here right end point.

If you want you can go to ElastiCache and then verify the endpoint information whatever you have created.

Okay, so I will go to read this OS cash here and you can see it here.

Let's click on notes and you can see the endpoint information here.

Okay.

Now let's go back to our documentation here GitHub.

And you have started it successfully verified the topology.

If you want you can also access your application also.

Which means you can click on the start equipment requisition and checkout should work for you okay so

let me click on home right.

And this is your cart.

I'll click on the cart and start equipment requisition.

And immediately it will start working okay.

Parallelly if you want you can also verify the logs also.

Okay I'll click on start Equipment requisition and you can see the request came to the checkout.

So these are the checkout logs.

Request came to it.

It has done a post here.

And then I'll click on continue.

And again one more request came here and I'll click on continue.

Right.

So this is the delivery information.

Again I'll click on continue.

And these are the uh funding information.

Payment information.

And again I got the request.

But if I click on purchase it will fail.

Why.

Because from here it will hand over the request to orders microservice.

So it should fail.

See it failed okay.

Again cart.

And if you want you can verify all these things.

See it is saying get address EBS order info.

It is failing okay.

So means like uh our orders microservice is down and then it is not able to look up that orders microservice.

And then so this is the host name.

It is failing because it's still not deployed at okay.

But what is the key concept here.

Let us understand that okay.

So this is the checkout microservice is having Elastic Cache as the cache here or okay.

So now whatever the record came in, whether it is storing in the cache or not, you need to verify,

right?

So that's the feeling we will have, right.

So you have verified this.

You have the records in RDS, MySQL database, all the catalog microservice, whenever it starts for

the first time, it will migrate all the data to the RDS, all the product catalog information okay.

So this is successful.

And cart microservice.

Currently you are seeing in DynamoDB the records live we have seen, we have explored.

So let's also see what is happening with the cache.

Right.

So that's our feeling right.

So for verification of that we need to deploy our ElastiCache for Redis client.

And then you go to that step zero three.

So this is the one right checkout to ElastiCache.

So you can deploy that and then verify okay.

So before doing that if you want you can also go to Verification parts zero three ElastiCache.

And this is the thing okay.

Checkout this client and checkout is the service account name we are using and this is the client and

Redis URL.

We are trying to get it from our configmap reference Configmap checkout from there only.

We are trying to get the redis URL directly for us, okay, and set it as a environment variable for

us.

So that is what we are doing.

Okay for our pod.

Okay, we will go here.

And I am trying to avoid lots of manual things here.

That's the key thing here.

So we will go to 14 retail store microservices and 14 zero two microservices with AWS data plane.

And here we have retail store manifest with data plane.

And here we will go to zero for verification parts.

And here we will deploy the zero three checkout elastic class Redis client.

And we'll run the command cube CTL get pods.

And we can see our Redis client is running now from four seconds.

And next step is connect to that Redis client means like connect to that respective Redis client pod.

And you are in that client pod now.

And now if you environment and then grep radius, you will find your Redis endpoint.

Okay.

So this is your Redis URL.

So this is your Redis endpoint okay.

So you got the endpoint also.

Now you can get your Redis host okay directly.

You can get your Redis host and then access it okay.

So you can run these commands host and port directly okay.

So you can from this URL you are trying to get this into Redis host and this into Redis port.

Okay.

So these are some of the additional tweaks okay.

Or directly you can give the URL here and port here and connect okay.

Anything is fine.

I'll try to get those things okay.

And then I will run this.

And then we can see here we have the Redis host.

This is the host Right.

And then we'll also run the command.

Is the reddest port.

Now the command here is redisai cli hyphen h dollar redis host and hyphen p dollar redis port.

Okay, so we are connecting to the CLI to this respective instance.

Okay.

Now we have successfully connected to this instance this ElastiCache instance we have connected okay.

Now let's come back here and then run the commands okay.

So one command we need to run is ping db size and keys.

Okay.

So let's run those things.

Okay.

We'll run the command ping.

And we got the pong which means we got the response.

And if we want we can also run the command DB size.

And it is saying integer two okay.

And if you want list all keys use only for quick checks not for large data sets Okay.

So keys and then star.

And it has two keys inside them.

Okay.

And in those things you can get one key and then verify.

Okay.

So we'll say get and provide the key information.

Copy.

And this you can see the complete information here.

Six.

F6 88 D43 four.

Is this by any chance mapping with.

See our information six F6 8434 okay.

It's mapping here, right?

So like this means like whatever is there it has cashed successfully.

And 301.

So this is another thing which it cashed okay.

And you can see the information of it which is Aqua Ace GT.

So that is what is having in the here right.

Aqua ace GT okay.

That is one thing you have seen.

Now let's say I have emptied the cart.

Now nothing is present in the cart and I will try to add something here.

I will try to explore here and from foods something like forget me pop or from accessories.

So temporal tick stopper.

I tried to add it to the cart and then if I go here to this items and try to click on run right.

So I have something here.

And under this we should have the information about that.

It is stored here ID customer ID and then item ID information.

Okay.

Now what I will do is I will move on to the next steps here okay.

Which is click on Start Equipment Requisition.

And immediately the request will come here for my checkout okay.

So here you can see right.

So this is the ID which is getting updated in the cache.

This is the record under this record cache is getting updated.

You can see slash update okay right I'll click on continue.

And one more time you can see it here.

And I'll click on continue.

And if you want we can click on purchase and it will fail.

But the request the thing is temporal tick stopper is the thing.

Now we'll go here and we'll say keys and we'll say get from this.

This ID is nothing but the same ID whatever you are seeing in the logs here.

So this is the ID.

Let me copy and try to get that ID information.

And here if you see here temporal tick stopper.

So it is updated in our cache are going to the elastic cache or not.

And you have seen it it as successful.

So we have deployed the checkout and we have verified whole flow is working as expected.

So I'll see you in the next lecture for orders microservice.

Until then bye bye.

Thank you.


Welcome back.

Let's go ahead and also focus on deploying the orders microservice.

In orders microservice.

We need to first update the RDS PostgreSQL endpoint and AWS.

SQS means like simple queue service queue name.

Also we need to update.

So we'll go to our terminal here and we will go here.

And we'll also clear this and we'll go to this one.

And here we have in our zero three AWS data plane Terraform manifest.

When we run the command Terraform output we can see the orders RDS PostgreSQL endpoint.

And also you have the SQS queue information.

So this is the SQS queue which we have created okay.

This is the complete queue URL.

And this is the name of the queue actually.

So these two things we need to update in our orders config map.

So let's go back here and we'll go to zero four.

Right.

Zero four and this is the service account orders.

And this is the config map.

And here you need to update your endpoint.

And here you need to update your SQS topic which means your queue name you need to update.

And then this is the standard orders deployment wherein you have the CSI secrets volume present and

its mount point also present here.

And you should also see the orders DB here.

Okay.

Right now let's go ahead and then deploy the orders service now.

So this is the thing here.

We will deploy the orders and verify the topology and also deploy the orders related PostgreSQL client.

And then connect to that PostgreSQL client and verify the orders.

And also we'll connect to the SQS SQS queue and then verify the messages also.

Right.

So now for SQS we can also see by a browser.

If you want we can even verify the things via browser also.

Okay, so now what we will do is we will go back to the terminal here and we will say cube CTL apply

hyphen f and in zero to retail store microservices we have zero for orders.

So let's the orders app get created.

And once it creates right we'll run the command get pods.

And it is.

And we'll also see get secrets right orders DB secret also should get created just 11 seconds before

it started.

Create it created and immediately our pod.

Once the secret is created, pod also should get created.

So it is currently getting created.

It might take a little bit of time.

Why?

Because orders pot takes time because it need to boot the Spring boot application, which is a Java

application and a heavy application.

Okay, so cube CTL get pods and we can see orders application is running.

Now we can go to the browser here and we can go to the topology and try to refresh the topology and

orders is also healthy.

And you can see the end point here.

So this is the orders Postgres DB endpoint.

Now we'll go here.

This is our application.

And we will try to access like something okay face change or formal wear.

This is the one.

We are adding it to our cart.

And we are starting the checkout process and we are moving on.

And here we will click on purchase and orders should be successful.

Okay, now you can see it has created some order here and it is successful okay.

Now what we will do is we will go back here and we'll go to this verification of pods.

And here in step zero four you will have orders to RDS PostgreSQL database verification.

First we need to deploy the orders PostgreSQL client pod.

So we will go to 0204.

Verification pods and deploy the orders PostgreSQL client pod.

And after that we will verify if the pod is up and running.

We can see the container is getting created.

So once it is created, it is created.

We will connect to that container first okay.

And then connect to the PostgreSQL database which is our AWS, RDS, PostgreSQL which is a managed database

in AWS.

So we'll try to run the command env pkg host.

This is the pkg host PostgreSQL database endpoint.

And this is the database okay.

So we will try to connect to that endpoint.

So anything is fine.

This are this.

Both should work for us.

And we connected to the Order's DB now.

And here you can run the command slash L to list all the databases and orders.

DB is the one which we are interested in okay.

And we will connect to that orders DB.

So we'll say click.

Q and we'll connect to that orders DB.

And then we will say list the table slash DT means it will list the tables.

And we have something called order items and orders.

Okay.

So here we will describe the table structure.

If we want we can describe the table structure for orders.

Right.

And let's wait for it to describe.

And you have the information about orders table.

Okay.

In the same line we will say select star from orders and verify okay.

How many things are there.

Earlier I might have added other orders also.

You can see all those things okay.

Now let's see our order ID here.

So this is starting with 1C173C.

This is the 11C173.

This is the order just now created for us Okay.

And now, if you want, you can also exit.

But we'll try to create one more order.

What is there?

Right.

So we'll go here home.

And then we will add some audio illusion or spinner and click on start and continue.

Continue and purchase.

And it created a order 9A3C 28 something okay.

And we'll come to the terminal and select star from orders.

You can see your order ID is present here.

So whatever we have created we can see that order information here.

So which means our complete our flow to this respective thing.

Orders to PostgreSQL is successful okay.

Now the next thing here is we will go to our SQS queue.

So how to go to SQS queue.

So we'll come back here and.

So here we will go to S Q.

S.

Okay.

Simple queue service.

And it should be in our US East one retail dev.

Orders.

Queue.

So you can see it is saying four messages are available.

You can see them right.

So those are the four messages available.

Currently we have four orders you have seen in the terminal.

So those four messages are available for a send and receive messages.

And here in the receive messages section you can poll for messages.

And you can see all the messages present here.

Now let's click on this one of the message here and see the order ID is 93 which should match here 9A3C

okay.

And if we want we can try one more here.

Okay.

So try to add one more thing.

So we'll explore and we'll go to vehicles and we'll add this Skycycle or Phantom Pursuit.

This one will add and will complete the entire process.

Right.

And then purchase.

Okay, so now it's order ID is E083.

Okay.

Now we'll go here and then we'll click on here and try it.

And then see how many messages are there.

We'll go here and go to the queues and try to refresh.

And we have five messages.

Okay.

Right.

And then here we'll click on Send and Receive.

And in receive messages we'll say poll for messages.

And we'll try to open the last one right which is at 448 1648 E083 DBD, and which should match here

with E083 DBD.

So this event, whatever we are talking about this one.

Right.

Even this is successful.

Okay.

So we have implemented the retail store application with persistent data plane running on AWS cloud.

And we have verified the end to end connectivity.

So from our microservices which are present in EKS cluster to AWS data plane.

So whatever things are required for that, like policies are trust policies are IAM roles, right.

Whatever are there are PII associations or whatever required we have implemented and tested end to end.

So this completes the retail store application with persistent data plane running on AWS cloud demo

for simple queue service.

I have also given the client pod where you can verify, but more effective for that is UI itself, which

means like on the UI itself, you can effectively verify instead of client pod.

Okay, so that's about SQS service.

So to summarize what we have implemented.

We have censored AWS secrets with Kubernetes from AWS Secrets Manager, deployed UI and ingress services

integrated each backend microservice.

Each backend microservice with AWS means like catalog with RDS, cart with DynamoDB, checkout with

ElastiCache, orders with PostgreSQL plus SQS and verified data and message flow end to end.

And we have implemented this.

Okay, now we can go ahead and then clean up whatever we have implemented.

I'll exit this and exit this.

And here we will come out of this and we will say here we are in 14 zero two I'll say cube CTL, delete,

hyphen r, hyphen f and retail store cart as with data plane.

So this should remove all the things whatever we have created okay so including client pods everything.

Okay.

So if something is not created then it will.

That is fine okay.

So it will say not found okay.

But rest all.

Everything will get cleaned up.

And if you run the command cube CTL get pods.

So we can see those clients are also getting terminating and nothing is present.

Get all also in default namespace, nothing should be present in.

Most importantly we need to verify cube CTL get ingress.

Our ingress service should be deleted.

It is going to cost us.

It is also not present for safety purpose.

We can also go to EC2 and then verify the same thing.

So we can go to EC2 here and go to the load balancers and ensure it is not present.

And if we want to also clean up our data plane.

So we can go here to section 14 zero one and we can go to zero three AWS Data plane Terraform manifest.

And we can run the command Terraform destroy auto approve.

So this completes the demo about retail store application with persistent data plane running on AWS

cloud.

I'll see you in the next demo.

Until then, bye bye.

Thank you.


